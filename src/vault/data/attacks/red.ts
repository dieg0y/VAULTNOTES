// attacks/red.ts — dataset de ataques de RED (L2/L3, Wi-Fi, routing).
//
// 25 entradas con categoria: 'Red': MITM on-path y sus vectores (ARP, DNS,
// DHCP, VLAN, Wi-Fi), ataques de infraestructura (BGP, STP, CDP/LLDP,
// routing interior), suplantación L2, interceptación física, bypass de
// NAC/802.1X y recon DNS. Las TÉCNICAS viven aquí; los fallos de
// implementación viven en ../vulnerabilities.ts (y el envenenamiento
// LLMNR/NBT-NS + relay NTLM vive allí como entrada combinada — no se
// repite aquí).
//
// UMBRELLA: RED-001 (MITM on-path) engloba como VECTORES a ARP spoofing
// (RED-002), DNS spoofing (RED-003), rogue DHCP (RED-005), evil twin
// (RED-011) y SSL stripping (RED-010) — por eso existen entradas propias
// de cada vector. Los sinónimos van en `alias`, nunca como fila aparte.
//
// IDs: RED-001..RED-025. 100% offline — se concatena en ./index.ts.
// No usar `export default`.

import type { AttackInfo } from './types';

export const RED_ATTACKS: AttackInfo[] = [
  {
    id: 'RED-001',
    nombre: 'Man-in-the-Middle (on-path)',
    alias: ['MITM', 'on-path attack', 'intermediario', 'hombre en el medio'],
    categoria: 'Red',
    severidad: 'Critical',
    mitre_attack: ['T1557'],
    descripcion_tecnica: 'Entrada UMBRELLA: el atacante se coloca en el camino lógico entre víctima y destino para leer y modificar sus comunicaciones. No es una técnica única sino un patrón que se materializa mediante vectores concretos: ARP spoofing (RED-002), envenenamiento de DNS (RED-003), rogue DHCP (RED-005), evil twin (RED-011) o SSL stripping (RED-010). Con TLS y validación estricta de certificados el on-path queda limitado a metadata y timing; sin ella, es control total de la sesión.',
    impacto_iam_soc: 'Robo de credenciales en claro, secuestro de sesión, inyección de contenido y redirección a phishing AiTM. Es el patrón tras muchos "logins válidos desde dentro": el origen del incidente es la posición en la red, no un malware.',
    como_funciona: [
      'Reconocer el segmento: nmap -sn 192.168.1.0/24 para inventariar hosts, gateway y resolver.',
      'Elegir el vector de posicionamiento: ARP spoof en segmentos conmutados, rogue DHCP/rogue AP para clientes inalámbricos, DNS spoof para redirección por nombre.',
      'Activar reenvío y manipular el flujo: sysctl net.ipv4.ip_forward=1 + bettercap -iface eth0 o mitmproxy --mode transparent.',
      'Escalar: capturar cookies de sesión, aplicar SSL stripping (RED-010) o pivotar a credenciales de dominio con Responder sobre el mismo segmento.',
    ],
    deteccion: {
      kql: 'DeviceNetworkEvents | where ActionType in ("ArpPoisoningDetected", "SuspiciousNetworkEvent") | summarize count() by DeviceName, RemoteIP | where count_ > 10 | take 20 // señales de posicionamiento on-path',
      spl: 'index=net sourcetype=cisco:ios "IP DUPLICATE" OR "GRATUITOUS ARP" OR "MACFLAP" | stats count by src_mac, src_ip | where count > 20',
    },
    mitigacion: [
      'Cifrar todo y validar certificados estrictamente (HSTS, TLS 1.2+, pinning donde aplique) — el on-path solo rinde contra tráfico en claro.',
      'Cortar los vectores L2: DHCP snooping + DAI, port security, 802.1X, DTP/CDP desactivado en puertos de acceso.',
      'Alertar de ARP duplicado, MAC flapping y respuestas DNS contradictorias con passive DNS interno.',
      'Segmentar por sensibilidad para acotar el radio de un atacante posicionado.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1557/', 'https://www.bettercap.org/'],
  },
  {
    id: 'RED-002',
    nombre: 'ARP Spoofing',
    alias: ['ARP poisoning', 'envenenamiento ARP', 'ARP cache poisoning'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: ['T1557.002'],
    descripcion_tecnica: 'El atacante envía gratuitous ARP falsos (respuestas no solicitadas) asociando su MAC a la IP del gateway o de otra víctima del mismo segmento de broadcast. Las cachés ARP se envenenan y el tráfico L3 de las víctimas empieza a atravesar la interfaz del atacante, que lo reenvía para mantener la sesión (stealth) o lo descarta (DoS).',
    impacto_iam_soc: 'Sniffing en redes conmutadas sin tocar los switches, captura de credenciales en claro y plataforma para SSL stripping o DNS spoofing. En SOC: un mismo MAC respondiendo por varias IPs, avisos de "IP duplicada" y MAC flapping en los switches.',
    como_funciona: [
      'Habilitar reenvío para no romper la sesión de la víctima: sysctl -w net.ipv4.ip_forward=1 y send_redirects=0.',
      'Envenenar en ambos sentidos (full-duplex): arpspoof -i eth0 -t 192.168.1.50 192.168.1.1 + arpspoof -i eth0 -t 192.168.1.1 192.168.1.50, o bettercap -iface eth0 -cap arp.spoof.targets 192.168.1.50.',
      'Capturar el tráfico desviado: tcpdump -i eth0 -w mitm.pcap host 192.168.1.50.',
      'Escalar: bettercap net.sniff con parsers de credenciales, SSL stripping (RED-010) o DNS spoofing (RED-003) sobre el flujo.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=cisco:ios "duplicate address" OR "IPDUPLICATE" OR "%SW_MATM-4-MACFLAP_NOTIF" | stats count by mac, ip | where count > 5',
      kql: 'DeviceNetworkEvents | where ActionType == "ArpPoisoningDetected" | project DeviceName, RemoteIP, LocalIP | take 20 // requiere DAI en el switch o sensor L2 del EDR',
    },
    mitigacion: [
      'Dynamic ARP Inspection (DAI) sobre la tabla de bindings de DHCP snooping en todos los switches de acceso.',
      'Limitar dominios de broadcast (VLANs pequeñas) y ARP estático/inspect en servidores críticos.',
      'Cifrar el tráfico sensible (TLS/SSH/SNMPv3) — el on-path sin cifrado no vale nada.',
      'Enviar syslog de switches al SIEM y alertar de gratuitous ARP anómalos.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1557/002/', 'https://www.bettercap.org/usage.html'],
  },
  {
    id: 'RED-003',
    nombre: 'Envenenamiento de DNS (spoofing y cache poisoning)',
    alias: ['DNS spoofing', 'DNS cache poisoning', 'envenenamiento de caché DNS', 'ataque Kaminsky'],
    categoria: 'Red',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2008-1447'],
    mitre_attack: ['T1557.003'],
    descripcion_tecnica: 'El atacante responde consultas DNS con datos falsos antes que el servidor legítimo, asociando el dominio objetivo a una IP que controla. Variantes: race de respuestas spoofeadas contra un resolver (bug Kaminsky, CVE-2008-1447), suplantación del resolver estando on-path (RED-001/002) y hijack del archivo hosts local (equivalente offline, requiere compromiso previo del host).',
    impacto_iam_soc: 'La víctima navega a un portal clonado sin notarlo: credenciales y MFA comprometidos con el servidor real intacto. En SOC: respuestas DNS que contradicen el passive DNS interno y dominios corporativos resolviendo fuera de sus rangos.',
    como_funciona: [
      'On-path con bettercap: -cap dns.spoof.domains intranet.corp --dns.spoof.address 192.168.1.66 responde las consultas del segmento envenenado por ARP (RED-002).',
      'Race Kaminsky: spoofer en scapy que adivina puerto/ID de una consulta por un subdominio inexistente (a12345.banco.com) hasta colar una respuesta autoritativa con TTL alto — ensucia la caché del resolver.',
      'Hijack local: modificar C:\\Windows\\System32\\drivers\\etc\\hosts (o /etc/hosts) apuntando portales internos a la IP del atacante.',
      'Payload: portal de login clonado (Evilginx) o redirección de actualizaciones/CDN internos para servir binarios propios.',
    ],
    deteccion: {
      kql: 'DeviceNetworkEvents | where RemotePort in (53, 5353) and Protocol == "UDP" | summarize dcount(RemoteIP), count() by DeviceName | where dcount_RemoteIP > 3 | take 20 // cambio de resolver o respuestas desde IPs no oficiales',
      spl: 'index=dns sourcetype=unbound_query | stats latest(answer) as ip by query | where like(query, "%.corp%") AND NOT cidrmatch("10.0.0.0/8", ip)',
    },
    mitigacion: [
      'DNSSEC con validación en resolutores internos y firmas verificadas por los clientes cuando sea posible.',
      'Parchear resolutores (randomización de puerto origen + 0x20: mitigación Kaminsky) y restringir recursión a clientes internos.',
      'Forzar DNS corporativo: bloquear UDP/TCP 53 y DoH saliente directo desde estaciones.',
      'Proteger el hosts file con GPO/ACL y alertar de su modificación (Sysmon/EDR file events).',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1557/003/', 'https://www.kb.cert.org/vuls/id/800113', 'CVE-2008-1447'],
  },
  {
    id: 'RED-004',
    nombre: 'DHCP Starvation',
    alias: ['agotamiento DHCP', 'DHCP pool exhaustion', 'DHCP starvation attack'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'El atacante bombardea al servidor DHCP con mensajes DHCPDISCOVER desde MACs falsas: cada petición reserva una lease distinta y el pool se agota. Los clientes legítimos se quedan sin IP (DoS de acceso a red) o, mejor aún para el atacante, queda el terreno preparado para que un rogue DHCP (RED-005) monopolice las respuestas.',
    impacto_iam_soc: 'DoS silencioso de acceso a red y fase preparatoria de rogue DHCP. En SOC: ráfagas de DHCPDISCOVER con MACs nuevas que nunca completan DHCPREQUEST y que no generan tráfico L3 posterior.',
    como_funciona: [
      'yersinia dhcp -attack 0 (DISCOVER en raw con MACs aleatorias) desde la CLI interactiva: yersinia -I → g → 1.',
      'Alternativa scriptable con scapy: sendp(Ether()/IP()/UDP()/BOOTP(chaddr=randmac())/DHCP(options=[("message-type", "discover"), "end"])) en bucle.',
      'Observar el pool caer en los logs del dhcpd y a los clientes legítimos sin renovación.',
      'Escalar: desplegar el rogue DHCP en el mismo dominio de broadcast (ver RED-005).',
    ],
    deteccion: {
      spl: 'index=dhcp sourcetype=dhcpd "DHCPDISCOVER" | stats count, dc(chaddr) as macs by src_ip | where count > 50 | head 10',
      kql: 'DeviceNetworkEvents | where RemotePort == 67 and Protocol == "UDP" | summarize count() by DeviceName, InitiatingProcessFileName | where count_ > 50 | take 20 // un host pidiendo decenas de leases: o es hipervisor o es yersinia',
    },
    mitigacion: [
      'DHCP snooping con rate-limit de peticiones por puerto (pps de DISCOVER limitado).',
      'Port security: máximo 1-2 MACs aprendidas por puerto de acceso.',
      '802.1X: sin autenticación no hay acceso al dominio de broadcast.',
      'Monitorizar el ratio DHCPDISCOVER/DHCPREQUEST por segmento — el starvation dispara discovers sin requests.',
    ],
    referencias: ['https://github.com/tomac/yersinia', 'https://datatracker.ietf.org/doc/html/rfc2131'],
  },
  {
    id: 'RED-005',
    nombre: 'Rogue DHCP Server',
    alias: ['DHCP rogue', 'DHCP falso', 'servidor DHCP no autorizado'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: [],
    descripcion_tecnica: 'Un servidor DHCP no autorizado responde más rápido que el legítimo y entrega a los clientes gateway y DNS controlados por el atacante (a menudo la propia IP del atacante en las opciones 3 y 6). Es el complemento natural del DHCP starvation (RED-004): primero se agota el pool oficial, luego el rogue se queda con todas las respuestas.',
    impacto_iam_soc: 'Control del default gateway (posición on-path de todo el tráfico del cliente) y del DNS (redirección a portales clonados). En SOC: DHCPACK procedente de un MAC/IP que no es el servidor oficial del segmento.',
    como_funciona: [
      'Montar el servidor: dnsmasq --interface=eth0 --dhcp-range=192.168.1.100,192.168.1.200,255.255.255.0,12h --dhcp-option=3,192.168.1.66 --dhcp-option=6,192.168.1.66 (gateway y DNS = atacante).',
      'Ganar la carrera: dnsmasq contesta el OFFER antes que el servidor Windows oficial por latencia.',
      'Mantener la ilusión: iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE para que Internet siga funcionando y nadie investigue.',
      'Combinar con DHCP starvation (yersinia, RED-004) para dejar sin pool al servidor legítimo.',
    ],
    deteccion: {
      spl: 'index=dhcp sourcetype=dhcpd "DHCPACK" | stats count by server_ip, server_mac | where server_ip != "192.168.1.1" | head 10',
      kql: 'DeviceNetworkEvents | where RemotePort == 68 and Protocol == "UDP" | summarize count() by RemoteIP, LocalIP | where count_ > 3 | take 20 // RemoteIP = origen del OFFER/ACK: comparar contra el DHCP oficial del segmento',
    },
    mitigacion: [
      'DHCP snooping: solo los puertos hacia el servidor legítimo son trusted — el resto descarta OFFER/ACK.',
      'DAI + IP source guard sobre la tabla de bindings (el rogue pierde toda la puerta de entrada).',
      'Separar la red de invitados del segmento corporativo.',
      'Baseline del MAC origen de DHCPACK por segmento y alerta de servidores no inventariados.',
    ],
    referencias: ['https://thekelleys.org.uk/dnsmasq/docs/dnsmasq-man.html', 'https://en.wikipedia.org/wiki/Rogue_DHCP'],
  },
  {
    id: 'RED-006',
    nombre: 'MAC Flooding (desbordamiento de tabla CAM)',
    alias: ['CAM table overflow', 'CAM flooding', 'desbordamiento CAM'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'El atacante inunda el switch con frames Ethernet con MACs origen falsas y aleatorias hasta desbordar la tabla CAM (MAC → puerto). El switch degrada a comportamiento hub: envía el unknown-unicast por TODOS los puertos de la VLAN. Resultado: sniffing del segmento sin necesidad de posicionamiento on-path.',
    impacto_iam_soc: 'Captura pasiva de tráfico intra-VLAN (sesiones internas, credenciales en claro). En SOC: alarmas "CAM table full" y un puerto de acceso con miles de MACs aprendidas — mucho antes de que nadie mire un pcap.',
    como_funciona: [
      'macof -i eth0 (dsniff) genera miles de frames/s con MACs e IPs origen aleatorias contra el puerto del switch.',
      'Esperar el desbordamiento de la tabla CAM y el modo flooding del switch.',
      'Capturar en modo promiscuo: tcpdump -i eth0 -w flooded.pcap "not arp and not broadcast".',
      'Post-proceso: tshark -r flooded.pcap -Y "http.request || ftp.request.command" o dsniff para extraer credenciales.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=cisco:ios "CAM table" OR "MAC table full" OR "%SW_MATM-4-MACFLAP_NOTIF" | stats count by port | head 20',
      kql: '// EDR no cuenta MACs aprendidas: correlar syslog del switch con pps del puerto (NetFlow) — un puerto de usuario no aprende 1000 MACs',
    },
    mitigacion: [
      'Port security: switchport port-security maximum 2 + violation restrict en puertos de acceso.',
      '802.1X/MAB: solo dispositivos autenticados aprenden direcciones en el puerto.',
      'Storm control de unknown-unicast/broadcast por puerto.',
      'Alertar del syslog de switches (MACFLAP, CAM full) y de saltos bruscos de MACs por puerto en el NMS.',
    ],
    referencias: ['https://monkey.org/~dugsong/dsniff/', 'https://www.kali.org/tools/dsniff/'],
  },
  {
    id: 'RED-007',
    nombre: 'Sniffing de red (captura de tráfico)',
    alias: ['packet sniffing', 'captura de paquetes', 'wiretapping', 'escucha de red'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: ['T1040'],
    descripcion_tecnica: 'Captura y análisis del tráfico que atraviesa la posición del atacante. En dominios de broadcast compartidos o tras un MAC flooding basta el modo promiscuo; en segmentos conmutados hace falta ser on-path (RED-001) o tener una posición de red (SPAN, TAP, firewall). El botín son los protocolos en claro: FTP, Telnet, HTTP, SNMPv1/v2, LDAP sin TLS y sesiones de BD sin cifrar.',
    impacto_iam_soc: 'Fuga silenciosa de credenciales y tokens que luego aparecen como "logins válidos" en IAM — el SOC ve el uso del secreto, no su robo. Inventariar protocolos en claro es la métrica directa de exposición.',
    como_funciona: [
      'Captura genérica: tcpdump -i eth0 -w cap.pcap "port 21 or port 23 or port 80 or port 161" o Wireshark en modo promiscuo.',
      'Extracción dirigida: tshark -r cap.pcap -Y "ftp.request.command == USER || ftp.request.command == PASS" o dsniff directamente.',
      'SNMP en claro: onesixtyone para community strings por diccionario y snmpwalk con la community descubierta.',
      'En segmento conmutado: primero ARP spoofing (RED-002) o MAC flooding (RED-006) para desviar el tráfico.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=pan:threat app IN ("ftp", "telnet", "snmp") | stats count by app, dest_ip | head 20 // el hallazgo NO es el sniffer: es el protocolo en claro',
      kql: 'DeviceNetworkEvents | where RemotePort in (21, 23, 80, 161) | summarize count() by RemotePort, RemoteIP | where count_ > 100 | take 20 // tráfico en claro = exposición a sniffing',
    },
    mitigacion: [
      'Eliminar protocolos en claro: SSH en vez de Telnet, SFTP/FTPS, SNMPv3 authPriv, HTTPS+HSTS, LDAP StartTLS.',
      'Alertar del uso interno de FTP/Telnet/HTTP en firewalls y proxies (debería ser cero).',
      'Cortar los vectores de posicionamiento: DAI, port security, 802.1X (RED-001/002/006).',
      'Si un legacy lo exige: VLAN dedicada con ACLs mínimas y monitorización reforzada.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1040/', 'https://www.wireshark.org/docs/', 'https://monkey.org/~dugsong/dsniff/'],
  },
  {
    id: 'RED-008',
    nombre: 'Port Scanning',
    alias: ['escaneo de puertos', 'port scan', 'reconocimiento de puertos'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: ['T1046'],
    descripcion_tecnica: 'Enumeración de puertos y servicios de un objetivo para construir el mapa de ataque. Tipos: TCP connect (-sT, handshake completo, auditable en los logs de la app), SYN half-open (-sS, nunca completa la conexión, invisible para la aplicación), UDP (-sU, lento, delatado por ICMP unreachable), FIN/Xmas/null (-sF/-sX/-sN, hechos para colarse por firewalls stateless) e idle scan (-sI, se esconde detrás de un host zombi).',
    impacto_iam_soc: 'Primer paso de casi toda intrusión: define qué ataques son viables. El scan lento y pausado (masivo con -T1) es la regla de detección más valiosa — el scan rápido ya nadie lo usa en producción.',
    como_funciona: [
      'Descubrimiento de hosts: nmap -sn 10.0.0.0/24 (o -Pn si el host dropea ICMP).',
      'Scan estándar: nmap -sS -sV --top-ports 1000 10.0.0.5 (SYN half-open + versiones de servicio).',
      'Evasión: nmap -sN/-sF/-sX 10.0.0.5, decoys con -D RND:10, fragmentación -f y timing -T1 (sneaky).',
      'UDP en servicios olvidados: nmap -sU -p 53,123,161,500 10.0.0.5.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=stream:tcp | stats dc(dest_port) as ports, dc(src_ip) as srcs, count by dest_ip | where ports > 100 AND ports < 500 | head 20 // muchos puertos tocados una sola vez cada uno',
      kql: 'DeviceNetworkEvents | summarize dcount(RemotePort) as nports, count() by DeviceName, RemoteIP | where nports > 100 | take 20 // peso extra: ConnectionFailed sin patrón de aplicación',
    },
    mitigacion: [
      'Firewall con rate-limiting por IP fuente y SYN proxy (mitiga scans SYN masivos).',
      'IPS con firmas de fingerprints de scan (NULL/FIN/Xmas, timing de nmap).',
      'DROP en vez de REJECT en puertos cerrados: menos telemetría para el atacante.',
      'Canary ports/IPs internas sin uso: solo un scan las tocaría → alerta de confirmación inmediata.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1046/', 'https://nmap.org/book/man.html', 'https://nmap.org/book/idlescan.html'],
  },
  {
    id: 'RED-009',
    nombre: 'VLAN Hopping',
    alias: ['salto de VLAN', 'VLAN hopping', 'switch spoofing', 'double tagging 802.1Q'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: [],
    descripcion_tecnica: 'Dos vías para cruzar la frontera de VLAN sin router: (1) switch spoofing — el atacante habla DTP y negocia un trunk con el switch, quedando con acceso a todas las VLANs del trunk; (2) double tagging 802.1Q — frame con dos tags: el switch de acceso quita el exterior (su native VLAN) y el tag interior viaja intacto por el trunk, saliendo en la VLAN víctima. El tráfico de retorno no llega al atacante (one-way), pero basta para inyección (explotación dirigida, LLMNR spoof).',
    impacto_iam_soc: 'Rompe la segmentación que el SOC da por hecha: tráfico de la VLAN de usuarios hacia VLAN de servidores sin cruzar ningún firewall. Se ve como DTP en un puerto de acceso y paquetes 802.1Q donde no debería haberlos.',
    como_funciona: [
      'Switch spoofing: yersinia dtp -attack 0 (negociación DTP) — el puerto pasa de access a trunk dinámico.',
      'Double tagging: scapy sendp(Ether()/Dot1Q(vlan=10)/Dot1Q(vlan=20)/IP(dst="10.20.0.5")/TCP()) desde un puerto cuya native VLAN es 10.',
      'Enumerar VLANs antes: nmap --script broadcast-cisco-discovery o CDP/LLDP spoof (RED-015) para leer VTP/VLANs del switch.',
      'Explotar el canal one-way: inyección de tráfico hacia la VLAN objetivo (exploits sin callback, poisoning de servicios broadcast).',
    ],
    deteccion: {
      spl: 'index=net sourcetype=cisco:ios "DTP" OR "trunk" OR "802.1Q" OR "%EC-..." | stats count by port, vlan | where count > 3 | head 20',
      kql: '// fuera del alcance del EDR: correlar syslog del switch (DTP up, cambio port-mode) con la CMDB — un trunk que no está en el diseño es hallazgo',
    },
    mitigacion: [
      'switchport nonegotiate + switchport mode access en TODOS los puertos de usuario (mata el switch spoofing).',
      'Native VLAN dedicada y sin hosts: switchport trunk native vlan 999 + vlan dot1q tag native.',
      'Podar VLANs en trunks: switchport trunk allowed vlan solo las necesarias.',
      '802.1X en el puerto: sin autenticación no se negocia nada.',
    ],
    referencias: ['https://github.com/tomac/yersinia', 'https://scapy.readthedocs.io/', 'https://en.wikipedia.org/wiki/VLAN_hopping'],
  },
  {
    id: 'RED-010',
    nombre: 'SSL Stripping',
    alias: ['SSL strip', 'degradación HTTPS a HTTP', 'downgrade HTTPS'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: ['T1557'],
    descripcion_tecnica: 'Estando on-path, el atacante mantiene HTTPS con el servidor real pero sirve HTTP a la víctima: le entrega el sitio en claro y reenvía las credenciales al original cifradas. La víctima solo ve una URL http:// sin candado (si es que mira). La evolución moderna es el bypass de HSTS (sslstrip+/hstshijack de bettercap): abusa de la granularidad de HSTS (vale para el host exacto, no para subdominios no cubiertos por includeSubDomains) y de favicon/cookies cacheadas para mantener a la víctima en HTTP.',
    impacto_iam_soc: 'Robo de credenciales con el servidor 100% parcheado y sin un solo error de certificado: no hay TLS alert, no hay ssl_errors. La única pista del SOC son URLs http:// con patrones de login en los logs del proxy.',
    como_funciona: [
      'Posicionamiento on-path: bettercap -iface eth0 -cap arp.spoof (RED-002) o rogue AP (RED-011).',
      'Clásico: sslstrip -l 8080 con iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-ports 8080.',
      'Bypass de HSTS: caplet hstshijack de bettercap (dns.spoof + hstshijack) para redirigir peticiones a dominios parcialmente cubiertos.',
      'Capturar credenciales del flujo HTTP y continuar la sesión legítima contra el servidor real.',
    ],
    deteccion: {
      spl: 'index=proxy sourcetype=squid:access | regex url="^http://[^/]*(login|signin|auth|sso)" | stats count by src_ip, url | head 20',
      kql: 'DeviceNetworkEvents | where RemoteUrl startswith "http://" and RemoteUrl has_any ("login", "signin", "sso", "auth") | summarize count() by DeviceName, RemoteUrl | where count_ > 5 | take 20 // en redes modernas, un login por HTTP ya es alerta',
    },
    mitigacion: [
      'HSTS con includeSubDomains + preload en TODOS los dominios de autenticación (incluido el apex, no solo www).',
      'Cookies Secure + HttpOnly: aunque se degrade el canal, la sesión no viaja en claro.',
      'Redirigir HTTP→HTTPS en el edge Y servir HSTS en la primera respuesta HTTPS.',
      'Alerta de URLs http:// con patrones de login en proxy/DNS — señal de stripping en curso.',
    ],
    referencias: ['https://moxie.org/software/sslstrip/', 'https://www.bettercap.org/modules/ethernet/hstshijack/', 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security'],
  },
  {
    id: 'RED-011',
    nombre: 'Evil Twin / Rogue AP',
    alias: ['AP rogue', 'punto de acceso falso', 'karma attack', 'evil twin'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: [],
    descripcion_tecnica: 'AP no autorizado que anuncia el mismo SSID que el corporativo (con señal mayor o tras un deauth masivo, RED-012): los clientes que recuerdan la red se asocian solos. Variante karma: el AP responde a TODOS los probe requests con el SSID que cada cliente busca, haciéndose pasar por cualquier red conocida de la víctima. Propina: portal captivo clonado o MITM transparente con NAT hacia Internet.',
    impacto_iam_soc: 'MITM inalámbrico total: credenciales del portal corporativo, captura de tráfico y pivote a la LAN. En SOC: un segundo BSSID con el SSID corporativo o clientes que "cambian de celda" sin causa aparente.',
    como_funciona: [
      'Montar el AP falso: hostapd con el ESSID corporativo (o airbase-ng) + dnsmasq para DHCP y iptables NAT para que Internet funcione y nadie sospeche.',
      'Automatizado: wifiphisher (rogue AP + portal captivo clonado + deauth integrado).',
      'Forzar la reconexión: aireplay-ng --deauth 0 -e CORP -a <MAC_AP_real> (RED-012) — el cliente cae en el AP más cercano.',
      'Karma: hostapd configurado para responder a cualquier probe request (ssid_list vacío/dinámico) y cosechar los perfiles Wi-Fi de la víctima.',
    ],
    deteccion: {
      spl: 'index=wips sourcetype=aruba:wids OR sourcetype=cisco:wlc | stats dc(bssid) as aps by ssid | where aps > 1 | head 20 // mismo ESSID anunciado desde varios BSSID',
      kql: '// WIPS/WLC es la fuente (no el EDR): ingestar traps de nuevo BSSID, cambio de canal y fingerprint RF al SIEM',
    },
    mitigacion: [
      'WPA2/WPA3-Enterprise (802.1X) con validación estricta del certificado RADIUS en el cliente — sin ese check, el evil twin es gratis.',
      'WIPS/WIDS con fingerprint RF y alertas de BSSID duplicado o SSID corporativo fuera de los APs aprobados.',
      'PMF (802.11w) para que el deauth previo no sirva de palanca.',
      'NAC tras la WLAN: la VLAN de invitados nunca debe mezclarse con recursos internos sin 802.1X.',
    ],
    referencias: ['https://github.com/wifiphisher/wifiphisher', 'https://w1.fi/cgit/hostap/plain/hostapd/hostapd.conf', 'https://www.kismetwireless.net/'],
  },
  {
    id: 'RED-012',
    nombre: 'Desautenticación Wi-Fi',
    alias: ['deauth', 'deauth flood', 'desasociación 802.11', 'Wi-Fi deauth attack'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'Envío de tramas de gestión deauth/disassociate falsificadas (spoofeadas como AP→cliente o cliente→AP) para expulsar clientes de la red. Las tramas de gestión 802.11 históricamente van SIN cifrado: cualquiera en radio alcance puede falsificarlas. Suele ser palanca, no objetivo: preparar la reconexión a un evil twin (RED-011) o forzar el 4-way handshake y capturarlo para crackear WPA offline.',
    impacto_iam_soc: 'DoS inalámbrico intermitente ("el Wi-Fi se cae cada rato") o precursor silencioso de evil twin/cracking. En SOC: patrones de desconexión en el WLC (reason 7 repetida) correlacionados con la geolocalización de un mismo host.',
    como_funciona: [
      'Interfaz en monitor: airmon-ng start wlan0 y enumerar AP/cliente con airodump-ng wlan0mon.',
      'Deauth dirigido: aireplay-ng --deauth 10 -a <BSSID> -c <MAC_cliente> wlan0mon (o mdk4 wlan0mon d -B <BSSID> para el flood).',
      'Preparar evil twin: aireplay-ng --deauth 0 continuo mientras hostapd/wifiphisher espera la reconexión (RED-011).',
      'Handshake capture: deauth puntual + airodump-ng --write cap → aircrack-ng -w rockyou.txt cap-01.cap (o hashcat -m 22000 con PMKID).',
    ],
    deteccion: {
      spl: 'index=wips sourcetype=cisco:wlc OR sourcetype=meraki:event "Deauth" OR "Disassociation" | stats count by ap_mac, client_mac, reason | where count > 10 | head 20 // reason 7 repetida por cliente = firma',
      kql: '// no llega al EDR: correlar eventos de disconnect del WLC (razones 1/7 en ráfaga por cliente) y RSSI del atacante desde WIPS',
    },
    mitigacion: [
      '802.11w (PMF, Protected Management Frames): cifra deauth/disassoc — obligatorio en WPA3-SAE.',
      'Migrar a WPA3-SAE con PMF required en cuanto los clientes lo soporten.',
      'WIPS que detecte ráfagas de deauth y triangule la fuente por RSSI.',
      'No descartarlo como "problema de RF": correlacionar las caídas de cliente con eventos de seguridad.',
    ],
    referencias: ['https://www.aircrack-ng.org/', 'https://github.com/aircrack-ng/mdk4', 'https://en.wikipedia.org/wiki/IEEE_802.11w-2009'],
  },
  {
    id: 'RED-013',
    nombre: 'BGP Hijacking',
    alias: ['prefix hijacking', 'route hijack', 'route leak', 'anuncio de prefijo ilegítimo'],
    categoria: 'Red',
    severidad: 'Critical',
    mitre_attack: [],
    descripcion_tecnica: 'Un AS anuncia por BGP prefijos o rutas más específicas que no le pertenecen: el longest-prefix match del enrutamiento global desvía el tráfico hacia el AS del atacante, que puede inspeccionarlo (y reenviarlo encubierto), blackholearlo o devolverlo. Puede ser error de configuración (route leak) o deliberado. Caso canónico: Pakistan Telecom anuncia 208.65.153.0/24 (YouTube) en 2008 y YouTube desaparece de Internet durante horas.',
    impacto_iam_soc: 'Interceptación o blackhole a escala Internet de servicios propios o de terceros vía tu AS: impacto en disponibilidad (DoS), confidencialidad (reroute) y confianza. En SOC: cambios de origin AS/AS path en route monitoring y prefijos marcados RPKI INVALID.',
    como_funciona: [
      'Origen: router BGP propio o un router de terceros comprometido con network 203.0.113.0/24 y redistribute hacia peers upstream.',
      'Más específico gana: anunciar /24 cuando el legítimo es /23 → el tráfico global prefiere tu ruta.',
      'MITM selectivo: reenviar el tráfico al destino real tras inspección (solo rinde si TLS es débil o hay CA controlada).',
      'Observar la propagación con BGPlay/routeviews y medir el alcance antes de que te des-conecten.',
    ],
    deteccion: {
      spl: 'index=bgp sourcetype=bgpstream | stats dc(origin_as) as origins, values(origin_as) by prefix | where origins > 1 | head 20 // MOAS: un prefijo anunciado por varios AS',
      kql: '// ingestar feeds BGPStream/RIPE RIS al SIEM y alertar de RPKI INVALID o cambio de origin AS en prefijos propios',
    },
    mitigacion: [
      'RPKI: publicar ROAs de TODOS los prefijos propios y validar estrictamente (reject INVALID) en el borde.',
      'Filtrado de rutas con prefix-lists + validación IRR hacia peers y clientes.',
      'Suscripción a route monitoring (BGPStream, RIPE RIS, Cloudflare Radar) y alerta de MOAS/origin changes.',
      'Para servicios críticos: anycast y múltiples transit — que un hijack no sea un single point of failure.',
    ],
    referencias: ['https://bgpstream.caida.org/', 'https://www.ripe.net/publications/docs/ripe-680', 'https://en.wikipedia.org/wiki/BGP_hijacking'],
  },
  {
    id: 'RED-014',
    nombre: 'Redirección ICMP',
    alias: ['ICMP redirect attack', 'redirección de router ICMP', 'ICMP type 5 attack'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'El mensaje ICMP Redirect (tipo 5) permite a un router avisar a un host de que existe una ruta mejor. El atacante spoofea ese mensaje aparentando venir del gateway real para que la víctima actualice su tabla de rutas y envíe el tráfico a una IP del atacante — un MITM L3 barato sin tocar ARP. Los SO modernos ignoran redirects por defecto, así que el impacto real se concentra en redes legacy y hosts mal configurados.',
    impacto_iam_soc: 'MITM L3 sin ruido ARP: si el host acepta redirects, su tráfico sale por el atacante. En SOC, un ICMP tipo 5 visible en NetFlow ya es hallazgo: no debería existir en una red bien diseñada.',
    como_funciona: [
      'Spoofear el redirect: scapy con IP(src="192.168.1.1")/ICMP(type=5, code=1) apuntando la "mejor ruta" a la IP del atacante.',
      'Verificar en la víctima: route print / netstat -rn muestra la ruta específica vía la IP del atacante.',
      'Con reenvío activado en el atacante, el flujo pasa por él sin romper nada (MITM transparente).',
      'Escalar: sniffing del tráfico redirigido (RED-007) o SSL stripping (RED-010) sobre el flujo.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=stream:icmp "icmp_type=5" | stats count by src_ip, dest_ip | head 20 // cualquier observación es sospechosa: no es tráfico legítimo moderno',
      kql: '// EDR no parsea ICMP: exportar NetFlow/streams con filtro proto=1 type=5 al SIEM',
    },
    mitigacion: [
      'Bloquear ICMP redirects en el borde (no hay caso de uso legítimo en redes planas bien diseñadas).',
      'Deshabilitar el procesado en hosts: net.ipv4.conf.all.accept_redirects=0 y GPO equivalente en Windows.',
      'Diseño: una única salida por segmento — que no exista un "mejor camino" distinto por host.',
      'Rutas estáticas monitorizadas en hosts críticos para invalidar redirects.',
    ],
    referencias: ['https://www.rfc-editor.org/rfc/rfc792', 'https://scapy.readthedocs.io/'],
  },
  {
    id: 'RED-015',
    nombre: 'Suplantación CDP / LLDP',
    alias: ['CDP spoofing', 'LLDP spoofing', 'CDP flood attack', 'falsificación CDP'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'CDP y LLDP son protocolos de descubrimiento L2 con los que switches/routers se identifican entre sí. El atacante emite frames CDP/LLDP falsos haciéndose pasar por un router o phone switch: puede recibir la voice VLAN en configuraciones legacy, aparecer como dispositivo de confianza o (CDP flood) desbordar la tabla de vecinos/TCAM del switch y provocar DoS del plano de control.',
    impacto_iam_soc: 'Pivote a VLANs "de confianza" (voz, gestión) sin 802.1X, fuga de topología (VLANs, IPs, versiones de IOS) y DoS del control plane. En SOC: vecinos CDP/LLDP que no están en la CMDB y cambios de adyacencia no planificados.',
    como_funciona: [
      'Enumerar vecinos primero: Wireshark con filtro cdp / lldp, o lldpcli show neighbors (VLANs de voz y gestión, modelos, IOS).',
      'Spoof CDP: yersinia cdp -attack 0 (vecino falso con DeviceID "SW-CORE") o scapy con Dot3()/LLC()/SNAP()/CDPv2().',
      'CDP flood: yersinia cdp -attack 1 — miles de vecinos falsos por segundo contra la tabla de vecinos del switch.',
      'Escalar: heredar la VLAN de voz por CDP (configuraciones VVID legacy) o hacerse pasar por el gateway "vecino".',
    ],
    deteccion: {
      spl: 'index=net sourcetype=cisco:ios "CDP" OR "LLDP" | stats dc(device_id) as nbrs by port | where nbrs > 3 | head 20',
      kql: '// fuera del EDR: NMS leyendo CISCO-CDP-MIB / lldpNeighbors y alertando de vecinos no inventariados',
    },
    mitigacion: [
      'no cdp enable en puertos de acceso (CDP solo en trunks/uplinks) y LLDP tx/rx selectivo.',
      '802.1X con VLAN dinámica: la voice VLAN por MAB/certificado del phone, no por lo que el dispositivo anuncie por CDP.',
      'Control Plane Policing (CoPP) para limitar el impacto del CDP flood en el switch.',
      'Baseline de adyacencias CDP/LLDP y alerta de vecinos desconocidos (el NMS ya lee el MIB: es correlación pura).',
    ],
    referencias: ['https://github.com/tomac/yersinia', 'https://scapy.readthedocs.io/', 'https://www.ieee802.org/1/pages/802.1ab.html'],
  },
  {
    id: 'RED-016',
    nombre: 'Manipulación de STP (root bridge)',
    alias: ['STP attack', 'root bridge attack', 'ataque de spanning tree', 'BPDU spoofing'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'El atacante inyecta BPDUs con bridge priority superior (número más bajo) y se convierte en root bridge de la VLAN: el spanning tree recalcula los caminos para que el tráfico de la VLAN confluya en el puerto del atacante — sniffing pasivo de todo el segmento sin tocar ARP. Con BPDUs de topology-change en bucle consigue además DoS (flooding periódico de unknown-unicast y recálculos).',
    impacto_iam_soc: 'Captura de tráfico intra-VLAN masiva y DoS por recálculo continuo de topología. En SOC: "root bridge changed" en el switch de acceso y avisos TCN (topology change) que no cuadran con ninguna ventana de mantenimiento.',
    como_funciona: [
      'Claim de raíz: yersinia stp -attack 0 (BPDUs de configuración claiming root con prioridad 0/1 — gana al default 32768).',
      'El spanning tree reconverge: el tráfico de la VLAN pasa a confluir en el puerto del atacante.',
      'Capturar: tcpdump -i eth0 -w stp.pcap en el puerto convertido en raíz del path.',
      'Variante DoS: yersinia stp -attack 1 (BPDUs TCN en bucle) → flooding periódico y conectividad intermitente.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=cisco:ios "%SPANTREE" OR "root bridge" OR "topology change" | stats count by vlan, port | where count > 10 | head 20',
      kql: '// fuera del EDR: traps SNMP del STP (new root / TCN) al SIEM con baseline del root bridge por VLAN',
    },
    mitigacion: [
      'BPDU Guard en todos los puertos de acceso: cualquier BPDU entrante → err-disable (ataque terminado).',
      'Root Guard en uplinks hacia el backbone para fijar dónde puede estar la raíz.',
      'Prioridades de root y secondary explícitas por VLAN (menores que 32768) — que nadie la gane "por defecto".',
      'PortFast + storm control en puertos de usuario para amortiguar los TCN.',
    ],
    referencias: ['https://github.com/tomac/yersinia', 'https://scapy.readthedocs.io/'],
  },
  {
    id: 'RED-017',
    nombre: 'MAC Spoofing',
    alias: ['suplantación de MAC', 'MAC cloning', 'clonado de MAC', 'spoofing de dirección MAC'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'El atacante adopta la dirección MAC de un dispositivo legítimo (impresora, phone IP, portátil aprobado) para suplantar su identidad L2: saltarse port security, pasar MAB (MAC Authentication Bypass) en redes sin 802.1X o heredar una IP reservada por DHCP. Es el disfraz más simple del acceso a red y prerrequisito habitual de los ataques de envenenamiento L2.',
    impacto_iam_soc: 'Los controles basados en MAC (NAC/MAB, ACLs, filtros Wi-Fi, reservas DHCP) se vuelven decorativos: la "identidad de dispositivo" es copiable. En SOC: MAC flapping (misma MAC en dos puertos) y fingerprints DHCP que contradicen el inventario — una "impresora" que hace DHCP como Windows.',
    como_funciona: [
      'Identificar el MAC objetivo: escucha pasiva (Wireshark, logs DHCP) o consulta a la CMDB — las impresoras se enumeran por SNMP.',
      'Clonar: macchanger -m 00:11:22:33:44:55 eth0 (Linux) o NetworkAddress en el registro/propiedades de la NIC en Windows.',
      'Renovar la identidad: dhclient -r && dhclient eth0 para heredar la lease/IP del dispositivo suplantado.',
      'Acceder: pasar MAB en NAC/WLAN, heredar ACLs L2 o mantener el sticky MAC del puerto.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=cisco:ios "MACFLAP" OR "moved from" | stats count by mac, from_port, to_port | head 20 // la misma MAC en dos puertos = clon en la red',
      kql: '// EDR ve las conexiones, no el L2: usar DHCP fingerprinting (opción 55 del cliente) para contradecir la identidad suplantada',
    },
    mitigacion: [
      '802.1X real (EAP-TLS por certificado): la autenticación no puede depender de la MAC.',
      'Port security sticky + violation restrict y alertas de MAC flapping desde los switches.',
      'DHCP fingerprinting (opción 55) contrastado con el tipo de dispositivo declarado en CMDB.',
      'No usar filtros por MAC como control de seguridad en Wi-Fi (solo como telemetría).',
    ],
    referencias: ['https://github.com/alobbs/macchanger', 'https://en.wikipedia.org/wiki/MAC_spoofing'],
  },
  {
    id: 'RED-018',
    nombre: 'Interceptación física del enlace (tap / inline)',
    alias: ['network tap físico', 'tap pasivo', 'splitter de fibra', 'rogue device físico'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: [],
    descripcion_tecnica: 'Intercepción por hardware: splitter pasivo en fibra, tap inline entre router y switch, o un mini-dispositivo implantado en el cableado (con radio/4G para exfiltrar sin tocar la red corporativa). Los taps pasivos no emiten tráfico anómalo ni aparecen en ninguna tabla de red: la única firma es física (pérdidas ópticas, latencia) u organizativa (acceso a patch panels y salas). Es la categoría donde viven los implants de catálogos estatales (NSA ANT).',
    impacto_iam_soc: 'Cero telemetría de red: el SOC solo vería el síntoma (pérdida de señal, eventos de link) — si el diseño asume "si no está en logs, no pasó", este ataque lo niega. La respuesta real es cifrar por capas y controlar el plano físico.',
    como_funciona: [
      'Acceso físico a patch panel/sala: colocar un splitter de fibra 50/50 o tap eléctrico y llevar la copia al equipo de captura.',
      'Inline activo: insertar un dispositivo bidireccional transparente en el path — MITM completo, no solo escucha.',
      'Dispositivo autónomo: mini-AP o ARM con VPN 4G saliente escondido junto al cableado — exfiltración sin tocar la red.',
      'OpSec: retirar la instalación tras la ventana de captura para que la auditoría física no la encuentre.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=snmp "linkDown" OR "port status" | stats count by host, port | where count > 3 | head 20 // correlar flap de link con visitas/mantenimientos físicos',
      kql: '// nada visible desde el EDR: monitorizar potencia óptica (dBm) de los SFP y alarmar de deltas de pérdida',
    },
    mitigacion: [
      'Cifrar por capas: IPsec/WireGuard entre sedes y TLS end-to-end — un tap pasivo debe capturar solo ruido.',
      'Control físico: armarios y patch panels cerrados, precintos numerados y bitácora de acceso a salas técnicas.',
      'Monitorizar potencia óptica y latencia de enlaces críticos (SNMP/transponders) y alertar de saltos.',
      'Auditoría periódica del cableado y conteo de dispositivos en el path en sedes sensibles.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1040/', 'https://en.wikipedia.org/wiki/Network_tap'],
  },
  {
    id: 'RED-019',
    nombre: 'Ataque WPS (Pixie Dust / fuerza bruta de PIN)',
    alias: ['WPS Pixie Dust', 'WPS PIN brute force', 'WPS attack', 'offline WPS attack'],
    categoria: 'Red',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'WPS empareja con un PIN de 8 dígitos cuyo último dígito es checksum → el espacio real es 10^7, y el protocolo valida el PIN en dos mitades (4+3 dígitos), dejando ~11.000 intentos. La fuerza bruta online (reaver) tarda horas/días; Pixie Dust (pixiewps) explota generadores débiles de los nonces E-S1/E-S2 en chipsets concretos y rompe el PIN offline en segundos. Con el PIN se recupera la passphrase WPA2 aunque sea de 63 caracteres.',
    impacto_iam_soc: 'Compromiso de WLANs donde "la passphrase es larga y compleja": el PIN WPS invalida esa fortaleza. Relevante en sedes pequeñas/oficinas domésticas corporativas y APs de invitados. En SOC: bloqueos WPS reiterados y asociaciones fallidas en los APs.',
    como_funciona: [
      'Recon: wash -i wlan0mon enumera APs con WPS activo, versión del método y estado de lock.',
      'Fuerza bruta online: reaver -i wlan0mon -b <BSSID> -vv (horas; los APs antiguos no bloquean bien).',
      'Pixie Dust (offline): reaver -i wlan0mon -b <BSSID> -K 1 (o pixiewps con los datos M1-M3) — rompe chipsets Realtek/Broadcom antiguos en segundos.',
      'Con el PIN recuperado: reaver extrae la WPA passphrase — acceso permanente a la WLAN.',
    ],
    deteccion: {
      spl: 'index=wips sourcetype=cisco:wlc "WPS" AND ("lock" OR "failed") | stats count by ap_mac, client_mac | where count > 5 | head 20',
      kql: '// no llega al EDR: alertas del WLC/AP sobre bloqueos WPS y ráfagas de asociación fallida',
    },
    mitigacion: [
      'Desactivar WPS y verificarlo empíricamente (en muchos routers "desactivado" sigue activo por bug de firmware).',
      'Actualizar firmware: las versiones actuales aleatorizan E-S1/E-S2 y bloquean tras N intentos.',
      'En redes corporativas: WPA2/3-Enterprise con 802.1X — WPS no entra en el diseño.',
      'Auditar los APs con wash desde dentro: comprobar el estado real, no el de la consola.',
    ],
    referencias: ['https://github.com/wiire/pixiewps', 'https://github.com/t6x/reaver-wps-fork-t6x', 'https://www.kali.org/tools/reaver/'],
  },
  {
    id: 'RED-020',
    nombre: 'KRACK (WPA2)',
    alias: ['Key Reinstallation Attack', 'ataque de reinstalación de clave', 'KRACK attack'],
    categoria: 'Red',
    severidad: 'High',
    cve_ejemplo: ['CVE-2017-13077', 'CVE-2017-13078', 'CVE-2017-13082'],
    mitre_attack: [],
    descripcion_tecnica: 'Fallo de diseño del 4-way handshake de WPA2: el AP reenvía el mensaje 3 si no recibe confirmación y el cliente, al procesar el reenvío, REINSTALA la clave PTK ya en uso. La reinstalación resetea el nonce y el contador de replay → el cifrado reutiliza keystream. El atacante (MITM inalámbrico) fuerza y captura esos reenvíos para descifrar tráfico y, con TKIP/GCMP, incluso inyectarlo. Las estaciones Linux/Android eran especialmente vulnerables (reinstalaban la GTK del mensaje 3).',
    impacto_iam_soc: 'Rompe la confidencialidad del Wi-Fi sin conocer la passphrase: credenciales de portales internos y sesiones en claro quedan expuestas incluso con WPA2-Enterprise. Hoy el riesgo residual son clientes IoT/legacy sin parchear de 2017.',
    como_funciona: [
      'Posicionamiento MITM inalámbrico: replicar el canal de la víctima y bloquear el AP real (deauth selectivo, RED-012) para que los frames del handshake pasen por el atacante.',
      'Bloquear el mensaje 4 → el AP reenvía el mensaje 3 → el cliente reinstala la PTK y resetea nonce/replay counter.',
      'Capturar el tráfico re-cifrado con los mismos nonces y recuperar el keystream por reutilización (XOR de textos cifrados conocidos).',
      'Herramientas de Vanhoef: krackattacks-scripts (replay automatizado del handshake) y verificación de M3 retransmitidos con scapy.',
    ],
    deteccion: {
      spl: 'index=wips sourcetype=cisco:wlc "eapol" OR "handshake" | stats count by client_mac, stage | where like(stage, "%retransmit%") AND count > 3 | head 20 // reenvíos de M3 sin justificación',
      kql: '// EDR no ve 802.11: monitorizar desde WLC/WIPS y, sobre todo, inventariar clientes sin parche 2017 (IoT, Android 6.x)',
    },
    mitigacion: [
      'Parchear clientes y APs: la vulnerabilidad es sobre todo del CLIENTE (wpa_supplicant 2.4-2.6, Android 6.x) — auditar IoT y legacy.',
      'Deshabilitar TKIP y GCMP: solo AES-CCMP (con TKIP/GCMP KRACK permite inyección, no solo lectura).',
      'Migrar a WPA3-SAE (diseño distinto, no vulnerable a KRACK).',
      'Cifrado superior: VPN/TLS encima del Wi-Fi para datos sensibles — mitiga cualquier debilidad L2.',
    ],
    referencias: ['https://www.krackattacks.com/', 'https://nvd.nist.gov/vuln/detail/CVE-2017-13077', 'https://github.com/vanhoefm/krackattacks-scripts'],
  },
  {
    id: 'RED-021',
    nombre: 'Ataques Bluetooth (bluejacking / bluesnarfing / bluebugging)',
    alias: ['bluejacking', 'bluesnarfing', 'bluebugging', 'ataques BT de proximidad'],
    categoria: 'Red',
    severidad: 'Low',
    mitre_attack: [],
    descripcion_tecnica: 'Tres técnicas de proximidad (~10 m en clase 2, más con antena direccional): bluejacking = envío de vCards/mensajes no solicitados (molestia, sin acceso a datos); bluesnarfing = abuso del perfil OBEX en dispositivos vulnerables para LEER agenda, calendario o IMEI sin emparejar; bluebugging = acceso al perfil serial (comandos AT) que permite llamar, enviar SMS o leer datos sin autorización. Requieren firmware viejo o configuración abierta: los stacks modernos ya no exponen OBEX sin consentimiento.',
    impacto_iam_soc: 'Exfiltración puntual de contactos corporativos (bluesnarfing) o uso del terminal como plataforma (bluebugging: SMS premium, reenvío de OTP). Severidad baja por el prerrequisito de proximidad, pero viable en entornos de alta densidad (eventos, transporte).',
    como_funciona: [
      'Descubrir objetivos: hcitool scan o btscanner (dispositivos en modo discoverable).',
      'Bluejacking: obexftp/btscanner con envío de vCard a dispositivos cercanos.',
      'Bluesnarfing: bluesnarfer -b <MAC> -C 200 explota el canal OBEX Push sin autenticación y descarga la agenda.',
      'Bluebugging: conexión al perfil serial (canal RFCOMM SPP) y comandos AT: AT+CPBS="SM";AT+CPBR=1,50 (leer contactos de la SIM) o marcado de llamadas.',
    ],
    deteccion: {
      spl: 'index=mobile sourcetype=mdm "bluetooth" AND ("pairing" OR "denied" OR "failed") | stats count by device, addr | where count > 3 | head 20',
      kql: '// MDM/telemetría del SO: eventos de emparejamiento no solicitado y activación de perfiles OBEX/serial sin interacción del usuario',
    },
    mitigacion: [
      'No discoverable / pairing off salvo necesidad, con MDM que fuerce Bluetooth off o solo perfiles aprobados.',
      'Actualizar firmware/SO del stack BT (los chips y stacks de 2005-2010 eran el objetivo).',
      'Desactivar/limitar perfiles OBEX Push y SPP en entornos sensibles.',
      'Sensibilizar: aceptar vCards o emparejamientos "no pedidos" es el vector de entrada.',
    ],
    referencias: ['https://en.wikipedia.org/wiki/Bluejacking', 'https://en.wikipedia.org/wiki/Bluesnarfing', 'https://en.wikipedia.org/wiki/Bluebugging'],
  },
  {
    id: 'RED-022',
    nombre: 'Rogue DHCPv6 / mitm6 (SLAAC y DNS IPv6)',
    alias: ['mitm6', 'DHCPv6 rogue', 'SLAAC attack', 'RA spoofing', 'IPv6 MITM'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: ['T1557'],
    descripcion_tecnica: 'Windows prefiere IPv6 sobre IPv4 en dual-stack: si nadie desplegó IPv6, el host igual ACCEPTA anuncios. mitm6 responde a las consultas DHCPv6/SOLICIT y Router Advertisements del segmento, se proclama router IPv6 y entrega como DNS (opción 6 del DHCPv6 / RDNSS del RA) al atacante — el host le pregunta a ÉL por cualquier nombre. Con DNS IPv6 controlado, los nombres internos (adfs.corp, portales) resuelven a la IP del atacante, que fuerza autenticación HTTP y la reenvía hacia donde le interesa (el relay NTLM vive en Vulnerabilidades como técnica combinada). Complemento IPv6 del rogue DHCP (RED-005), no lo mismo: distinta pila, distintas detecciones y ni siquiera hace falta agotar el pool v4.',
    impacto_iam_soc: 'La mayoría de redes corporativas no usan IPv6 pero no lo BLOQUEAN: cada segmento dual-stack es una superficie de posicionamiento gratis. Para el SOC es un punto ciego clásico — nadie mira DHCPv6/RA en los logs del switch. Señales: tráfico ICMPv6 RA/DHCPv6 donde no hay despliegue IPv6 documentado, y hosts con DNS IPv6 nuevo de un día para otro.',
    como_funciona: [
      'mitm6 -d corp.local en el segmento: responde DHCPv6 SOLICIT y SLAAC/RA, asignando al atacante como gateway y DNS IPv6 de los hosts',
      'Los hosts (Windows default) consultan el DNS del atacante por IPv6 ANTES que el DNS v4 corporativo',
      'Responder con A/AAAA del .corp hacia la IP del atacante para los servicios de autenticación (ADFS/portal)',
      'El tráfico forzado al atacante alimenta relay de autenticación hacia LDAPS/HTTP (la técnica de relay completa: Vulnerabilidades) o captura directa',
    ],
    deteccion: {
      kql: 'DeviceNetworkEvents | where RemotePort in (547, 546) or RemoteIP startswith "fe80::" | where LocalIP !startswith "fe80::" | take 20 // tráfico DHCPv6 en una red sin IPv6 desplegado = rogue; + ICMPv6 type 134 (RA) desde MACs no-router',
      spl: 'index=net dest_port IN (546, 547) OR protocol=ipv6-icmp | stats count by src, dest, mac | where src NOT IN (known_routers) | sort - count',
      sigma: 'net_rogue_dhcpv6_ra (custom)',
    },
    mitigacion: [
      'Decisión explícita: si no hay IPv6, BLOQUEAR IPv6 en switches (RA guard / DHCPv6 guard / no ipv6 unicast-routing) y deshabilitar IPv6 en hosts donde no aplique',
      'Si hay IPv6: RA Guard y DHCPv6 Guard con whitelist de routers legítimos, como se hace con ARP inspection en v4',
      'Alertas de RA/DHCPv6 desde MACs no autorizadas y de hosts que cambian su DNS IPv6',
      'SMB signing y LDAP signing + NTLMv2 en LDAPS — cortan la utilidad del relay que mitm6 alimenta',
    ],
    referencias: ['https://blog.fox-it.com/2018/01/11/mitm6-compromising-ipv4-networks-via-ipv6/', 'https://github.com/fox-it/mitm6', 'https://attack.mitre.org/techniques/T1557/'],
  },
  {
    id: 'RED-023',
    nombre: 'Bypass de 802.1X / NAC',
    alias: ['NAC bypass', 'MAB abuse', 'MAC Authentication Bypass abuse', 'EAP-Logoff attack', 'guest VLAN escape'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: ['T1557'],
    descripcion_tecnica: 'El portátil se conecta a la red corporativa sin credenciales: en vez de romper 802.1X, se abusa de sus excepciones. (1) MAB: los dispositivos que no saben EAP (impresoras, cámaras) se autentican por MAC — el atacante suplanta la MAC de una impresora inventariada y entra con su perfil (a menudo con acceso amplio por VLAN de "dispositivos"). (2) EAP-Logoff: trama EAPOL-Logoff falsa que "desconecta" al legítimo dejando el puerto auth-open. (3) guest/quarantine VLAN: el perfil de invitado con acceso "limitado" que en la práctica deja pasar SMB/DNS/relays. (4) Hub dual-homed: un mini-hub oculto comparte la sesión 802.1X legítima con el equipo del atacante.',
    impacto_iam_soc: 'Ingreso físico/lógico sin tocar credenciales NI exploits: la inversión en NAC se anula con una MAC clonada. Para el SOC: la MISMA MAC (o MAC del fabricante HP* de impresora) en dos puertos/switches a la vez, cambios de MAC en un mismo puerto y EAPOL-Logoff en puertos de usuarios — telemetría que existe en los switches pero casi nunca llega al SIEM.',
    como_funciona: [
      'Recon del vecindario: CDP/LLDP y fingerprints DHCP para identificar impresoras/dispositivos MAB autorizados del segmento',
      'Clonar identidad: cambiar la MAC del portátil a la de la impresora inventariada (el NAC la reconoce y asigna su perfil/VLAN)',
      'En un puerto de usuario: enviar EAPOL-Logoff spoofeado para cerrar la sesión del legítimo o aprovechar un perfil guest/quarantine mal aislado',
      'Persistencia física: mini-hub entre la toma y el host legítimo comparte la sesión autenticada con un segundo equipo',
    ],
    deteccion: {
      kql: 'DeviceNetworkEvents | where MACAddress in (mac_inventory_printers) | summarize ports=dcount(DeviceName) by MACAddress, bin(TimeGenerated, 1h) | where ports > 1 | take 20 // misma MAC en dos sitios; + eventos del switch: dot1x auth-fail, EAPOL-Logoff, MAC flapping',
      spl: 'index=net | stats dc(port) as ports, dc(switch) as switches by mac, bin(_time, 1h) | where ports > 1 | sort - _time | head 20 // + dot1x_supplicant events Category="auth-fail"',
      sigma: 'nac_mab_duplicate_mac (custom)',
    },
    mitigacion: [
      'Per-VLAN assignment + ACLs para perfiles MAB (las impresoras no necesitan SMB ni RDP): el bypass hereda solo lo que el perfil permite',
      'Configurar dot1x: single-host por puerto, inactivity timer corto y manejo EAPOL-Logoff con re-autenticación — no dejar puerto auth-open tras logoff',
      'Ingesta al SIEM de eventos dot1x/MAB, ACL dinámica por dispositivo (no por clase), y alertas de MAC flapping/learn duplicado de los switches',
      'Guest/quarantine VLAN con deny-all intra-VLAN y solo egress a internet: un perfil de invitado que puede hablar SMB no es aislamiento',
    ],
    referencias: ['https://www.optiv.com/insights/source-code/blog/attacking-8021x', 'https://www.firewalls.com/blog/802-1x-bypass', 'https://en.wikipedia.org/wiki/MAC_authentication_bypass'],
  },
  {
    id: 'RED-024',
    nombre: 'Enumeración DNS (AXFR y subdominios)',
    alias: ['DNS recon', 'zone transfer attack', 'DNS enumeration', 'subdomain brute force', 'DNSSEC walking'],
    categoria: 'Red',
    severidad: 'Low',
    mitre_attack: ['T1590.002'],
    descripcion_tecnica: 'El DNS es un mapa del dominio digital antes de pisarlo: pedir AXFR a los servidores autoritativos (si están mal configurados responde con TODA la zona: hosts, IPs internas, nombres de servicio) y bruteforcar subdominios (dev., vpn., mail., staging.) contra resolvers públicos con diccionarios. NSEC/NSEC3 walking: si la zona firma con DNSSEC y no usa NSEC3+white-lie, se puede "caminar" la zona orden alfabético. Cada nombre descubierto alimenta phishing creíble, targeting VPN/portales y finding de superficie expuesta. Es la fase RECON externa previa a casi todo lo demás (complemento del port scanning RED-008 — este enumera nombres, no puertos).',
    impacto_iam_soc: 'La ventana de exposición externa en un solo comando: un AXFR abierto entrega la lista de activos que "nadie sabía que existía". Para el SOC externo apenas hay telemetría propia: se detecta por AXFR desde IPs no-slave hacia los autoritativos y picos de NXDOMAIN desde una misma IP en los resolvers públicos — o mejor: hazlo tú antes (recon de tu propio dominio) y financia el descubrimiento en CHANGE control.',
    como_funciona: [
      'dig @ns1.corp.com corp.com AXFR +noall +answer — si el autoritativo permite la transferencia a cualquiera, la zona entera cae',
      'Subdomain brute: dnsrecon -d corp.com -t brt -D wordlist o subfinder/amass pasivos (cert-transparency logs: crt.sh enumera nombres vistos en certificados)',
      'DNSSEC walking: walk una zona NSEC (rrsig démonstration) listando nombres existentes en orden lexicográfico',
      'Correlación pasiva: OSINT (crt.sh, censys, shodan) sobre los nombres descubiertos para mapear IPs/ports expuestos',
    ],
    deteccion: {
      kql: '// DNS propio (autoritativo): sumarizar AXFR permitidos vs deny por source; los permitidos solo desde IPs de slaves\nDnsEvents | where Name == "corp.com" and ResultData == "AXFR" | summarize cnt=count() by ClientIP | where ClientIP !in (slave_ips) | take 20 // y en resolver público: dc(Name) alto con ratio NXDOMAIN alto por IP',
      spl: 'index=dns | stats dc(query) as doms, count as total, eval(sum(status="NXDOMAIN")) as nx by src | where doms > 500 OR (nx / total) > 0.8 | sort - doms | head 20',
      sigma: 'dns_axfr_from_unauthorized (custom)',
    },
    mitigacion: [
      'AXFR: allow-list de slaves en TODOS los autoritativos (y probarlo: dig desde fuera debe fallar) — check de hardening básico y eterno',
      'DNSSEC con NSEC3 (o white-lie/minimization de datos) para cerrar el walking; en DNS interno, bloquear consultas recursivas externas',
      'Gobernanza de superficie: monitoreo externo continuo (crt.sh, amass pasivo contra tu propio dominio) y baja de registros muertos — nombres viejos son superficie y phishing creíble',
      'En resolvers públicos/cloud: rate-limit por IP y alertas de volumetría (los bruteforcers generan miles de NXDOMAIN)',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1590/002/', 'https://digi.ninja/projects/zonetransfer.php', 'https://crt.sh/'],
  },
  {
    id: 'RED-025',
    nombre: 'Envenenamiento de routing interior (RIP / OSPF / EIGRP)',
    alias: ['OSPF poisoning', 'RIP route injection', 'routing protocol attack', 'LSA spoofing', 'rogue router interior'],
    categoria: 'Red',
    severidad: 'High',
    mitre_attack: ['T1557'],
    descripcion_tecnica: 'El interior de la red confía en protocolos que (históricamente) no autentican: un atacante en el segmento habla RIP (broadcast 224.0.0.9, routers sin auth: aceptan rutas nuevas al instante) o se hace OSPF-vecino (224.0.0.5/6, hello + LSA) y publica LSAs que le dan la ruta preferida hacia el tráfico de las víctimas (0.0.0.0/0 o subnets específicas). EIGRP: hello sin auth → vecino falso → topología falsa. Es el MITM a escala de backbone (el mismo patrón del MITM on-path RED-001, pero por capa 3 interna) — distinto del BGP hijack (RED-013: exterior, entre AS).',
    impacto_iam_soc: 'Interceptar TODO el tráfico inter-sede/oficina sin tocar un solo host: la posición del atacante se convierte en el camino de la red. Detección: las rutas nuevas aparecen sin cambio registrado y las adjacencias OSPF cambian (vecino NUEVO de un día para otro desde una MAC no-router); en SIEM con syslog de routing (poca gente lo ingesta).',
    como_funciona: [
      'RIP: responder a los broadcasts 224.0.0.9 con rutas de métrica 1 hacia 0.0.0.0/0 desde una herramienta (Loki, scapy) — los routers RIP sin auth las instalan',
      'OSPF: capturar un hello (con área, autenticación si la hay — MD5 roto o inexistente), inyectarse como ABR y publicar LSAs type 5 (externas) que redirigen subnets a tu IP',
      'EIGRP: hello sin auth → formar adyacencia con el router legítimo y anunciar rutas con métrica ganadora',
      'Resultado: default/subnets enrutadas al atacante → sniffing + manipulación de todo lo que cruza (protocolos internos, LDAPS, SMB, herramientas de gestión)',
    ],
    deteccion: {
      kql: '// syslogs de routing (ingestarlos!): OSPF neighbor changes y rutas nuevas sin change ticket\nSyslogEvent | where Message contains "OSPF" and Message contains_any ("adjacency", "NEIGHBOR", "state change") | where Interface in (core) | take 20 // + rutas nuevas con origination externa (BGP no) fuera de ventana de cambio',
      spl: 'index=routing | search "OSPF-5-ADJCHG" OR "RIP" OR "EIGRP-NEIGHBOR" | stats count by host, _time | sort - _time | head 20 // y: route-table snapshots diff cada 15 min — rutas nuevas = alerta',
      sigma: 'routing_neighbor_change_unplanned (custom)',
    },
    mitigacion: [
      'Autenticación de protocolo en TODA la red interna: OSPF/EIGRP/RIPv2 con HMAC-SHA (no MD5 legacy) y áreas/tareas diseñadas — sin esto todo lo demás es cosmético',
      'Passive interfaces: los protocolos de routing NO deben hablar en segmentos de usuario/access — solo entre routers (ni hello ni updates en VLANs de oficina)',
      'Ingesta de routing logs al SIEM + baseline de adyacencias y rutas: alerta de vecino nuevo, LSA externa nueva y cambios de ruta fuera de ventana',
      'Filtrado L2-L3: bloquear 224.0.0.5/6/9/10 fuera de los enlaces core-distribution con ACLs en los switches — el hello del atacante nunca llega al router',
    ],
    referencias: ['https://www.hackerfactor.com/blog/index.php?/archives/29-Attacking-Routers.html', 'https://www.cisco.com/c/en/us/support/docs/ip/open-shortest-path-first-ospf/13703-22.html', 'https://attack.mitre.org/techniques/T1557/'],
  },
];
