// attacks/dos.ts — dataset de ataques DoS / DDoS (L3/L4/L7).
//
// 15 entradas con categoria: 'DoS': el umbrella DDoS (DOS-001) y sus
// vectores concretos — volumétricos (SYN/ICMP/UDP), de reflección y
// amplificación (DNS/NTP/memcached), de aplicación (HTTP flood, slow
// HTTP, NXDOMAIN, HTTP/2 rapid reset) y de parseo (XML bomb). Los
// ataques legacy (ping of death, teardrop, land) se mantienen como
// referencia histórica con severidad Low.
//
// Distinción clave para no duplicar conceptos: DOS-008 (HTTP flood) es
// VOLUMEN de peticiones; DOS-009 (slowloris/RUDY) son conexiones
// ABIERTAS con volumen mínimo. "Smurf" es alias/variante de ICMP flood
// (DOS-003), no una entrada propia.
//
// IDs: DOS-001..DOS-015. 100% offline — se concatena en ./index.ts.
// No usar `export default`.

import type { AttackInfo } from './types';

export const DOS_ATTACKS: AttackInfo[] = [
  {
    id: 'DOS-001',
    nombre: 'DoS / DDoS (botnets y vectores)',
    alias: ['DDoS', 'denegación de servicio distribuida', 'ataque volumétrico', 'botnet flood'],
    categoria: 'DoS',
    severidad: 'Critical',
    mitre_attack: ['T1498'],
    descripcion_tecnica: 'Entrada UMBRELLA: degradar o tumbar un servicio saturando un recurso, desde una fuente (DoS) o desde miles coordinadas por botnet / DDoS-for-hire (DDoS). Tres familias de vectores: volumétricos L3/L4 (SYN flood DOS-002, ICMP/UDP DOS-003/004, amplificación DNS/NTP/memcached DOS-010..012), de aplicación L7 (HTTP flood DOS-008, slow HTTP DOS-009, NXDOMAIN DOS-013, rapid reset DOS-014) y de protocolo/parseo (XML bomb DOS-015). La reflección usa spoofing de IP origen para que la respuesta de terceros vaya a la víctima; botnet canónica: Mirai (IoT).',
    impacto_iam_soc: 'Pérdida de disponibilidad de servicios públicos, coste de egress y daño colateral en infra compartida. Es el incidente que se anuncia solo (NetFlow fuera de baseline); la tarea real del SOC es tipificar el vector rápido para elegir mitigación y no quemar el enlace interno.',
    como_funciona: [
      'Reclutar/rentar capacidad: botnet IoT (Mirai y derivados), booters/stressers o amplificadores abiertos (Shodan: product:"memcached", open resolvers).',
      'Elegir vector según objetivo: volumétrico para el enlace, L7 para tumbar la app sin pasar del CDN, amplificación para multiplicar ancho de banda.',
      'Reflección con spoofing: enviar consultas con src = IP de la víctima hacia miles de amplificadores (DOS-010/011/012).',
      'Sostener y rotar: los booters alternan vectores y fuentes cada pocos minutos — las campañas de vector único son cada vez menos frecuentes.',
    ],
    deteccion: {
      spl: 'index=netflow | stats sum(bytes) as b, dc(dest_ip) as targets by src_ip | where b > 10000000000 | sort - b | head 20 // violación brutal del baseline de tráfico',
      kql: 'DeviceNetworkEvents | summarize count(), dcount(RemoteIP) by DeviceName | where count_ > 100000 | take 20 // cuando la víctima es interna: flood hacia un servidor propio',
    },
    mitigacion: [
      'Scrubbing/CDN delante de los servicios públicos (anti-DDoS del ISP o proveedor) — absorber lo volumétrico lejos del origen.',
      'BCP38/anti-spoofing en el borde: tu red no debe emitir ni aceptar tráfico con origen inválido.',
      'Rate limiting por IP/ASN en el edge y WAF para vectores L7 (CAPTCHA/JS challenge para bots simples).',
      'Runbook DDoS: contactos del ISP/upstream, blackhole temporal de la IP atacada, anycast para dispersar.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/', 'https://www.cloudflare.com/learning/ddos/what-is-a-ddos-attack/'],
  },
  {
    id: 'DOS-002',
    nombre: 'SYN Flood',
    alias: ['inundación SYN', 'half-open connection flood', 'SYN attack'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1498.001'],
    descripcion_tecnica: 'El atacante abre conexiones TCP y nunca responde al SYN-ACK: el servidor acumula miles de half-open connections que llenan la backlog queue y consumen memoria/handling mientras rechaza clientes legítimos. Con IP spoofing la fuente es inbloqueable; con botnet, cada zombie abre y abandona conexiones reales. Los SYN cookies rompen el DoS clásico: el servidor codifica el estado del handshake en la secuencia TCP y no reserva memoria por conexión incompleta.',
    impacto_iam_soc: 'El servicio TCP rechaza conexiones legítimas con el proceso vivo y la CPU baja: diagnóstico confuso si solo se miran métricas de servidor. Señal inequívoca: cola de SYN_RECV llena y ratio SYN/SYN-ACK desequilibrado en los flows.',
    como_funciona: [
      'Single-source con spoofing: hping3 -S --flood --rand-source -p 443 203.0.113.10 (miles de SYN/s sin completar handshake).',
      'Distribuido: cada bot abre sockets reales y los abandona tras el SYN-ACK — sin spoofing, más difícil de filtrar.',
      'Low-rate: hping3 -S --rate 50 por fuente para colarse bajo los rate-limits por IP y acumular backlog igualmente.',
      'Medir el umbral en el objetivo: ss -s y netstat -ant | grep SYN_RECV | wc -l antes/después.',
    ],
    deteccion: {
      spl: 'index=netflow proto=tcp tcp_flags="S" | stats sum(packets) as syn, dc(src_ip) as srcs by dest_ip, dest_port | where syn > 100000 AND syn/srcs > 500 | head 20 // SYN por fuente desproporcionado',
      kql: 'DeviceNetworkEvents | where Protocol == "TCP" and ActionType == "ConnectionFailed" | summarize count() by DeviceName, RemoteIP | where count_ > 10000 | take 20 // miles de fallos = zombie o reflejo',
    },
    mitigacion: [
      'SYN cookies activos (net.ipv4.tcp_syncookies=1; SYN proxy en F5/Cloudflare) — sin cookies, cualquier backlog cae.',
      'Backlog amplio + tcp_synack_retries bajo: liberar half-opens antes.',
      'Anti-spoofing (BCP38) y SYN proxy en el firewall para la variante spoofeada.',
      'CDN/LB delante: que los backends no vean nunca el SYN original.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/001/', 'https://www.cloudflare.com/learning/ddos/syn-flood-attack/', 'https://linux.die.net/man/8/hping3'],
  },
  {
    id: 'DOS-003',
    nombre: 'ICMP Flood y Smurf',
    alias: ['inundación ICMP', 'ping flood', 'smurf attack', 'ICMP amplificado por broadcast'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1498.002'],
    descripcion_tecnica: 'ICMP flood: saturar el enlace o la CPU de red de la víctima con echo requests (importa el pps, no los bytes — cada paquete lo procesa el router/firewall). Smurf (variante histórica de reflección): echo request spoofeado con origen = víctima hacia la dirección de broadcast de redes terceras; todos los hosts del segmento responden a la víctima (amplificación por número de hosts). Las redes modernas tienen directed-broadcast desactivado, así que el smurf puro es raro; el ICMP flood directo sigue sirviendo para saturar enlaces pequeños y freír appliances inline.',
    impacto_iam_soc: 'Saturación del acceso WAN y CPU en firewalls/IPS que procesan cada ICMP. En SOC: pps ICMP muy por encima del baseline; el smurf se ve como ICMP entrante simultáneo desde miles de fuentes distintas.',
    como_funciona: [
      'Flood directo: hping3 --icmp --flood --rand-source 203.0.113.10 (o ping -f -s 1400 como root).',
      'Smurf: scapy sr(IP(src="IP_victima", dst="10.0.0.255")/ICMP()) hacia broadcasts de redes con directed-broadcast activo.',
      'Variantes caras de responder: ICMP timestamp/mask/information requests que generan más procesado que la consulta.',
      'Medir: % de pps ICMP sobre el total del enlace (sar/netflow) — por encima del 20-30% ya es anómalo.',
    ],
    deteccion: {
      spl: 'index=netflow proto=icmp | stats sum(packets) as pps, dc(src_ip) as srcs by dest_ip | where pps > 100000 | sort - pps | head 20',
      kql: 'DeviceNetworkEvents | where Protocol == "ICMP" | summarize count() by RemoteIP | where count_ > 10000 | take 20 // si el host es la víctima, agrupar por LocalIP',
    },
    mitigacion: [
      'Rate-limit ICMP en el borde y filtrar directed-broadcast en routers (no ip directed-broadcast).',
      'BCP38 anti-spoofing: el smurf y la reflección ICMP requieren poder falsificar el origen.',
      'Permitir solo el ICMP necesario (echo, unreachable, frag-needed) con límite de pps.',
      'Verificar que firewall/IPS limitan el estado ICMP: si procesan cada paquete, mueren antes que el enlace.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/002/', 'https://www.cloudflare.com/learning/ddos/smurf-ddos-attack/'],
  },
  {
    id: 'DOS-004',
    nombre: 'UDP Flood',
    alias: ['inundación UDP', 'UDP frag attack', 'ataque de fragmentación UDP'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1498'],
    descripcion_tecnica: 'Ráfagas de datagramas UDP a puertos aleatorios del objetivo: el host responde con ICMP port unreachable (coste por paquete) y las tablas de estado/CPU de los firewalls sufren más que el propio enlace. Variante frag attack: paquetes UDP fragmentados (o con fragmentos superpuestos) que fuerzan reassembly en el path y saltan filtros que solo inspeccionan el primer fragmento. Sin conexión que cerrar, no hay "connection failed" que contar: el DoS es puro volumen/procesado.',
    impacto_iam_soc: 'Saturación con procesos sanos y CPU de red alta — de nuevo el "todo parece OK pero no responde". La variante fragmentada es además evasión de IPS que no reensambla. En SOC: pps UDP con dc(dest_port) alto y tormenta de ICMP unreachable.',
    como_funciona: [
      'Flood básico: hping3 --udp --flood --rand-source -p 53 203.0.113.10 (o nping --udp --rate 1000000).',
      'Puertos aleatorios: hping3 --udp --flood -s 1024:65535 para multiplicar los port unreachable del host.',
      'Frag attack: scapy fragment(IP(dst="203.0.113.10", flags="MF")/UDP()/Raw(load="A"*1480), fragsize=8) — el filtro del path solo ve fragmentos.',
      'Amplificar: UDP es la capa de todos los vectores de reflección (DNS/NTP/memcached — DOS-010..012).',
    ],
    deteccion: {
      spl: 'index=netflow proto=udp | stats sum(packets) as pps, dc(dest_port) as ports by dest_ip | where pps > 500000 AND ports > 100 | head 20',
      kql: 'DeviceNetworkEvents | where Protocol == "UDP" | summarize dcount(RemotePort) as ports, count() by RemoteIP | where count_ > 50000 and ports > 100 | take 20',
    },
    mitigacion: [
      'Exponer solo los puertos UDP publicados; el resto DROP silencioso (el unreachable también cuesta).',
      'Rate limiting por IP/ASN y timeouts de conntrack UDP cortos.',
      'IPS/LB que reensamblan fragmentos antes de filtrar (o drop-fragments en el borde si el servicio no los necesita).',
      'Scrubbing/CDN para lo volumétrico: la válvula de escape es capacidad, no tuning fino.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/', 'https://www.cloudflare.com/learning/ddos/udp-flood-attack/'],
  },
  {
    id: 'DOS-005',
    nombre: 'Ping of Death',
    alias: ['PoD', 'ping de la muerte', 'ICMP oversized'],
    categoria: 'DoS',
    severidad: 'Low',
    mitre_attack: [],
    descripcion_tecnica: 'ICMP echo con carga tal que el paquete IP ensamblado supera los 65.535 bytes teóricos del campo de longitud total: las pilas TCP/IP de 1995-1997 (Windows 95/NT, Linux viejo, routers) hacían overflow del buffer de reassembly y colgaban o reiniciaban. Mito hoy: cualquier stack moderno descarta el paquete oversized. Se conserva como lección de que el reassembly es código que corre antes de cualquier validación (misma clase de bug que Teardrop, DOS-006).',
    impacto_iam_soc: 'Relevancia operativa casi nula; valor forense/educativo. Si aparece como hallazgo en un audit, es la señal de que hay appliances embebidos abandonados (impresoras, PLC, IoT) con stacks sin parchear en algún rincón.',
    como_funciona: [
      'Clásico: ping -s 65510 203.0.113.10 (Linux fragmenta la carga y el objetivo de 1996 reensambla más de 64 KB).',
      'Equivalente moderno: scapy fragment(IP()/ICMP()/Raw(load="A"*66000)) para probar el reassembly de un appliance.',
      'Verificar en lab: el objetivo legacy se cuelga o reinicia al reensamblar.',
      'Hoy solo afecta a equipos embebidos sin parche desde hace 20 años.',
    ],
    deteccion: {
      spl: 'index=net sourcetype=stream:icmp | stats max(length) as max_len by src_ip | where max_len > 65535 | head 10 // cualquier ocurrencia = legacy presente',
      kql: '// EDR no parsea tamaño IP: exportar desde firewall/IPS con filtro length>65535 o fragment-offset anómalo',
    },
    mitigacion: [
      'Parchear (hecho por defecto desde 1997-98 en cualquier stack soportado).',
      'Inventariar y reemplazar dispositivos embebidos con stacks antiguos (PLC, impresoras viejas).',
      'Filtrar ICMP oversized/fragmentado en el borde si hay segmentos legacy.',
      'En auditoría: probar reassembly con fragmentos superpuestos (Teardrop-like), no confiar en "el ping grande".',
    ],
    referencias: ['https://www.cloudflare.com/learning/ddos/ping-of-death-attack/', 'https://en.wikipedia.org/wiki/Ping_of_death'],
  },
  {
    id: 'DOS-006',
    nombre: 'Teardrop',
    alias: ['ataque teardrop', 'fragmentos IP superpuestos', 'overlapping fragments attack'],
    categoria: 'DoS',
    severidad: 'Low',
    cve_ejemplo: ['CVE-1999-0258'],
    mitre_attack: [],
    descripcion_tecnica: 'Envío de fragmentos IP con offsets superpuestos o incoherentes: al reensamblar, los kernels de 1997-1998 (Windows 95/NT, Linux < 2.0.32) movían memoria con longitudes incoherentes y colgaban. La variante newtear siguió tirando parches parciales. Hoy sigue siendo relevante contra appliances que reensamblan en firmware (impresoras, concentradores VPN antiguos): el reassembly es código complejo que se ejecuta antes de cualquier validación de aplicación.',
    impacto_iam_soc: 'Igual que PoD (DOS-005): riesgo residual concentrado en appliances legacy. No hay telemetría de reassembly en el SOC: la señal es el crash/reboot inexplicable del appliance más un pcap con offsets incoherentes en la ventana del incidente.',
    como_funciona: [
      'Clásico: teardrop 203.0.113.10 (herramienta de 1997) — dos fragmentos ICMP/UDP con offsets solapados.',
      'scapy: fragment(IP(dst="x")/UDP()/Raw(load="A"*2000), fragsize=1400) y reescribir el offset del segundo fragmento para solaparlo.',
      'newtear/teardrop2: variantes con offset solapado distinto que burlaban parches parciales de 1997.',
      'Post-crash: captura de la ventana para confirmar la firma (fragmentos del mismo ip_id con offsets incoherentes justo antes del reinicio).',
    ],
    deteccion: {
      spl: 'index=net sourcetype=stream:ip frag_offset>0 | stats count, values(frag_offset) as offs by src_ip, ip_id | where count > 1 | head 20 // más de un fragmento por id: revisar solapamiento',
      kql: '// sin telemetría EDR: correlar reboot/crash de appliance con pcap de la ventana (offsets incoherentes)',
    },
    mitigacion: [
      'Parchear kernels y appliances (validar coherencia de offsets antes de reensamblar).',
      'Firewall/IPS con drop de overlapping fragments (por defecto en la mayoría de equipos actuales).',
      'Reemplazo de embebidos sin soporte (inventario de "cosas con IP y firmware viejo").',
      'Ring buffer de pcaps en appliances críticos para poder confirmar la firma tras un crash.',
    ],
    referencias: ['https://en.wikipedia.org/wiki/Teardrop_attack', 'https://www.cve.org/CVERecord?id=CVE-1999-0258'],
  },
  {
    id: 'DOS-007',
    nombre: 'Land Attack',
    alias: ['ataque land', 'paquete land', 'src=dst attack'],
    categoria: 'DoS',
    severidad: 'Low',
    mitre_attack: [],
    descripcion_tecnica: 'Paquete SYN (u otro) con IP y puerto ORIGEN idénticos a los de DESTINO (p. ej. de 203.0.113.10:80 hacia 203.0.113.10:80). Las pilas TCP/IP de 1997 (Windows 95/NT, BSD, Cisco IOS) entraban en bucle contestándose a sí mismas hasta congelarse. Totalmente mitigado en stacks actuales y atrapado por cualquier filtro anti-spoofing; su valor residual es como test de sanidad del filtrado de borde.',
    impacto_iam_soc: 'Cero como amenaza, útil como canary: si un paquete src=dst cruza el borde hasta un host interno, el anti-spoofing no existe. No genera eventos de seguridad: solo el firewall/IPS lo reporta si el drop está logado.',
    como_funciona: [
      'Clásico: land 203.0.113.10 80 (herramienta original de 1997) o hping3 -S --flood -s 80 -p 80 -a 203.0.113.10 203.0.113.10.',
      'scapy: sr(IP(src="203.0.113.10", dst="203.0.113.10")/TCP(sport=80, dport=80, flags="S")).',
      'Test de filtrado: si el host responde (SYN-ACK a sí mismo = bucle), el borde no valida spoofing.',
      'Variante: ICMP echo src=dst contra routers legacy con el mismo efecto de loop.',
    ],
    deteccion: {
      spl: 'index=netflow | where src_ip == dest_ip | stats count by src_ip, dest_port | head 10 // debe devolver 0 filas; si no, el anti-spoofing del borde está roto',
      kql: 'DeviceNetworkEvents | where RemoteIP == LocalIP | take 10 // tráfico "hacia sí mismo" = regla de filtrado que falla',
    },
    mitigacion: [
      'Anti-spoofing (BCP38/uRPF) en el borde: si el src=dst se filtra, no existe ataque.',
      'Parcheo (implícito desde hace 20 años en cualquier stack soportado).',
      'Usarlo como test periódico de firewalls: un paquete land debe morir en el borde.',
      'Descartar en ACL el tráfico entrante cuyo origen sea la propia IP pública.',
    ],
    referencias: ['https://en.wikipedia.org/wiki/Land_attack', 'https://www.rfc-editor.org/rfc/rfc2827'],
  },
  {
    id: 'DOS-008',
    nombre: 'HTTP Flood',
    alias: ['inundación HTTP', 'GET flood', 'POST flood', 'Layer 7 DDoS'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1499.002'],
    descripcion_tecnica: 'Miles de peticiones HTTP de apariencia legítima (GET de recursos pesados, POST de formularios, llamadas a endpoints caros como búsquedas o login) desde una botnet, con el objetivo de agotar CPU/BD/pool de conexiones del backend SIN saturar el ancho de banda: cabe en 100 Mbps y tumba igual. Distinguir de DOS-009: aquí el patrón es VOLUMEN de peticiones, no conexiones abiertas a goteo.',
    impacto_iam_soc: 'Aplicación caída con el enlace y el LB sanos; el coste se traslada a backend y a terceros (API de pago, pasarelas de búsqueda). En SOC: requests/s por IP/ASN sobre baseline, URLs repetidas con parámetros aleatorios (cache-busting) y poca variedad de user-agents.',
    como_funciona: [
      'Simple: hulk.py o GoldenEye http://portal.ejemplo/ — URLs aleatorias contra el mismo handler dinámico.',
      'Orientado a coste: repetir GET /search?q=<random> (endpoint caro) o POST de login (BCrypt carísimo por petición).',
      'Distribución: HOIC/LOIC en modo HTTP con scripts de boost o botnet real con requests espaciados.',
      'Evadir la cache: parámetros aleatorios (?cb=123456) para que cada petición sea MISS y llegue al backend.',
    ],
    deteccion: {
      spl: 'index=web sourcetype=access_combined | stats count as reqs, dc(useragent) as uas, dc(uri_path) as paths by client_ip | where reqs > 500 AND uas < 5 | head 20 // muchas peticiones, poca variedad',
      kql: 'AppHttpRequest | summarize count(), dcount(UserAgent) by SourceIp | where count_ > 500 and dcount_UserAgent < 3 | take 20',
    },
    mitigacion: [
      'WAF + rate limiting por IP/ASN con JS challenge o CAPTCHA tras umbral.',
      'Cachear agresivamente y separar estático de dinámico: que el flood muera en el CDN.',
      'Cuotas y colas en los endpoints caros (búsqueda, reportes, login) por cliente/API key.',
      'Bot management (fingerprint TLS/HTTP/JS) para discriminar bots de usuarios reales automáticamente.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1499/002/', 'https://www.cloudflare.com/learning/ddos/http-flood-ddos-attack/', 'https://github.com/jseidl/GoldenEye'],
  },
  {
    id: 'DOS-009',
    nombre: 'Slowloris / R-U-Dead-Yet (slow HTTP)',
    alias: ['slowloris', 'slow HTTP attack', 'RUDY', 'low-and-slow attack'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1499'],
    descripcion_tecnica: 'Familia low-and-slow: agotar las CONEXIONES ABIERTAS del servidor con un volumen de tráfico mínimo (kb/s). Slowloris abre N conexiones y envía cabeceras HTTP a cuentagotas (nunca envía el \\r\\n\\r\\n final): cada conexión queda "a medias" ocupando un slot del pool de threads. R-U-Dead-Yet (RUDY) hace POST con Content-Length enorme y transmite el body a bytes/s. Basta un solo host y el tráfico no dispara ninguna alerta de volumen.',
    impacto_iam_soc: 'Servidor web saturado con 0 bytes/s anómalos y CPU normal: el clásico "no hay evidencia de ataque" en los dashboards de red. La señal está en el servidor: conexiones longevas con pocos bytes y peticiones nunca completadas (timeouts de request).',
    como_funciona: [
      'Cabeceras a goteo: slowhttptest -c 1000 -H -i 15 -r 200 -t GET -u https://portal.ejemplo -x 30 (mil conexiones enviando una cabecera cada 15 s).',
      'RUDY: slowhttptest -c 500 -B -i 110 -r 200 -t POST -u http://portal.ejemplo/form -b 2000 (POST de 2 MB a 1 byte/s).',
      'slowloris.py original: python3 slowloris.py -s 500 portal.ejemplo (500 sockets con headers parciales y keep-alive).',
      'Comprobar el efecto en el target: conexiones establecidas con recv de 0 bytes y pool de threads agotado (ss -s, worker states).',
    ],
    deteccion: {
      spl: 'index=lb sourcetype=f5:ltm OR sourcetype=nginx:plus | stats avg(duration) as dur, count by client_ip | where dur > 60 AND count > 50 | head 20 // conexiones longevas con bajo throughput',
      kql: 'AppHttpRequest | summarize avg(ResponseTimeSeconds) as t, count() by SourceIp | where t > 30 and count_ > 50 | take 20 // peticiones nunca completadas + timeouts',
    },
    mitigacion: [
      'Timeouts cortos: mod_reqtimeout (Apache), client_header_timeout/client_body_timeout (nginx) — 20-30 s, no minutos.',
      'Límite de conexiones por IP y tamaño máximo de cabecera/body con descarte temprano del body lento.',
      'Reverse proxy/LB delante: que los backends solo reciban peticiones COMPLETAS.',
      'Alerta específica: peticiones con duración > P99 por IP — el low-and-slow solo se ve midiendo tiempo, no bytes.',
    ],
    referencias: ['https://github.com/gkbrk/slowloris', 'https://github.com/shekyan/slowhttptest'],
  },
  {
    id: 'DOS-010',
    nombre: 'Amplificación DNS',
    alias: ['DNS amplification', 'reflejo DNS', 'DNS reflection attack', 'abuso de open resolvers'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1498.002'],
    descripcion_tecnica: 'Reflección + amplificación: el atacante envía consultas DNS con la IP de la víctima como origen (spoofing) hacia miles de open resolvers; cada uno responde directamente a la víctima. Con consultas de respuesta grande (type ANY sobre registros TXT enormes, zonas firmadas con DNSSEC) la respuesta puede ser 50-70× la consulta: 1 Gbps de consultas se convierte en 50-70 Gbps de respuestas. La víctima ve "Internet entero" atacándola; el atacante nunca recibe el tráfico de ataque.',
    impacto_iam_soc: 'Dos caras: como víctima, el enlace público muere con tráfico DNS aparentemente legítimo de miles de resolutores; como origen (open resolver propio), tu infra amplifica ataques a terceros y acaba en blocklists. En SOC: ratio bytes respuesta/consulta anómalo en UDP 53 y respuestas hacia IPs que nunca preguntaron.',
    como_funciona: [
      'Encontrar amplificadores: scans masivos (Shodan "recursion: true") o verificar uno a uno con dig @<IP> ripe.net ANY.',
      'Medir el factor: dig @<resolver> ripe.net ANY devuelve ~3 KB por una consulta de ~60 bytes.',
      'Reflejar: scapy/hping3 con IP(src="IP_victima")/UDP(sport=53, dport=53)/DNS(qr=0, qname="ripe.net", qtype="ANY") hacia la lista de resolutores.',
      'Los booters industrializan el paso 3 con listas de amplificadores refrescadas a diario.',
    ],
    deteccion: {
      spl: 'index=netflow proto=udp dest_port=53 | stats sum(bytes_in) as q, sum(bytes_out) as r by src | eval ratio=r/q | where ratio > 2 | head 20 // tu resolver respondió mucho más de lo que le preguntaron',
      kql: 'DeviceNetworkEvents | where LocalPort == 53 and Protocol == "UDP" | summarize count() by RemoteIP | where count_ > 1000 | take 20 // respuestas a consultas que ese host nunca hizo',
    },
    mitigacion: [
      'Cerrar la recursión abierta: allow-recursion/access-control solo a clientes internos (BIND/unbound).',
      'Response Rate Limiting (RRL) en resolutores y autoritativos.',
      'BCP38 en tu AS: sin spoofing no hay reflección — mitigación de ecosistema.',
      'Como víctima: anycast + scrubbing y bloquear UDP 53 entrante hacia IPs que no publican DNS.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/002/', 'https://www.cloudflare.com/learning/ddos/dns-amplification-attack/'],
  },
  {
    id: 'DOS-011',
    nombre: 'Amplificación NTP (monlist)',
    alias: ['NTP amplification', 'monlist attack', 'reflejo NTP', 'amplificación UDP 123'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1498.002'],
    descripcion_tecnica: 'El comando monlist (modo 7, GET_MONLIST) de ntpd antiguos devuelve los últimos 600 clientes que consultaron al servidor: hasta ~200-500 paquetes UDP de respuesta por UNA consulta de 8 bytes → amplificación típica de 556×. El atacante spoofea la IP de la víctima en la consulta y cientos de servidores NTP públicos le envían las listas enteras. Corregido en ntpd 4.2.7 (monlist deshabilitado por defecto, CVE-2013-5211), pero quedan servidores viejos en inventario.',
    impacto_iam_soc: 'Fue uno de los mayores generadores de DDoS de 2013-2014 (ataques de 100-400 Gbps con botnets pequeñas). Como origen: tu NTP público antiguo amplifica ataques a terceros; como víctima: el enlace muere con tráfico UDP 123 de miles de fuentes. En SOC: pps en UDP 123 y ratio respuesta/consulta de ~500×.',
    como_funciona: [
      'Verificar si un NTP es vulnerable: ntpdc -c monlist <IP> (si devuelve la lista, amplifica) o nmap -sU -p 123 --script ntp-monlist <IP>.',
      'Medir el factor: comparar bytes de la consulta (8) contra la respuesta (lista de 600 entradas fragmentada).',
      'Reflejar: paquetes mode 7 con opcode GET_MONLIST y src spoofeado hacia miles de NTP públicos vulnerables.',
      'Escalar con listas de amplificadores obtenidas por scans masivos (Shodan + verificación monlist).',
    ],
    deteccion: {
      spl: 'index=netflow proto=udp dest_port=123 | stats sum(packets_out) as pkts, dc(dest_ip) as peers by src | where pkts/peers > 10 | head 20 // un paquete entrante genera decenas salientes',
      kql: 'DeviceNetworkEvents | where RemotePort == 123 and Protocol == "UDP" | summarize count() by RemoteIP | where count_ > 100 | take 20 // ráfagas de decenas de paquetes por fuente = monlist',
    },
    mitigacion: [
      'Actualizar ntpd a ≥4.2.7 o migrar a chrony (monlist no existe: API distinta).',
      'restrict default noquery kod en ntpd.conf: el servidor público solo debe servir tiempo (mode 3).',
      'BCP38/anti-spoofing como mitigación de ecosistema.',
      'Rate limiting de UDP 123 en el borde si no eres NTP público (nadie legítimo lo necesita hacia ti).',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/002/', 'https://www.cloudflare.com/learning/ddos/ntp-amplification-attack/', 'https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2013-5211'],
  },
  {
    id: 'DOS-012',
    nombre: 'Amplificación Memcached',
    alias: ['memcrashed', 'amplificación memcached', 'reflejo memcached', 'UDP 11211 abuse'],
    categoria: 'DoS',
    severidad: 'Critical',
    mitre_attack: ['T1498.002'],
    descripcion_tecnica: 'memcached expuesto a Internet con UDP activo: una consulta de ~15 bytes (get <clave>) devuelve el VALOR almacenado — hasta 1 MB por paquete UDP. Factor de amplificación récord: hasta 51.000×. Caso canónico: GitHub 2018, 1.35 Tbps de pico sin botnet — solo amplificadores y spoofing. Bonus del mal diseño: la misma exposición permite leer datos cacheados por UDP sin autenticación.',
    impacto_iam_soc: 'El vector de amplificación más eficiente conocido: con unos cientos de servidores se alcanza el Tbps. Además del DoS, hay fuga de datos (las claves plantadas o reales se leen remotamente). En SOC: cualquier paquete UDP 11211 desde Internet hacia infra propia es hallazgo crítico inmediato.',
    como_funciona: [
      'Encontrar objetivos: Shodan product:"memcached" port:11211 (quedan residuales tras la limpieza de 2018).',
      'Plantar la munición: echo -e "set a 0 0 1048576\\r\\n<payload 1 MB>\\r\\n" | nc -u <memcached> 11211.',
      'Reflejar: paquete UDP "get a\\r\\n" con IP(src="IP_victima") hacia cada memcached — la respuesta de 1 MB sale hacia la víctima.',
      'Referencia de escala: GitHub 2018 — 1.35 Tbps sin una sola máquina zombie.',
    ],
    deteccion: {
      spl: 'index=netflow direction=inbound proto=udp dest_port=11211 | stats count by src_ip | head 10 // cualquier paquete 11211 desde Internet hacia dentro es CRÍTICO',
      kql: 'DeviceNetworkEvents | where RemotePort == 11211 and Protocol == "UDP" | take 10 // tráfico UDP 11211 = misconfiguración grave o reflejo en curso',
    },
    mitigacion: [
      'memcached NUNCA en Internet: bind a 127.0.0.1 o a una red interna (es requisito de diseño, no tuning).',
      'Deshabilitar UDP: arrancar con -U 0 (el puerto UDP cerrado — la opción existe exactamente para esto).',
      'Filtrar 11211 en el borde entrante y saliente + BCP38 para el spoofing.',
      'Si hubo exposición: rotar los datos cacheados (eran legibles) y auditar los accesos.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1498/002/', 'https://github.blog/2018-03-01/ddos-incident-report/', 'https://www.cloudflare.com/learning/ddos/memcached-ddos-attack/'],
  },
  {
    id: 'DOS-013',
    nombre: 'NXDOMAIN Flood (water torture)',
    alias: ['DNS water torture', 'inundación NXDOMAIN', 'random subdomain attack'],
    categoria: 'DoS',
    severidad: 'High',
    mitre_attack: ['T1499'],
    descripcion_tecnica: 'El atacante bombardea los resolutores con consultas de subdominios ALEATORIOS e inexistentes (a7f3c1.ejemplo.com, b9x2k.ejemplo.com...). Cada NXDOMAIN: (1) falla siempre en caché (el nombre nunca se repite), (2) escala hasta el autoritativo de la víctima, (3) llena la tabla de cache con negativos. Resultado: resolutores lentos y autoritativo saturado — el dominio de la víctima parece caído para los usuarios reales sin haber tocado su servidor web.',
    impacto_iam_soc: 'El DoS se nota en TODO lo que usa el DNS del dominio (portal, correo, API): incidencia difusa que parece "del proveedor". En SOC: rate de NXDOMAIN por cliente fuera de baseline y caída del cache hit ratio — dos métricas que hay que tener instrumentadas antes del incidente.',
    como_funciona: [
      'Generador simple: while true; do dig +short $RANDOM.ejemplo.com @resolver; done — nombres únicos aleatorios.',
      'Industrial: dnsperf con una lista de etiquetas aleatorias generadas (10-20 chars) contra el resolver corporativo o el autoritativo.',
      'Reflejo: consultas con src spoofeada para que los resolutores de terceros ataquen al autoritativo de la víctima.',
      'Verificar el efecto: latencia de resolución (dig +stats) y cache hit ratio del resolver degradando con el pps.',
    ],
    deteccion: {
      spl: 'index=dns sourcetype=unbound | stats count(eval(rcode="NXDOMAIN")) as nx, count as total by client | eval ratio=nx/total | where ratio > 0.5 AND total > 1000 | head 20',
      kql: 'DnsEvents | where Result == "NonExistentDomain" | summarize count(), dcount(Name) by ClientIP | where count_ > 1000 and dcount_Name > 900 | take 20 // nombres únicos aleatorios = firma inequívoca',
    },
    mitigacion: [
      'Response Rate Limiting (RRL) en autoritativos y límites de qps por cliente en resolutores.',
      'Cache agresivo de negativos (RFC 2308) y serve-stale para degradar con gracia.',
      'En resolutores internos: límites por /24 y detección de "un solo origen genera el 80% de las qps".',
      'Anycast del autoritativo y separación de dominios críticos para que el flood de uno no arrastre al resto.',
    ],
    referencias: ['https://attack.mitre.org/techniques/T1499/', 'https://github.com/DNS-OARC/dnsperf'],
  },
  {
    id: 'DOS-014',
    nombre: 'HTTP/2 Rapid Reset',
    alias: ['rapid reset attack', 'CVE-2023-44487', 'stream cancellation attack'],
    categoria: 'DoS',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2023-44487'],
    mitre_attack: ['T1498'],
    descripcion_tecnica: 'Abuso del protocolo HTTP/2: en una única conexión TLS se abren streams (requests) y se cancelan inmediatamente con RST_STREAM. El servidor procesa el request (buffers, parsing, routing) y justo después recibe el reset. En bucle a miles de streams/s por conexión, los límites de "streams concurrentes" y de bytes nunca se disparan: solo se agota la CPU del edge. Campañas de agosto 2023 contra Google/AWS/Cloudflare con picos de más de 300 M rps; TLS 0-RTT se usó como multiplicador para crear conexiones nuevas casi gratis.',
    impacto_iam_soc: 'Tumbó infraestructura de los mayores proveedores del mundo: cualquier servidor HTTP/2 sin el parche de octubre de 2023 sigue vulnerable. En SOC: ratio RST_STREAM/requests por conexión (visible en métricas de Envoy/nginx) y CPU del edge anómala con poco tráfico en bytes.',
    como_funciona: [
      'Establecer la conexión HTTP/2 y abrir un stream con el request (frame HEADERS) — el trabajo del servidor arranca.',
      'Cancelar inmediatamente: RST_STREAM con error CANCEL — el servidor libera el stream pero ya procesó el request.',
      'Bucle: repetir a miles de/s en decenas de conexiones paralelas (el límite de streams concurrentes nunca se alcanza porque se cancelan).',
      'Multiplicar con TLS 0-RTT (session resumption) para abrir conexiones nuevas casi gratis y evadir rate limits por handshake.',
    ],
    deteccion: {
      spl: 'index=lb sourcetype=nginx:plus | stats sum(http2_rst_streams) as rst, sum(requests) as reqs by client_ip | eval ratio=rst/reqs | where ratio > 0.8 | head 20 // casi todo lo abierto se cancela',
      kql: '// métricas del LB/proxy (Envoy rst_per_conn, nginx http2) al SIEM, correladas con CPU del edge — el EDR no ve este plano',
    },
    mitigacion: [
      'Parchear LBs/proxies/servidores (nginx 1.25.3+, Apache 2.4.58+, Envoy/golang actualizados — octubre 2023).',
      'Limitar la tasa de streams NUEVOS y de RST_STREAM por conexión (no solo los concurrentes) — el fix clave de Envoy/Cloudflare.',
      'Si no se puede parchear: desactivar HTTP/2 en el edge temporalmente (cae performance, no disponibilidad).',
      'CDN delante: los patches de los grandes absorben el ataque antes de tu origen.',
    ],
    referencias: ['https://nvd.nist.gov/vuln/detail/CVE-2023-44487', 'https://blog.cloudflare.com/technical-breakdown-http2-rapid-reset-ddos-attack/', 'https://httpwg.org/specs/rfc9113.html'],
  },
  {
    id: 'DOS-015',
    nombre: 'XML Bomb (billion laughs)',
    alias: ['billion laughs attack', 'bomba XML', 'XML entity expansion', 'quadratic blowup'],
    categoria: 'DoS',
    severidad: 'Medium',
    mitre_attack: [],
    descripcion_tecnica: 'Payload XML minúsculo con entidades DTD anidadas que el parser EXPANDE en memoria: la clásica billion laughs define entidades donde cada nivel referencia 10 veces al anterior (&l9; = 10× &l8;...) hasta 10^9 expansiones — un archivo de <1 KB se convierte en GB de RAM/CPU en el parser. Variante quadratic blowup: expandir una entidad grande repetida dentro de otra grande (sin anidar, esquiva los límites de profundidad). Mata cualquier endpoint que acepte XML sin deshabilitar DTD: SOAP, feeds y — ojo — assertions SAML.',
    impacto_iam_soc: 'Aplicación caída u OOM con un solo POST: el DoS más barato que existe y sin red implicada. Muy relevante en stack SOAP/SAML corporativo. En SOC: requests XML pequeños con timeouts/OOM del backend y contadores de parser disparados.',
    como_funciona: [
      'Payload clásico: <!ENTITY l0 "lol"><!ENTITY l1 "&l0;&l0;&l0;..."> ... anidando 9-10 niveles → 10^9 "lol" al expandir.',
      'Quadratic blowup: <content>&big;&big;...×10</content> con &big; de 100.000 chars → 1 GB expandido sin anidación.',
      'Entregarlo: curl -d @bomb.xml -H "Content-Type: text/xml" https://api.ejemplo/soap (o inflar una AuthnRequest SAML en el IdP).',
      'Sostener: repetir el POST cada pocos segundos para que el proceso nunca se recupere del GC/OOM.',
    ],
    deteccion: {
      spl: 'index=web sourcetype=access_combined uri IN ("*/saml*", "*/soap*", "*xml*") | stats avg(bytes_in) as sz, count by client_ip | where sz < 1000 AND status >= 500 | head 20 // request pequeño + error 5xx = candidato a bomb',
      kql: 'AppHttpRequest | where RequestContentType startswith "text/xml" or RequestContentType startswith "application/xml" | summarize avg(RequestSize), count() by SourceIp | where count_ > 20 | take 20 // correlar con errores/OOM del backend',
    },
    mitigacion: [
      'Deshabilitar DTD siempre: Java setFeature("http://apache.org/xml/features/disallow-doctype-decl", true), Python defusedxml, libxml2 sin NOENT.',
      'Límites del parser: cap de entity expansion y de nodos (limites JAXP jdk.xml.entityExpansionLimit y equivalentes).',
      'WAF: regla trivial sobre body XML con "<!ENTITY" o multiplicidad de "&x;" en menos de 1 KB.',
      'Timeouts y aislamiento de proceso por request para que un parse loco no arrastre el pool entero.',
    ],
    referencias: ['https://owasp.org/www-community/attacks/XML_Entity_Expansion', 'https://cwe.mitre.org/data/definitions/776.html', 'https://en.wikipedia.org/wiki/Billion_laughs_attack'],
  },
];
