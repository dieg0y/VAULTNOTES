/**
 * socCheatSheet.ts — CHEATSHEET del PILAR 3: SOC / BLUE TEAM.
 *
 * 48 entradas CS-SOC-001..CS-SOC-048: la respuesta rápida del analista L1
 * ante phishing y fraude de correo (triage, clic, credenciales, adjuntos,
 * BEC, spoof, alcance, purga), cuentas comprometidas (risky sign-in,
 * impossible travel, MFA fatigue, reglas de buzón, robo de token/PRT,
 * kill switch, DLP de descarga masiva, credenciales en repos públicos),
 * password spray y brute force (incl. SSH externo con fail2ban), eventos
 * críticos de Windows (4740/4720/4728/4732/4688/1102/104/7045), procesos
 * y EDR (Defender, PowerShell -enc, LOLBins, linaje, cuarentena vs
 * remove), persistencia (tareas, run keys, WMI, startup, web shells),
 * lateral y C2 (PsExec/WinRM, beaconing, DNS tunneling, rogue AP),
 * ransomware/infostealer/aislamiento, plantillas de caza KQL/SPL/Sigma y
 * extracción de payloads de SQLi desde logs de WAF.
 *
 * Regla de contenido (cheatsheetCore): fix = 3-8 líneas, cada línea un paso
 * o comando/consulta concreta y REAL (KQL de Sentinel/MDO, SPL de Splunk,
 * PowerShell de Graph/Defender/Exchange, cmdlets AD y binarios de Windows);
 * verify = cómo confirmar que quedó resuelto; tags ES/EN para búsqueda
 * fuzzy. Problemas en es-CO, comandos en su idioma original, MITRE entre
 * paréntesis cuando aplica. Sin duplicar para llegar a número: cada entrada
 * es un problema distinto.
 */

import { type PillarCheatEntry } from './cheatsheetCore';

export const SOC_CHEATSHEET: PillarCheatEntry[] = [
  {
    id: 'CS-SOC-001',
    title: 'Correo reportado por el usuario — triage en 10 minutos',
    category: 'Phishing / Email',
    problem: 'El usuario reportó con el add-in de Phishing un correo sospechoso: hay que decidir rápido si es caso aislado o campaña y si llegó a más buzones (T1566).',
    fix: [
      'Traer el correo a contexto: EmailEvents | where EmailDirection == "Inbound" | where Timestamp > ago(24h) | where Subject contains "nomina" or SenderMailFromDomain == "{{DOMAIN}}" | project Timestamp, NetworkMessageId, Subject, SenderFromAddress, SenderMailFromDomain, RecipientEmailAddress, DeliveryAction, ThreatTypes',
      'Ver si EOP actuó después de la entrega: EmailPostDeliveryEvents | where Timestamp > ago(24h) | where Action == "ZAP" | project Timestamp, NetworkMessageId, Action, ThreatTypes',
      'Revisar la URL reportada: EmailUrlInfo | where Timestamp > ago(24h) | where Url has "{{DOMAIN}}" | project NetworkMessageId, Url, UrlDomain — y chequearla en VirusTotal / feed propio',
      'Clasificar: legítimo (responder al usuario y marcar como no-phishing), spam o phish; si ThreatTypes contiene "Phish" y DeliveryAction == "Delivered" ya es incidente',
      'Si es phish entregado: seguir a contención (CS-SOC-002 clic / CS-SOC-003 credenciales) y a búsqueda de alcance (CS-SOC-007)',
      'Documentar en {{TICKET_ID}}: NetworkMessageId, remitente, asunto, veredicto y hash de adjuntos (con estos campos se arma la evidencia de la campaña)',
    ],
    verify: 'El reporte quedó clasificado y respondido al usuario; si fue phish entregado, existe incidente abierto con la contención iniciada.',
    tags: ['phishing', 'report', 'triage', 'emailevents', 'explorer', 'zap', 'add-in', 'campaign', 'correo', 't1566', 'submission', 'emailpostdeliveryevents'],
  },
  {
    id: 'CS-SOC-002',
    title: 'Hizo clic en el enlace del phishing — contener al usuario',
    category: 'Phishing / Email',
    problem: 'El usuario admite que hizo clic en el enlace del correo sospechoso pero dice que no ingresó nada: hay que verificarlo, no creerlo (T1566.002).',
    fix: [
      'La URL exacta y a quién más llegó: EmailUrlInfo | where Url has "{{DOMAIN}}" | join kind=inner EmailEvents on NetworkMessageId | project Timestamp, RecipientEmailAddress, Url, DeliveryAction',
      'Buscar conexiones desde los equipos de los destinatarios: DeviceNetworkEvents | where Timestamp > ago(24h) | where RemoteUrl has "{{DOMAIN}}" | project Timestamp, DeviceName, RemoteUrl, RemoteIP, InitiatingProcessFileName',
      'Revisar la IP y la URL en VirusTotal antes de decidir severidad (redirección a infra conocida-bad = alta)',
      'Si hubo conexión, ver qué pasó después: DeviceProcessEvents | where DeviceName == "{{HOSTNAME}}" | where Timestamp > datetime({{TIMESTAMP}}) | project Timestamp, FileName, ProcessCommandLine, InitiatingProcessFileName | order by Timestamp asc',
      'Entrevistar al usuario: qué vio, si descargó algo, si ingresó datos — y verificarlo igual con los SigninLogs (CS-SOC-003)',
      'Sin ejecución y sin entrega de datos: monitorear 24h; con ejecución o descarga: contención del host (CS-SOC-039)',
    ],
    verify: 'DeviceNetworkEvents no registra nuevas conexiones a la URL/IP en 24h y Get-MpThreat no reporta detecciones nuevas en el equipo.',
    tags: ['clic', 'click', 'url', 'devicenetworkevents', 'contencion', 'phishing', 't1566.002', 'spear phishing link', 'usuario', 'user', 'verificar', 'virustotal'],
  },
  {
    id: 'CS-SOC-003',
    title: 'Ingresó credenciales en sitio falso — cuenta comprometida',
    category: 'Phishing / Email',
    problem: 'El usuario ingresó usuario y contraseña en la página del phishing: hasta que se demuestre lo contrario, las credenciales están en manos del atacante (T1566.002 → T1078).',
    fix: [
      'Fijar la línea de tiempo: hora del clic (DeviceNetworkEvents de la entrada anterior) y primeras acciones posteriores de la cuenta',
      'Buscar uso de la cuenta: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where Timestamp > datetime({{TIMESTAMP}}) | project Timestamp, IPAddress, Location, ResultType, AppDisplayName | order by Timestamp asc',
      'Ejecutar el kill switch sin esperar más evidencia: reset + revocación de sesiones (CS-SOC-014)',
      'Buscar persistencia inmediata: reglas de buzón (CS-SOC-012) y re-registro de MFA: Search-UnifiedAuditLog -StartDate (Get-Date).AddDays(-3) -EndDate (Get-Date) -UserIds "{{USERNAME}}@{{DOMAIN}}" -Operations "User registered security info"',
      'Cruzar las IPs nuevas de la cuenta contra la infra de la campaña: misma IP del phishing = confirmación directa de robo',
      'Elevar a incidente de cuenta comprometida y avisar al Service Desk el bloqueo (evita re-open por "no puedo entrar")',
    ],
    verify: 'SigninLogs de {{USERNAME}} solo muestra inicios desde redes conocidas tras el reset y la auditoría no registra métodos de MFA nuevos.',
    tags: ['credenciales', 'credentials', 'phishing', 't1566.002', 't1078', 'reset', 'revocar', 'signinlogs', 'compromiso', 'account compromise', 'password', 'incidente'],
  },
  {
    id: 'CS-SOC-004',
    title: 'Adjunto malicioso (ISO/macro) — trazar detonación y limpiar',
    category: 'Phishing / Email',
    problem: 'El correo traía un adjunto que las barreras no detuvieron y el usuario lo abrió: hay que averiguar si solo llegó o si ejecutó (T1566.001).',
    fix: [
      'El adjunto y su hash: EmailAttachmentInfo | where Timestamp > ago(24h) | where FileName endswith ".iso" or FileName endswith ".lnk" or FileType == "docm" | project Timestamp, NetworkMessageId, FileName, FileType, SHA256',
      'Si aterrizó en endpoints: DeviceFileEvents | where DeviceName == "{{HOSTNAME}}" | where FileName endswith ".iso" or FileName endswith ".docm" | project Timestamp, FileName, FolderPath, SHA256, InitiatingProcessFileName',
      'Buscar ejecución posterior (montaje de ISO, powershell, wscript): DeviceProcessEvents | where DeviceName == "{{HOSTNAME}}" | where Timestamp > ago(6h) | project Timestamp, FileName, ProcessCommandLine, InitiatingProcessFileName | order by Timestamp asc',
      'Revisar detecciones previas: Get-MpThreat | fl ThreatName, SeverityID, DidThreatExecute y Get-MpThreatDetection | fl InitialDetectionTime, ProcessName, Resources, CleaningActionID',
      'Ejecución confirmada o dudosa: contención del host (CS-SOC-039) + full scan: & "C:\\Program Files\\Windows Defender\\MpCmdRun.exe" -Scan -ScanType 2',
      'Mandar el SHA256 a sandbox, registrar los IOCs en {{TICKET_ID}} y purgar el adjunto de otros buzones (CS-SOC-008)',
    ],
    verify: 'Get-MpThreat no lista amenazas activas, el full scan termina limpio y el adjunto está purgado de los buzones afectados.',
    tags: ['adjunto', 'attachment', 'iso', 'lnk', 'docm', 'get-mpthreat', 'emailattachmentinfo', 'devicefileevents', 't1566.001', 'sandbox', 'detonacion', 'cuarentena'],
  },
  {
    id: 'CS-SOC-005',
    title: 'Fraude BEC — el "CEO" pide una transferencia urgente',
    category: 'Phishing / Email',
    problem: 'Finanzas recibió un correo del "gerente" pidiendo una transferencia urgente y confidencial: Business Email Compromise, el ataque más rentable y el menos técnico (T1534).',
    fix: [
      'Ver el remitente REAL (no el display name): EmailEvents | where Timestamp > ago(7d) | where RecipientEmailAddress startswith "finanzas" | project Timestamp, SenderFromAddress, SenderMailFromAddress, SenderMailFromDomain, Subject',
      'Comparar el dominio contra el legítimo: lookalikes de un carácter (rnicrosoft, exarnple) — whois para ver la edad del dominio',
      'Parar el pago: llamar al supuesto remitente por teléfono conocido — NUNCA responder el mismo correo (la sesión puede estar comprometida)',
      'Si ya se transfirió: escalar de inmediato a Fraudes del banco (hay ventana de recall) y a liderazgo — incidente mayor desde el minuto uno',
      'Bloquear el dominio remitente: New-TransportRule -Name "Bloqueo BEC {{DOMAIN}}" -SenderDomainIs {{DOMAIN}} -Quarantine $true -Priority 0',
      'Reportar el dominio como phishing a Microsoft y registrar el caso con la traza completa del correo en {{TICKET_ID}}',
    ],
    verify: 'Finanzas confirma que no se realizó el pago (o el recall iniciado) y el dominio queda en cuarentena: una prueba de envío cae retenida.',
    tags: ['bec', 'fraude', 'ceo fraud', 'transferencia', 'business email compromise', 't1534', 'new-transportrule', 'finanzas', 'whois', 'pretexting', 'recall', 'lookalike'],
  },
  {
    id: 'CS-SOC-006',
    title: 'Dominio interno spoofeado — falla de SPF/DKIM/DMARC',
    category: 'Phishing / Email',
    problem: 'Los usuarios reciben correos "internos" que en realidad vienen de afuera: alguien está suplantando el dominio propio y el correo entra porque la autenticación lo permite (T1566.003).',
    fix: [
      'Confirmar el spoof: EmailEvents | where Timestamp > ago(7d) | where SenderMailFromDomain == "{{DOMAIN}}" | project Timestamp, NetworkMessageId, RecipientEmailAddress, DeliveryAction, DeliveryLocation, ThreatTypes',
      'Revisar la autenticación del mensaje en el Explorador de Defender (detalles del mensaje: SPF/DKIM/DMARC pass o fail)',
      'Medir el alcance: EmailEvents | where SenderMailFromDomain == "{{DOMAIN}}" | summarize mensajes = count(), destinatarios = dcount(RecipientEmailAddress)',
      'Si el registro DMARC está en p=none: planear subir a p=quarantine y luego p=reject, monitoreando los reportes agregados antes de cada paso (primero saltan los legítimos mal configurados)',
      'Endurecer EOP contra el hard-fail de SPF: Set-HostedContentFilterPolicy -Identity Default -MarkAsSpamSpfRecordHardFail $true',
      'Si el spoof entregó payloads: tratarlo como campaña completa (alcance CS-SOC-007 y purga CS-SOC-008)',
    ],
    verify: 'Pruebas de From spoofeado desde afuera caen en cuarentena y el registro TXT _dmarc de la zona publica p=quarantine o superior.',
    tags: ['spoofing', 'spf', 'dkim', 'dmarc', 't1566.003', 'emailevents', 'dominio', 'domain impersonation', 'anti-spoofing', 'eop', 'p=none', 'txt'],
  },
  {
    id: 'CS-SOC-007',
    title: 'Buscar otros destinatarios de la campaña (EmailEvents)',
    category: 'Phishing / Email',
    problem: 'Confirmado un correo de phishing: falta saber a cuántos llegó y a quiénes, antes de que caiga el siguiente clic.',
    fix: [
      'Pivot por remitente + asunto: EmailEvents | where SenderMailFromAddress == "phisher@{{DOMAIN}}" | where Timestamp > ago(7d) | project Timestamp, RecipientEmailAddress, DeliveryAction, DeliveryLocation, NetworkMessageId | order by Timestamp asc',
      'Por IOC de la campaña: EmailEvents | where Timestamp > ago(24h) | where ThreatTypes has "Phish" | summarize destinatarios = makeset(RecipientEmailAddress), mensajes = count()',
      'DeliveryAction "Delivered" + DeliveryLocation en Inbox = población en riesgo; Junk o Quarantine = ya contenido',
      'Cruzar con clics: DeviceNetworkEvents | where Timestamp > ago(24h) | where RemoteUrl has "{{DOMAIN}}" | project Timestamp, DeviceName, RemoteUrl, RemoteIP — los equipos que tocaron la URL',
      'Exportar la lista desde el portal (Export) y adjuntarla a {{TICKET_ID}}: es el scope del incidente',
      'Con la lista cerrada: purga (CS-SOC-008) + comunicado interno con la señal del phishing (sin crear pánico)',
    ],
    verify: 'El número de destinatarios del summarize coincide con la lista exportada y cada afectado tiene seguimiento o contención.',
    tags: ['campaign', 'destinatarios', 'recipients', 'emailevents', 'kql', 'pivotar', 'scope', 'alcance', 'phishing', 'makeset', 'summarize', 'export'],
  },
  {
    id: 'CS-SOC-008',
    title: 'Purgar la campaña de los buzones (PurgeType SoftDelete)',
    category: 'Phishing / Email',
    problem: 'El correo malicioso sigue entregado en buzones de usuarios: hay que retirarlo de forma masiva, controlada y auditada.',
    fix: [
      'Conectar a Security & Compliance PowerShell: Connect-IPPSSession',
      'Crear la búsqueda acotada: New-ComplianceSearch -Name "CS-{{TICKET_ID}}-phish" -ExchangeLocation All -ContentMatchQuery "(subject:nomina) AND (from:phisher@{{DOMAIN}})"',
      'Ejecutarla y revisar el alcance: Start-ComplianceSearch "CS-{{TICKET_ID}}-phish" — luego (Get-ComplianceSearch "CS-{{TICKET_ID}}-phish").Items',
      'Validar el query dos veces ANTES de purgar: un filtro ancho toca correo legítimo de toda la empresa',
      'Purgar en soft (recuperable, auditado): New-ComplianceSearchAction -SearchName "CS-{{TICKET_ID}}-phish" -Purge -PurgeType SoftDelete',
      'Verificar el resultado: Get-ComplianceSearchAction "CS-{{TICKET_ID}}-phish_Purge" | fl Status, Results — y re-correr la búsqueda: debe devolver 0 items',
      'Registrar la acción en {{TICKET_ID}} y notificar a los afectados qué se retiró y por qué',
    ],
    verify: 'Get-ComplianceSearchAction muestra Status Completed y una nueva búsqueda con el mismo query devuelve 0 items.',
    tags: ['purge', 'softdelete', 'new-compliancesearchaction', 'compliance search', 'purga', 'remove message', 'borrar correo', 'exchange', 'connect-ippssession', 'campaign', 'cleanup', 'eop'],
  },
  {
    id: 'CS-SOC-009',
    title: 'Sign-in de riesgo en Entra ID — revisar y remediar',
    category: 'Cuentas / Identidad',
    problem: 'Entra ID marcó un inicio de sesión como de riesgo (credenciales filtradas, viaje atípico) y hay que decidir si es el usuario o el atacante.',
    fix: [
      'Listar la señal: SigninLogs | where RiskState in ("atRisk", "confirmedCompromised") | where Timestamp > ago(24h) | project Timestamp, UserPrincipalName, IPAddress, Location, RiskLevelDuringSignIn, ResultType | order by Timestamp desc',
      'Correlacionar: nueva IP + UserAgent nuevo + app sensible (Exchange Online) + ResultType 0 = compromiso probable',
      'Confirmar con el usuario por un canal DISTINTO al chat de Teams (la sesión del atacante puede ser la que responde)',
      'Confirmado: Confirm-MgRiskyUserCompromised -RiskyUserId <id> (marca el riesgo y dispara las políticas de remediación)',
      'Falso positivo: Dismiss-MgRiskyUser -RiskyUserId <id> y documentar por qué (VPN nueva, viaje real, cambio de ISP)',
      'Mantener activa la política de riesgo "requerir MFA": es la red de seguridad automática para la próxima',
    ],
    verify: 'Get-MgRiskyUser muestra al usuario en RiskState remediated (o dismissed) y los accesos posteriores vienen de redes conocidas.',
    tags: ['riesgo', 'risky signin', 'entra id', 'azure ad', 'signinlogs', 'identity protection', 'get-mgriskyuser', 'confirm compromised', 'dismiss', 't1078', 'riskstate', 'remediado'],
  },
  {
    id: 'CS-SOC-010',
    title: 'Impossible travel — ¿atacante o VPN corporativa?',
    category: 'Cuentas / Identidad',
    problem: 'Dos inicios de sesión exitosos desde países distintos en minutos: impossible travel es ruido de la VPN el 80% de las veces y robo de sesión el resto (T1078).',
    fix: [
      'La secuencia completa: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where Timestamp > ago(24h) | project Timestamp, IPAddress, Location, AutonomousSystemNumber, AppDisplayName, ResultType | order by Timestamp asc',
      'Regla práctica: mismo ASN en ambos accesos (el de la VPN o el de la nube) = falso positivo casi seguro',
      'Sospechoso real: ASN residencial o móvil foráneo + UserAgent distinto + apps que el usuario no usa (IMAP, SMTP AUTH)',
      'Confirmar con el usuario (viaje, móvil personal, hotel) — la honestidad del usuario define la mitad de estos casos',
      'Verdadero positivo: contención de cuenta (CS-SOC-014) y bloqueo de la IP o geo en acceso condicional / perímetro',
      'Registrar el veredicto y la evidencia (los dos sign-ins) en {{TICKET_ID}} para afinar la detección',
    ],
    verify: 'SigninLogs del usuario tras la contención solo muestra la ubicación legítima y cero intentos desde la IP foránea.',
    tags: ['impossible travel', 'viaje imposible', 'signinlogs', 'geo', 'vpn', 'asn', 'falso positivo', 'false positive', 't1078', 'robo de sesion', 'session hijack', 'useragent'],
  },
  {
    id: 'CS-SOC-011',
    title: 'MFA fatigue — bombardeo de push: la contraseña ya no es secreta',
    category: 'Cuentas / Identidad',
    problem: 'El usuario recibe decenas de notificaciones MFA que no pidió: alguien ya tiene su contraseña y espera que apruebe una por cansancio (T1621).',
    fix: [
      'Confirmar el patrón: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where ResultType == 500121 | summarize intentos = count() by bin(Timestamp, 15m), IPAddress | order by intentos desc',
      'Lectura correcta: el primer factor PASA y solo falla el push → tratar la contraseña como comprometida desde ya',
      'Reset inmediato: Set-MgUserPassword -UserId {{USERNAME}}@{{DOMAIN}} -NewPassword $nueva -ForceChangePasswordNextSignIn $true',
      'Bloquear la IP y revisar si probó otras cuentas: SigninLogs | where IPAddress == "{{SOURCE_IP}}" | where Timestamp > ago(24h) | summarize intentos = count(), exitos = countif(ResultType == 0) by UserPrincipalName',
      'Defensa estructural: number matching en Authenticator (Entra ID > Authentication methods > Configure) — reduce la fatiga casi a cero',
      'Educar al usuario: nunca aprobar un push no solicitado; reportarlo e iniciar el reset de una',
    ],
    verify: 'SigninLogs ya no registra ResultType 500121 para el usuario y este reporta cero pushes no solicitados.',
    tags: ['mfa fatigue', 'push bombing', 'mfa', '500121', 'number matching', 't1621', 'autenticacion', 'authenticator', 'fatiga', 'second factor', 'push', 'multifactor'],
  },
  {
    id: 'CS-SOC-012',
    title: 'Reglas de buzón del atacante — hallarlas y quitarlas (InboxRule)',
    category: 'Cuentas / Identidad',
    problem: 'La cuenta parece normal pero el atacante sigue leyendo todo: reglas que reenvían, borran o mueven correo a carpetas ocultas (T1137.005).',
    fix: [
      'Listar las reglas: Get-InboxRule -Mailbox "{{USERNAME}}@{{DOMAIN}}" | fl Name, Enabled, RedirectTo, ForwardTo, MoveToFolder, DeleteMessage',
      'Bandera roja: RedirectTo o ForwardTo a dominios externos, MoveToFolder "RSS Feeds", DeleteMessage activo o regla con nombre inocente ("limpieza")',
      'Cómo entró: Search-UnifiedAuditLog -StartDate (Get-Date).AddDays(-7) -EndDate (Get-Date) -UserIds "{{USERNAME}}@{{DOMAIN}}" -Operations "New-InboxRule","Set-InboxRule","Remove-InboxRule" — la IP de creación es la del atacante',
      'Quitar: Remove-InboxRule -Mailbox "{{USERNAME}}@{{DOMAIN}}" -Identity "nombre-regla"',
      'Revisar el reenvío a nivel de buzón (la otra ruta): Get-Mailbox {{USERNAME}} | fl ForwardingAddress, ForwardingSmtpAddress',
      'Cerrar con el kill switch (CS-SOC-014): la regla se creó desde una sesión robada y sigue viva hasta revocar',
    ],
    verify: 'Get-InboxRule solo muestra reglas legítimas, ForwardingSmtpAddress en blanco y la auditoría no registra reglas nuevas en 24h.',
    tags: ['inbox rule', 'regla de buzon', 'new-inboxrule', 'get-inboxrule', 'remove-inboxrule', 't1137.005', 'persistencia', 'persistence', 'forwarding', 'reenvio', 'search-unifiedauditlog', 'exchange'],
  },
  {
    id: 'CS-SOC-013',
    title: 'Robo de token/PRT — la cuenta entra sin contraseña',
    category: 'Cuentas / Identidad',
    problem: 'Inicios de sesión no interactivos desde IPs desconocidas: no hay spray ni phishing nuevo, la cookie o PRT robada hace todo el trabajo (T1550.004).',
    fix: [
      'La firma: AADNonInteractiveUserSignInLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where Timestamp > ago(7d) | summarize sesiones = count(), apps = makeset(AppDisplayName) by IPAddress, Location | order by sesiones desc',
      'Sospechoso: ASN residencial o móvil que el usuario no usa + Exchange Online/OWA + horario raro mientras el usuario está en la oficina',
      'Buscar el punto del robo en su equipo: DeviceProcessEvents | where DeviceName == "{{HOSTNAME}}" | where ProcessCommandLine has "MiniDump" or ProcessCommandLine has "comsvcs" | project Timestamp, FileName, ProcessCommandLine (dump de LSASS incluye cookies y PRT)',
      'Remediación completa: Revoke-MgUserSession + reset (CS-SOC-014); si robaron tokens de refresco, forzar re-registro de MFA',
      'Si el PRT era del equipo: el host está comprometido → aislar (CS-SOC-039) y re-provisionar',
      'Elevar a Tier 2 con la línea de tiempo completa: robo de token con uso exitoso es incidente de severidad alta',
    ],
    verify: 'AADNonInteractiveUserSignInLogs del usuario tras la revocación solo muestra IPs y ASNs conocidos de la empresa.',
    tags: ['prt', 'token theft', 'aadnoninteractiveusersigninlogs', 'pass the cookie', 't1550.004', 'lsass', 'minidump', 'refresh token', 'revocar', 'no interactivo', 'sesion robada', 'robo'],
  },
  {
    id: 'CS-SOC-014',
    title: 'Revocar sesiones y forzar reset — el kill switch de cuenta',
    category: 'Cuentas / Identidad',
    problem: 'Cuenta confirmada como comprometida: hay que cortar de una vez TODAS las sesiones web, los tokens de refresco y la contraseña vigente.',
    fix: [
      'Revocar refresh tokens y cookies de sesión: Revoke-MgUserSession -UserId {{USERNAME}}@{{DOMAIN}}',
      'Reset con cambio obligatorio: Set-MgUserPassword -UserId {{USERNAME}}@{{DOMAIN}} -NewPassword $nueva -ForceChangePasswordNextSignIn $true',
      'Si el atacante enrroló MFA: quitar sus métodos: Remove-MgUserAuthenticationPhoneMethod -UserId {{USERNAME}}@{{DOMAIN}} -PhoneAuthenticationMethodId <id> — y re-registrar presencial o por videollamada',
      'Confirmar el corte: Get-MgUser -UserId {{USERNAME}}@{{DOMAIN}} -Property refreshTokensValidFromDateTime | fl refreshTokensValidFromDateTime (queda en la hora de la revocación)',
      'Acompañar el re-login del usuario: evita el ticket de "no puedo entrar" al Service Desk',
      'Registrar la hora exacta en {{TICKET_ID}}: cualquier sign-in con token anterior a esa marca = actividad del atacante',
    ],
    verify: 'refreshTokensValidFromDateTime quedó actualizado, el usuario re-entra con la nueva contraseña y no hay accesos con tokens previos.',
    tags: ['revocar', 'revoke', 'revokemgusersession', 'set-mguserpassword', 'kill switch', 'sesiones', 'sessions', 'refresh token', 'contencion', 'containment', 't1078', 'cuenta comprometida'],
  },
  {
    id: 'CS-SOC-015',
    title: 'Password spray — una IP, muchas cuentas, pocos intentos cada una',
    category: 'Ataques de Contraseña',
    problem: 'Una IP prueba una misma contraseña contra decenas de cuentas con pocos intentos por cuenta para no bloquear: low and slow (T1110.003).',
    fix: [
      'La firma: SigninLogs | where Timestamp > ago(24h) | where ResultType == 50126 | summarize intentos = count(), cuentas = dcount(UserPrincipalName) by IPAddress | where cuentas > 10 | order by cuentas desc',
      'Timing del spray: SigninLogs | where IPAddress == "{{SOURCE_IP}}" | where Timestamp > ago(24h) | summarize primera = min(Timestamp), ultima = max(Timestamp), intentos = count(), exitos = countif(ResultType == 0)',
      'Exitos > 0: ir directo a CS-SOC-018 (el spray encontró puerta abierta)',
      'Bloquear la IP en el perímetro o acceso condicional y vigilar ASNs vecinos (los sprays rotan de IP)',
      'Confirmar que Smart Lockout sostuvo la línea (cuentas sin bloqueo masivo: el spray lo evita a propósito)',
      'Documentar el IOC (IP, ASN, ventana) en {{TICKET_ID}} y compartirlo con los gestores de correo y TI',
    ],
    verify: 'SigninLogs ya no registra 50126 desde la IP bloqueada y el conteo de cuentas afectadas quedó documentado.',
    tags: ['password spray', 'spray', 't1110.003', '50126', 'signinlogs', 'dcount', 'countif', 'low and slow', 'una ip muchas cuentas', 'credencial', 'bloqueo', 'ioc'],
  },
  {
    id: 'CS-SOC-016',
    title: 'Brute force a una sola cuenta — atajar y verificar el éxito',
    category: 'Ataques de Contraseña',
    problem: 'Miles de intentos fallidos contra UNA cuenta desde una o muchas IPs: fuerza bruta directa o credential stuffing de botnet (T1110.001).',
    fix: [
      'Medir: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where ResultType == 50126 | summarize intentos = count(), ips = dcount(IPAddress) by bin(Timestamp, 1h) | order by intentos desc',
      'Muchas IPs contra una cuenta = stuffing de botnet: el bloqueo por IP no sirve, pensar en geo/ASN o bloqueo temporal',
      'Cortar el acceso: Set-MgUser -UserId {{USERNAME}}@{{DOMAIN}} -AccountEnabled $false (con aprobación: impacto de negocio) o dejar que Smart Lockout sostenga',
      'Lo que importa de verdad: ¿entró alguna vez? SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where ResultType == 0 | where Timestamp > ago(24h) | project Timestamp, IPAddress, Location — un 0 entre mil fallos = comprometida',
      'Con éxito confirmado: kill switch (CS-SOC-014) y revisar reglas de buzón (CS-SOC-012)',
      'Avisar al usuario y revisar la contraseña en breaches públicos para entender el vector',
    ],
    verify: 'Los 50126 cesan tras la contención y el check de exitos queda negativo (o la cuenta ya remediada).',
    tags: ['brute force', 'fuerza bruta', 't1110.001', '50126', 'credential stuffing', 'botnet', 'smart lockout', 'signinlogs', 'bloqueo temporal', 'intentos', 'verificar exito', 'cuenta'],
  },
  {
    id: 'CS-SOC-017',
    title: '4625/4771 masivos desde un host interno — spray ya adentro',
    category: 'Ataques de Contraseña',
    problem: 'Un equipo de la red interna genera cientos de 4625/4771 contra cuentas de dominio: o es un servicio con contraseña vieja o el atacante ya está dentro.',
    fix: [
      'Medir por origen: SecurityEvent | where EventID == 4625 | where TimeGenerated > ago(24h) | summarize intentos = count(), cuentas = dcount(TargetUserName) by Computer, IpAddress | where intentos > 100 | order by intentos desc',
      'El 4771 (Kerberos) refuerza: SecurityEvent | where EventID == 4771 | where IpAddress == "{{SOURCE_IP}}" | summarize intentos = count(), cuentas = dcount(TargetUserName)',
      'Identificar al dueño del host: Get-ADComputer -Identity {{HOSTNAME}} -Properties LastLogonDate, OperatingSystem, Description | fl',
      'Caso benigno clásico: servicio o mapping con contraseña vencida — pocas cuentas, patrón constante, desde un servidor: fix de Service Desk',
      'Caso maligno: dcount(TargetUserName) alto y nombres secuenciales (juan1, juan2) = herramienta de spray → aislar {{HOSTNAME}} YA (CS-SOC-039)',
      'Preservar evidencia antes de limpiar: exportar los 4625/4771/4624 del host a {{TICKET_ID}}',
    ],
    verify: 'SecurityEvent de {{HOSTNAME}} no genera más 4625/4771 tras contenerlo o corregir el servicio con credencial vieja.',
    tags: ['4625', '4771', 'kerberos', 'spray interno', 'securityevent', 'get-adcomputer', 'contrasena vieja', 'internal spray', 'aislar', 't1110', 'dc', 'host origen'],
  },
  {
    id: 'CS-SOC-018',
    title: 'El spray tuvo éxito — encontrar el sign-in ganador',
    category: 'Ataques de Contraseña',
    problem: 'Entre miles de 50126 hay UN ResultType 0 que importa: la cuenta que abrió la puerta y que probablemente nadie ha notado (T1110.003 → T1078).',
    fix: [
      'Pivot directo: SigninLogs | where IPAddress == "{{SOURCE_IP}}" | where Timestamp > ago(24h) | summarize exitos = countif(ResultType == 0), fallos = countif(ResultType == 50126) by UserPrincipalName | where exitos > 0 | order by exitos desc',
      'Qué hizo después: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where ResultType == 0 | project Timestamp, AppDisplayName, IPAddress, ClientBrowser | order by Timestamp asc',
      'Persistencia inmediata: reglas de buzón (CS-SOC-012) y MFA enrrolado: Search-UnifiedAuditLog -StartDate (Get-Date).AddDays(-3) -EndDate (Get-Date) -UserIds "{{USERNAME}}@{{DOMAIN}}" -Operations "User registered security info"',
      'Remediar con el kill switch completo (CS-SOC-014): reset + revocación + revisión de MFA',
      'Buscar propagación: EmailEvents | where SenderFromAddress == "{{USERNAME}}@{{DOMAIN}}" | where Timestamp > ago(24h) | project Timestamp, RecipientEmailAddress, Subject (BEC interno desde la cuenta)',
      'Cazar al resto: la misma {{SOURCE_IP}} contra VPN, OWA u otros servicios (correlación en el SIEM) y actualizar el IOC',
    ],
    verify: 'El sign-in exitoso documentado, la cuenta remediada (RiskState remediated) y cero sesiones del atacante activas.',
    tags: ['spray exitoso', 'countif', 'resulttype 0', 'cuenta comprometida', 't1110.003', 't1078', 'pivot', 'signinlogs', 'kill switch', 'remediacion', 'hunter', 'exito'],
  },
  {
    id: 'CS-SOC-019',
    title: '4740 masivos — la ola de bloqueos como señal de ataque',
    category: 'Eventos Windows',
    problem: 'El Service Desk reporta una ola de cuentas bloqueadas: detrás puede haber un spray interno o una herramienta probando un diccionario contra el dominio.',
    fix: [
      'Agrupar: SecurityEvent | where EventID == 4740 | where TimeGenerated > ago(4h) | summarize bloqueos = count(), victimas = dcount(TargetUserName) by Computer | order by bloqueos desc',
      'El origen del bloqueo está en el mensaje: Get-WinEvent -FilterHashtable @{LogName="Security"; Id=4740} -MaxEvents 20 | fl TimeCreated, Message (línea Caller Computer Name)',
      'Cruzar con 4625/4771 de la misma ventana (CS-SOC-017): los lockouts son los intentos que superaron el umbral',
      'Un Caller + muchas víctimas = spray interno: aislar {{HOSTNAME}} (CS-SOC-039) y buscar 4624 tipo 3 desde ese host (lateral: CS-SOC-034)',
      'Un usuario recurrente bloqueado desde su propio PC: credential caching, fix de Service Desk, no incidente',
      'Desbloquear a las víctimas legítimas: Unlock-ADAccount <cuenta> solo tras confirmar el origen contenido',
    ],
    verify: 'La ola de 4740 se detiene con el origen contenido y las cuentas legítimas desbloqueadas funcionan sin re-bloqueo.',
    tags: ['4740', 'lockout', 'bloqueos masivos', 'spray', 'caller computer', 'securityevent', 'unlock-adaccount', 'get-winevent', 'dc', 'ola', 't1110', 'umbral'],
  },
  {
    id: 'CS-SOC-020',
    title: '4720 — cuenta nueva fuera de proceso: persistencia barata',
    category: 'Eventos Windows',
    problem: 'Se detectó una cuenta de dominio creada sin ticket de alta: el atacante que crea su propia cuenta no necesita volver a robar credenciales (T1136.002).',
    fix: [
      'El evento: SecurityEvent | where EventID == 4720 | project TimeGenerated, Computer, SubjectUserName, TargetUserName',
      'Primera regla: confirmar por el canal correcto (ticket de RRHH, el creador reconoce la acción) ANTES de tocar nada',
      'Radiar la cuenta: Get-ADUser {{USERNAME}} -Properties Enabled, LastLogonDate, MemberOf, Description, whenCreated | fl',
      'Bandera roja: whenCreated de madrugada, Description vacía o "backup", creador = cuenta de servicio o usuario sin rol de altas',
      'Confirmada maliciosa: Disable-ADAccount {{USERNAME}} y a cuarentena: Get-ADUser {{USERNAME}} | Move-ADObject -TargetPath "OU=Quarantine,DC={{DOMAIN}},DC=local"',
      'Cazar su uso: SecurityEvent | where EventID == 4624 | where TargetUserName == "{{USERNAME}}" | project TimeGenerated, Computer, LogonType — y revisar grupos (CS-SOC-021)',
    ],
    verify: 'La cuenta deshabilitada en OU de cuarentena, su creación auditada en {{TICKET_ID}} y sin 4624 posteriores.',
    tags: ['4720', 'cuenta nueva', 'account creation', 't1136.002', 'get-aduser', 'disable-adaccount', 'move-adobject', 'rogue account', 'persistencia', 'alta fuera de proceso', 'cuarentena', 'ad'],
  },
  {
    id: 'CS-SOC-021',
    title: '4728/4732 — metieron a alguien a un grupo con poder',
    category: 'Eventos Windows',
    problem: 'Aparece una adición a un grupo sensible (Domain Admins, Administradores): escalada de privilegios con un solo evento (T1098.007).',
    fix: [
      'El evento: SecurityEvent | where EventID in (4728, 4732, 4756) | project TimeGenerated, Computer, SubjectUserName, TargetUserName, MemberName — SubjectUserName agrega, TargetUserName es el grupo, MemberName el agregado',
      '4728 grupo global, 4732 grupo local de dominio, 4756 universal: los tres importan si el grupo es sensible',
      'Confirmar el estado actual real: Get-ADGroupMember -Identity "Domain Admins" -Recursive | ft Name, SamAccountName, objectClass',
      'Preguntar al autor: ticket de cambio y aprobación — sin aprobación es incidente, no gestión',
      'Revertir: Remove-ADGroupMember -Identity "Domain Admins" -Members {{USERNAME}} -Confirm:$false',
      'Cazar qué hizo ya con el poder: 4720 (cuentas nuevas, CS-SOC-020) y 4688 de la cuenta agregada en las horas siguientes',
    ],
    verify: 'Get-ADGroupMember del grupo vuelve a su estado previo y la reversión queda auditada en {{TICKET_ID}}.',
    tags: ['4728', '4732', '4756', 'grupos', 'escalada', 'privilege escalation', 't1098.007', 'domain admins', 'get-adgroupmember', 'remove-adgroupmember', 'membername', 'ad'],
  },
  {
    id: 'CS-SOC-022',
    title: '4688 — padre raro, hijo sospechoso (Office lanza PowerShell)',
    category: 'Eventos Windows',
    problem: 'Un proceso de Office, navegador o wscript lanza cmd o powershell: el árbol padre-hijo delata la macro o el exploit (T1059.001).',
    fix: [
      'La firma en MDE (la más rica): DeviceProcessEvents | where FileName in ("cmd.exe", "powershell.exe", "wscript.exe") | where InitiatingProcessFileName in ("WINWORD.EXE", "EXCEL.EXE", "OUTLOOK.EXE", "wscript.exe") | project Timestamp, DeviceName, FileName, ProcessCommandLine, InitiatingProcessFileName',
      'En eventos de DCs y servidores: SecurityEvent | where EventID == 4688 | where ParentProcessName endswith "WINWORD.EXE" | where NewProcessName endswith "powershell.exe" | project TimeGenerated, Computer, NewProcessName, ProcessCommandLine',
      'La línea de comandos del hijo manda: si trae -enc o IEX/DownloadString → CS-SOC-026 y CS-SOC-027 de inmediato',
      'Buscar el documento padre: DeviceFileEvents | where DeviceName == "{{HOSTNAME}}" | where InitiatingProcessFileName in ("WINWORD.EXE", "EXCEL.EXE") | project Timestamp, FileName, FolderPath, SHA256',
      'Descarga o conexión posterior: LOLBins (CS-SOC-027) y beaconing (CS-SOC-035) definen la severidad real',
      'IOC completo al {{TICKET_ID}}: hash del documento, padre → hijo y línea de comandos; con ejecución real, contención (CS-SOC-039)',
    ],
    verify: 'El árbol queda documentado, el hash del documento en sandbox/Defender y el host contenido si hubo descarga o C2.',
    tags: ['4688', 'parent child', 'padre hijo', 'office macro', 't1059.001', 'deviceprocessevents', 'initiatingprocess', 'winword', 'process tree', 'macro', 'newprocessname', 'arbol'],
  },
  {
    id: 'CS-SOC-023',
    title: '1102/104 — borraron el log: ese ES el incidente',
    category: 'Eventos Windows',
    problem: 'El Security log de un DC o servidor se limpió sin ventana de cambio: nadie legítimo borra evidencia a las 3 a.m. (T1070.001).',
    fix: [
      'Confirmar ambos: SecurityEvent | where EventID == 1102 | project TimeGenerated, Computer, SubjectUserName — y System 104: Get-WinEvent -FilterHashtable @{LogName="System"; Id=104} -MaxEvents 5 | fl TimeCreated, Message',
      'El 1102 registra QUIÉN lo limpió: SubjectUserName es la cuenta a investigar (admin comprometido o cuenta de servicio)',
      'Preservar lo que queda ANTES de que rote: wevtutil epl Security C:\\temp\\security-{{HOSTNAME}}.evtx',
      'Reconstruir la ventana borrada desde el SIEM (los eventos ya ingeridos siguen ahí) y desde los otros DCs',
      'Buscar la actividad previa al wipe: 4720, 4728, 4698 y 7045 en los 30 minutos anteriores (cuentas, grupos, tareas, servicios)',
      'Tratar el host como comprometido: aislar (CS-SOC-039) y forense — el borrado es anti-forense de algo peor',
    ],
    verify: 'El .evtx exportado queda resguardado, el SubjectUserName del 1102 investigado y el host aislado para el analista.',
    tags: ['1102', '104', 'log clearing', 'borrado de logs', 't1070.001', 'anti forense', 'wevtutil', 'get-winevent', 'security log', 'dc', 'evidencia', 'system log'],
  },
  {
    id: 'CS-SOC-024',
    title: '7045 — servicio nuevo instalado: el binPath dice la verdad',
    category: 'Eventos Windows',
    problem: 'Event 7045 en el System log: el malware usa servicios para ejecutar como SYSTEM y sobrevivir reinicios (T1543.003) — y PsExec también deja este rastro.',
    fix: [
      'El evento con detalle: Get-WinEvent -FilterHashtable @{LogName="System"; Id=7045} -MaxEvents 10 | fl TimeCreated, Message (Service Name, File Name, Start Mode, Service Type)',
      'Banderas rojas: binPath en C:\\Users, C:\\Windows\\Temp o ProgramData; Start Mode auto; nombre random o casi legítimo (defenderd, svchosts)',
      'En el host: sc qc <servicio> (BINARY_PATH_NAME y START_TYPE) y Get-CimInstance Win32_Service -Filter "Name=\\"<servicio>\\"" | fl Name, PathName, StartMode, StartName',
      'El binario bajo la lupa: Get-FileHash <ruta-del-binario> -Algorithm SHA256 y a sandbox o VirusTotal',
      'Malicioso: Stop-Service <servicio> -Force; sc.exe config <servicio> start= disabled; cuarentena del binario (CS-SOC-029)',
      'Cazar el resto del fleet: el mismo binPath/7045 en otros hosts el mismo día (search en el SIEM) y el vector de entrada (correo o PsExec: CS-SOC-034)',
    ],
    verify: 'sc query <servicio> devuelve STOPPED, el binario cuarentenado y ningún otro host reporta 7045 del mismo binario.',
    tags: ['7045', 'servicio', 'service install', 't1543.003', 'sc qc', 'win32_service', 'get-ciminstance', 'binpath', 'persistencia', 'psexesvc', 'get-filehash', 'system log'],
  },
  {
    id: 'CS-SOC-025',
    title: 'Alerta de Defender — triage con Get-MpThreat',
    category: 'Procesos / EDR',
    problem: 'Saltó una alerta de Defender en un equipo: hay que confirmar el alcance, si ya la neutralizó y si queda algo vivo.',
    fix: [
      'El agente primero: Get-MpComputerStatus | fl AMServiceEnabled, RealTimeProtectionEnabled, AntivirusSignatureLastUpdated, QuickScanEndTime — sin firmas o con RTP apagado, el "limpio" no vale',
      'Las amenazas: Get-MpThreat | fl ThreatName, SeverityID, DidThreatExecute — DidThreatExecute en true = ejecutó, sube la severidad',
      'El detalle de la detección: Get-MpThreatDetection | fl InitialDetectionTime, ProcessName, Resources, CleaningActionID — Resources lista los archivos implicados',
      'Correlacionar la causa: vino por correo (CS-SOC-004), bajó por LOLBin (CS-SOC-027) o lo dejó una tarea (CS-SOC-030)',
      'Full scan para cerrar: & "C:\\Program Files\\Windows Defender\\MpCmdRun.exe" -Scan -ScanType 2',
      'Con ejecución o persistencia adicional: contención del host (CS-SOC-039) y elevación con la línea de tiempo completa',
    ],
    verify: 'Get-MpThreat sin amenazas activas, el full scan termina limpio y la causa raíz documentada en {{TICKET_ID}}.',
    tags: ['defender', 'get-mpthreat', 'get-mpcomputerstatus', 'mpcmdrun', 'antivirus', 'alert triage', 'edr', 'full scan', 'cuarentena', 'rtp', 'detection', 'mde'],
  },
  {
    id: 'CS-SOC-026',
    title: 'PowerShell -enc — decodificar el base64 del atacante',
    category: 'Procesos / EDR',
    problem: 'Un powershell.exe con -EncodedCommand apareció en 4688 o en DeviceProcessEvents: hasta decodificarlo no se sabe qué hizo (T1059.001 + T1027).',
    fix: [
      'Capturarlo: DeviceProcessEvents | where FileName == "powershell.exe" | where ProcessCommandLine has_any ("-enc", "-EncodedCommand") | project Timestamp, DeviceName, ProcessCommandLine | take 10',
      'También en eventos clásicos: SecurityEvent | where EventID == 4688 | where ProcessCommandLine has "-enc" | project TimeGenerated, Computer, ProcessCommandLine',
      'Decodificar (PowerShell casi siempre codifica en UTF-16LE): [Text.Encoding]::Unicode.GetString([Convert]::FromBase64String("AQUFB..."))',
      'Si no sale legible, probar UTF8: [Text.Encoding]::UTF8.GetString([Convert]::FromBase64String("AQUFB..."))',
      'Del payload extraer los IOCs: URLs, IPs, rutas, IEX/DownloadString, -nop -w hidden — y cazarlos (CS-SOC-027 y CS-SOC-035)',
      'Si hay ScriptBlock Logging, el payload ya está decodificado en el log: Get-WinEvent -FilterHashtable @{LogName="Microsoft-Windows-PowerShell/Operational"; Id=4104} -MaxEvents 20 | fl TimeCreated, Message',
    ],
    verify: 'El payload decodificado registrado en {{TICKET_ID}} con sus IOCs extraídos y bloqueados o cazados.',
    tags: ['encodedcommand', 'base64', 'powershell', '-enc', 't1059.001', 't1027', 'decodificar', 'decode', 'frombase64string', 'obfuscacion', '4104', 'scriptblock'],
  },
  {
    id: 'CS-SOC-027',
    title: 'LOLBins — certutil, rundll32, regsvr32 descargando payload',
    category: 'Procesos / EDR',
    problem: 'Binarios firmados de Windows usados para bajar malware: el AV no los marca porque son legítimos — la línea de comandos los delata (T1105).',
    fix: [
      'certutil: DeviceProcessEvents | where FileName == "certutil.exe" | where ProcessCommandLine has_any ("-urlcache", "-verifyctl") | project Timestamp, DeviceName, ProcessCommandLine, InitiatingProcessFileName',
      'rundll32 y regsvr32 (squiblydoo): DeviceProcessEvents | where FileName in ("rundll32.exe", "regsvr32.exe") | where ProcessCommandLine has "http" | project Timestamp, DeviceName, ProcessCommandLine',
      'Los otros del manual: DeviceProcessEvents | where FileName in ("bitsadmin.exe", "msiexec.exe", "curl.exe") | where ProcessCommandLine has "http" | project Timestamp, DeviceName, ProcessCommandLine',
      '¿Aterrizó algo?: DeviceFileEvents | where DeviceName == "{{HOSTNAME}}" | where Timestamp > ago(6h) | project Timestamp, FileName, FolderPath, SHA256, InitiatingProcessFileName',
      'Conexión al C2: DeviceNetworkEvents | where DeviceName == "{{HOSTNAME}}" | where isnotempty(RemoteUrl) | project Timestamp, RemoteUrl, RemoteIP, InitiatingProcessFileName | take 20',
      'Hashes a sandbox y Get-MpThreat, URL e IP a bloqueo en proxy y Defender, y elevar si hubo ejecución posterior',
    ],
    verify: 'DeviceFileEvents sin binarios nuevos del vector, la URL/IP bloqueada y el LOLBin no reaparece en 24h.',
    tags: ['lolbin', 'lolbins', 'certutil', 'rundll32', 'regsvr32', 't1105', 'ingress tool transfer', 'descarga', 'download', 'bitsadmin', 'urlcache', 'squiblydoo'],
  },
  {
    id: 'CS-SOC-028',
    title: 'Linaje de procesos — reconstruir quién lanzó a quién',
    category: 'Procesos / EDR',
    problem: 'Un proceso es sospechoso pero sin contexto no hay caso: saber quién lo lanzó (y quién lanzó al que lo lanzó) define la respuesta (T1059).',
    fix: [
      'El árbol: DeviceProcessEvents | where DeviceName == "{{HOSTNAME}}" | where Timestamp between (datetime({{TIMESTAMP}}) .. (30m)) | project Timestamp, FileName, ProcessCommandLine, InitiatingProcessFileName, InitiatingProcessCommandLine, InitiatingProcessParentFileName | order by Timestamp asc',
      'Subir por las ramas: repetir el query con InitiatingProcessFileName == "<proceso-padre>" hasta llegar a winlogon.exe, services.exe o explorer.exe',
      'Pista de oro: InitiatingProcessParentFileName == "wmiprvse.exe" o "taskeng.exe" → el origen fue WMI o una tarea (persistencia: CS-SOC-030 y CS-SOC-032)',
      'Correlación con eventos clásicos: SecurityEvent | where EventID == 4688 | where Computer == "{{HOSTNAME}}" | project TimeGenerated, NewProcessName, ProcessCommandLine, SubjectUserName, ParentProcessName',
      'Proceso raíz legítimo + cuenta que lo lanzó = punto de entrada (correo, credencial o exploit) y severidad del caso',
      'Documentar el árbol completo (quién → quién → comando) en {{TICKET_ID}}: es el mapa de qué contiene y qué borrar',
    ],
    verify: 'El árbol llega hasta el proceso raíz con la cuenta de origen y el punto de entrada identificado.',
    tags: ['linaje', 'process lineage', 'process tree', 'deviceprocessevents', 'initiatingprocess', 'wmiprvse', 'taskeng', 'proceso padre', 'contexto', 'who launched', '4688', 'arbol'],
  },
  {
    id: 'CS-SOC-029',
    title: 'Cuarentena, remove o preservar — qué hacer con la amenaza',
    category: 'Procesos / EDR',
    problem: 'Defender detectó y hay que decidir la acción: borrar sin preservar destruye evidencia y limpiar sin contener termina en re-infección.',
    fix: [
      'El estado actual: Get-MpThreat | fl ThreatName, SeverityID, DidThreatExecute — y Get-MpThreatDetection | fl InitialDetectionTime, ProcessName, Resources, CleaningActionID',
      'Cuarentena (default y casi siempre correcta): aísla el archivo sin borrarlo — reversible y preserva la muestra para forense',
      'Remove (borrado definitivo): solo cuando la amenaza está confirmada y el host NO va a análisis forense completo',
      'Incidente abierto: ANTES de remover, preservar: wevtutil epl + copia del binario + Get-FileHash <ruta> -Algorithm SHA256 a la carpeta de evidencia',
      'Amenaza viva o C2 activo: aislar el host PRIMERO (CS-SOC-039) — la limpieza sin contención = re-infección',
      'Post-remediación siempre: full scan (MpCmdRun.exe -Scan -ScanType 2) y revisar persistencia (tareas, run keys, servicios: CS-SOC-030/031/024)',
    ],
    verify: 'Get-MpThreat limpio o cuarentenado, el full scan en 0 y hash más binario preservados como evidencia del caso.',
    tags: ['cuarentena', 'quarantine', 'remove', 'remove-mpthreat', 'evidencia', 'evidence', 'contencion', 'limpieza', 'remediation', 'get-mpthreat', 're-infeccion', 'forense'],
  },
  {
    id: 'CS-SOC-030',
    title: 'Tarea programada nueva — la persistencia favorita (4698)',
    category: 'Persistencia',
    problem: 'Se detectó una tarea programada creada fuera de ventana de cambio: schtasks ejecuta como SYSTEM al arrancar o cada N minutos (T1053.005).',
    fix: [
      'El evento de auditoría: SecurityEvent | where EventID == 4698 | project TimeGenerated, Computer, SubjectUserName, TaskName, TaskContent — TaskContent trae el XML con la acción',
      'La creación por proceso: DeviceProcessEvents | where ProcessCommandLine has "schtasks" and ProcessCommandLine has "/create" | project Timestamp, DeviceName, ProcessCommandLine, InitiatingProcessFileName',
      'En el host: schtasks /query /tn "<nombre-tarea>" /v /fo LIST — y el inventario completo: schtasks /query /fo LIST /v | findstr /i "TaskName Task To Run Autor"',
      'Banderas rojas: acción powershell -enc, binario en AppData/Temp/ProgramData, autor distinto del dueño del host, corrida cada X minutos',
      'Eliminar: schtasks /delete /tn "<nombre-tarea>" /f y poner el binario de la acción a revisión (Get-FileHash + Get-MpThreat)',
      'Cazar la fuente: quién la creó (SubjectUserName / InitiatingProcess) y desde dónde llegó (correo o lateral) — el host merece 48h de vigilancia',
    ],
    verify: 'schtasks /query ya no lista la tarea, el binario de la acción cuarentenado y sin recreación en 48h.',
    tags: ['schtasks', 'tarea programada', 'scheduled task', '4698', 't1053.005', 'persistencia', 'persistence', 'taskcontent', 'autorun', 'query v', 'findstr', 'delete tn'],
  },
  {
    id: 'CS-SOC-031',
    title: 'Run keys — el auto-start del registro (T1547.001)',
    category: 'Persistencia',
    problem: 'El malware se reinstala tras cada reinicio: las claves Run/RunOnce en HKCU y HKLM son el auto-start más usado y el más fácil de revisar.',
    fix: [
      'Las cuatro del manual: reg query "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run" — y las de HKLM, RunOnce y Wow6432Node con la misma ruta cambiando la raíz',
      'En MDE: DeviceRegistryEvents | where RegistryKey endswith "\\CurrentVersion\\Run" or RegistryKey endswith "\\CurrentVersion\\RunOnce" | project Timestamp, DeviceName, RegistryKey, RegistryValueName, RegistryValueData, InitiatingProcessFileName',
      'Banderas rojas: valores hacia AppData/Temp/ProgramData, powershell/cmd/wscript/rundll32, nombres casi legítimos ("OneDriveUpd")',
      'Comparar contra un host sano del mismo rol: el diff de valores define qué sobra',
      'Limpiar: reg delete "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run" /v "<valor>" /f y cuarentena del binario apuntado (Get-FileHash)',
      'No olvidar Winlogon: reg query "HKLM\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon" /v Shell (debe ser solo explorer.exe)',
    ],
    verify: 'reg query de las claves espeja al host sano y el binario eliminado no reaparece tras reinicio.',
    tags: ['run key', 'autorun', 't1547.001', 't1547', 'registry', 'deviceregistryevents', 'reg query', 'reg delete', 'persistencia', 'persistence', 'runonce', 'winlogon'],
  },
  {
    id: 'CS-SOC-032',
    title: 'Suscripción WMI — persistencia silenciosa (T1546.003)',
    category: 'Persistencia',
    problem: 'EventFilter más CommandLineEventConsumer en root\\subscription: malware que vive en el repositorio WMI y casi ninguna herramienta de barrido lo lista.',
    fix: [
      'Los tres componentes: Get-WmiObject -Namespace root\\subscription -Class __EventFilter | fl Name, Query — y Get-WmiObject -Namespace root\\subscription -Class __CommandLineEventConsumer | fl Name, CommandLineTemplate',
      'El pegamento: Get-WmiObject -Namespace root\\subscription -Class __FilterToConsumerBinding | fl Filter, Consumer',
      'Banderas rojas: Query con IntervalTimer o "WITHIN", CommandLineTemplate con powershell -enc o rundll32, nombres aleatorios',
      'Quién lo implantó: DeviceProcessEvents | where ProcessCommandLine has "mofcomp" | project Timestamp, DeviceName, ProcessCommandLine (mofcomp compila el .mof que crea la suscripción)',
      'Eliminar en orden: primero el binding, luego consumer y filter: Get-WmiObject -Namespace root\\subscription -Class __FilterToConsumerBinding | Remove-WmiObject — repetir con cada clase',
      'Full scan después (el consumer baja el payload real) y vigilancia del host por 48h',
    ],
    verify: 'Get-WmiObject de las tres clases en root\\subscription vuelve a solo objetos del sistema y el payload cuarentenado.',
    tags: ['wmi', 'event filter', 'commandlineeventconsumer', 't1546.003', 'subscription', 'mofcomp', 'remove-wmiobject', 'persistencia', 'persistence', 'root subscription', 'silencioso', 'binding'],
  },
  {
    id: 'CS-SOC-033',
    title: 'Carpeta Startup — el auto-start más simple del mundo',
    category: 'Persistencia',
    problem: 'Un .lnk o .bat en la carpeta de inicio ejecuta en cada logon: sin registro, sin servicios, sin WMI — solo un archivo en una carpeta (T1547.001).',
    fix: [
      'Las dos carpetas: dir "%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup" y dir "C:\\ProgramData\\Microsoft\\Windows\\Start Menu\\Programs\\StartUp"',
      'En MDE: DeviceFileEvents | where FolderPath endswith "\\Start Menu\\Programs\\Startup" or FolderPath endswith "\\Start Menu\\Programs\\StartUp" | project Timestamp, DeviceName, FileName, FolderPath, SHA256, InitiatingProcessFileName',
      'Inspeccionar los .lnk: $s = (New-Object -ComObject WScript.Shell).CreateShortcut("<ruta>.lnk"); $s.TargetPath; $s.Arguments — el target manda',
      'Banderas rojas: target a powershell/cmd/rundll32, argumentos -enc o largos, archivo escrito por un navegador (descarga directa)',
      'Borrar el archivo, hashear antes (Get-FileHash) y revisar TODOS los perfiles: dir C:\\Users\\*\\AppData\\Roaming\\Microsoft\\Windows\\Start Menu\\Programs\\Startup',
      'Buscar al autor: DeviceProcessEvents del InitiatingProcessFileName que escribió el archivo = vector de la infección',
    ],
    verify: 'Ambas carpetas limpias en todos los perfiles y el logon siguiente del usuario no ejecuta nada anómalo.',
    tags: ['startup folder', 'carpeta de inicio', 't1547.001', 'lnk', 'shortcut', 'wscript.shell', 'createshortcut', 'autostart', 'devicefileevents', 'logon', 'persistencia', 'programdata'],
  },
  {
    id: 'CS-SOC-034',
    title: 'PsExec/WinRM anómalo — movimiento lateral (4624 tipo 3)',
    category: 'Lateral / C2',
    problem: 'Un host hace logons de red contra decenas de equipos en minutos: o un técnico con un script o un atacante moviéndose lateralmente (T1021.002).',
    fix: [
      'El patrón: SecurityEvent | where EventID == 4624 | where LogonType == 3 | where TimeGenerated > ago(24h) | summarize destinos = dcount(Computer), sesiones = count() by TargetUserName | where destinos > 20 | order by destinos desc',
      'La firma de PsExec: el servicio PSEXESVC en el destino: SecurityEvent | where EventID == 7045 | where ServiceName == "PSEXESVC" | project TimeGenerated, Computer, SubjectUserName — o DeviceProcessEvents | where FileName == "PSEXESVC.exe" | project Timestamp, DeviceName',
      'WinRM: DeviceNetworkEvents | where DeviceName == "{{HOSTNAME}}" | where RemotePort in (5985, 5986) | summarize destinos = dcount(RemoteIP) by InitiatingProcessFileName',
      'Separar el ruido: el admin legítimo opera en horario laboral, con su cuenta, sobre hosts de su ámbito; el lateral mezcla servidores y estaciones, fuera de horas, con cuentas recién comprometidas',
      'Confirmado lateral: contener la cuenta origen (CS-SOC-014) y el host (CS-SOC-039), revisando el 4688 del origen',
      'La lista de destinos tocados es el scope del incidente: en cada uno revisar tareas, servicios y run keys',
    ],
    verify: 'El origen contenido, la lista de destinos documentada en {{TICKET_ID}} y sin nuevos 4624 tipo 3 masivos desde el origen.',
    tags: ['psexec', 'winrm', '5985', 'movimiento lateral', 'lateral movement', 't1021.002', 't1021', '4624', 'logon type 3', 'psexesvc', 'admin share', 'ipc'],
  },
  {
    id: 'CS-SOC-035',
    title: 'Beaconing — el latido del C2 en el tráfico',
    category: 'Lateral / C2',
    problem: 'Conexiones salientes regulares como reloj hacia una misma IP: el malware hace check-in en intervalos fijos y eso lo delata (T1071).',
    fix: [
      'La firma (KQL): DeviceNetworkEvents | where DeviceName == "{{HOSTNAME}}" | where Timestamp > ago(24h) | summarize conexiones = count(), primera = min(Timestamp), ultima = max(Timestamp) by RemoteIP, RemotePort | where conexiones > 50 | order by conexiones desc',
      'Regularidad = beacon: intervalos casi idénticos con jitter bajo → máquina, no usuario',
      'El equivalente SPL (proxy o netflow): index=network dest="{{SOURCE_IP}}" | streamstats current=f last(_time) as prev by src | eval delta=_time-prev | stats count avg(delta) as promedio stdev(delta) as jitter by src, dest | where count > 50 and jitter < promedio*0.2',
      'Quién llama: DeviceNetworkEvents | where RemoteIP == "{{SOURCE_IP}}" | project Timestamp, RemoteUrl, RemotePort, InitiatingProcessFileName — el proceso que genera el latido',
      'Perfilar el destino: whois y TI de la IP, dominio recién registrado, hosting sin relación con el negocio',
      'Bloquear la IP/URL en proxy y Defender, y con proceso identificado: aislar (CS-SOC-039) y cazar el binario (CS-SOC-027/028)',
    ],
    verify: 'DeviceNetworkEvents del host sin conexiones a la IP tras el bloqueo y el proceso responsable identificado.',
    tags: ['beaconing', 'c2', 't1071', 'intervalos', 'jitter', 'devicenetworkevents', 'streamstats', 'check-in', 'llama a casa', 'trafico regular', 'exfiltracion', 'netflow'],
  },
  {
    id: 'CS-SOC-036',
    title: 'DNS tunneling — exfiltración por el puerto 53',
    category: 'Lateral / C2',
    problem: 'Miles de queries DNS con labels larguísimos y siempre únicos hacia un mismo dominio: los datos viajan escondidos dentro de las consultas (T1071.004).',
    fix: [
      'La firma (Sentinel, conector DNS): DnsEvents | where TimeGenerated > ago(1h) | summarize queries = count(), unicos = dcount(Name) by ClientIP | where unicos > 500 | order by unicos desc',
      'El equivalente SPL: index=dns sourcetype=stream:dns | bin _time span=5m | stats dc(query) as subdominios_unicos by _time, src | where subdominios_unicos > 200',
      'Confirmar el patrón: <cadena-aleatoria-de-40-caracteres>.dominio-malo.com con labels SIEMPRE distintos y largos',
      'Señales de apoyo: queries TXT frecuentes, NXDOMAIN alto y dominio raíz recién registrado (whois)',
      'Bloquear el DOMINIO en el resolver (RPZ o regla del firewall DNS), no solo la IP: el túnel va por DNS recursivo',
      'Cuantificar lo que se fue: labels largos = datos exfiltrados → activar el flujo de respuesta de datos (legal/privacidad) y aislar el host origen',
    ],
    verify: 'El dominio bloqueado resuelve NXDOMAIN, el volumen de queries del cliente vuelve a lo normal y el volumen exfiltrado quedó estimado.',
    tags: ['dns tunneling', 't1071.004', 'exfiltracion', 'puerto 53', 'dnsevents', 'labels', 'subdominios', 'nxdomain', 'rpz', 'resolver', 'txt', 'data exfil'],
  },
  {
    id: 'CS-SOC-037',
    title: 'Ransomware — sombras borradas y renombres masivos',
    category: 'Ransomware / Malware',
    problem: 'vssadmin delete shadows + archivos mutando de extensión + nota de rescate: hay cifrado activo y la contención se mide en minutos (T1486 + T1490).',
    fix: [
      'La firma letal: DeviceProcessEvents | where FileName in ("vssadmin.exe", "wbadmin.exe", "bcdedit.exe", "wmic.exe") | where ProcessCommandLine has_any ("delete shadows", "delete catalog", "recoveryenabled no", "shadowcopy delete") | project Timestamp, DeviceName, ProcessCommandLine',
      'En eventos: SecurityEvent | where EventID == 4688 | where ProcessCommandLine has "vssadmin" and ProcessCommandLine has "delete" | project TimeGenerated, Computer, ProcessCommandLine',
      'Contención INMEDIATA: aislar el host (CS-SOC-039) — y si hay varios hosts, aislar el segmento desde red: cada minuto cifra más',
      'No apagar si se puede evitar: la memoria del proceso de cifrado es evidencia — el aislamiento de red la preserva',
      'Verificar backups ANTES de prometer restauración: los atacantes buscan y borran backups primero (revisar repos, snapshots y copias offline)',
      'Cazar al patient zero: DeviceProcessEvents del host 30 minutos antes del primer renombre (entrada por correo, VPN o RDP expuesto) y elevar a crisis',
    ],
    verify: 'El host aislado sin más actividad de cifrado (DeviceProcessEvents sin renombres), backups verificados intactos y el scope de hosts cerrado.',
    tags: ['ransomware', 'vssadmin', 'shadow copies', 't1486', 't1490', 'delete shadows', 'cifrado', 'rescate', 'wbadmin', 'bcdedit', 'contencion total', 'patient zero'],
  },
  {
    id: 'CS-SOC-038',
    title: 'Infostealer — si tocó LSASS, asume TODAS las credenciales robadas',
    category: 'Ransomware / Malware',
    problem: 'Proceso haciendo dump de LSASS o leyendo perfiles de navegador: cookies, contraseñas guardadas y tokens ya viajaron hacia el atacante (T1003.001 + T1555).',
    fix: [
      'La firma LSASS: DeviceProcessEvents | where ProcessCommandLine has_any ("MiniDump", "comsvcs.dll", "procdump") | project Timestamp, DeviceName, FileName, ProcessCommandLine',
      'En eventos (con SACL configurada en el proceso): SecurityEvent | where EventID == 4656 | where ObjectName contains "lsass" | project TimeGenerated, Computer, SubjectUserName, ProcessName',
      'Stealers de navegador: DeviceFileEvents | where FileName in ("Login Data", "Cookies", "Local State") | where FolderPath contains "AppData" | project Timestamp, DeviceName, FolderPath, InitiatingProcessFileName',
      'Regla de oro: no se "verifica" si usaron las credenciales — se rotan: todos los usuarios que logonearon en el host más las cuentas locales (incluida la de admin local)',
      'Si el host es DC: escalar de inmediato (golden ticket en juego) — rotar krbtgt dos veces y revisar robo de PRT (CS-SOC-013)',
      'Aislar el host (CS-SOC-039), forense completo y revisar re-uso de esas contraseñas en otras cuentas (los datos del stealer se venden)',
    ],
    verify: 'Credenciales rotadas y sesiones revocadas (CS-SOC-014), el host aislado y el binario del dump o stealer identificado.',
    tags: ['infostealer', 'lsass', 't1003.001', 'minidump', 'comsvcs', 't1555', 'navegador', 'cookies', 'dump', 'rotar', 'krbtgt', 'credenciales'],
  },
  {
    id: 'CS-SOC-039',
    title: '¿Contener o aislar? — la decisión del host',
    category: 'Ransomware / Malware',
    problem: 'Hay actividad sospechosa en un equipo y hay que decidir el nivel de contención sin tumbar un servicio crítico ni destruir evidencia.',
    fix: [
      'Aislamiento COMPLETO cuando: ransomware activo, C2 con ejecución, dump de LSASS o borrado de logs — corta todo el tráfico externo y mantiene la conexión con Defender',
      'Contención restrictiva cuando: solo beaconing sin ejecución o descarga sin detonación — limita el tráfico a lo mínimo mientras se investiga',
      'Servidor crítico o DC: avisar al dueño del servicio ANTES de aislar (aislar un DC = caída de autenticación) y tener la ventana y el rollback listos',
      'Ejecutar desde Microsoft Defender XDR: página del dispositivo > Isolate machine (Full) — requiere sensor MDE activo y con conectividad',
      'Verificar que tomó: DeviceNetworkEvents | where DeviceName == "{{HOSTNAME}}" | order by Timestamp desc | take 10 — solo debe verse tráfico de Defender',
      'Liberar (Release from isolation) solo con la causa raíz cerrada, y validar conectividad con el usuario (login, DNS, apps)',
    ],
    verify: 'DeviceNetworkEvents muestra solo tráfico de Defender tras aislar, el dueño del servicio enterado y la decisión con justificación en {{TICKET_ID}}.',
    tags: ['aislar', 'isolate', 'contain', 'contencion', 'defender xdr', 'machine isolation', 'selective', 'full', 'decision', 'critical server', 'release', 't1489'],
  },
  {
    id: 'CS-SOC-040',
    title: 'Plantilla de caza KQL — de la hipótesis a la query',
    category: 'Threat Hunting / Tooling',
    problem: 'Llegó una técnica nueva del intel o una hipótesis propia y hay que convertirla en una query de Advanced Hunting reutilizable y rápida.',
    fix: [
      'Estructura base (tabla → tiempo → entidad → proyección): DeviceProcessEvents | where Timestamp > ago(7d) | where FileName == "mimikatz.exe" | project Timestamp, DeviceName, ProcessCommandLine, InitiatingProcessFileName | order by Timestamp desc',
      'Correlación proceso-red: DeviceProcessEvents | where Timestamp > ago(24h) | where FileName == "powershell.exe" | join kind=inner (DeviceNetworkEvents | where Timestamp > ago(24h) | project DeviceName, RemoteIP, RemoteUrl, InitiatingProcessFileName) on DeviceName | take 50',
      'Volumen con agregados: ... | summarize hits = count() by DeviceName, FileName | where hits > 10 | order by hits desc',
      'Excluir el ruido temprano: | where InitiatingProcessFileName !in ("SCCM.exe", "MonitoringHost.exe") — filtrar herramientas de gestión ANTES del summarize',
      'Precisión probada → detection rule (portal: hunting > Create detection rule): solo reglas con falsos positivos bajos pasan a producción',
      'Guardar la query en la librería del equipo con la hipótesis como comentario: una caza sin documentación se pierde',
    ],
    verify: 'La query corre acotada (menos de 30s con proyección temprana), los hits coinciden con la hipótesis y quedó guardada con autor y fecha.',
    tags: ['kql', 'advanced hunting', 'plantilla', 'template', 'join', 'summarize', 'detection rule', 'hunting', 'caza', 'sentinel', 'defender xdr', 'hipotesis'],
  },
  {
    id: 'CS-SOC-041',
    title: 'Plantilla de caza SPL (Splunk) — index y stats primero',
    category: 'Threat Hunting / Tooling',
    problem: 'Hay que montar la búsqueda en Splunk sin derretir el index: filtros exactos al principio, stats temprano y campos bien extraídos.',
    fix: [
      'Index y sourcetype SIEMPRE primero (nunca index=*): index=ad sourcetype=WinEventLog:Security EventCode=4720 | stats count values(Account_Name) as cuentas by host | sort - count',
      'Rango de tiempo acotado (24h/7d) y proyección temprana: ... | table _time host Account_Name (menos campos, más velocidad)',
      'El patrón de masa para spray: index=ad sourcetype=WinEventLog:Security (EventCode=4625 OR EventCode=4771) | stats count dc(Account_Name) as cuentas by Source_Network_Address | where cuentas > 10 | sort - cuentas',
      'Ráfagas temporales: ... | bin _time span=5m | stats count by _time, Account_Name | where count > 20',
      'Campos crudos que faltan: | rex field=_raw "Caller Computer Name:\\s+(?<caller>[^\\r]+)" — rex cuando el sourcetype no parsea',
      'Vigilancia continua: Save As > Alert con schedule (cron) y trigger por conteo — la caza puntual se vuelve detección',
    ],
    verify: 'La búsqueda corre en el rango acotado, el job inspector no muestra "results truncated" y los campos extraídos quedan poblados.',
    tags: ['spl', 'splunk', 'search', 'stats', 'index', 'sourcetype', 'eventcode', 'rex', 'bin span', 'alert', 'siem', 'dc'],
  },
  {
    id: 'CS-SOC-042',
    title: 'Regla Sigma — leerla y traducirla al SIEM local',
    category: 'Threat Hunting / Tooling',
    problem: 'Llegó una regla Sigma del intel o de la comunidad: hay que entender qué detecta, validarla y convertirla en query del SIEM propio.',
    fix: [
      'Leer el YAML en orden: title (qué), status, description y references (contexto), logsource (producto/servicio — define contra qué tabla o index se corre), detection (la lógica), tags',
      'Detection: selection (condiciones en AND dentro del bloque), filters (exclusiones) y condition ("selection and not filter") — es la traducción literal al where',
      'En logsource WinEventLog el EventID dentro de selection manda: EventID: 4698 con TaskName o CommandLine adicionales → where EventID == 4698 and ...',
      'Los tags llevan MITRE: attack.t1053.005 o attack.execution — alimentan la matriz del incidente y el reporte',
      'Convertir con sigma-cli: sigma check regla.yml (valida) y sigma convert -t splunk -p sysmon_windows regla.yml (genera la SPL lista)',
      'Para KQL no hay pipeline oficial: traducir a mano selection por selection y validar con 7 días de datos — los falsos positivos se ajustan con filters, no tocando la lógica',
    ],
    verify: 'La query convertida corre en el SIEM con hits manejables (falsos positivos ajustados) y la regla versionada en el repo de detecciones.',
    tags: ['sigma', 'yaml', 'detection engineering', 'sigma convert', 'sigma check', 'logsource', 'selection', 'condition', 'mitre', 'tags', 'yml', 'traduccion'],
  },
  {
    id: 'CS-SOC-043',
    title: 'Web shell en IIS — hallar el archivo y el request que la plantó',
    category: 'Persistencia',
    problem: 'El WAF alertó requests POST extraños hacia un .aspx recién creado en el wwwroot del IIS de {{HOSTNAME}}: hasta demostrar lo contrario, hay web shell plantada (T1505.003).',
    fix: [
      'Archivo nuevo en el webroot: DeviceFileEvents | where DeviceName == "{{HOSTNAME}}" | where Timestamp > ago(24h) | where FolderPath has_any ("wwwroot", "inetpub", "www") | where FileName endswith ".aspx" or FileName endswith ".asp" or FileName endswith ".php" or FileName endswith ".jsp" | project Timestamp, FileName, FolderPath, SHA256, InitiatingProcessFileName (w3wp.exe o java creando script = bandera roja)',
      'El request que la plantó (SPL del WAF/proxy): index=web sourcetype=access_combined method=POST status=200 uri_path="*.aspx*" | table _time, src, uri_path, bytes_in, bytes_out — el POST justo antes de la hora de creación del archivo',
      'Quién escribió el archivo: SecurityEvent | where EventID == 4663 | where ObjectName has ".aspx" | project TimeGenerated, Computer, SubjectUserName, ProcessName (requiere SACL del folder; sin SACL, el InitiatingProcessCommandLine de DeviceFileEvents da el linaje)',
      'Confirmar sin ejecutarla: el SHA256 a VirusTotal/feeds y leer el contenido con strings — JAMÁS abrir la URL desde la red corporativa (el hit notifica al atacante)',
      'Contener: aislar el host o sacar el site offline (CS-SOC-039), snapshot forense ANTES de borrar, retirar el archivo y cerrar el vector (upload sin validación, plugin/CMS desactualizado)',
      'Cazar shells hermanas: el mismo patrón de DeviceFileEvents en TODOS los servidores web 7d atrás y barrido de IOCs (SHA256, IP, URI) en el SIEM (CS-SOC-040)',
    ],
    verify: 'Ningún archivo de script creado por el proceso web en 48h en los servidores monitoreados y los IOCs de la shell barridos sin más hits.',
    tags: ['web shell', 'iis', 'aspx', 't1505.003', 'w3wp', 'wwwroot', 'devicefileevents', '4663', 'access_combined', 'post upload', 'backdoor', 'server software component'],
  },
  {
    id: 'CS-SOC-044',
    title: 'DLP: descarga masiva — contener y cerrar el alcance',
    category: 'Cuentas / Identidad',
    problem: 'La alerta DLP marca que una cuenta se descargó miles de archivos de SharePoint/OneDrive en poco tiempo: insider que se va o cuenta comprometida (T1213).',
    fix: [
      'Dimensionar: OfficeActivity | where Timestamp > ago(24h) | where UserId == "{{USERNAME}}@{{DOMAIN}}" | where Operation == "FileDownloaded" | summarize archivos = count() by SiteUrl, ClientIP | order by archivos desc — sitios, volúmenes y desde qué IP',
      '¿Cuenta comprometida o persona?: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where Timestamp > ago(24h) | project Timestamp, IPAddress, Location, ResultType, ClientApp — IP/ubicación/ClientApp nuevo = compromiso; todo desde la oficina de siempre = insider',
      'El UserAgent delata la herramienta: descargas masivas con cliente de sync/API en vez de navegador = preparación automatizada — guardar el UserAgent de OfficeActivity como IOC del caso',
      'Contener: revocar sesiones y reset (CS-SOC-014) — con insider confirmado, coordinar con legal/RRHH ANTES de confrontar (la evidencia de OfficeActivity ya quedó capturada)',
      'Cerrar el alcance: total de archivos y sitios, clasificación de lo descargado, ventana exacta y UserAgent, firmados en {{TICKET_ID}} — es el insumo del flujo de privacidad si hay datos personales',
      'Elevar: volumen de datos personales/financieros = activar el flujo de incidente de datos (plazos de notificación) — un caso DLP no se cierra como ticket de soporte',
    ],
    verify: 'Alcance documentado (archivos, sitios, ventana, UserAgent) en el ticket, cuenta contenida sin destruir la evidencia y remisión al flujo de privacidad si aplica.',
    tags: ['dlp', 'descarga masiva', 'officeactivity', 'filedownloaded', 'sharepoint', 'onedrive', 't1213', 'exfiltracion', 'insider', 'data loss', 'revocar sesiones', 'privacidad'],
  },
  {
    id: 'CS-SOC-045',
    title: 'Rogue AP — confirmarlo y ubicarlo antes de tocarlo',
    category: 'Lateral / C2',
    problem: 'El WIDS (o un usuario atento) reporta un SSID casi idéntico al corporativo con señal fuerte en la oficina: posible rogue AP o evil twin (T1557).',
    fix: [
      'Primer filtro: SSID sospechoso vs legítimo (un solo carácter de diferencia = evil twin) — y el BSSID (MAC del AP) contra el inventario: netsh wlan show networks mode=bssid (SSID, BSSID, canal y señal %)',
      'La señal delata la distancia: medir el % desde dos puntos del piso (misma sala y pasillo) — donde la señal pica en ambas mediciones está el AP (a 90% de señal estás a metros)',
      'Vendor del BSSID: los 3 primeros octetos (OUI) → un AP doméstico (TP-Link, Mercusys, D-Link) en zona corporativa es casi seguro rogue',
      'En el SIEM: CommonSecurityLog del sensor WIDS/controlador WLAN con los eventos de asociación del BSSID — cuántos clientes corporativos ya cayeron (cada uno es potencial exposición de credenciales)',
      'Contener: bloquear el BSSID en el WLAN controller, ubicar el puerto del switch por la MAC (show mac address-table) y retirar físicamente el AP — foto y evidencia a {{TICKET_ID}}',
      'Si hubo clientes conectados al evil twin: cada usuario se trata como robo de credenciales (CS-SOC-003) — un portal cautivo falso captura usuario y contraseña',
    ],
    verify: 'BSSID bloqueado, AP retirado físicamente, ningún cliente corporativo reconecta al SSID falso y los usuarios expuestos pasaron por reset.',
    tags: ['rogue ap', 'evil twin', 'bssid', 'ssid', 'wids', 'wifi', 'netsh wlan', 'mode=bssid', 'oui', 'triangulacion', 't1557', 'wlan controller'],
  },
  {
    id: 'CS-SOC-046',
    title: 'SSH brute force externo — atajar con fail2ban y leer el auth.log',
    category: 'Ataques de Contraseña',
    problem: 'El auth.log de {{HOSTNAME}} (servidor Linux expuesto) se llena de "Failed password" desde IPs externas: brute force SSH en curso (T1110.001).',
    fix: [
      'Medir la magnitud: grep -c "Failed password" /var/log/auth.log — y las IPs top: grep "Failed password" /var/log/auth.log | grep -oE "([0-9]{1,3}[.]){3}[0-9]{1,3}" | sort | uniq -c | sort -rn | head',
      'Lo que importa de verdad: ¿alguien entró?: grep "Accepted password" /var/log/auth.log | tail -20 — una línea Accepted desde la IP del brute force = incidente completo, no intento fallido',
      'Estado de fail2ban: fail2ban-client status sshd (current banned y total) — y ban manual inmediato: fail2ban-client set sshd banip {{IP}}',
      'Ajustar la cárcel en /etc/fail2ban/jail.local ([sshd] maxretry=5, findtime=10m, bantime=1h; reincidentes: bantime=-1) → systemctl restart fail2ban',
      'Blindar de verdad: PasswordAuthentication no y PermitRootLogin no en /etc/ssh/sshd_config → systemctl restart sshd — con solo llaves, el brute force muere',
      'Si hubo un Accepted: rotar credenciales (CS-SOC-014), revisar sesiones (who, w) y cazar persistencia Linux (crontab -l, systemctl list-timers, ~/.ssh/authorized_keys)',
    ],
    verify: 'fail2ban-client status sshd muestra la IP baneada, el conteo de "Failed password" cae a cero y sshd rechaza contraseñas.',
    tags: ['ssh', 'brute force', 'fail2ban', 'auth.log', 'failed password', 't1110.001', 'passwordauthentication', 'jail.local', 'banip', 'sshd_config', 'permitrootlogin', 'linux'],
  },
  {
    id: 'CS-SOC-047',
    title: 'SQLi en los logs del WAF — extraer el payload con SPL',
    category: 'Threat Hunting / Tooling',
    problem: 'El WAF bloqueó una ráfaga de inyección SQL contra el portal de {{HOSTNAME}}: extraer el payload exacto, confirmar que nada pasó y blindar (T1190).',
    fix: [
      'La ráfaga bloqueada: index=waf sourcetype=waf:blocked attack_type="SQL Injection" | stats count dc(uri_path) as uris values(signature) as firmas by src_ip | sort - count',
      'Extraer el payload de los access logs: index=web sourcetype=access_combined | rex field=uri_query "id=(?<payload>[^&]+)" | where match(payload, "(?i)union.+select|sleep|or 1=1") | table _time, src_ip, uri_path, payload, status',
      'Lo que importa: ¿pasó algo? — las bloqueadas viven en el WAF, las que PASARON en el web server: mismas queries con status=200 en access_combined, y time-based SLEEP: stats avg(response_time) p95(response_time) by uri_path (respuesta de segundos = la base respondió)',
      'Del lado de la app: el log de errores SQL en la ventana de la ráfaga (index=app "SQL syntax") correlacionado con el src_ip — el error de sintaxis vivo = la query llegó a la base',
      'IOCs y bloqueo: src_ip + patrón + uri a {{TICKET_ID}}, regla de bloqueo temporal en el edge/WAF y mirar si la misma IP venía probando semanas atrás (index=web src_ip={{IP}} earliest=-30d | stats count by uri_path)',
      'Blindar de fondo: queries parametrizadas (la raíz), la firma del WAF de detect a block y rate-limit por IP — sin esto, la ráfaga vuelve mañana',
    ],
    verify: 'El payload extraído y los IOCs documentados en el ticket, cero requests con status=200 que casen con el patrón tras el bloqueo y la firma del WAF en modo block.',
    tags: ['sqli', 'sql injection', 'waf', 'payload', 't1190', 'rex', 'uri_query', 'access_combined', 'union select', 'sleep', 'time-based', 'index=web'],
  },
  {
    id: 'CS-SOC-048',
    title: 'Credenciales en repo público — verificar, rotar y purgar el historial',
    category: 'Cuentas / Identidad',
    problem: 'El monitor de repos reporta credenciales de Nexora S.A. expuestas en un repo público: hay que verificar, contener y rotar sin asumir que "nadie la vio" (T1552.001).',
    fix: [
      'Verificar qué es: el snippet del repo (contraseña, token, llave), a qué sistema abre la puerta y desde cuándo está publicado (git log del archivo) — un secreto con meses de exposición se asume usado',
      'Buscar actividad ANTES de rotar: SigninLogs | where UserPrincipalName == "{{USERNAME}}@{{DOMAIN}}" | where Timestamp > ago(30d) | project Timestamp, IPAddress, Location, ResultType — y para apps/service principals: AADNonInteractiveUserSignInLogs | where AppId == "{{APP_ID}}" | summarize intentos = count() by IPAddress | order by intentos desc',
      'ROTAR INMEDIATAMENTE (la rotación no espera más evidencia): reset + revocar sesiones (CS-SOC-014); tokens/PAT: regenerar en el sistema dueño; llaves SSH: retirar del authorized_keys y generar nuevas',
      'Purga real del repo: borrar el archivo NO basta (queda en el historial de git) — rewrite del historial si es repo propio, o contacto al dueño/takedown si es de tercero; asumir que alguien ya lo clonó',
      'Barrido de re-uso: la misma contraseña en otros sistemas (la gente repite), buscar el patrón del secreto en los repos internos y revisar OfficeActivity/AzureActivity del dueño de la credencial en 30d',
      'Que no se repita: pre-commit hooks + secret scanning en el pipeline (gitleaks/trufflehog) — el fix es de proceso, no de rotar más rápido',
    ],
    verify: 'La credencial vieja rechazada (prueba de login controlada), la nueva en uso, el historial del repo reescrito y secret scanning activo en CI.',
    tags: ['credenciales expuestas', 'repo publico', 'github', 'secret scanning', 'gitleaks', 't1552.001', 'rotacion', 'token', 'pat', 'git history', 'filtracion', 'leak'],
  },
];

export const SOC_CHEATSHEET_COUNT = SOC_CHEATSHEET.length;

export const SOC_CHEATSHEET_CATEGORIES: string[] = Array.from(
  new Set(SOC_CHEATSHEET.map((c) => c.category))
);
